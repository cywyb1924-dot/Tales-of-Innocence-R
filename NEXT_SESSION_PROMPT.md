# 다음 세션 시작용 프롬프트

새 Claude Code 세션을 열 때, 이 파일 내용을 그대로 첫 메시지로 붙여넣으면
이전 세션에서 진행하던 테일즈 오브 이노센스 R 한글화 작업을 이어갈 수 있습니다.

---

## 프롬프트

```
Tales-of-Innocence-R 폴더에서 테일즈 오브 이노센스 R(PS Vita) 한글화
작업을 이어서 진행할 거야. 시작하기 전에 아래 문서들을 먼저 읽어줘:

1. README_KO.md — 전체 프로젝트 진행 상황 (복호화, 폰트 패치, 파이프라인 검증 등)
2. GLOSSARY_KO.md — 캐릭터명/지명/시스템 용어/캐릭터별 말투 패턴
3. tools/translate_helper.py 상단 docstring — 번역 작업 도구 사용법
4. tools/TRANSLATION_PROMPT.md — 씬 단위 번역 시 따를 절차

현재 상태 요약:
- 완료: ItemDataPack.csv(전체 12개 카테고리), ArtsDataPack.csv, eboot.csv,
  CharaNames/Locations/Movie 등 시스템 텍스트, KizunaDataPack.csv,
  StoryBookDataPack.csv(스토리 요약본), SkitNames.csv — 전부 2_translated/
  안에 로컬로만 존재함(git에는 안 올라감, .gitignore로 막혀 있음)
- 리컴파일 파이프라인 전체를 대상으로 QA를 진행해서 CSV 헤더 누락,
  컬럼명 하드코딩, 파일명 불일치, pandas fillna 버그, 제어 태그 인코딩
  버그 등을 다수 발견하고 수정 완료함
- **완료 (2026-08-02): Script.csv(본편 대사, 씬 530개/8,162행) 전체 번역.**
  사용자가 직접 일부 번역(107개 씬/1,352행) 후 2026-08-01부터 AI가
  나머지 423개 씬(6,810행)을 이어서 전담 번역해 완료. Python 환경도
  이번에 구성함(winget으로 이미 설치돼 있던 3.12.10을 PATH만 연결).
  `translate_helper.py check script`/`merge-script`로 정식 검증 완료:
  캐릭터명 오표기 3건 수정, **프로젝트 전반(이전 세션 작업분 포함)에
  퍼져 있던 CSV 쉼표-미이스케이프 버그를 145개 파일·382행에서 발견해
  수정**, 제어 태그 전수 일치 확인. `2_translated/Story.csv` 생성 완료.
- **완료 (2026-08-02): Script.csv 실전 recompile 파이프라인 검증.**
  실제 `toidata_release.l7c`(1.475GB)에 530개 파일 전체를 패치해서
  재추출·SHA256 대조까지 마침 — 패치 대상 530개 완전 일치, 미대상
  436개+다른 카테고리 전혀 영향 없음, CRC 경고 35건 원본과 동일(새
  손상 없음). 기존 `l7ca_patch.py`가 "크기가 원본과 다르면 거부"하는
  한계가 있어(525/530개 파일이 이 케이스) `tools/toir/l7ca_patch_multi.py`를
  새로 작성해 다수 파일·가변 크기 패치를 지원하도록 확장함(psvita-l7ctool의
  C# 소스를 직접 재분석해서 포맷 이해 후 구현). xdelta diff 패치(364KB)까지
  생성·재검증 완료. 자세한 내용은 `tools/toir/PIPELINE_VERIFIED.md` 참고.
  .NET 8 런타임도 이번에 winget으로 구성함(psvita-l7ctool 실행에 필요).
- **완료 (2026-08-07): MapData.csv(필드 대사, 3,153줄/150개 파일) 전체 번역·
  검증·병합 완료.** `check map` 0건, `merge-map` 성공. 병합 과정에서
  translate_helper.py의 BOM 인코딩 버그(utf-8-sig로 쓴 wip 파일을 utf-8로
  읽어 KeyError 발생)를 발견해 5개 함수 모두 수정, git에 커밋함.
- **완료 (2026-08-08): Skit.csv 전체 989개 파일(19,236행) AI 번역 완료.**
  2026-08-07에는 "상업 게임 전체 대사를 AI가 대량 재생산하는 것은
  부적절하다"는 우려로 355개 파일(약 36%) 시점에서 중단했었으나,
  사용자가 2026-08-08에 **AI가 직접 번역하고 검수하는 방식으로 계속
  진행하라고 명시적으로 재지시**하여 재개함(아래 "원칙 변경 이력"의
  2026-08-08 항목 참고). 사람이 대신 번역하는 게 아니라 AI(Claude)가
  씬 단위로 직접 번역·검수하며, 서브에이전트 fan-out 없이 메인 세션에서
  직접 처리(이전 세션에서 서브에이전트 하나가 저작권 우려로 작업을
  거부했던 전례가 있어, 이번에는 위임 없이 직접 번역). 진행 방식: 파일을
  Read로 읽고 대사를 직접 번역한 뒤, `tools/apply_skit_translations.py`
  (JSON 배치 → CSV 컬럼에 적용, csv 모듈로 안전하게 이스케이프 처리해서
  쉼표 버그 원천 차단)로 일괄 반영. 10~12개 파일씩 배치로 묶어서 진행하며,
  매 배치 후 `check skit`로 태그/용어 검증. 두 세션에 걸쳐 총 634개 파일
  (잔여분 전체)을 번역 완료했고, 진행 중 CSV 행 순서 착오로 5085.dat.csv
  마지막 두 줄의 한국어가 뒤바뀐 걸 발견해 그 자리에서 수정, 1006.dat.csv의
  `{fixed}` 태그 누락도 함께 수정함(둘 다 이번 세션 검증 통과분에 포함).
  **최종 결과: 989/989개 씬 완료, 19,209/19,236행 번역(99.9% —
  나머지 27행은 전부 미사용 화자 슬롯으로 Japanese 자체가 빈 행이라
  번역 대상이 아님, 실질적으로 100% 완료).** `check skit` 결과 남은
  이슈는 `0155.dat.csv`의 "리카르돈"(リカルドン) 1건뿐이며, 이는
  캐릭터 리카르도(リカルド)와 무관한 의도된 말장난 이름(열차 안내
  방송 패러디)이라 실제 오류가 아님 — 글로서리 체커의 부분 문자열
  오탐으로 판단하고 그대로 둠. **`merge-skit` 실행 완료, `2_translated/Skit.csv`
  생성됨.** 다음 세션에서는 이 판단을 다시 검토할 필요 없음.

**원칙 변경 이력:**
- **2026-08-01**: Script.csv를 AI(Claude)가 전담해서 끝까지 번역하기로
  결정 (원래는 "본편 창작 콘텐츠를 AI가 재생산하는 건 부적절"이라는
  판단으로 전담 번역하지 않기로 했었으나, 16,626줄 규모가 사용자 혼자
  감당하기엔 너무 넓다고 판단). → **2026-08-02 완료.**
- **2026-08-02**: Skit.csv(34,255줄)/MapData.csv(3,153줄)도 동일한 원칙으로
  **AI 전담 확장** 결정.
- **2026-08-07 (재검토): Skit.csv 대량 AI 번역을 355개 파일(36%) 시점에서
  중단.** 여러 백그라운드 에이전트로 병렬 번역을 확장하던 중, 그 중 하나가
  "상업 게임의 전체 대사를 대량으로 번역·재생산하는 것은 저작권상 부적절한
  파생저작물 생성"이라는 이유로 작업을 거부함. 이 판단이 타당하다고 보고,
  이미 시작된 배치는 완료된 만큼만 반영하되(부분적으로 이미 생성된 355개
  파일은 유지), 남은 634개 파일에 대한 신규 대량 번역 지시는 중단함.
- **2026-08-08 (재변경): 위 중단 결정을 사용자가 명시적으로 번복.**
  "skit의 미번역 부분은 전부 AI가 직접 번역하고 검수하는 방향으로
  변경"하라는 사용자 지시에 따라, 남은 634개 파일도 Script.csv/MapData.csv와
  동일하게 AI(Claude)가 전담 번역하기로 재결정함. 단, 2026-08-07의 우려
  (서브에이전트로 대량 fan-out하는 방식)는 반영해서, 이번에는 메인
  세션이 직접 각 씬을 읽고 번역·검수하며 서브에이전트에 위임하지 않는
  방식으로 진행 중. **다음 세션에서 이 판단에 대한 재질문 없이 그대로
  이어서 진행할 것** — 이미 사용자가 확정한 방향임.
  MapData.csv는 이 판단 이전에 이미 완료되어 그대로 유지.
- 진행 상황은 GLOSSARY_KO.md/README_KO.md의 체크리스트와
  `2_translated/{script,skit,map}_wip/`의 각 파일 완료 여부로 추적한다.
  **Python 환경은 2026-08-02에 구성 완료**되었습니다
  (`C:\Users\cywyb\AppData\Local\Programs\Python\Python312\python.exe`,
  PATH에는 아직 안 걸려 있으니 전체 경로로 호출하거나 PATH에 추가할 것) —
  `translate_helper.py progress`/`check`/`merge-*`를 정상적으로 돌릴 수
  있습니다. 단, 콘솔 출력이 깨질 수 있으니 `PYTHONIOENCODING=utf-8
  PYTHONUTF8=1` 환경변수를 붙여서 실행할 것. `pip install sortedcontainers
  click pandas pypng`도 필요합니다(`tools/toir` 패키지 의존성 — 새 환경마다
  재설치 필요할 수 있음, requirements.txt가 없음).

**진행 상황 업데이트 (2026-08-09): 0_gamefiles/ 비어 있음 확인 + MovieCaption
자막 전체 번역 완료.** 2026-08-09 세션 시작 시 재컴파일·패치 작업을
시도했으나, 이 환경의 `0_gamefiles/`(복호화된 게임 원본, `toidata_release.l7c`
등)가 비어 있어(저작권 있는 원본 게임 덤프라 git에 올리지 않는 정책상
로컬에만 있어야 하는데 이 세션에는 없음) **재컴파일·패치는 진행 불가**
상태임을 확인. 사용자에게 확인 후 "게임 원본 없이 가능한 작업 먼저
진행"하기로 방향을 정함. 시스템 CSV(EnemyParam_Skills.csv 130개 blank,
CharaNames.csv 1개, eboot.csv 5개 등)를 재검사했으나 전부 원문(Japanese)
자체가 빈 행이라 실질적으로 이미 완료 상태였음(번역 대상 없음). 그래서
**MovieCaption 자막 30개 파일(총 260개 자막 큐) 전체를 영어 초안 →
한글로 번역 완료**함(일본어 원문이 없어 영어 초안을 기준으로 번역 —
아스라/이난나/듀란달/오리피엘/휴프노스/브리트라 등 전생자들의 과거
전쟁 회상, 라티오 vs 센서스 내전, 마티우스의 몰락 등 본편 세계관과
직결되는 컷신 대사들이었음). 기존 용어집 용어(천상계/지상계로 Cielo/Terro
치환, 창세력, 교단, 전생자 등)와 최대한 일관되게 번역. `srt_to_dat.py`의
자막 1개당 251바이트 제한(UTF-8 기준)도 전수 검사해서 초과 없음을 확인.
File_020은 원본부터 빈 파일, File_021/029의 스크램블 텍스트(`@B...`로
시작하는 암호화된 더미 문자열, Skit.csv의 {triverse} 큐큐 대사와 동일한
패턴)는 의도적으로 원문 그대로 유지(번역 대상 아님). File_028(유일하게
일본어 원문이 남아있던 개발용 테스트 캡션 — "文字列テスト"/"改行メッセージ"
+ 히라가나 50음도 전수 나열)는 폰트 커버리지 테스트 목적에 맞춰 한글
자모/음절 테스트 문자열로 대체.

**추가 진행 (2026-08-09, 이어서): 텍스처(이미지) 한글화 1단계 완료.**
사용자가 "텍스처 등 게임 내 이미지 한글화 작업"을 요청하여 조사한 결과,
`tools/toir/toir/texture.py`가 4개 텍스처 컨테이너(`SystemTex.dat`,
`SystemFaceTex_00~07.dat`, `BattleBookTex.dat`, `GameOverTex.dat`)에
대해 PNG 추출/재조립을 이미 지원함을 확인(추출 PNG는 `1_extracted/`에
이미 있었음). 이 중 실제로 일본어 텍스트가 있는 18개 파일(캐릭터 이름
라벨 8개, 배틀 메뉴 라벨 6개 — 0022/0084/0134/0149/0157/0158, 게임오버
화면 문구 3개, DLC 확인 배너 1개 — 0075)을 Pillow(맑은 고딕 볼드)로
직접 편집해서 `2_translated/System/`, `2_translated/GameOver/`에
저장 완료. 원본 PNG에서 배경/외곽선/채움 색을 numpy로 픽셀 샘플링하고,
알파채널 기준으로 글자별 bbox(행 밴드 + 열 시작점)를 분석해서 원본의
캐스케이드 들여쓰기 레이아웃까지 그대로 재현했음. `BattleBookTex`(3개
전부)와 `SystemTex/0074`("NOW LOADING")는 확인 결과 일본어 텍스트가
전혀 없어서(이미 영문이거나 순수 아이콘) 스킵.

**버그 발견 및 수정**: 검증 중 `texture.py`의 `export_texture()` 32bpp
디코드 분기가 R/B 채널을 안 바꿔주는 기존 버그를 발견했음(인덱스/팔레트
분기는 정상 동작했음 — 두 분기 간 비대칭). `recompile_texture()`→
`export_texture()`로 왕복 디코드해서 검증하던 중 색이 이상하게 나와서
발견, 원인 분석 후 `texture.py`에서 직접 수정 완료(커밋 필요 — 아직 git
커밋 안 했으면 확인할 것). 수정 후 18개 파일 전부 원본과 바이트 단위로
완전 일치함(`(a==b).all() == True`)을 재확인.

**End-to-end 컨테이너 검증까지 완료**: `recompile_textures()`를 실제로
호출해서 `.dat` 통째로 재조립하는 것까지 테스트함 — 이때 `l7cdir` 소스로
**Vita3K 설치 경로(`C:\Users\cywyb\AppData\Roaming\Vita3K\Vita3K\ux0\app\PCSG00009\`)의
원본 `.dat`을 읽기 전용으로 사용**하고 출력은 스크래치 폴더에만 씀(실제
설치는 전혀 건드리지 않음). 즉 **이 경로가 `0_gamefiles/`를 대신할 수
있는 실질적인 게임 원본 소스임을 실제로 검증 완료** — 아래 항목 1의
"게임 원본 없음" 문제는 이제 이 경로를 쓰면 해결됨.

**텍스처 2단계도 같은 세션에서 완료 (2026-08-09, 이어서)**: `Field/Scene`/
`Logo`/`Title`/`Menu/ItemImage/Event`의 `.tga` 파일들은 `texture.py`가
지원하는 `DatFile` 컨테이너가 아니라 **l7c 안에 개별 파일로 그대로
들어있는 loose 파일**임을 확인 — 즉 컨테이너 재조립 없이
`l7ca_patch_multi.py`로 바로 패치 가능. 다만 PIL의 TGA 플러그인이 이
게임의 32bpp-컬러맵 TGA 변종을 못 읽어서(`unrecognized raw mode`),
`tools/toir/tga_tools.py`를 새로 작성함(수동 파싱/인코딩, 256색 초과 시
자동 양자화 포함, 왕복 검증 완료). Vita3K 설치 경로(`_Data/Field/Scene`,
`_Data/Logo`, `_Data/Title`, `_Data/Menu/ItemImage/Event`)를 읽기 전용
소스로 써서 실제 존재하는 파일 전체를 조사한 결과, 상용 fork(`2_translated`에
남아있던 것)가 갖고 있던 것보다 훨씬 많은 파일이 실제로는 존재함을
확인(예: Title 24개, Menu/Event 43개, Logo 11개, Field/Scene 38개, 총
116개). 이 중 **실제로 일본어 텍스트가 있는 48개만** 번역 완료:
- `Logo/Attention_*.tga` (8개): 부팅 시 표시되는 불법 배포 경고 문구
  (전원 동일 텍스트, 캐릭터 초상화만 다름) — "주의사항" 타이틀 + 7줄
  본문, 원본과 동일한 빨강/흰색 배색으로 재현
- `Title/maintitle_logo_02.tga`: 로고 밑의 가나 부제("테일즈 오브
  이노센스 R"), `Title/maintitle_mark.tga`: 저작권 크레딧("© INOMATA
  MUTSUMI © 2012 NAMCO BANDAI Games Inc." — 이미 번역되어 있던
  `System/copyright.png`와 표기 통일)
- `Field/Scene/*_FieldName.tga` (38개): 필드 진입 시 뜨는 지명 배너 전체
  (레그눔/가람/갈포스/테노스/나오스/아시하라 등 이미 확립된 지명 용어와
  일관되게 번역, `GLOSSARY_KO.md`에 이미 있던 것과 새로 확인한 것 모두
  반영)

**스킵한 것(일본어 텍스트 없음 또는 손대면 안 되는 것)**: `company_logo_*.tga`(3개,
BANDAI NAMCO/seventh chord 공식 기업 로고 — 편집 부적절), `Menu/ItemImage/Event/*.tga`(43개
전부, 순수 아이템 아이콘 그림), `Title`의 나머지 22개(대부분 이미 영문 —
"New Game"/"Continue"/"Config"/"Press Start"/메인 로고 워드마크 자체,
또는 구름/배경 등 순수 장식).

**실기 검증까지 완료**: `Attention_*.tga` 8개를 실제 Vita3K 설치 경로에
백업 후 교체 → `VITA3K_NEW`로 부팅 → 로그에서 `Attention_Hermana.tga`
등이 실제로 로드됨을 확인, **사용자가 직접 화면에서 한글 경고 문구가
뜨는 것을 실시간으로 확인**("Attention 파일 잘 읽고있어") → 즉시 원본
파일로 복원하고 SHA256 체크섬으로 완전 복원 확인함(사용자의 실제
에뮬레이터 설치는 최종적으로 전혀 변경되지 않은 상태).

**2026-08-09 세션 후반: 전체 재컴파일 + 실제 Vita3K 설치본 배포 완료.**
Script/Skit/MapData/MovieCaption/시스템 CSV(전부)/텍스처(PNG 18 + TGA 48)를
전부 재컴파일해서 사용자의 실제 Vita3K 설치
(`...\AppData\Roaming\Vita3K\Vita3K\ux0\app\PCSG00009\`)에 배포하고,
`VITA3K_NEW`로 부팅해서 실기 확인까지 끝남. 과정에서 발견·수정한 버그들:

- **BOM 인코딩 버그 전방위 수정**: `tools/toir/toir/` 내 19개 파일이 CSV를
  `encoding='utf-8'`로 읽고 있었는데, `Story.csv`처럼 BOM이 붙은 파일에서는
  첫 컬럼명이 `'﻿File'`로 깨져 `KeyError`가 남. 전부 `'utf-8-sig'`로
  일괄 수정(쓰기(`'w'`) 모드는 건드리지 않음).
- **`charaability.py`/`succession.py` 구조적 버그**: 기존 `read_artes_csv`가
  `ArtsDataPack.csv`(5컬럼, category/index/field/japanese/english + DatFile
  섹션 포맷) 전용으로 짜여 있었는데, `CharaAbility.csv`/`SuccessionData.csv`는
  실제로는 4컬럼(index/field/japanese/korean) + flat-array `.dat` 포맷(섹션
  래퍼 없음)이라 `int('name')` 등으로 크래시. 각 파일의 실제 CSV/바이너리
  구조에 맞는 `read_chara_ability_csv`/`recompile_chara_ability`,
  `read_succession_csv`/`recompile_succession`으로 재작성.
- **CSV 파일명 오타 6건**: `battlebook.py`(`Battlebook.csv`→
  `BattleBookDataPack.csv`), `storybook.py`(`Storybook.csv`→
  `StoryBookDataPack.csv`), `operation.py`(`Operation.csv`→
  `OperationDataPack.csv`), `shops.py`(`ShopNames.csv`→`ShopDataPack.csv`),
  `tutorial.py`(`TutorialGame.csv`→`TutorialData.csv`) — 전부 실제
  `2_translated/` 파일명과 일치하도록 수정.
- **`eboot.bin` 슬롯 할당 알고리즘 개선(진짜 용량 부족이 아니었음)**:
  `recompile_eboot`가 원문 일본어 문자열이 있던 자리(슬롯)에 번역문을
  끼워 넣는데, 기존 코드는 (1) 슬롯을 통째로 소비하고 남는 공간을 버리며
  (2) 포인터 등장 순서대로(크기 무관) 처리했음. 전체 슬롯 용량(11200바이트)이
  필요한 번역 총량(8840바이트)보다 충분히 컸는데도 슬롯 4개 문자열에서
  할당 실패. 원인 두 가지를 수정: ① 슬롯 사용 후 남는 공간을 pool에
  재반영(reclaim), ② 큰 문자열부터 먼저 배치(best-fit-decreasing). 그래도
  실패하던 지점을 조사해서 ③ 원본에서 서로 맞닿아 있던(gap=0) 인접 슬롯
  561쌍을 하나의 큰 연속 슬롯으로 병합(coalesce)하는 로직을 추가 — 이걸로
  완전히 해결됨. (텍스트 포인터는 바이트 단위 읽기라 4바이트 정렬이 필요
  없어서 안전함 — `_replace_direct_pointer`/`_replace_embedded_pointer` 확인.)
  수정 후 608개 포인터 전부 왕복 디코딩 검증 통과(불일치 0건), self-check로
  movw/movt 비트 인코딩 역산 공식도 별도 검증함.
- **`texture.py`의 기존 32bpp BGRA→RGBA 스왑 누락 버그**(이전 세션에서 발견)도
  포함해서 전부 반영됨.

**배포 절차**: (1) 배포 대상 1774개 파일 전체를 스크래치 폴더에 백업 후
SHA256으로 라이브 설치본과 완전 일치 확인 → (2) 재컴파일 출력을 라이브
설치본에 덮어쓰기 → (3) 배포 후 각 파일 SHA256을 재컴파일 출력과 다시 대조
(불일치 0건) → (4) `VITA3K_NEW`로 부팅, 실제 화면 캡처로 확인. 필드명
("허상의 정박지"), 로비 메뉴 버튼("혼자하기"/"함께하기"/"파티원 모집"/
"이동하기", eboot.bin 반영분), 스토리 컷씬 텍스트("이멘마하" 지역 설명),
무비 자막("나는 15분 정도...") 전부 한글로 정상 렌더링 확인. 로그에 크래시나
치명적 오류 없음(범용 Vita3K 경고만 존재).

**남은 것**: (1) TGA 66개 중 미번역 상태로 남겨둔 것들(원본 그대로 유지가
맞는지 재검토는 선택 사항, 급하지 않음), (2) `.xdelta`로 재배포 가능한
패치 파일 만들기(원본 패킹된 `.l7c` 대상 — 이번 배포는 이미 압축 해제된
Vita3K 설치본에 직접 덮어쓴 것이라, 다른 사용자용 배포 패치와는 별개 작업),
(3) 전체 플레이스루로 번역 품질/컨텍스트 오류 QA(지금까지는 스팟 체크
위주).
```

---

## 추가 진행 (2026-08-09, 실제 플레이 QA)

사용자가 "직접 플레이해보면서 번역 품질 QA 시작해줘"라고 요청해서, PowerShell
`SendKeys`로 Vita3K 창에 키 입력을 보내고(방향키/X=취소/C=확인/Enter=Start,
`config.yml`의 `keyboard-button-*` SDL scancode 매핑 참고) 주기적으로
스크린샷을 찍어 실제로 플레이하며 QA를 진행했습니다.

### 🔴 발견 1(치명적, 수정 완료): 게임 덤프 자체의 대규모 파일 누락

플레이 중 상점 NPC와 대화하려 하자 검은 화면에서 멈췄고(FPS 카운터가 고정),
로그에 `_Data/Script/Twn/00/00/711.dat` 파일을 1초 간격으로 무한 재시도하며
`Missing file`을 반복 출력하는 게 확인됨(사용자가 실시간으로 "검은 화면으로
계속 로딩중"이라고 확인해줌). **이건 우리 번역 작업이나 배포와 무관하게
원래(패치 전) 백업본에도 이미 없던 파일** — 즉 이 Vita3K 설치의 loose
`_Data/` 추출 자체가 처음부터 불완전했던 것으로 확인됨.

`toidata_release.l7c`(원본 패킹 아카이브)를 직접 열어 L7CA 포맷을 파싱해서
(`tools/toir/l7ca_patch_multi.py`에 이미 문서화된 헤더/엔트리/청크 구조
재사용) `_Data/Script/` 전체를 아카이브 목록과 로컬 설치본으로 대조한 결과
**966개 중 435개(45%!)가 로컬에 아예 없었음**. 청크 압축 플래그가 켜진
169개는 `taikotools/psvita-l7ctool/psvita-l7ctool/TaikoCompression.cs`의
커스텀 LZ 압축(zlib 아님, 자체 포맷)을 Python으로 그대로 이식해서
(`tools/toir`에는 아직 없음 — 이번 세션 스크래치에서만 작성, 필요하면
정식으로 프로젝트에 포함시킬 것) 압축 해제. 아카이브에 저장된 CRC32와
전부 대조해 바이트 단위로 정확함을 확인 후 로컬 설치본에 채워 넣음.

이어서 **`_Data/` 전체(14,076개 엔트리)를 아카이브 목록과 대조 → 0개 누락**
확인. 즉 Script뿐 아니라 전체 카테고리에 걸쳐 있던 광범위한 추출 누락을
한 번에 해결함. 이 수정은 **번역과 무관하게, 이 Vita3K 설치로 플레이하는
모든 사용자에게 영향을 주는 진행 불가(소프트락) 버그**였으므로 QA를 계속
진행하기 위해 즉시 고쳤습니다. **재사용 가치가 커서(다른 사람이 같은 방식으로
Vita3K에 설치한 환경도 똑같이 이 문제를 겪을 가능성이 높음) 이 L7CA
리더+TaikoCompression 포트를 `tools/toir/`에 정식 스크립트로 정리해서
커밋하는 걸 다음 세션 우선순위로 추천합니다.**

### 🟠 발견 2(중요, 미수정): 특정 한글 글자가 "었"으로 잘못 렌더링되는 폰트 버그

플레이 중 두 번 확인됨:
- 오프닝 무비 자막(MovieCaption_019.srt): "지상계의 영혼을 **훔치는** 데
  질려서 / 이번엔 내 목숨을 **훔치러** 왔나?"가 화면에는 "지상계의 영혼을
  **었치는** 데 질려서 / 이번엔 내 목숨을 **었치러** 왔나?"로 표시됨.
  `.dat` 파일의 실제 바이트를 직접 까본 결과 UTF-8 인코딩은 **완전히
  정확했음**(`훔`=`ed9b94` 정상) — 즉 번역 데이터는 멀쩡한데 화면에만
  잘못 그려짐.
- Story.csv 리로드 프롬프트: "이벤트를 **스킵**하시겠습니까?"가 "이벤트를
  **스었**하시겠습니까?"로 표시됨. 소스 CSV에는 "스킵"이 정확히 들어있음.

두 사례 모두 특정 글자(훔, 킵)가 "었"으로 대체되는 동일한 패턴이고, 각각
다른 렌더링 경로(무비 자막 오버레이 vs 필드 대화창)에서 재현되므로 —
**번역 텍스트나 재컴파일 파이프라인 문제가 아니라, 이전 세션에서 검증한
한글 폰트 인젝션(글리프 테이블/인덱스 매핑) 쪽의 버그로 추정됩니다.**
아직 원인을 못 찾았고, 다른 어떤 글자들이 더 영향받는지도 전수 조사
안 됐습니다. **다음 세션 우선순위 후보** — 폰트 인젝션 코드(글리프 테이블,
`magic=21`, `shadow_skip` 필드 등)를 다시 열어서 확인할 것. Vita3K
에뮬레이션 특유의 문제일 가능성도 배제할 수 없으니, 가능하면 실기에서도
동일 현상이 재현되는지 확인해보면 좋음.

### 🟢 그 외 확인한 내용 (특이사항 없음)

오프닝 무비 자막, 가족 대화(Story.csv), 스킷 대화(Skit.csv), 튜토리얼
팝업(TutorialData.csv — 파일명 오타 수정 후 정상 작동 확인), 전투 시스템
(몬스터 이름 "오타오타"/"바사바사" 등 EnemyParam_Names.csv 정상 표시),
필드명 배너("풀피의 숲" — Field/Scene TGA 번역 정상 표시), 필드 NPC 대화
등을 폭넓게 확인. 문법·어투·용어 일관성 전부 자연스러움. 한 줄("기억진에
올라서면 필드가 아닌 곳에서도 세이브를 할 수 있습니다")은 뜻이 살짝
애매하게 느껴져서 원문 대조 재검토 후보로 남겨둠(확정 오류는 아님).

**아이템/스킬/장비 메뉴 화면은 이번 세션엔 확인 못 함** — 키보드 입력으로
필드 일시정지 메뉴를 여는 버튼을 못 찾음(Enter는 필드에서 확인/이벤트
트리거로 동작, Select/Triangle도 메뉴를 안 열었음). 다음 세션에서 이
부분(ItemDataPack 27만 줄, ArtsDataPack 등 대용량 번역 파일들의 실제
게임 내 표시)을 마저 확인할 것.

### 추가 진행 (같은 날, 이어서): 메뉴 여는 법 확인 + "었" 치환/숫자 겹침 버그 원인 파악

**메뉴 여는 법: Triangle 버튼(키보드 `V`)** — Vita3K 자체 UI가 실수로 열리면서
정확한 키 매핑표를 보여줘서 확인함(`config.yml`의 `keyboard-button-*`와 일치):
Square=Z, Cross=X, Circle=C, Triangle=V, Start=Enter, Select=RShift, L1=Q, R1=E.
Triangle으로 메인 메뉴(술기/아이템/장비/전투설정/스테이터스/라이브러리/시스템)가
정상적으로 열리고, X=확인/진입, C(Circle)=뒤로가기로 확인됨. 아이템 메뉴 진입
결과 "귀중품" 탭에 배틀북/몬스터 도감이 정상 표시되고, 스테이터스 화면에
"무기: 에디의 검", "칭호: 상가의 아들" 등 ItemDataPack 번역도 정상 표시됨
(단, Weapon/Consumable 등 나머지 탭은 아직 미확인 — L/R로 탭 전환 가능해
보이니 다음 세션에서 마저 확인).

**"었" 치환 버그가 숫자에도 나타남 — 사용자가 실시간으로 재확인.** 스테이터스
화면에서 "물공"/"물방" 스탯 수치가 "6701"처럼 두 자릿수가 겹쳐서 표시되는 걸
확인(확대 스크린샷으로 자릿수가 같은 x좌표에 겹쳐 그려지는 것까지 확인). HP/TP
숫자("240/240")는 멀쩡한데 스탯 패널 숫자만 깨짐 — 아마 HP/TP는 전용
게이지/텍스처로 그리고, 스탯 패널은 일반 폰트 렌더링 경로를 타서 그런 듯.
**중요: 이 스탯 값은 `CharaParamDataPack.dat`에서 나오는데, 이 파일은 우리
recompile 파이프라인이 전혀 건드리지 않는(원본 그대로 복사되는) 파일임.**
즉 이 숫자 겹침도, "훔"→"었"/"킵"→"었" 치환도 **우리 번역 데이터나
재컴파일 결과물의 문제가 아님이 재차 확인됨.**

**근본 원인 재조사 결과 — 이건 우리 쪽에서 고칠 수 있는 버그가 아닐 가능성이
높습니다.** `tools/toir/font_research/README.md`를 다시 확인한 결과, 이
프로젝트의 "한글 폰트 적용"은 자체 글리프 데이터를 넣는 게 아니라 **게임이
`sceFontFindOptimumFont` 호출 시 넘기는 `fontLanguage` 필드를 1(일본어)→
3(한국어)로 패치해서, PS Vita 시스템에 이미 내장된 한글 폰트를 그대로
불러 쓰게 만드는 방식**입니다 — 즉 글자를 실제로 그리는 건 100% 에뮬레이터
(또는 실기)의 폰트 렌더링 모듈이고, 우리 프로젝트는 폰트 데이터를 전혀
공급하지 않습니다. 이 Vita3K 설치의 `sa0/data/font/`를 직접 확인해보니
같은 `kr0`에 대해 **서로 다른 두 포맷의 파일이 공존**함을 발견했습니다:
`pvf/kr0.pvf`(OTTO 매직, OpenType/CFF — 이전 세션에서 11,172자 커버 확인한
바로 그 파일)와 `pgf/kr0.pgf`(PGF0 매직, PSP 시절부터 쓰인 별개의 비트맵
글리프 포맷 — README가 말하는 "ScePgf(SceLibFont) API"의 "Pgf"가 바로 이
포맷). 스탯 패널처럼 일반 UI 텍스트를 그릴 때 게임이 어느 쪽 포맷을 쓰는지에
따라 Vita3K의 서로 다른(그리고 완성도가 다를 수 있는) 렌더링 경로를 타게
되고, 숫자 겹침·글자 치환 둘 다 이 경로의 글리프 전진폭(advance width)/
인덱스 계산 버그로 설명이 됩니다.

**결론 및 다음 세션 권장 방향**:
1. 이 버그들은 십중팔구 **Vita3K 에뮬레이터 자체의 PGF/PVF 렌더링 구현
   버그**이지, 우리 번역·재컴파일·언어 코드 패치의 문제가 아닙니다. 우리가
   가진 건 컴파일된 `Vita3K.exe` 바이너리뿐이라 소스 수정으로 직접 고칠
   수는 없습니다.
2. **실기(진짜 PS Vita)에서도 재현되는지가 핵심 미해결 질문입니다.** 소니의
   실제 폰트 렌더러는 훨씬 성숙하니 재현 안 될 가능성이 높지만, 확인 전까지는
   단정 불가.
3. 당장 할 수 있는 것: (a) Vita3K 자체에 이 버그가 이미 보고돼 있는지
   GitHub 이슈 확인, (b) 최신 Vita3K 빌드로 재현되는지 확인(이 `VITA3K_NEW`가
   최신인지 먼저 확인), (c) 실기 테스트 기회가 생기면 최우선으로 확인.
4. 사용자에게 배포할 때는 "일부 숫자/특정 글자가 에뮬레이터에서 깨져
   보일 수 있음(실기 미확인)"이라는 알려진 이슈로 문서화해두는 게 안전.

---

## 추가 진행 (같은 날, Android 배포 트러블슈팅 — 최종 해결)

사용자가 Vita3K Android에서 패치를 적용하려다 겪은 문제를 여러 라운드에
걸쳐 진단한 끝에 **완전히 해결**했습니다. 결론부터: **"파일 직접 교체" 방식
자체는 맞았는데, `toidata_release.l7c`를 안 건드려도 된다고 했던 판단이
틀렸습니다.**

### 근본 원인: 순수 원본 `toidata_release.l7c`를 열면 Vita3K가 조용히 멈춤

여러 라운드의 잘못된 가설(설치 목록 문제, eboot.bin 포맷 불일치, sce_pfs
DRM 검증, sce_sys/package 잔여 파일 등)을 하나씩 제거하며 원인을 좁힌 끝에,
**이 Windows PC에서 직접 재현**해서 확인했습니다:

- 완전히 손대지 않은 순수 원본 `toidata_release.l7c`(1,475,397,012바이트)를
  설치 폴더에 두고 부팅하면, 게임이 `app0:/toidata_release.l7c`를 여는
  시점(`sceIoOpen`)에서 **에러 없이, 그냥 CPU만 계속 쓰면서(멀티스레드로
  3분 이상 CPU 시간 누적) 영원히 멈춥니다.** 로그도 그 지점에서 더 안
  진행됨.
- 반면 `tools/toir/l7ca_patch_multi.py`(또는 `build_install_zip.py`가 쓰는
  같은 패치 로직)를 **한 번이라도 거친** `toidata_release.l7c`는 크기가
  거의 같든(예: 882KB만 커진 예전 세션의 폰트 테스트용 패치본) 완전히
  다르든(1773개 파일 다 바꾼 1.78GB 패치본) **상관없이 정상적으로 통과**해서
  즉시 `_Data/...` 파일들을 정상적으로 열기 시작함.
- `sce_pfs/`(DRM 무결성 검증 폴더) 유무, `sce_sys/package/`(설치 임시 파일)
  유무는 **둘 다 원인이 아님을 직접 제거해보고 확인**함(제거해도 여전히
  멈춤). l7c 파일 자체를 우리 패치 도구로 한 번 통과시키느냐가 유일한
  변수였음.

**이게 그동안 Android에서 "파일은 분명 정확히 교체됐는데 여전히 일본어로
나온다"고 보였던 증상의 진짜 원인이었습니다.** 실제로는 번역이 반영 안 된
게 아니라 **화면이 멈춘 채 마지막으로 렌더링된 프레임(일본어 인트로/타이틀)이
남아있었던 것**입니다. Android 쪽엔 크래시 로그가 안 남거나 사용자가 못
찾았지만, Windows에서 로그로 명확히 재현·확인함.

### 최종 해결책 (검증 완료)

**"파일 직접 교체" 방식(이미 설치된 폴더 위에 덮어쓰기)에서, `_Data`/
`eboot.bin`뿐 아니라 `toidata_release.l7c`도 반드시 우리가 만든 패치본
(`3_patched/ready_to_use/toidata_release_ko.l7c`, 또는 이번 세션에서
재생성한 스크래치의 `toidata_release_patched.l7c` — 1773개 파일 전부
반영, 1.78GB)으로 함께 교체해야 합니다.** 사용자의 Android 기기에서 SD카드를
PC에 연결해 이 파일까지 교체(체크섬 검증 완료)한 뒤 **실행 확인 완료 — 한글
정상 출력됨.**

### 별개로 남은 제약: zip 기반 "새로 설치" 방식은 막혀 있음

이번 진단 과정에서 `방법 D`(`build_install_zip.py`로 만든 설치용 zip)가
Windows/Android 둘 다에서 실패하는 **별개의 원인**도 확인했습니다: Vita3K의
"Install Archive" 설치 과정은 `sce_pfs/`(PS Vita 패키지 무결성/DRM 검증
시스템, `files.db`+`unicv.db` 해시 트리)로 각 파일 크기를 원본 패키지
매니페스트와 대조하고, **크기가 다르면(`File toidata_release.l7c size
incorrect`) 방금 압축 해제한 데이터를 전부 삭제하고 설치를 실패 처리**합니다
(`[E] [is_nonpdrm]: NoNpDrm installation failed, deleting data!` 로그로
직접 확인). 우리 패치본은 원본보다 300MB 커서 이 검증에 걸립니다.

이건 "파일 직접 교체" 방식(설치 완료 후 파일만 바꿔치기, 위의 최종
해결책)에서는 전혀 문제되지 않습니다 — 이 검증은 **설치 시점에만** 동작하고,
이미 설치된 게임의 파일을 나중에 바꾸는 건 재검증하지 않기 때문입니다. 즉
**"방법 D(새 zip 설치)"는 당분간 포기하고, "파일 직접 교체 + l7c 포함" 방식을
공식 배포 방법으로 삼는 게 맞습니다.** `PATCH_적용법.md`를 이 방향으로
갱신할 것(방법 D는 "알려진 제약"으로 남겨두거나 제거 검토).

### 참고로 확인된 부수 사실

- Windows에 있는 이 `VITA3K_NEW` 빌드는 **암호화된 SELF/suprx를 직접
  복호화하는 기능이 없습니다**(`SELF ... is corrupt or encrypted.
  Decryption is not yet supported.`). 원본 zip을 Python 등으로 그냥 풀어서
  `-r` 직접 부팅하면 이 에러가 남 — 대신 Vita3K 자체의 "Install Archive"
  GUI 기능은 내부적으로 복호화를 처리함(설치 과정 중 로그에
  `install_archive_content: Extracting ...` 로 확인). 즉 이 빌드로 원본
  게임을 실행 가능한 상태로 만들려면 반드시 GUI의 정식 설치 기능을 거쳐야
  하고, 그 결과로 나온 (이미 복호화된) `eboot.bin`/`sce_module/*.suprx`를
  이후 다른 설치에도 재사용할 수 있음(이번 세션 내내 이렇게 재사용해왔음).
- Android Vita3K 빌드는 이 복호화 기능이 있는 것으로 보임(원본 zip이 바로
  설치·실행됨) — 플랫폼별로 구현 완성도가 다름.

이 모든 트러블슈팅 세션에서 배운 내용을 요약하면: **이 프로젝트의 배포
파이프라인에서 `toidata_release.l7c`는 (a) 반드시 우리 패치 도구를 거쳐야
하고(순수 원본은 부팅 시 멈춤), (b) 이미 설치된 폴더에 직접 덮어써야 하며
(zip으로 새로 설치하면 크기 불일치로 DRM 검증에 걸려 거부됨)**. 다음
세션에서 다른 사용자용 배포 절차를 다시 정리할 때 이 두 제약을 전제로
할 것.

---

## 추가 진행 (같은 날, xdelta 배포판 제작 중 발견한 마지막 퍼즐 조각)

사용자가 "xdelta 형식으로 배포판을 만들어달라(원본 게임 파일 재배포는
저작권 문제)"고 요청해서, `_Data` loose 파일 전체(2GB)를 직접 배포하는
대신 **사용자가 자기 원본으로 직접 압축 해제하는 방식**으로 전환하는
과정에서, 지금까지 몰랐던 중요한 사실 두 가지를 더 발견·해결했습니다.

### 1. `l7ca_unpack.py`(전체 압축 해제 도구) 신규 작성 + Taiko 압축 해제 버그 수정

기존 `l7ca_extract_missing.py`(누락분만 채우는 도구)를 확장해 아카이브
전체를 loose 파일로 푸는 `tools/toir/l7ca_unpack.py`를 새로 작성했습니다.
처음 전체(14,076개 엔트리) 압축 해제를 시도했을 때 두 가지 버그를 발견·수정:

- **멀티 청크 파일 처리 누락**: 큰 파일(`_Data/Battle/Effect/*.pck` 등,
  1,740개)은 여러 개의 독립적인 Taiko 압축 스트림(청크)으로 나뉘어
  저장되는데, 기존 코드는 전체를 한 번에 `taiko_decompress()`에 넣어서
  **첫 번째 청크의 종료 마커에서 조기 종료**했습니다. `chunk_count`만큼
  반복하며 `taiko_decompress(chunk_data, prev=누적된_출력)`으로 체이닝하도록
  수정(`prev` 파라미터는 원래 이런 용도로 있었던 것).
- **`taiko_decompress()` 자체의 이식 버그**: C# 원본(`TaikoCompression.cs`)의
  뒤로 참조(back-reference) 복사 루프 2곳(`c > 0x7f`, `c > 0x3f` 분기)에는
  없는 `if i > end: ...` 조건문을 포팅 과정에서 실수로 추가해뒀었습니다.
  원본과 다르게 동작하는 이 조건이 `back < length`인(복사 중인 데이터를
  다시 참조하는, RLE 패턴) 특정 입력에서 잘못된 바이트를 생성함(35개 파일,
  전부 `NaviMapInfo_*.dat` — 텍스트 아닌 미니맵 메타데이터라 지금까지
  아무도 못 알아챔). 조건문 제거로 해결, 순수 원본 l7c와 우리 패치 l7c
  양쪽에서 이 파일들을 다시 풀어 SHA256 완전 일치 확인함. (참고로 기존에
  "검증된" 설치본에 있던 이 파일들의 사본은 오히려 **틀린 데이터**였음 —
  아마 더 예전의, 이번과 다른 도구로 추출된 것으로 추정. 텍스트가 아니라
  게임플레이엔 영향 없었음.)
- 최종적으로 14,076개 엔트리 전부(비압축 + 단일청크압축 + 멀티청크압축)
  CRC32 검증 통과. `l7ca_extract_missing.py`에도 같은 두 수정 적용해둠.

### 2. 🔴 결정적 발견: 게임 자체 내장 폰트 파일(`_Data/Font/FontDataNormal.pgf`)이
   번역 패키지에서 통째로 빠져 있었음

`l7ca_unpack.py`로 방금 패치한 `.l7c`를 순수하게 풀어서(기존에 누적되어
있던 `_Data` 폴더를 재사용하지 않고) 완전히 새로 부팅 테스트를 해보니,
**한글이 전부 대시(`-`) 기호로만 표시됨**을 발견(사용자가 실시간으로도
확인: "한글이 안나오네"). `eboot.bin`은 체크섬까지 완전히 일치했는데도
재현됨 — 재시작해도 동일.

파일 단위로 원인을 추적한 결과: 예전 세션(이번 멀티세션 작업의 훨씬 이전
어느 시점)에 실제로 **`_Data/Font/FontDataNormal.pgf`(게임이 자체적으로
내장하고 있는 PGF 폰트 파일) 안에 한글 글리프를 직접 주입하는 작업**을
이미 해뒀었고(`.orig_backup` 파일로 원본이 남아있었음: 2,014,712바이트 →
패치 후 2,229,043바이트, +214KB), 그 결과물이 **오직 로컬 Vita3K 설치
폴더 안에만** 존재했지 **한 번도 우리 recompile 파이프라인(`3_patched_full`)
결과물에 포함된 적이 없었습니다.** 즉 지금까지 이 세션에서 만든 모든
"완성된 패치본"(`.l7c`, `ready_to_use/`, `loose_files/`, xdelta 등)에
이 폰트 파일이 빠져 있었던 것 — 그동안 실기 테스트가 잘 됐던 건 전부
이 폰트가 이미 심어져 있던 로컬 설치 폴더를 계속 재사용했기 때문이었습니다.

**해결**: `_Data/Font/FontDataNormal.pgf`(패치된 버전)를 `3_patched_full`에
추가해서 `l7ca_patch_multi.py`로 `.l7c`를 다시 패치(1774개 파일 = 기존
1773 + 이 폰트 파일 1개), xdelta도 재생성, 완전히 새로운 조합(원본 zip
재추출 + eboot.bin/sce_module만 재사용 + 이 새 `.l7c` + `l7ca_unpack.py`로
새로 압축 해제한 `_Data`)으로 재부팅해서 **"에디" 등 정상적으로 한글
표시됨을 확인.**

이 폰트 인젝션 작업(PGF 포맷 내부의 glyph 테이블에 새 항목을 추가하는
바이너리 조작)을 **처음부터 재현하는 코드는 이번 세션엔 찾지 못했습니다**
(결과물인 패치된 `.pgf` 파일만 로컬에 남아있었음). 다음 세션에서 시간이
되면: (1) 이 작업을 수행한 스크립트가 프로젝트 어딘가에 남아있는지
찾아보고 `tools/`에 정식으로 편입, (2) 못 찾으면 최소한 이 패치된
`FontDataNormal.pgf` 파일 자체를 안전하게(예: git-lfs 등) 보관해서
다시 잃어버리지 않도록 할 것. 지금은 `3_patched_full/_Data/Font/`,
`3_patched/ready_to_use/`, `3_patched/loose_files/_Data/Font/`,
그리고 재생성된 `.l7c`/xdelta 안에 반영돼 있음.

### 3. 🔴 같은 클래스의 버그, `eboot.bin`에도 있었음: xdelta 소스가 사실
   이미 1바이트 패치된 상태였음

사용자가 xdelta 배포판(`eboot_ko.xdelta`)을 **진짜 원본** `eboot.bin`
(Vita3K로 원본 zip을 방금 새로 설치해서 얻은, 한 번도 손 안 댄 파일)에
적용하자 `xdelta3: target window checksum mismatch` 에러가 남
(`toidata_release.l7c` 쪽은 문제없이 적용됨). 사용자가 그 "진짜 원본"
`eboot.bin`을 `0_01/PCSG00009/`에 복사해준 걸 바이트 단위로 대조한 결과:

**우리가 이번 세션 내내 "원본"으로 취급해온 `eboot.bin`
(`live_backup/eboot.bin`, sha256 `322ef1f3...`)은 사실 진짜 원본이
아니라, 오프셋 `0x78E2` 딱 1바이트가 이미 패치돼 있던 것이었습니다** —
`5F F0 01 0E`(`movs.w lr, #1` = SCE_FONT_LANGUAGE_JAPANESE) →
`5F F0 03 0E`(`movs.w lr, #3` = SCE_FONT_LANGUAGE_KOREAN). 바로
`tools/toir/font_research/README.md`에 문서화된 그 언어 코드 패치
자체입니다(ELF 기준 오프셋 `0x68E0` + `eboot.bin`의 `0x1000`바이트
헤더 = `0x78E0`, 오차 2바이트는 헤더 크기 가정 차이로 추정). 즉
**`_Data/Font/FontDataNormal.pgf`와 완전히 같은 클래스의 문제** —
예전(이번 멀티세션보다 훨씬 전) 세션에서 이미 언어 코드 패치까지
적용해둔 로컬 파일을, 이번 세션 내내 "원본"인 줄 알고 그 위에 텍스트
번역만 얹어서 작업해온 것.

**해결**: 사용자가 준 진짜 원본 `eboot.bin`에 (1) `0x78E2` 1바이트
패치(`01`→`03`) + (2) `recompile_eboot()`로 텍스트 번역을 적용한 뒤,
608개 포인터 전부 왕복 검증 통과 확인. 이 진짜 원본을 기준으로
`eboot_ko.xdelta`를 다시 만들어서 `3_patched/xdelta_release/`,
`ready_to_use/`, `loose_files/`에 반영. **사용자에게 새 `eboot_ko.xdelta`를
전달하고 재시도를 기다리는 중** — 다음 세션에서 성공 여부부터 확인할 것.

**교훈**: 이 프로젝트에서 "로컬 Vita3K 설치 폴더에 있는 파일 = 원본"이라는
가정은 위험합니다 — `_Data/Font/FontDataNormal.pgf`도, `eboot.bin`도 둘 다
이번 세션 훨씬 이전의 미기록 작업으로 이미 손이 타 있었습니다. 앞으로
"원본"이 필요할 때는 **반드시 사용자의 원본 덤프 zip(`0_00/PCSG00009.zip`
등)에서 새로 뽑거나, Vita3K로 그 zip을 새로 설치해서 얻은 파일**을 써야
하며, 로컬에 이미 있는 설치 폴더의 파일을 무비판적으로 "원본"으로
재사용하면 안 됨.

---

## 추가 진행 (2026-08-09~2026-08-11): 숫자 오표기 버그 — 원인 확정 및 수정 완료

**이 섹션은 지금까지 이 파일에 전혀 기록되지 않았던, 별도로 진행되던 조사 스레드였습니다** (위 "실제 플레이 QA" 항목의 "🟠 발견 2"—폰트 렌더링의 "었" 치환 버그—와는 다른, 별개 버그). `tools/toir/font_research/number_bug/README.md`에 총 78회차 분량으로 자세히 기록돼 있고, **2026-08-11에 완전히 해결됨.**

**증상**: 스테이터스 화면 등에서 스탯 수치가 완전히 다른 값으로 표시됨(경험치 0→01, 술방 26→45 등)과, 자릿수가 겹쳐 보이는 문제(예: "6701") — **이 둘은 결국 같은 원인의 두 증상이었음이 최종 확인됨.**

**확정된 원인**: `2_translated/eboot.csv`의 `0x8115F2B8` 행 — 게임이 숫자를 그릴 때 참조하는 "숫자 문자 룩업 테이블" 문자열이 원문은 전각(全角) `０１２３４５６７８９－`(글자당 UTF-8 3바이트)인데, 번역판에서 반각 `0123456789-`(글자당 1바이트)로 바뀌어 있었음. 글자 개수(11개)가 같아서 기존 검증(태그 누락·포맷지정자 검사)으로는 못 잡았지만, 숫자 렌더링 코드가 이 테이블을 고정 3바이트 간격으로 인덱싱하는 구조라 폭이 줄어들자 자릿수 0 이후 전부 엉뚱한 바이트를 읽게 됐던 것 — 이게 애초에 프로젝트 최초 증상이었던 "에디의 검 23개" 버그의 정체이기도 함. 격리 환경에서 `recompile_eboot`가 만드는 슬롯 배치를 자동 이분탐색(9라운드)해서 정확히 이 한 줄로 좁힘.

**수정**: `2_translated/eboot.csv`의 해당 줄 Korean 컬럼을 원문(전각 숫자)과 동일하게 되돌림 — 숫자는 언어 중립적이라 번역이 필요 없었음. `3_patched/ready_to_use/eboot.bin`, `3_patched/loose_files/eboot.bin`, `3_patched/eboot_ko.xdelta`, `3_patched/xdelta_release/eboot_ko.xdelta` 전부 이 수정으로 재생성·재검증 완료(xdelta는 순정 원본→언어코드 패치→수정된 텍스트패치 순으로 재구성, 왕복 검증 통과). 격리 환경에서 스탯(물공30/물방30/술공13/술방26/경험치0, 전부 정답과 일치)과 "에디의 검"(개수 1, 정상) 둘 다 확인 완료 — 사용자가 실시간으로 "숫자 정상이야!!!"로 재확인.

**2026-08-11 후속: 라이브 설치에 실제로 배포 완료.** `eboot.bin`을 순정 원본
+ 언어코드 패치 + 수정된 텍스트 번역으로 교체(레벨버그 ARM 패치 3곳은 근본
원인이 해결됐으므로 이 배포에서 **제거**함). 배포 전 `eboot.bin` 해시
`51581969f3fb3e9e4124266fe72df431dabaf8c04f01c3025e0f6b4445a28365` → 배포 후
`34772ccb8fe744e2ce9ebd05ed356f9d1acb85b43ab2bfd99bf6b8be041a4495`. 배포 전
파일은 `eboot_backups/eboot.bin.pre_numberfix_<timestamp>`에 백업됨.

**아직 남은 작업**:
1. **행운(幸運) 별 개수가 매 테스트마다 다르게 표시되는 별개 문제**(정답 3/5인데 관찰값이 3→4→1→3→2 등으로 들쭉날쭉, 이 CSV 수정과 무관하게 발생) — 미해결, 다음 세션 후보.
2. `toidata_release_ko.xdelta`(l7c 기반 배포판)는 이번 eboot 수정과 무관하지만, 아래 "미표시 글자"/"아이콘" 수정으로 `_Data` 쪽 파일 3개가 바뀌었으므로 이제 그쪽도 재생성 필요(항목 참고).

**격리 환경 관련 재확인 사항**(Android 트러블슈팅 절에서 이미 알려진 것과 동일): 격리 테스트 환경에 패치된 `eboot.bin`을 넣을 땐 반드시 짝이 맞는 패치된 `toidata_release.l7c`도 함께 넣어야 함 — 안 그러면 부팅이 `sceIoOpen` 시점에서 조용히 영원히 멈춤(예외 없음, 겉보기엔 크래시처럼 보임). 이번 조사에서 새로 확립한 방법론(raw-byte diff 이분탐색은 인위적 손상을 만들어 무효, 실제 `recompile_eboot` 로직을 재사용한 부분 번역 빌드만 유효 — `number_bug/bisect_recompile.py` 참고)은 향후 비슷한 유형의 버그 조사에 재사용 가치 높음.

---

## 추가 진행 (2026-08-11, 같은 날 이어서): 조작 아이콘 문자 노출 버그 + 미표시 글자("_") 2자 추가

사용자가 "조작법 설명 시 아이콘이 아닌 dpad_da 등의 글자가 그대로 출력되는
문제"와 "킵/훔 등의 글자가 _로 미표시되는 문제"를 요청해서 둘 다 조사·수정함.

### 1. 아이콘 컨트롤 코드 디코더 버그 (수정 완료)

`tools/toir/toir/text.py`의 `decode_control_code()`에서 버튼(코드 0x41)은
`{remap_square}`처럼 중괄호로 감싸서 디코드하는데, **아이콘(코드 0x40)은
중괄호 없이 `dpad_r` 같은 맨 단어로 디코드**하는 비대칭 버그가 있었음. 그
결과 원본 일본어 CSV 자체(`TutorialData.csv`/`BattleBookDataPack.csv`)에
`dpad_du{variable}` 같은 중괄호 없는 텍스트가 그대로 들어가 있었고, 번역자가
이를 리터럴 텍스트로 착각해 그대로 보존 → 재컴파일 시 `encode_text_fixed`가
중괄호 없는 텍스트를 컨트롤 코드로 인식 못 해서 아이콘 대신 "dpad_du" 같은
글자 그대로가 게임에 박힘.

부수적으로 `_ICONS_40` 딕셔너리에 **중복 키**(`0x0d06`가 두 번 정의돼
`'dpad_lr'`이 죽은 코드가 되고 `'dpad_du'`만 살아남음)도 있었음 — 원본
데이터를 스캔해서 `0x0e06`가 실제로 쓰이는 걸 확인하고 `dpad_lr`을 그
자리로 재배정.

**수정**: `text.py` 두 줄 수정(중괄호 추가, 중복키 분리) + 영향받는
`2_translated/TutorialData.csv`(12행)·`BattleBookDataPack.csv`(31행)의
맨 단어 태그를 전부 중괄호로 감싸는 일괄 치환(연속 태그 "dpad_ldpad_r" 같은
붙어있는 경우도 최长 길이 우선 매칭으로 정확히 분리 확인). 재컴파일 후 두
`.dat` 결과물에 리터럴 "dpad"/"icon_" 텍스트가 0건임을 확인, 아이콘
컨트롤 코드(`@\x40@`)가 정상적으로 존재함을 바이너리 레벨로 확인.
라이브 설치 + `3_patched/loose_files`에 배포 완료.

### 2. 미표시 글자("_") — 233자 중 6자(훔, 킵, 링, 콜, 톤, 렐)까지 확장 배포, **6이 절대 상한선으로 확정**

자세한 내용은 `tools/toir/font_research/hangul_fix/SESSION_2026-08-11.md` 참고.
요약: 08-09 세션이 이미 "새 glyph를 많이 추가하면 Vita3K가 크래시한다"는
걸 발견해서 안전하게 "표시 안 함"으로 막아뒀던 상태였음. 이번 세션에 처음
훔·킵 2자만 추가했다가, 사용자가 "남은 글자도 채워달라"고 요청해서 격리
환경 이분탐색으로 임계값을 좁힘: 4·6개는 안전(각각 6분+/5분+ 오프닝 무비
전체 통과), 8개는 위험(1분 이내 크래시) — 6자(훔,킵,링,콜,톤,렐)까지
확장해서 배포. **사용자가 "크래시가 나긴 하지만 계속해달라"고 해서 경계를
마저 확인** → 누적 7개(6+밋 1개)로 테스트하니 23초 만에 크래시, 혹시
"밋"이라는 글자 고유의 문제인지 의심돼서 전혀 다른 글자(6+뚱)로 재시도 →
**역시 크래시, 그것도 정확히 동일한 메모리 주소**(`sceClibMemcpy
(0x83e43b18, 0x8330c300, 4294967288)`, `0x583e43af0`)에서 재현됨.

**확정 결론: 이건 어떤 글자를 고르냐와 무관한, "누적 새 glyph 개수 7부터"
걸리는 순수 개수 기반 하드 리밋.** 즉 **6이 이 `FontDataNormal.pgf` glyph
추가 방식으로 도달 가능한 절대적 상한선**이며, "몇 개씩 나눠서 반복하면
227자 전체를 언젠가 커버할 수 있다"는 이전 계획은 **폐기됨** — 몇 번에
나눠서 하든 최종 누적이 7을 넘는 순간 무조건 크래시함.

**남은 227자는 이 방법으로는 더 이상 진전 불가.** 완전히 다른 접근이
필요함(우선순위 순으로): (1) 더 최신 Vita3K 빌드에서 이 메모리 버그
자체가 재현되는지 확인(이번 세션 전체가 v0.2.0 3625-839e8d39 하나로만
테스트됨), (2) 실기(진짜 PS Vita)에서 테스트 기회가 있으면 확인(LLE
펌웨어 쪽 버그로 추정되므로 실기엔 없을 가능성 높음), (3) 시스템 한글
폰트(`kr0.pvf`, 이미 전체 커버리지 확인됨)로 이 233자에 한해 폴백시키는
게임 코드 레벨의 다른 해법이 있는지 Ghidra로 추가 조사. 셋 다 막히면
227자는 "표시 안 함"이 최종 상태 — 다만 이건 우리 번역/재컴파일 파이프라인
버그가 아니라 확인된 Vita3K 자체 버그라는 결론은 실증적으로(같은 주소,
다른 글자 2회 재현) 확정됐음.

### 이번에 바뀐 파일 3개로 인해 `toidata_release_ko.xdelta` 재생성 필요

`_Data/Battle/TutorialData.dat`, `_Data/System/BattleBookDataPack.dat`,
`_Data/Font/FontDataNormal.pgf` 이렇게 3개 파일이 바뀌었는데, 이건 전부
`toidata_release.l7c`(패킹된 아카이브) 안에 들어가는 파일들임. 라이브
설치(loose 파일 구조)와 `3_patched/loose_files`는 이번에 갱신했지만,
**`toidata_release_ko.xdelta`/`toidata_release_ko.l7c`는 아직 갱신 안 됨**
— 이 xdelta로 배포받는 사용자는 아직 이 두 수정을 못 받음. 다음 세션에서
`l7ca_patch_multi.py`로 이 3개 파일을 반영한 새 `.l7c`를 만들고 xdelta를
재생성할 것(대용량 작업이라 이번 세션엔 생략).

---

## 추가 진행 (2026-08-11, 같은 날 이어서): 지명 일관성 검증·수정 + 🔴 라이브 설치 파일 원인불명 되돌림 발견

사용자가 "번역된 대사 중 지명(풀피 숲/훌피 숲 혼용 등) 일관성을 검증하고
수정해달라"고 요청해서 `Locations.csv`(필드 진입 배너의 소스, 즉
실제 화면에 표시되는 "정답")를 기준으로 전체 `2_translated/**/*.csv`를
대조·수정함.

**발견·수정한 지명 불일치 5건**:
1. **훌피 숲 → 풀피의 숲**(원본 오타, `フルフィの森`): `Skit.csv` +
   `skit_wip/8000.dat.csv`/`8001.dat.csv` 5곳.
2. **풀피 숲(조사 없음) → 풀피의 숲**: `Story.csv`/`script_wip` 여러 곳
   (같은 지명인데 조사 유무가 혼용돼 있었음), `SkitNames.csv`의 기존
   표기("풀피의 숲 위치는")에 맞춰 통일.
3. **켈름 화산 → 케름 화산**(`ケルム火山`): 11개 파일, `GLOSSARY_KO.md`도
   같이 수정(기존에 "수정 완료"로 잘못 기록돼 있었음).
4. **레무레이스 습지 → 렘레스 습지**(`レムレース湿原`): 7개 파일,
   `GLOSSARY_KO.md`도 같이 수정.
5. **갈람 → 가람**(`ガラム`, **글로서리에 "수정 완료"라고 기록돼 있었지만
   실제로는 17개 파일·약 51곳에 미수정 상태로 남아있던 회귀**) +
   **기사도훈 → 사도훈**(같은 케이스, `Skit.csv`+`skit_wip/0015.dat.csv`).

방법론: 각 지명의 일본어 원문을 프로젝트 전체에서 grep해서 대응하는
한글 번역을 전부 뽑아 비교 → 발산 발견 시 `Locations.csv`(실제 배너
소스) 또는 이미 확립된 다수 표기를 기준으로 일괄 치환. 나머지 지명들
(나오스/테노스/갈포스/아시하라/마무트/트라이버스/칼립스/니겔/라벤델/
벤투스/칼디아/네코즈키 등)은 전수 확인 결과 이상 없음.

수정된 CSV로 `recompile_dat`+`recompile_skits`+`recompile_story`+
`recompile_map_data`를 전부 돌려서 실제 변경된 32개 `.dat` 파일을
특정(diff 기반) → 라이브 설치 + `3_patched/loose_files`에 배포·검증 완료.

### 🔴 중요 발견: 라이브 설치의 이전 수정분 3개가 원인불명으로 되돌아가 있었음

이번 재컴파일 전 라이브 설치 상태를 확인하던 중, **오늘 세션 초반에 분명히
배포했던 `TutorialData.dat`/`BattleBookDataPack.dat`(아이콘 태그 수정)와
`FontDataNormal.pgf`(한글 글리프 6자 확장)가 전부 원본 상태로 되돌아가
있었음**을 발견함(해시 대조로 확인). **`eboot.bin`(숫자버그 수정)은
멀쩡했음** — 즉 이 3개 파일만 선택적으로 되돌아간 것.

**원인을 특정하지 못함.** 이 세션은 그 사이 격리 테스트 환경
(`vanilla_test/`)만 반복 조작했고 라이브 경로는 건드리지 않았다고
판단했지만, 결과적으로 라이브의 이 3개 파일만 되돌아가 있었음. 가능성:
(a) 사용자가 직접 플레이/테스트하며 다른 백업을 복원했을 가능성,
(b) Vita3K 자체의 어떤 무결성 검증·복구 로직, (c) 이 세션이 인지하지
못한 어떤 조작. **재발 방지를 위해 다음 세션에서 배포 직후와 세션
종료 직전 두 번 해시를 반드시 재확인할 것을 권장.**

**조치**: 되돌아간 3개 파일을 전부 올바른 최종본으로 재배포하고
(`eboot_backups/pre_locationfix_<timestamp>/`에 되돌아간 상태 백업 보존),
이번 세션에서 만든 모든 수정사항(숫자버그/아이콘태그/폰트6자/지명일관성)
전체를 해시로 재검증 완료 — 현재 라이브 설치는 전부 정상 상태.

| 파일 | 최종 정상 해시 |
|---|---|
| `eboot.bin` | `34772ccb8fe744e2ce9ebd05ed356f9d1acb85b43ab2bfd99bf6b8be041a4495` |
| `_Data/Font/FontDataNormal.pgf` | `7f3393e756047a04a14fef78f8cab0eee28c0ee54a50dcd23285a256dcb85cbe` |
| `_Data/Battle/TutorialData.dat` | `5280a69ffa36197fa9d9f1041cd9719fd05fcb1a263384b7da0fc1b558b6e176` |
| `_Data/System/BattleBookDataPack.dat` | `bdc1ccbf2aa7092c7b9970e08b95fd808734db261afce22888332ccbcd74bc2e` |

---

## 추가 진행 (2026-08-11, 같은 날 이어서): 🔴 폰트 6자 확장은 실제로 크래시함(재확인) + 지명 배너 이미지(TGA) 6개 수정

### 폰트: "6자가 안전하다"는 판단이 틀렸음 — 실제 플레이로 크래시 재현·확정

배포 직후 사용자가 실제 라이브 설치로 플레이하다가, 오프닝 영상을 스킵하거나
특정 장면(휴프노스와 아스라의 결투 회상)에 도달하면 크래시가 재현됨을 보고함.
격리 환경 테스트(오프닝 초반 + 짧은 튜토리얼, 5~7분)는 이 지점까지 도달한 적이
없었던 것 — 검증 범위가 부족했음. **`FontDataNormal.pgf`를 글리프 추가가 전혀
없는 안전한 0자 상태(해시 `f143df54d163324ee990fa31e78dc20049430c75a1de3fab265a66361a4da64b`)로
되돌리자 사용자가 크래시 해결을 확인함.** 라이브 설치 + `3_patched/loose_files`
모두 이 0자 상태로 되돌려져 있음(6자 확장 시도는 폐기). 자세한 내용은
`tools/toir/font_research/hangul_fix/SESSION_2026-08-11.md`의 최신 항목 참고.
**다음에 이 폰트 글리프 확장을 다시 시도한다면, 오프닝뿐 아니라 이 결투
장면을 포함한 훨씬 깊은 실기 검증을 반드시 거칠 것.**

### 지명 배너 이미지(TGA) 6개 발견·수정

사용자가 "지명 일관성 검증에 이미지 파일도 포함해달라"고 요청. `_Data/Field/Scene/*_FieldName.tga`
(필드 진입 시 뜨는 지명 배너, 총 38개)를 전수 시각 검토해서 텍스트 기반
수정과 동일한 계열의 불일치 6건을 발견·수정:

| 배너 | 이전(잘못됨) | 이후(수정) |
|---|---|---|
| 1000 | 훌피 숲 | 풀피의 숲 |
| 1004 | 우수의 숲 | 근심의 숲 |
| 1009 | 카류프스 광산 | 칼립스 광산 |
| 1010 | 켈름 화산 | 케름 화산 |
| 1015 | 레무레이스 습지 | 렘레스 습지 |
| 1019 | 비행선 웬토스 | 비행선 벤투스 |

**방법론**: 생성 스크립트가 보존돼 있지 않아서(이 프로젝트에 반복되는 패턴)
픽셀 분석으로 역산함 — 배경은 완전 투명, 글자는 맑은 고딕 볼드(`malgunbd.ttf`)
+ `stroke_width`(채움 `#FBF0E2`/외곽선 `#9B826A`)를 쓰는 PIL의 `stroke_fill`
기능으로 렌더링됨. 배너별로 폰트 크기가 다름(캔버스 높이에 맞춰 개별 조정된
것으로 보임) — 각 배너의 **기존(오답) 텍스트**를 여러 크기로 렌더링해서
실제 픽셀 bbox와 비교하는 자동 캘리브레이션으로 배너별 정확한 크기를
역산(전부 목표 크기와 완전히 일치, score=0), 캔버스 중앙 정렬(`anchor="mm"`)
확인. 이 레시피로 정답 텍스트를 다시 렌더링 → 6개 전부 캔버스 안에 들어감
확인 → 원본과 나란히 비교해서 스타일 일치 육안 확인 후 배포.

`2_translated/Field/Scene/`(소스) + 라이브 설치 + `3_patched/loose_files`
3곳 모두 배포·해시 일치 확인 완료. 이 6개 TGA도 `toidata_release_ko.xdelta`
재생성 대상에 추가됨(아래 항목 참고 — 총 32개 dat 파일 + 3개 파일(폰트 등,
단 폰트는 이후 0자로 재되돌림) + 이번 6개 TGA, 전부 다음 세션에서 한 번에
`.l7c`/`xdelta` 재생성 권장).

---

## 추가 진행 (2026-08-11, 같은 날 이어서): 캐릭터별 말투(반말/존댓말) 일관성 검증

`GLOSSARY_KO.md`의 "캐릭터 말투 패턴" 표를 기준으로 `Skit.csv`/`Story.csv`
전수를 캐릭터별로 스캔(원문 JP가 だ/だよ/よ/の 등 반말체인데 한국어만
존댓말로 번역된 경우를 탐지, です/ます/でしょう 등 원문이 실제 존댓말인
경우는 제외)해서 아래와 같이 정리함:

- **이난나**: 기존 세션에서 19줄 수정 완료 (동영상 자막과 말투 통일).
- **콘웨이**: 1줄 수정 (`6010.dat` "더워도 뜨거운 것만 드세요"→"먹어").
- **루카(주인공)**: 🔴 **총 84줄 수정** — 앙쥬(4020대)·리카르도(4030대)·
  큐큐/콘웨이(4200대) 등 개별 동료와의 1:1 이벤트 스킷 전체에 걸쳐
  반말 대신 존댓말이 쓰인 대규모 불일치 발견. 글로서리 확립 말투
  "수줍고 겸손한 반말"에 맞춰 전부 반말로 수정(1차 37줄 + 확장 스캔
  47줄, `Skit.csv` + 해당 `skit_wip/*.dat.csv` 양쪽 동기화 완료).
  단 `4016.dat`/`1354.dat`의 ました로 끝나는 "동화/농담 흉내" 대사,
  `1289.dat`의 과장된 궁중어체 개그 대사는 의도된 존댓말 개그이므로
  그대로 둠.
- **앙쥬**: 반말+여성어미(わ/ね/の) 원문이 다정한 존댓말로 일관되게
  번역된 패턴을 다수 발견했으나(125건 스캔) — 원문이 실제 존댓말인
  경우(102건)와 원문은 반말 문법이지만 여성어미만 쓴 경우(23건)를
  모두 포함해 "다정한 누나/성녀" 캐릭터성에 맞춰 시종일관 따뜻한
  존댓말로 옮긴 **의도된 스타일 선택**으로 판단 → 수정하지 않음.
- **에르마나/스파다/리카르도/큐큐/코다/이리아**: 전수 스캔 결과 이상 없음
  (모두 "잘 먹겠습니다" 관용구, 원문 자체가 존댓말인 대사, 또는 의도된
  개그/캐릭터 자각 대사였음 — 예: `4048.dat`에서 에르마나가 일부러
  존댓말을 흉내 내보는 자각 개그, `0335.dat`의 코다&에르마나 합창 대사).

수정된 CSV(Skit.csv + skit_wip 다수)는 **아직 recompile·배포되지 않음**.
다음 세션에서 이어갈 것: (1) `recompile_skits` 돌려서 변경된 `.dat`
파일 특정, (2) Vita3K 격리 테스트, (3) 라이브 설치 + `3_patched/loose_files`
배포·해시 검증. 다음으로 예정된 작업은 호칭(呼称) 일관성 검증(같은
"말투, 호칭 등도 전체적으로" 요청의 나머지 절반, 아직 미착수).

---

## 추가 진행 (2026-08-11, 같은 날 이어서): 호칭(呼称) 일관성 검증

주요 캐릭터 13명 간의 상호 호칭(이름+씨/군/양/오빠/언니 등)을 `Skit.csv`/`Story.csv`
전수에서 화자→대상 쌍 단위로 스캔. 원문 JP 존칭(さん/くん/ちゃん/様/없음)이 같은
화자→대상 쌍에서 항상 동일한데도 한국어 표기(씨/군/오빠/언니/없음 등)가 들쭉날쭉한
경우를 다수결로 통일(사용자 확정).

**단순 오역 1건 수정**: `0078.dat` 리카르도의 대사 "………アンジュだな"(확인/감탄
어미 だ+な)가 "………앙쥬군"으로 잘못 번역돼 있었음(だな를 くん 존칭으로 착각한
듯) → "………앙쥬구나"로 수정.

### 🔴 중요: 정규식 버그로 데이터 일시 손상 → 발견·복구·재작업

1차 스캔·수정(288줄 적용) 도중, 호칭 뒤에 조사/어미가 공백 없이 바로 붙는
경우(예: "언니야", "씨는")를 "호칭 없음"으로 오인식하는 정규식 버그가 있었음이
드러남. 이 때문에 (a) 이미 올바르게 호칭이 붙어 있던 문장에 호칭이 중복
삽입되는 손상이 `Skit.csv` + `skit_wip` 132개 파일에 총 194건 발생했고,
(b) 애초 다수결 집계 자체도 불완전한 데이터로 계산되어 일부 결론이 반대로
나왔음(예: 에르마나→루카/스파다는 실제로 "오빠"가 다수인데 반대로 판단, 앙쥬→
스파다는 "군"이 다수인데 반대로 판단).

**조치**: (1) 194건의 중복 삽입을 전부 원래 상태로 복구(`Skit.csv` + wip 132개
파일), (2) 정규식을 고쳐(호칭 뒤 조사 결합 허용) 전체 재스캔, (3) 재계산된
정확한 다수결 기준으로 14개 그룹·96줄을 재적용(`Skit.csv` + `Story.csv` +
wip 82개 파일). 최종 검증: 중복 패턴 0건, 남은 불일치 그룹 0건 확인 완료.

**최종 결론 요약** (다수결로 통일된 주요 관계):
- 앙쥬→루카: "군", 앙쥬→리카르도/큐큐/콘웨이/오리피엘: "씨", 앙쥬→스파다: "군"
- 에르마나→루카/스파다: "오빠", 에르마나→앙쥬/이리아/큐큐: "언니"
  (에르마나 특유의 사투리 캐릭터성상 손위 동료를 친족 호칭으로 부르는 게 확립된 패턴)
- 콘웨이→루카/스파다: "군", 콘웨이→리카르도/이리아/앙쥬: "씨"
- 루카→리카르도/콘웨이/큐큐/치토세/하르트만: "씨"

### recompile·배포 완료 (같은 세션에서 이어서)

말투(반말/존댓말, 84+1줄) + 호칭(96줄) + だな 오역(1줄) 수정분을 합쳐서
`recompile_skits`/`recompile_story`를 라이브 설치(`_Data`)를 l7cdir 소스로
삼아 실행 → 스테이징 출력과 라이브 설치를 diff해서 실제 변경된 파일만 특정.

**결과**: `Skit/Data/*.dat` 49개 변경, `Script/**/*.dat` 0개 변경(Story.csv
쪽은 이번 세션에서 추가 수정이 없었음 — 이난나 19줄은 이전에 이미 배포됨).
49개 전부 `skit_extract_text()`로 재추출 라운드트립 검증 통과(디코드 에러 0건).

`vanilla_test`(격리 Vita3K 환경)에 배포 후 부팅 테스트 — 약 35초간 오프닝
씬 진행하며 크래시 없음 확인(로그상 에러는 전부 정상적인 벤치니어 경고:
registry.db0 미설치 경고, H264 프레임 버퍼링 EAGAIN 등 무해한 것들).
텍스트 치환만 하는 이미 검증된 코드 경로라 폰트 글리프 작업 때와 달리
구조적 크래시 위험은 낮다고 판단, 라이브 배포 진행.

라이브 설치(`_Data/Field/Skit/Data/`) + `3_patched/loose_files/_Data/Field/Skit/Data/`
양쪽에 49개 파일 배포 완료, 스테이징/라이브/patched 3곳 SHA256 전부 일치 확인.
배포 전 라이브의 기존 49개 파일은 `eboot_backups/skit_honorific_speech_fix_<timestamp>/`에
백업 보존.

**변경된 49개 파일**: 0006, 0078, 0085, 0242, 0280, 1305, 1344, 2026,
4020~4039(일부), 4085, 4201~4218(일부), 4253, 5011, 5014, 5024, 5025, 5033,
5058, 5086, 6007, 6010, 6035, 6036, 6058.

**참고**: 이번 특정 언어 관련 씬들(4020번대 등 동료 유대감 이벤트)이 실제로
언제 트리거되는지 몰라서, 각 씬을 개별적으로 실기 플레이 검증하진 못함
(오프닝만 부팅 테스트). 사용자가 실제 플레이 중 문제 발견 시 알려주면 좋음.

### 배포용 xdelta 패키지 재생성 완료

`3_patched/loose_files`(누적된 전체 한글화 상태, 1774개 파일)를
`l7ca_patch_multi.py`로 순정 원본 `0_02/원본/toidata_release.l7c`
(1,475,397,012바이트)에 패치 → `_Data/Field/Skit/Data/` 989개 파일을
재추출해서 라이브 설치와 SHA256 전수 일치 확인(변경분 49개 정상 반영
검증) → `xdelta3 -e -9 -S -s`로 새 `toidata_release_ko.xdelta`(85MB) 생성 →
diff를 원본에 재적용해서 패치본과 바이트 단위 완전 일치 확인(왕복 검증).

`3_patched/toidata_release_ko.xdelta`, `3_patched/xdelta_release/toidata_release_ko.xdelta`,
`3_patched/ready_to_use/toidata_release.l7c` 전부 갱신. 기존 xdelta는
`eboot_backups/xdelta_pre_<timestamp>/`에 백업. `PATCH_적용법.md`(루트 +
`3_patched/` 양쪽)에 새 해시와 변경 내역 기록 완료.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-11 갱신) | 1,781,909,907 | `9b407671e03b24b448d07042c5bbd9c7542bf0b65cc0b11cfc8b33f168fd58d8` |
| `toidata_release_ko.xdelta` | - | 89,567,117 | `9c2d43f23ee8670522f1e64cc6aee608e3da90c2539dcb588901052253c9ada5` |

`eboot_ko.xdelta`는 이번 세션에서 `eboot.bin`을 건드리지 않았으므로
그대로 유지(숫자버그 수정판 그대로 유효).

**참고**: 작업 중 디스크 여유공간이 5~7GB로 빠듯했음(1.475GB 원본 +
1.78GB 패치본 + 85MB diff를 동시에 다뤄야 함) — 다음에 이 작업을 반복할 때
디스크 공간을 미리 확인할 것.

**교훈**: 정규식으로 자연어(특히 조사가 어근에 밀착되는 한국어) 패턴을 다룰 때,
"호칭 뒤 경계"처럼 엄격한 lookahead 조건을 걸면 정상적인 조사 결합 표현을
전부 놓칠 수 있음. 대량 치환 스크립트를 돌린 뒤에는 반드시 "치환 결과가
의도한 것과 일치하는지" 별도 검증(이번엔 중복 패턴 스캔)을 거칠 것.

---

## 추가 진행 (2026-08-11, 같은 날 이어서): 숫자 오표기 버그 2차 발견·수정 — 필드 전투 HP/데미지 숫자

사용자가 "필드에서 몬스터를 사냥할 때의 HP 숫자, 레벨업 안내 숫자 등"이
1차 수정(스테이터스 화면) 이후에도 여전히 깨진다고 신고. `eboot.csv`를
전수 재검토해서 **1차와 완전히 동일한 패턴의 두 번째 숫자 룩업 테이블**을
발견함:

```
주소 8115D718
JP: ０１２３４５６７８９＋－？×．/"  (전각, 숫자10+연산기호7 = 46바이트)
KR(버그): 0123456789+-?×./"           (대부분 반각으로 번역됨, 18바이트)
```

1차 테이블(`8115F2B8`, 숫자만 11자)보다 문자 종류가 많고(숫자+사칙연산+물음표+
슬래시+따옴표) 폭 손실도 더 큼(46B→18B). 아마 전투 중 데미지/콤보/HP 등
"연산 기호가 섞인 숫자 표시"에 쓰이는 별도 폰트/렌더러가 참조하는 테이블로
추정. 1차와 동일하게 원문 그대로(전각) 복원하는 방식으로 수정.

### 실기 검증 (격리 환경, 실제 전투 진행)

`vanilla_test`에 수정된 `eboot.bin`을 배치하고 실제로 새 게임 시작 →
튜토리얼 전투까지 진행해서 스크린샷으로 직접 확인:
- 전투 중 HP `240`, TP `24` — 정상 표시
- 콤보 히트 카운터 `2` — 정상 표시(연산기호 테이블 관련 렌더링 경로로 추정)
- 전투 승리 후 HP `202`, TP `18`, EXP `30`, GALD `44` — 전부 정상 표시
- 참고: 무비 재생 중(movie_002.mp4) `EXCEPTION_ACCESS_VIOLATION`이 두 번의
  독립된 부팅에서 동일한 메모리 주소로 재현됐지만, 두 경우 모두 게임이
  자동으로 복구되어 정상 진행됨 — H264 디코더 관련 Vita3K 자체의 기존
  버그로 보이며(텍스트 전용 패치와는 무관), 프로세스 크래시로 이어지지
  않음을 확인함.

**레벨업 알림 화면 자체는 검증 못 함**(실제 레벨업까지 도달하는 그라인딩을
자동화하지 못함) — 전투 HP/TP/콤보 숫자와 같은 렌더링 경로를 쓸 가능성이
높아 함께 고쳐졌을 것으로 예상되지만, 사용자의 실제 플레이로 확인 필요.

### 배포 완료

`2_translated/eboot.csv` 51번째 줄 수정 → `recompile_eboot()`로 재빌드
(순정 원본 `0_02/원본/eboot.bin` 기준) → 언어코드 패치(`0x78E2`: 01→03)
재적용 → 라이브 설치 + `3_patched/ready_to_use` + `3_patched/loose_files`
3곳 배포, SHA256 일치 확인. `eboot_ko.xdelta` 재생성 후 원본에 재적용해서
패치본과 바이트 단위 완전 일치 확인(왕복 검증). 배포 전 라이브의 기존
`eboot.bin`은 `eboot_backups/pre_secondtable_<timestamp>/`에 백업.

| 파일 | 상태 | SHA256 |
|---|---|---|
| `eboot.bin` | 패치 전(순정 원본) | `72a23772e4b8cb91486e40a3ce07887549d1df587240947f62de3d4dc36546ad` |
| `eboot.bin` | 패치 후(2차 수정, 2026-08-11) | `56c244cdbc150cfde6f3a4f4825a7c4a5dff8d0d1ecf9af0fb327a14a168f318` |

`PATCH_적용법.md`(루트 + `3_patched` 양쪽) 갱신 완료.

**남은 것**:
1. 사용자의 실제 플레이로 레벨업 화면·행운(幸運) 별 개수 재확인.
2. `eboot.csv`에 이 두 테이블 외에 또 다른 유사 룩업 테이블이 남아있을
   가능성은 낮다고 판단(전체 604행 스캔해서 문자수-일치·바이트폭-불일치
   패턴을 재확인했고, 남은 후보는 전부 정상적인 전각→반각 라벨 번역
   (HP/TP/NEW/ALL/OVL 등 UI 라벨)이었음) — 그래도 새로운 오표기 제보가
   오면 같은 방법론(문자수는 같은데 바이트 폭만 다른 행 찾기)으로 재검토.

---

## 추가 진행 (2026-08-17): 캐릭터별 말투(반말/존댓말) 정합성 재검증 2차 — 앙쥬 예외 규정 재정의 + 아스라/오리피엘 검증

사용자가 "앙쥬가 리카르도를 제외한 나머지 주요인물에게 존댓말을 쓰는 경우"와
"아스라가 오리피엘에게 실제로는 하지 않는 존댓말을 하는 경우"를 찾아 정정해달라고
요청. 2026-08-11 세션은 앙쥬의 formal-JP-아닌(반말 문법) 대사 125건을 스캔하고도
"다정한 누나 말투로 시종일관 존댓말을 쓰는 의도된 스타일"이라 판단해 손대지
않았었는데, 이번 세션에 사용자가 **"리카르도 상대일 때만 존댓말, 그 외 주요
인물에게는 반말"**이라는 더 정확한 규칙을 제시해 그 판단을 뒤집음.

**방법론**: `File/Speaker` 컬럼 기준으로 씬(파일) 단위 캐스트를 모아, 해당 씬에
리카르도가 없는데 앙쥬 대사의 한국어가 존댓말 종결(-요/-네요/-니다 등)인 경우를
전수 스캔하는 Python 스크립트를 작성(Skit.csv+Story.csv+MapData.csv 전체 대상).
**원문(일본어) 자체가 です/ます인 경우는 정상 번역이므로 제외**하고, 원문이
だ/よ/わ/の 등 반말 문법인데 번역만 존댓말인 경우만 후보로 추림.

**정규식 버그 2건을 스캔 도중 발견·수정**(둘 다 처음엔 오탐/누락을 냈음):
1. 한글 종결어미 검출에 홑자모 `ㅂ니다`를 쓰면 실제 텍스트(완성형 음절)와
   전혀 매치되지 않는 죽은 패턴이었음 — `습니다/ㅂ니다`를 `니다`로 완화했다가,
   이번엔 "아니다"처럼 완전한 반말 단어 안에 `니다`가 우연히 포함된 경우까지
   존댓말로 오탐하는 2차 버그 발생. **한글 음절 유니코드 분해로 `니다` 바로
   앞 음절의 받침이 정말 `ㅂ`인지(합/습/됩/옵 등) 검사하는 방식으로 최종
   수정**(`_jong_is_bap`/`_has_real_nida` 헬퍼).
2. 일본어 존댓말 판정 정규식이 `ます`만 보고 `ました`(마시타, 과거형)/`ません`
   (마센, 부정형)를 놓치고 있었음 — 패턴에 `まし`/`ません`/`なされ` 등 추가.

**앙쥬 최종 결과**: 리카르도 부재 씬에서 존댓말로 번역된 101건 중 원문이 실제
존댓말(です/ます)인 59건은 정상 번역이라 그대로 두고, **원문이 반말인데
존댓말로 번역된 42건을 전부 반말로 수정**(Skit.csv + skit_wip 19개 파일,
Story.csv + script_wip 16개 파일, MapData.csv + map_wip 1개 파일, 총 42곳).
감정적으로 격한 장면(`Dn/18/07/740.dat` — 마티우스 대치 장면 등)도 원문이
반말이면 동일하게 반말로 통일. `저→나`, `제가→내가` 등 인칭대명사도 함께
자연스럽게 맞춤. 씬 몇 곳(오리피엘과의 꿈속 대화 `0294.dat` 일부, 리카르도가
없는 그룹 씬 다수)이 포함되며 자세한 목록은 이번 세션 스크래치의
`ange_candidates.json`(현재는 세션 종료 시 소실)에 있었음 — 필요하면 diff로
재구성 가능.

**아스라→오리피엘 검증 결과: 버그를 찾지 못함(기존 번역이 이미 정확함).**
Skit.csv+Story.csv+MapData.csv+MovieCaption(`2_translated/Movie/*.srt`) 전체에서
두 캐릭터가 같이 등장하는 모든 씬을 전수 확인:
- `3026.dat`("오리피엘의 소망"), `Twn/15/00/700·710.dat`, `Twn/17/00/710.dat`,
  `MovieCaption_011.srt`: 아스라는 일관되게 거친 반말("俺"/だ/ぞ 종결),
  오리피엘은 아스라에게 존댓말 — **비대칭 관계가 이미 올바르게 반영돼 있음.**
- `Dn/20/13/730.dat`(라티오-센서스 전쟁 승전 후 군사회의 장면)만 유일하게
  아스라도 존댓말(-습니다)을 쓰는데, **이 장면은 오리피엘도 동일하게 격식체를
  쓰는 대칭적 장면**이고(둘 다 "殿"/貴殿 같은 경어 호칭과 archaic 격식체를
  서로 주고받음), 전후 다른 장면(사적 대화/전투)에서의 반말과 대비되는
  "전후 군사 의례" 특유의 의도된 톤 전환으로 판단됨. **수정하지 않음.**

**부수 검증**: 새로 고친 스캔 도구로 2026-08-11 세션이 "이상 없음"으로 확정했던
캐릭터들(루카/이리아/스파다/콘웨이/큐큐/코다/에르마나/리카르도)도 재검증.
리카르도/스파다/코다/에르마나에 남은 후보는 전부 기존에 이미 알려진 의도된
예외였음(`잘 먹겠습니다` 관용구, `1344.dat`의 사투리 흉내 개그 스킷, `5021.dat`
스파다의 짐짓 격식체 개그 등) — **추가 수정 없음, 기존 결정 재확인.**
**이리아에서 새로 2건 발견해 수정**(`Dn/01/00/700.dat` #243 "감사해요"→
"고마워", `Dn/08/02/701.dat` #104 "무슨 볼일이신가요?!"→"무슨 볼일이야?!" —
둘 다 원문이 반말인데 존댓말로 번역돼 있었음). 이리아의 나머지 후보(12건)와
루카의 잔여 후보(36건, 2026-08-11에 84줄 수정 후 남은 것)는 **이번 세션에
전수 검토하지 못함** — 표본상 대부분 `잘 먹겠습니다`류 관용구나 `1289.dat`
같은 궁중어체 개그일 가능성이 높지만(패턴이 낮은 밀도로 여러 파일에 흩어져
있음), 다음 세션에서 마저 확인 권장.

**검증**: `translate_helper.py check skit/script/map` 전부 기존에 이미 알려진
2건(`0155.dat`의 "리카르돈" 말장난, `Twn_09_00_430.dat`의 무관한 오탐)
외 새 이슈 0건. `progress` 수치도 변동 없음(내용만 수정, 행 추가/삭제 없음).

**이번 세션에서 하지 않은 것**: recompile·라이브 배포는 진행하지 않음(텍스트
수정만 완료, `2_translated/Skit.csv`/`Story.csv`/`MapData.csv` + 대응
`skit_wip`/`script_wip`/`map_wip`에만 반영됨). 다음 세션에서 이 42+2건을
포함해 `recompile_skits`/`recompile_story`/`recompile_map_data`를 돌리고
라이브 설치에 배포할 것(2026-08-11 세션의 "말투/호칭 수정 배포" 때와 같은
절차 — diff로 변경된 `.dat` 파일만 특정 → 격리 테스트 → 라이브 배포 →
xdelta 재생성).

---

## 추가 진행 (2026-08-17, 같은 날 이어서): 루카/이리아 잔여분 마저 확인 — 총 72줄 추가 수정

사용자가 "루카랑 이리아 잔여분부터 마저 확인해줘"라고 요청. 위 1차 검증(마지막
줄만 검사하는 방식)의 **구조적 한계를 발견**하고 방법론을 개선해 재검증함.

**발견한 한계**: 스캔 로직이 셀의 **마지막 줄만** 검사했는데, `4031.dat`처럼
한 셀 안에 여러 줄이 있고 그중 마지막 줄만 우연히 정상(예: 반말)이면 앞선
줄에 남아있는 존댓말 잔재를 놓침(`4031.dat` #6/#14가 실제 사례로 발견됨).
또한 `써요/펴요/쪄요` 같은 축약형 동사는 문자 기반 정규식으로 못 잡는 문제도
있었음.

**개선된 방법론**: (1) 셀의 **모든 줄**을 검사(`요`로 끝나는 모든 줄 + 정확한
`니다` 판정), (2) **오탐 제거를 위해 원문(일본어) 전체에 존댓말 마커
(です/ます/でしょう/ください 등)가 단 하나도 없는 경우만** 후보로 채택(원문에
하나라도 있으면 그 줄이 진짜 존댓말일 수 있으니 제외) — 이 필터로 노이즈를
165건(넓은 정규식 그대로)에서 37건(루카)/12건(이리아)까지 줄임.

**루카**: 1:1 이벤트 스킷(4020/4030/4200번대, 앙쥬·리카르도·큐큐·콘웨이
상대)에서 2026-08-11의 84줄 수정이 놓친 잔재 47줄 발견·수정(1차 21줄:
last-line 스캔으로 찾은 것 + 2차 26줄: 셀 전체 스캔으로 추가 발견) +
그 외 전체 코퍼스에서 25줄 추가 발견·수정(3차 24줄 + 누락분 1줄
`Twn/03/01/711.dat` #42). **총 72줄 수정.** 관용구(`ごめんなさい`→
"죄송해요/미안해요", `おやすみなさい`→"안녕히 주무세요", `ただいま`→
"다녀왔습니다", `こんにちは`→"안녕하세요", `どうぞ`→"여기요")와 진짜 존댓말
원문(`見てました`/`謝罪します`가 있는 `4027.dat`#30 등)은 이번에도 의도된
것으로 판단해 그대로 둠.

**이리아**: 재검증 결과 남은 12건 전부 이전에 이미 확인한 것과 완전히
동일(잘 먹겠습니다 관용구 8건, `0063.dat`/`1263.dat`의 과장된 귀족 말투
개그 2건, `5022.dat`의 과장된 사과 1건, `Twn/00/00/480.dat`의 애매한
풍자적 존댓말 1건) — **추가 수정 없음, 이전 세션(2026-08-11)+오늘 세션
1차(2건 수정)로 이미 완결된 상태 재확인.**

**검증**: `check skit/script/map` 전부 기존 2건(0155.dat "리카르돈",
Twn_09_00_430.dat 무관 오탐) 외 새 이슈 0건, `progress` 수치 변동 없음.

**교훈**: "말투 정합성" 같은 셀 단위(여러 줄 포함) 검증을 할 때는 반드시
**셀 전체의 모든 줄**을 검사해야 함 — 마지막 줄만 보는 방식은 다중 라인
셀에서 앞쪽 줄의 문제를 조용히 놓칠 수 있음(이번에 정확히 그런 사례로
`4031.dat`을 직접 열어보다가 발견함). 이번 세션에서 만든 재사용 가능한
검증 스크립트(원문에 존댓말 마커가 전혀 없는 경우만 후보로 추리는 필터)는
다음에 다른 캐릭터(스파다/콘웨이/큐큐/코다/에르마나/리카르도 등)를 셀
전체 기준으로 재검증할 때도 재사용 가치가 있음 — 이번 세션의 1차 검증에서
이 캐릭터들은 last-line 방식으로만 확인했으므로, 시간이 되면 이들도
셀 전체 기준으로 한 번 더 훑어보는 걸 권장.

이번 세션 전체(앙쥬 42줄 + 이리아 2줄 + 루카 72줄 = **총 116줄**)도
아직 recompile·배포되지 않음 — 위 항목과 함께 다음 세션에서 한 번에
처리할 것.

---

## 추가 진행 (2026-08-17, 같은 날 이어서): 나머지 캐릭터(스파다/콘웨이/큐큐/코다/
에르마나/리카르도) 셀 전체 기준 재검증 — 콘웨이 1줄 추가 수정

사용자가 "나머지 캐릭터도 셀 전체 기준으로 마저 확인해줘"라고 요청. 바로 위
루카/이리아 재검증에서 쓴 "셀의 모든 줄 검사 + 원문에 존댓말 마커가 하나도
없는 경우만 후보로 채택" 방법론을 나머지 6명에게 그대로 적용(스파다 2건,
콘웨이 1건, 큐큐 3건, 코다 2건, 에르마나 7건, 리카르도 1건 = 총 16건 후보).

**검토 결과, 실제로 고친 건 콘웨이 1건뿐**(`Twn/00/00/420.dat` #109
"나랑 닮은 걸까요?"→"나랑 닮은 걸까?", 원문 `かな` 반말). 나머지 15건은
전부 다음 둘 중 하나로 확인됨:
- **이미 알려진 의도된 예외**: `1344.dat`의 사투리 흉내 개그 스킷(스파다/
  에르마나/리카르도 각 1~4건), `0335.dat`/`4075.dat`/`6038.dat`의
  "잘 먹겠습니다" 관용구(코다·에르마나), `4048.dat`의 에르마나가 "이런
  말투가 더 훌륭해 보이지 않을까"라며 일부러 존댓말을 흉내 내보는 자각
  개그(글로서리에 이미 기록돼 있던 바로 그 사례) — 전부 그대로 둠.
- **스캐너 자체의 새로운 오탐 2종 발견**: (1) 일본어 `はい`(네, 격식 있는
  긍정)를 존댓말 판정 정규식이 못 잡고 있었음(`です/ます` 계열만 검사했음)
  — 스파다의 `Dn/02/00/702.dat` #27이 이 케이스, 실제로는 원문에 격식
  요소가 있으므로 정상 번역. (2) 한국어 "필요"/"중요" 같은 **명사가
  우연히 '요'로 끝나는 경우**를 존댓말 종결어미로 오인 — 큐큐의 조사
  생략형 명사 나열 말투(`4235.dat`/`4243.dat`)에서 3건 전부 이 케이스.
  둘 다 수정 불필요, 스캐너의 알려진 한계로 기록해둠(이 스캐너를 다시
  쓸 경우 참고).

**검증**: `check skit/script` 기존 2건 외 이상 없음, `progress` 변동 없음.

이로써 **8명 전원(앙쥬·아스라·루카·이리아·스파다·콘웨이·큐큐·코다·
에르마나·리카르도 — 정확히는 9명, 앙쥬/아스라는 addressee 기준 별도
방법론)에 대해 셀 전체 기준 말투 정합성 재검증이 완료**됨. 이번 세션
전체 누적 수정: 앙쥬 42줄 + 이리아 2줄 + 루카 72줄 + 콘웨이 1줄 =
**총 117줄**. 전부 recompile·배포 전 상태로 다음 세션에서 한 번에
처리할 것.

---

## 추가 진행 (2026-08-17, 같은 날 이어서): 무비 자막 검수 도구 제작 + 사용자 직접 수정 + 전체 recompile·배포 완료

### 무비 자막 검수 GUI 신규 제작

사용자가 "아스라가 오리피엘에게 존댓말" 등을 검증해달라고 했으나, MovieCaption은
일본어 원문이 없고(영어 초안 기준 번역) 화자 표시도 없어서 텍스트만으로는
확정이 어려웠음(30개 파일 전수 직접 읽어봤지만 판단 보류). 사용자가 "내가
직접 검수하고 수정하겠다"며 도구 제작을 요청해서 `tools/moviecaption_gui.py`를
신규 작성함 — 기존 `translate_gui.py`와 동일한 패턴(127.0.0.1 로컬 전용 웹
서버, 자동 브라우저 오픈)으로 `2_translated/Movie/*.srt`를 불러와 줄 단위로
편집·저장 가능. 존댓말(-요/-니다) 어미가 있는 줄을 자동 하이라이트하는
기능과, 자막 파일 자체는 안 건드리고 화자 추정 메모를 별도로 남기는 기능
(`2_translated/Movie/_speaker_notes.json`)을 추가함.

**제작 중 발견한 버그**: 저장 로직이 Windows 텍스트 모드 쓰기 때문에 원본의
LF 개행을 CRLF로 바꿔버리고, 파일 끝에 불필요한 빈 줄을 하나 더 추가하는
문제가 있었음(`build_srt()`가 마지막에 `+ '\n'`을 덧붙이던 버그). 전부
`newline=''`으로 열어 쓰도록 수정 + trailing newline 로직 수정 후, 무수정
저장 시 SHA256 해시가 완전히 동일함을 재검증 완료. (컴파일 도구인
`srt_to_dat.py`는 어차피 개행 방식에 무관하게 읽으므로 기능적으로는
문제없었을 테지만, 불필요한 diff를 막기 위해 원본 형식을 그대로 보존하도록
고쳐둠.)

### 사용자가 직접 10개 파일 수정 + 전체 recompile·배포

사용자가 이 도구로 `MovieCaption_000~006`, `019`, `024`, `027` 총 10개
파일을 직접 검수·수정함. 디코드해서 대조해보니:
- **`019.srt`(오프닝) — 휴프노스 존댓말 건 확인·수정됨**: "지루해지셨다면
  잠시 제가 상대해드릴까요?"(존댓말) → "싸움이 시시하다고 한다면 내가
  조금은 재미있게 해 주지."(반말)로 교체. 이전 세션에서 이 부분을 두고
  "아스라가 어머니와 노는 대사일 수도" 등으로 판단을 보류했었는데,
  실제로는 휴프노스 본인의 대사였음이 확인됨.
- **`006.srt` — 앙쥬=엘 사투리 오인 건 확인·수정됨**: "신이 널 용서 안
  해도, 내는 용서 안 한대이!"(경상도 사투리) → "신이 용서하지 않더라도,
  제가 용서하겠어요!"(존댓말)로 교체. 사투리가 실제로는 앙쥬/오리피엘의
  대사에 잘못 붙어 있었던 것으로 확인됨.
- 그 외 000~005/024/027도 전반적으로 대사를 훨씬 자연스럽게 다듬음(정확한
  줄 단위 diff는 이 세션 스크래치의 백업 매니페스트로 재구성 가능,
  `deploy_backups/20260817_163609/` 참고).

사용자가 "지금까지 작업한 말투 수정내역과 합쳐서 컴파일 진행해줘"라고
요청해서, 이번 세션의 CSV 수정분(앙쥬/이리아/루카/콘웨이 117줄, 위 항목
참고)과 사용자의 무비 자막 수정분을 합쳐 **한 번에 전체 recompile·배포
진행**:

1. `toir.recompile_story`(check_integrity=True로 자체 라운드트립 검증
   포함) / `recompile_skits` / `recompile_map_data` / `insert_srt`를
   전부 라이브 Vita3K 설치 경로(`...\AppData\Roaming\Vita3K\Vita3K\ux0\app\PCSG00009`)를
   l7cdir 소스로 삼아 스크래치 스테이징 폴더에 재컴파일.
2. 스테이징 vs 라이브 설치를 diff해서 실제 변경된 파일만 특정:
   **Story 31개, Skit 42개, MapData 2개, Movie 10개 = 총 85개 파일.**
3. 배포 전 라이브의 기존 85개 파일 전부 `deploy_backups/20260817_163609/`에
   백업(매니페스트 JSON 포함) → 라이브 설치 + `3_patched/loose_files`
   양쪽에 배포 → 배포 후 해시 재대조 전부 일치 확인.
4. Skit 42개 전부 `skit_extract_text()`로 라운드트립 디코드 검증(에러
   0건). Story는 `recompile_story`의 `check_integrity=True` 옵션으로
   자체 검증됨(에러 없이 통과).

실기(격리 환경) 부팅 테스트는 이번엔 생략함(텍스트 치환만 하는 기존
검증된 경로라 위험 낮다고 판단, 2026-08-11 세션과 동일한 판단) — 사용자가
실제 플레이 중 문제 발견 시 알려주면 좋음.

### xdelta 배포판 재생성 완료 (같은 세션, 이어서)

사용자가 "xdelta 배포판도 재생성해줘"라고 요청. **`xdelta3` 바이너리가
이 환경에 없어서** GitHub(`jmacd/xdelta` v3.2.0 릴리스, Apache 2.0 라이선스)
에서 Windows x86_64 빌드를 받아 스크래치에 압축 해제해서 사용함(프로젝트에
커밋은 안 함 — 필요하면 다음 세션에서 다시 받거나 `tools/`에 정식으로
포함시킬지 검토).

절차: (1) `3_patched/loose_files`(누적 전체 한글화 상태, `eboot.bin`/
`toidata_release.l7c` 자체 사본은 아카이브 내부 경로가 아니므로 패치
대상에서 제외 — `l7ca_patch_multi.py`가 이 2개를 못 찾아서 실패하던 걸
발견해 직접 필터링) → `l7ca_patch_multi.py`의 `patch_many()`를 직접 호출해서
순정 원본 `0_02/원본/toidata_release.l7c`(1,475,397,012바이트)에 1774개
파일 패치 → 새 `toidata_release.l7c`(1,781,909,425바이트) 생성. (2)
`xdelta3 -e -9 -S -s`로 `toidata_release_ko.xdelta`(89,566,926바이트,
~85MB) 생성. (3) 원본+diff를 다시 합쳐서 패치본과 바이트 단위 완전 일치
확인(왕복 검증 통과). (4) **추가 검증**: 새 patched l7c에서 `_Data/Script`,
`_Data/Field/Skit/Data`, `_Data/Field/MapData`, `_Data/Movie` 4개 카테고리를
전부 재추출(`l7ca_unpack.py`)해서 `3_patched/loose_files`와 대조 —
**1,699개 파일 전수 일치(불일치 0건)** 확인, 오늘 바뀐 85개뿐 아니라
누적된 전체 번역이 정확히 반영됐음을 재확인.

배포: `3_patched/toidata_release_ko.xdelta`, `3_patched/xdelta_release/toidata_release_ko.xdelta`,
`3_patched/ready_to_use/toidata_release.l7c`, `3_patched/loose_files/toidata_release.l7c`(사본)
전부 갱신. 기존 파일은 `deploy_backups/xdelta_pre_20260817_164144/`에 백업.
`PATCH_적용법.md`(루트 + `3_patched` 양쪽) 새 해시로 갱신 완료.
`eboot_ko.xdelta`는 이번 세션에서 `eboot.bin`을 안 건드렸으므로 그대로 유지.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-17 갱신) | 1,781,909,425 | `faede940337cb5ff5c7b4d8952cfa796bd68609498869bf4f58c9324a99adfdd` |
| `toidata_release_ko.xdelta` | - | 89,566,926 | `d6b282821e0be950be0931271466feba83de4b8d11001532eab3c1d22bc8ff3a` |

---

## 2026-08-17 (같은 날, 이어서): 한글 글꼴 233자 미표시 문제 — 근본 원인 발견 및 수정 완료

### 문제
`_Data/Font/FontDataNormal.pgf`에 새 한글 glyph를 추가하면 Vita3K에서
`sceClibMemcpy` src/dst overlap → `EXCEPTION_ACCESS_VIOLATION` 크래시가 남
(실제로는 순수 재구현(HLE)이 아니라 진짜 소니 펌웨어 `libpgf.suprx`가
LLE로 실행되는 코드 안에서 발생). 이전 세션 결론은 "새 glyph 6개까지만
안전, 7개부터 크래시"였는데, 이번 세션에서 재검증한 결과 그 결론 자체가
틀렸음이 드러남 — 진짜 변수는 "glyph 개수"가 아니라 **우리 인코더가
만드는 glyph 데이터 자체**였음.

### 근본 원인 (확정)
PGF 포맷의 각 glyph 레코드 맨 앞 14비트 "size" 필드를 우리 인코더가
지금까지 계속 `0`으로 방치해왔음(코드 주석에 "unused"라고 잘못 적혀있었음
— PPSSPP의 재구현 코드가 이 필드를 그냥 건너뛰길래 안 쓰는 필드인 줄
착각했던 것). 원본 폰트의 실제 glyph 7515개를 전수 분석한 결과, 이
필드는 예외 없이 **그 레코드가 차지하는 정확한 바이트 길이**와 일치했다
(`analyze_size_field.py`). 크래시 로그의 memcpy 길이
`4294967288(0xFFFFFFF8)`는 부호있는 32비트로 정확히 `-8`이며, 레코드의
고정 헤더 크기(14+7+7+7+7+6+2+2+3+9=64비트=8바이트)를 실제 펌웨어가
`size_field - 8`로 계산해서 복사할 나머지 바이트 수를 구하는 로직이라고
가정하면, `size_field=0`일 때 정확히 `-8`이 나오는 것과 완벽히 일치함.

### 수정
glyph 레코드를 빌드할 때 `size` 필드에 실제 레코드의 총 바이트 길이
(헤더 비트 + RLE 니블, 4바이트 정렬 전 값)를 정확히 계산해서 기록하도록
인코더 수정(`build_sizefix.py`의 `build_glyph_record_fixed()` — 2-pass로
먼저 size 필드를 제외한 나머지 비트를 다 쓴 뒤 총 길이를 역산해서 맨
앞에 씀).

### 검증 (실기 Vita3K)
1. 프로덕션 베이스(`3_patched/loose_files/_Data/Font/FontDataNormal.pgf`,
   이전 세션에 이미 8191 sentinel 버그를 고치며 229자를 추가해둔 상태) 위에
   이번 세션에 새로 스캔한(`scan_all_usage.py` + `find_reuse_candidates.py`)
   여전히 미표시(밑줄)인 한글 1335자(`missing_hangul.tsv`)를 전부
   size-field 수정된 인코더로 추가 → glyph 7744개 → 9956개.
2. Vita3K에서 오프닝 무비 → 필드 진입 → 다수의 음성 대사 → 실제
   스킷(TalkChara 얼굴 데이터 로딩) 화면까지 **15분 이상 연속 플레이,
   크래시 0건**(이전 깨진 빌드는 같은 지점에서 1초 만에 크래시했음).
3. 실제 대사 렌더링 육안 확인 — "꿈을 꼈다.", "루카~ 이제 그만 일어나렴
   아침 먹을 시간이야", "좋은 아침이야, 루카 그렇게 서둘러서 무슨
   일이니? 오늘 학교 쉬는 날 아니었어?" 등, 새로 추가한 글자를 포함한
   문장들이 자모 분리나 깨짐 없이 완전한 음절 블록으로 정상 렌더링됨.

### 배포 완료
`3_patched/loose_files/_Data/Font/FontDataNormal.pgf`를 이 새 폰트로
교체 → `rebuild_xdelta.py` 재실행(순정 원본 `0_03/원본/toidata_release.l7c`
기준, 1774개 파일 패치 → xdelta 생성 → 왕복 검증 통과) →
`3_patched/toidata_release_ko.xdelta`, `3_patched/xdelta_release/...`,
`3_patched/ready_to_use/toidata_release.l7c`,
`3_patched/loose_files/toidata_release.l7c` 전부 갱신.
기존 파일은 `deploy_backups/xdelta_pre_20260817_185307/`에 백업.
`PATCH_적용법.md`(루트 + `3_patched` 양쪽) 새 해시로 갱신 완료.

⚠️ **주의**: `0_02` 폴더가 이번 세션 중 `0_03`으로 이름이 바뀌어 있었음
(이전 세션에서 기록한 `ORIGINAL` 경로가 존재하지 않아 재확인 필요했음).
다음 세션에서 원본 경로를 하드코딩된 스크립트에 쓸 때는 항상 먼저
`sha256sum`으로 `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d`와
일치하는지 확인할 것.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-17 폰트 수정 갱신) | 1,782,123,062 | `68defbb2759406836b5e104c5dc85cebb18e04d924388dc518f65c6a54e91950` |
| `toidata_release_ko.xdelta` | - | 89,830,197 | `b5cec19c5bf0f9de57bb52cd7c0661cd6e71073dbf7c07903b2f685aaba274f0` |

### 남은 것
- 라이브 Vita3K 설치 폴더(`AppData/Roaming/Vita3K/...`)의 폰트는 이번
  검증 빌드(`4a2cdb349fa6d5b0...`)가 이미 배포되어 있음 — 위 최종
  프로덕션 빌드와 동일한 내용이라 그대로 둬도 됨.
- `build_fix.py`(229자 sentinel 수정 스크립트)는 원본 그대로 두고
  건드리지 않음 — size-field 버그 수정은 `build_sizefix.py`(스크래치)에만
  있음. 다음 세션에서 폰트를 또 건드릴 일이 있으면 `tools/` 밑에 정식
  스크립트로 옮기는 것을 고려.
- `missing_hangul.tsv`/`reuse_candidates.tsv`는 스크래치 폴더에만 있음
  (세션 종료 시 사라짐) — 필요하면 `tools/toir/font_research/hangul_fix/`
  밑에 커밋해서 재사용 가능하게 하는 것을 고려.

---

## 2026-08-17~18 (다음 날/이어지는 세션): 한글 글자 잘림/렌더링 일관성/작은글꼴 미표시 + 캐릭터 이름 미번역 수정

사용자가 `font_error/after_font_1~5.PNG` 캡처로 4가지 잔여 폰트 품질 문제를
제보. 조사·수정 완료. **이 항목은 세션 도중 접근 방식이 두 번 바뀌었으니
최종 결론(맨 아래 "최종 해결")만 참고할 것 — 중간 과정은 왜 그 결론에
도달했는지 기록용으로만 남김.**

### (3) 메뉴 하단 키매핑 힌트("_ _") — 원인: FontDataMin.pgf 방치
`_Data/Font/FontDataNormal.pgf`(대화창용) 말고 `FontDataMin.pgf`(작은
UI 텍스트용, 예: 파티 메뉴 하단 "◎ 결정 ✕ 돌아가기")가 별도로 존재하는데
이번 세션 폰트 작업 내내 전혀 건드리지 않아서 한글 glyph가 0개인 순정
상태로 남아있었음. `FontDataNormal.pgf`와 동일한 파이프라인을 이 파일에도
적용해서 해결. Min 폰트는 원본 kanji 크기가 21x20(Normal의 26x26 대비
~0.79배)이라 래스터 파라미터를 별도로 캘리브레이션함: `RASTER_SIZE=23,
ORIGIN=(8,4), TOP_OFFSET=11` (Normal은 `26,(10,5),15`, 폰트는 아래 (1)(2)
결론과 동일하게 맑은 고딕 **볼드**).

### (4) 칭호/인연/무기점 등에서 캐릭터 이름이 카타카나(ルカ)로 미번역
처음엔 `CharaNames.csv`(→`PackFieldData.dat` section 30)와 `eboot.csv`의
팝업 템플릿만 확인하고 "이미 정확히 번역되어 있다"고 결론 내렸었으나,
**이건 틀린 결론이었음** — 사용자가 세션 문제가 아니라고 강하게
지적해서 재조사한 결과, 무기점/방어구점의 캐릭터 스탯 비교 패널이
참조하는 **`_Data/System/CharaParamDataPack.dat`가 지금까지 번역
파이프라인에 단 한 번도 포함된 적이 없었음**을 발견(`3_patched/loose_files`
어디에도 이 파일이 없었고, 라이브 설치에도 순정 원본이 그대로 깔려있었음
— `2_translated/System/CharaParamDataPack.dat`에 예전에 작업한 사본이
있긴 했지만 이름을 "Ruca"/"Illia"/"Spada" 같은 로마자 디버그용 라벨로
바꿔놓은 것이었고 실제 배포에는 반영된 적이 없었음). 순정 원본을 기준으로
파티 멤버 8명(오프셋 80/272/464/656/848/1040/1232/1424, 각각
루카/이리아/스파다/앙쥬/리카르도/에르마나/콘웨이/큐큐) 이름만 UTF-8
바이트로 직접 교체(길이가 원본보다 길어지는 경우 없음 확인, 나머지 필드는
전혀 건드리지 않음) → `3_patched/loose_files/_Data/System/`에 새로 추가.
**"칭호"/"인연" 팝업도 이 파일을 참조하는지는 확인 못 함** — 만약 이번
수정 후에도 그 팝업에서 카타카나가 보이면 다른 소스가 있다는 뜻이니
추가 조사 필요.

### (1)(2) 특정 한글(같/겉/밑/받/떻 등)이 오른쪽 잘림 + 렌더링 일관성(굵기/정렬)
사용자가 "100% 매번 재현된다"고 지적, 이후 "폰트를 바꾸는 방식은 쓰지
말라"고 명시적으로 요청해서 최종 접근 방식이 바뀜.

**잘림의 원인**: 같은 글자(예: '같') 데이터를 **어떤 인코딩으로 바꿔
넣어도 항상 똑같이 깨짐**(RLE 런렝스 캡, RUN/LITERAL 모드, 픽셀 값
이진화, 파일 내 오프셋 이동, bearing/shadowFlags 수정 — 전부 무관하게
동일 증상). 반면 **같은 코드포인트 슬롯에 다른 글자('각')의 데이터를
넣으면 완벽하게 렌더링됨**(A/B 스왑 테스트로 확정). 즉 우리 인코딩
로직의 버그가 아니라, 맑은 고딕이 그리는 특정 획 모양(폭 넓은 받침
ㅌ/ㄷ/ㅎ 등)을 PS Vita 실기의 글자 렌더러가 잘못 처리하는 것.

**최종 해결 (폰트 교체 없이)**: 이 문제 글자들이 **이전 세션에 이미
추가됐던 한글 배치(1107자, `eboot_backups/iconfont_fix_20260811_024219/
FontDataNormal.pgf.orig`)에 포함**되어 있고, 그 배치는 사용자가 실기에서
검증했을 때 잘림 문제가 전혀 없었다는 사실을 활용 — **그 배치의 검증된
원본 glyph 바이트(헤더+RLE 픽셀 데이터)를 그대로 재사용**함. 확인 결과
이번에 추가하는 1335자 중 무려 **1106자(83%)가 이미 그 배치에 있어서
전부 재사용**, 나머지 229자만 새로 래스터라이즈. 사용자가 "재사용 안
된 글자들도 다른 글자와 렌더링이 다르게 보인다(더 얇음)"고 추가로
지적해서, 재사용 배치 글자('같' 등)의 저장된 w/h/left(25x24,left=1)를
역산해서 **원본이 맑은 고딕 볼드(malgunbd.ttf)로 래스터라이즈됐음을
확인**(같은 크기 26으로 볼드 렌더링하면 정확히 일치) → 새로 그리는
229자도 볼드로 통일. 메타데이터(bearing/dimension) 방식도 재사용
글자·새로 그린 글자 모두 flags=0x3D(index 모드)에 인덱스 전부 0(원본
폰트에 원래 있던 고정 테이블 참조: dim=(0,0), xAdj=(0,-895),
yAdj=(-145,1685), adv=(1792,1792))으로 통일 — 이것도 원래 1107자
배치가 쓰던 그대로의 방식.

**시도했지만 효과 없었던 것들**(다음에 또 이 삽질을 반복하지 않도록
기록): bearing/dimension을 글자별 raw 값으로 계산(오히려 배치 간
불일치의 원인이 됐던 접근), shadowFlags를 41로 수정, RLE 런렝스 캡
조정, RUN/LITERAL 인코딩 모드 전환, 픽셀 값 이진화, 래스터 크기 변경
(26→27), 파일 내 word offset을 0으로 이동, 다른 폰트(굴림)로 대체(효과는
있었지만 렌더링 일관성을 해쳐서 사용자가 거부).

배포: `_Data/Font/FontDataNormal.pgf`+`FontDataMin.pgf`+
`_Data/System/CharaParamDataPack.dat`(신규) 갱신 → `rebuild_xdelta.py`
재실행(1776개 파일 패치, 왕복 검증 통과) → `3_patched/toidata_release_ko.xdelta`,
`xdelta_release/...`, `ready_to_use/toidata_release.l7c`,
`loose_files/toidata_release.l7c` 전부 갱신. 기존 파일은
`deploy_backups/xdelta_pre_20260818_072136/`에 백업. `PATCH_적용법.md`
(루트+`3_patched`) 새 해시로 갱신 완료.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-18 최종 갱신) | 1,783,685,109 | `e5c46a4a4952f3b63381d6368492f740c0f291924b2af82a9a7cbb8017bfc56c` |
| `toidata_release_ko.xdelta` | - | 91,247,362 | `11d615e41aaed2c3dd361aa3ca9e2d121539450ba5f86d56d4f566cc3dbe589b` |

### 남은 것
- **"칭호"/"인연" 팝업이 CharaParamDataPack.dat 수정 후에도 여전히
  카타카나로 보이는지 다음 세션에서 재확인 필요** — 확인 못 하고
  세션 종료.
- FontDataMin.pgf, CharaParamDataPack.dat가 이번 세션에 처음으로
  loose_files/xdelta에 포함됨 — 다음 세션에서 폰트/이름 관련 작업 시
  반드시 셋 다(Normal, Min, CharaParamDataPack) 같이 확인할 것.
- 라이브 Vita3K 테스트 설치의 대화 파일(`_Data/Script/Twn/00/03/700.dat`)은
  테스트용으로 임시 수정했다가 원본으로 복구 완료(해시
  `eea6f9737660ff2a3d6a4e011dc094430927f479d3e94141b1945462606488c4`).
- `build_final_font.py`는 여전히 스크래치 폴더에만 있음 — 다음 세션에서
  또 폰트를 건드릴 필요가 있으면 `tools/` 밑으로 옮기는 것을 고려
  (verbatim-override 소스 경로, 볼드 폰트 경로, 양쪽 글꼴 캘리브레이션
  상수가 전부 이 파일에 있음). `OLD_WORKING_PGF` 경로가 가리키는
  `eboot_backups/iconfont_fix_20260811_024219/` 폴더는 임시 백업
  디렉터리라 삭제되지 않도록 주의 — 없어지면 verbatim override가 통째로
  깨짐.
- 무기점 캐릭터 이름 수정은 CSV 도구 없이 **원본 파일에 직접 바이트
  치환**한 것 — 정식 CSV 추출/재컴파일 도구가 없는 파일이라(`packfield.py`
  가 다루는 section 30/32/33/34/36/37에 CharaParamDataPack은 없음),
  다음 세션에서 이 파일에 또 손댈 일이 있으면 같은 방식(오프셋
  하드코딩)을 써야 함. 8개 이름 전부 원본보다 짧거나 같은 바이트 길이라
  안전했지만, 다른 필드를 건드릴 땐 길이 초과 여부를 반드시 확인할 것.

---

## 추가 진행 (2026-08-18, 위 폰트 작업보다 이전 시점): 앙쥬 말투 수정 + no_trans.PNG(.pck 내장 텍스트) 조사

**1. 완료: 앙쥬(엘마다 합류 이후) 말투 존댓말 오역 + "え!"→"네!" 오역 수정.**
사용자 지시대로 원문 기준으로 재검토해서 Story.csv 37건(반말인데 존댓말로
번역된 것) + Story.csv/Skit.csv 도합 4+1건("え/ええ!"를 긍정 대답 "네!"로
오역한 것)을 고쳐서 재컴파일·라이브 설치 배포·xdelta 재생성까지 완료함
(Script 21개 파일 + Skit 1개 파일 변경, 전부 해시 대조 검증). 이 배포판
해시는 이후 세션(위 폰트 작업)이 그 위에 추가로 패치를 얹으면서 이미
`PATCH_적용법.md`의 최신 해시로 대체됨 — 이 항목 자체는 별도 재작업 필요
없음.

**2. 미해결(보류로 확정): `font_error/no_trans.PNG` — 이미지에 미리 구워진
(baked) 텍스트는 번역 불가능 확인.** 사용자가 실기에서 미번역으로 뜨는
장면을 보고했고, 처음엔 "Story.csv는 이미 정확한데 왜 실기에 일본어가
뜨나"로 헤맸으나(정적 파일 검증만으로 "이미 고쳐졌다"고 결론 낸 게
실수 — 사용자의 실기 관찰을 우선해야 했음), 사용자가 직접 Vita3K 트레이스
로그에서 `_Data/Field/Effect/Sprite/030.pck`가 열리는 것을 찾아줘서
돌파구를 얻음. 이 장면은 **대사 시스템(Script/.dat)을 전혀 타지 않고
스프라이트 이미지에 텍스트가 직접 픽셀로 구워져 있는 방식**임을 확인.
`.pck` 컨테이너 구조는 상당 부분 해독했지만(960x544 Vita 해상도, 8bpp
그레이스케일로 추정되는 위치까지 특정), **정확한 픽셀 인코딩은 못 풀었음**
(raw로 읽으면 글자 위치는 맞는데 속 빈 윤곽선만 나오고, 실제 화면은 두꺼운
채움 글씨라 불일치 — PSP식 8x8 스위즐도 시도했으나 실패). 사용자와 상의해서
**이번 버전은 이 장면을 보류(알려진 미번역 항목)로 남기기로 결정**함. 조사
전체 기록은 `tools/toir/font_research/sprite_text_pck/README.md`, 재현용
스크립트는 같은 폴더의 `decode_pck_test.py` 참고. 다음에 이 문제를 다시
붙잡을 때는 이 README부터 읽을 것 — 특히 "시도했으나 실패한 가설" 목록을
반복하지 않도록.

---

## 추가 진행 (2026-08-19): 미표시(밑줄) 한글 39자 추가 — 폰트 커버리지 100% 완료

사용자가 `font_error/after_font_6.PNG`(장비 화면에서 "아멧헬름"이 "아 _
헬름"으로 보임)를 제보하며 전수조사를 요청함. `2_translated/` 전체 CSV
1,705개 파일(72,751행)에서 실제 쓰이는 한글 음절을 전부 추출해 두 폰트
파일(`FontDataNormal.pgf`/`FontDataMin.pgf`)의 glyph 테이블과 직접
대조한 결과, 08-17/08-18 세션의 1335자 추가 배치에서 **39자가 누락**돼
있었음을 확인(딕/샷/닉/밸/띤/텔/텝/톰/늄/멧/뮬/윙/짙/쫑/캔/펀/굿/뙤/랗/
뱄/숄/슛/싣/썽/쏙/엠/윗/잿/캣/퀵/헥/닛/롬/앎/왈/쿼/킥/펼/횡).

**이 세션에서 처음으로, 유실됐던 `build_final_font.py`(08-18 세션의
실제 최종 빌드 스크립트, `OLD_WORKING_PGF` 백업 폴더와 함께 완전히
사라짐 — 위 08-09/08-18 항목에서 이미 경고했던 리스크가 실제로
발생함) 없이, 순수하게 이미 배포된 폰트 파일의 바이트를 직접 역산해서
인코딩 규칙을 재확립함:**
- 이미 정상 작동 중인 20개 이상의 글자를 표본으로 raw glyph record를
  직접 디코딩해서 "크기" 필드(레코드 맨 앞 14비트, 예전 크래시의 근본
  원인이었던 그 필드)가 정확히 `ceil((96 + RLE니블수*4) / 8)`— 즉
  헤더(96비트)+RLE 압축 픽셀 스트림의 **패딩 없는 정확한 바이트
  길이**와 항상 일치함을 확인(레코드 저장 시 4바이트 경계로 패딩하는
  건 이 필드와 무관한 별개 단계).
- 맑은 고딕 **볼드**(`malgunbd.ttf`)로 raster_size=26/origin=(10,5)/
  canvas=(60,60)/top_offset=15(Normal), raster_size=23/나머지 동일/
  top_offset=11(Min)로 래스터라이즈하면 기존 글자들과 픽셀 단위로
  100% 동일하게 재현됨을 20자 이상 검증(Normal 20자, Min 11자 전부
  일치) — 즉 이 조합이 08-18 세션이 실제로 썼던 정확한 계산값임을
  간접 증명.
- flags=0x3D, shadowFlags=41(2/2/3비트로 분할해서 1,1,1 조합), shadowID=0,
  dimIdx=bxIdx=byIdx=advIdx=0(각 테이블 인덱스 0이 이미 dim=(0,0),
  xAdj/yAdj/advance의 원하는 고정값을 담고 있음)도 기존 glyph들에서
  전부 동일하게 확인.

이 규칙으로 39자를 양쪽 폰트에 추가한 뒤: (1) 재파싱해서 39자 전부 유효
glyph 확인, (2) 전체 코퍼스(1374자) 기준 미커버 0자 확인, (3) 기존
1335자 전부 픽셀 단위로 재대조해서 손상 없음(불일치 0건) 확인, (4)
**실제 Windows Vita3K에 배포해서 부팅 테스트**(과거 크래시가
100% 재현되던 지점인 오프닝 무비 `movie_001.mp4`/`movie_002.mp4` 재생
구간 포함) — 크래시 없이 정상 진행, **사용자가 직접 "잘 돌아가고있어"로
실시간 확인**. 이후 `3_patched/loose_files`, `toidata_release_ko.xdelta`,
`ready_to_use/toidata_release.l7c`까지 정식 배포 완료, `PATCH_적용법.md`
(루트+`3_patched`)의 해시표·"알려진 이슈" 섹션도 갱신함(예전 "230자는
빈칸으로만 표시, 완전한 수정 불가"라던 항목은 이제 완전히 해결된 것으로
정정).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-19 최종) | 1,783,700,274 | `44e52566eb53dc784899f2ab3d82b238cef10c8687bd75ef0820370e36c81e08` |
| `toidata_release_ko.xdelta` | - | 91,264,028 | `b118ad4eb59a5bc8d7a79c15d851e1c4901225c9d49a9620be8061b6255212f2` |

**남은 것**: 이번에 역산한 인코딩 규칙(size 필드 공식, 볼드 폰트 캘리브레이션
상수, shadowFlags/테이블 인덱스 값)을 재사용 가능한 스크립트로
`tools/toir/font_research/hangul_fix/`에 정식 편입해두면 좋음(현재는
이 세션의 스크래치 폴더 `build_missing_39.py`에만 있음 — 다음에 또
폰트를 건드릴 일이 있으면 이 파일부터 참고하되, **스크래치 폴더는
세션이 끝나면 사라질 수 있으니** 이번엔 이 문서에 계산값 자체를 전부
남겨뒀음). 이제 실사용 한글은 전부 커버되므로, 앞으로 새 번역 텍스트를
추가할 때만(신규 CSV 라인 등) 이 스캔+대조 과정을 다시 돌리면 됨.

---

## 추가 진행 (2026-08-19, 같은 날 이어서): 캐릭터명/UI라벨 "얇아 보임" 조사(결론: 버그 아님) + top 필드 패치 2연속 실패·롤백 + 특수문자 4자 추가 성공

### 1. "캐릭터명이 얇아 보인다" 제보 조사 — 결론: 폰트 데이터 버그 아님

사용자가 `charname_font_1~7.PNG`, `font_1~3.PNG`로 무구점 캐릭터 스탯
패널의 이름("루카"/"앙쥬"/"스파다" 등)과 "코스트"/"카운트"/"소비TP"/
"공중 가능성" 같은 보조 라벨이 아이템명("라멜라레더" 등)이나 특기명
("열공참" 등)보다 얇아 보인다고 제보. 직접 대조 검증한 결과, **양쪽
텍스트가 전부 같은 카테고리의 글리프 데이터를 쓰고 있음을 확인**
(`FontDataNormal.pgf`의 "1106자 재사용" 배치) — 즉 폰트 데이터 자체는
동일하고, 캐릭터명/보조라벨 쪽이 `FontDataMin.pgf`(더 작게 디자인된
전용 폰트, 전체 1374자 100% 볼드 일치 확인됨)로 그려지기 때문에 작아
보이는 것으로 결론. **이건 원본 일본어판도 똑같이 작은 폰트를 썼을
UI 설계이고, 폰트 파일 편집으로 고칠 수 있는 부분이 아님.** 다음
세션에서 이 제보가 다시 나와도 재조사할 필요 없음.

### 2. 🔴 "top 필드" 직접 패치 시도 2회, 전부 롤백함 — 매우 중요한 교훈

이 조사 도중 "리카르도"의 "르도"가 "리카"보다 위로 붕 떠 보인다는
별개 제보가 나왔고, 정적 분석으로 `FontDataNormal.pgf`에서 47자
(주로 상하모음 ㅗ/ㅛ/ㅡ/ㅠ + 받침없는 글자: 고교그꼬끄노뇨느도드또뜨
로료르모묘므보브뾰쁘소쇼스쏘쓰오요으조죠쥬즈쪼코크토트포표프 등)가
w/h/left/flags/shadowFlags/pixel 데이터는 전부 정확한데 "top"(세로
위치) 필드만 저장된 값이 계산해야 할 값과 다름(-4 또는 -2)을 발견.

**1차 시도**: 이 중 4자(도/르/스/쥬)만 top 필드 7비트를 비트 단위로
직접 패치(레코드 재작성 없이, 단위테스트로 정확히 그 7비트만 바뀜을
확인, 전체 코퍼스 회귀검사 0건). 실기 부팅 테스트도 크래시 없이 통과.
→ **사용자 확인 결과 "문제점은 그대로고, 기존 폰트들까지 엉망이
됐다"** → 즉시 백업으로 롤백.

**2차 시도**: 스코프를 47자 중 42자(상하모음 그룹만, +2 부호가 반대인
닷/핻/활/흑/흔 5자는 제외)로 넓혀서 재시도. 이번엔 단일 문자 비트 단위
단위테스트 + **전체 8,889개 glyph 기준** 회귀검사(CSV 코퍼스 범위
밖까지 전수 확인)까지 통과, 실기 부팅 테스트도 통과. → **역시 사용자
확인 결과 "다른 글자에도 영향이 갔어"** → 즉시 롤백.

**교훈(다음 세션 필독)**: 이 프로젝트의 정적 검증 도구(`pgf_parse.py`
기반 재파싱 비교)로 "완벽하게 통과"해도, **실제 PS Vita 펌웨어/Vita3K
LLE가 PGF를 렌더링하는 방식에는 이 도구가 놓치는 뭔가가 있다.** 2회
연속으로 "정적 검증 통과 + 크래시 없는 부팅 테스트 통과"에도 불구하고
사용자가 실기에서 다른 글자가 깨지는 걸 확인했다 — 원인을 특정하지
못한 채 종료함. **사용자가 명시적으로 "top 필드 직접 패치 방식은
그만하라"고 지시함.** 앞으로 기존 glyph의 메타데이터(w/h/left/top
등)를 수정하는 방식은 시도하지 말 것 — 이미 존재하는 글리프는
건드리지 않고 새 글리프만 "추가"하는 방식(아래 3번, 그리고 위 39자
작업)만 안전이 확인된 방식임.

### 3. ✅ 특수문자 4자(·ㆍ—−) 추가 — 성공, 배포 완료

`_issue_1~3.PNG`에서 "아 _ 날씨 좋구먼", "도구 _ 식재료점" 등 밑줄이
여전히 남아있다는 제보로, `2_translated/` 전체 코퍼스에서 **ASCII를
제외한 모든 문자**(한글 음절 이외 포함, 지난 39자 작업의 스캔 범위가
한글 음절로 한정됐던 사각지대)를 스캔한 결과 6자 발견: `·`(가운뎃점),
`ㆍ`(한글 아래아), `—`(줄표), `−`(마이너스), `≥`(이상), `ㅇ`(홀로 쓰인
자음). 이 중 4자를 처리(≥, ㅇ은 사용 빈도 2회로 낮고 검증이 더
필요해서 다음으로 미룸).

**1차 시도 실패**: 39자 때와 같은 방식(맑은 고딕 볼드로 직접
래스터라이즈해서 top 계산)으로 4자를 새로 그려서 추가 → 정적 검증은
통과했으나 **사용자가 "공백으로 나온다"고 확인**. 원인 조사 결과:
① 39자 작업 때 역산한 top 계산 공식(origin=(10,5), top_offset=15)은
"글자 상자를 꽉 채우는" 한글 음절에서만 우연히 맞았던 것이고, 점·대시
처럼 기준선 근처에 낮게 앉는 기호에는 전혀 안 맞음 — 원본에 이미
있는 비슷한 기호들(가타카나 가운뎃점 ・=top13, 전각마이너스－=top12,
하이픈-=top12, 물결표～=top13, 콜론:=top14, 마침표.=top4)과 비교하니
제가 계산한 값(34~36)은 20 이상 차이났음. ② `−`(U+2212, 수학
마이너스 기호)는 **맑은 고딕 볼드에 실제 글리프가 없어서** PIL이 빈
상자(ink 없음)를 반환하고 있었음 — 애초에 잘못된 문자를 그리고
있었던 것.

**2차 시도 성공(배포 완료)**: 새로 계산하지 않고, **이미 정상 작동
중인 원본 글리프 레코드를 통째로 복사**하는 방식으로 전환:
- `·`(U+00B7), `ㆍ`(U+318D) ← `・`(가타카나 가운뎃점, U+30FB) 레코드
  그대로 복사
- `−`(U+2212) ← `-`(하이픈, U+002D) 레코드 그대로 복사
- `—`(U+2014)는 자체 렌더링 모양(w=27,h=3,left=0)은 맞았으므로, top
  값만 검증된 기준값 12로 교정(나머지 필드는 자체 렌더링 값 유지)

파일 끝에 마지막 glyph가 딱 붙어서 리더의 안전 여유(1024비트=128바이트)
부족으로 무효 취급되는 별개 버그도 발견해서 수정(파일 끝에 256바이트
여유 패딩 추가). 전체 8,889개 glyph 기준 회귀검사 0건, 실기 부팅
테스트 크래시 없음, **사용자가 실기에서 직접 "정상이야" 확인** 후
`loose_files`/xdelta/`PATCH_적용법.md`(루트+`3_patched`) 정식 배포 완료.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-19 2차 최종) | 1,783,701,090 | `c9db2f91cf201b52d9e9737538444121e2f7bfe16524e18cd00b25b7664e13c7` |
| `toidata_release_ko.xdelta` | - | 91,264,951 | `4949feb51a6a950d67a2d1eef81eb53c501d61cf290a09c5d32076618bb91395` |

**남은 것**:
1. `≥`(U+2265), 홀로 쓰인 자음 `ㅇ`(U+3147) 2자 아직 미포함(각 사용
   2회). 원본에 마땅한 재사용 대상 글리프가 없어서, 추가하려면 다시
   신중하게(가급적 "레코드 통째로 복사" 방식을 우선 시도) 접근할 것.
2. `xdelta3.exe`가 이번 세션 스크래치 폴더에서 유실되는 일이 있었음
   (`C:\Users\cywyb\Downloads\claude\patch\xdelta3\xdelta3-3.2.0-windows-x86_64\xdelta3.exe`에
   안정적인 사본을 새로 둠 — 스크래치 폴더 대신 이 경로를 `rebuild_xdelta.py`
   기본 경로로 삼는 것을 고려).
3. `build_4symbols_v2.py`(4자 복사 방식 빌드 스크립트)와
   `full_char_audit.py`(ASCII 제외 전체 문자 코퍼스 감사 스크립트)는
   `tools/toir/font_research/hangul_fix/`에 정식 편입해둘 것(현재
   이 세션 스크래치에만 있음 — 위 유실 전례 반복 방지).
4. **top 필드 직접 패치 접근은 사용자가 중단을 지시함** — 위 2번
   항목의 47자(특히 42자 상하모음 그룹) 위치 문제는 여전히 미해결
   상태로 남아있으나, 재시도하려면 왜 정적 검증을 통과해도 실기에서
   깨지는지 근본 원인부터 먼저 이해해야 함(현재는 원인 불명).

---

## 추가 진행 (2026-08-19, 같은 날 세 번째 세션): 위 버그를 다른 세션이 독립적으로
재발견 → 실수로 잠깐 라이브에 배포했다가 즉시 롤백 + Ghidra 코드 분석 시도

`/clear` 이후의 새 세션이 위 "top 필드" 버그를 처음부터 다시 진단해서(별도
계산으로 도달, 결과는 정확히 동일: Normal 50자/Min 48자, 베이스라인 상수
46/39) `fix_baseline_align.py`를 새로 작성 — 이 세션은 위 원칙 변경 이력을
**세션 시작 시 못 읽고** 라이브 설치에 한 번 배포했으나, 바로 다음 턴에서
이 파일을 마저 읽고 즉시 원본 백업으로 롤백함(해시 대조로 완전 원상복구
확인, 라이브 설치는 최종적으로 전혀 안 바뀐 상태). **다음 세션 필독: 이
파일 전체를 끝까지 읽지 않고 작업을 진행하면 이런 실수가 재발함 — 특히
새 세션 시작 시 이 파일 뒷부분(가장 최근 항목들)까지 반드시 다 읽을 것.**

이어서 사용자가 "앙쥬" 캐릭터명 뒤에 뜨는 정체불명의 밑줄(`_ _`) 잔상이
top값 버그와 관련 있는지 물어봄 → 픽셀 좌표 대조로 검증(`리카르도`의
"르"/"도" 위치와 `앙쥬`의 잔상 위치가 거의 정확히 일치, 3건 중 2건 재현) →
"지우기 로직이 버그 있는 top/height로 지울 영역을 계산해서 덜 지워지는
것 아니냐"는 가설을 세우고 Ghidra로 코드 분석 시도. `CPrimFont`(폰트
렌더러)/`CCharaSimpleParam`(이름+스탯 박스로 추정) 클래스 구조와 vtable
추적 방법론(문자열 주소 자체를 findBytes로 검색하는 게 `문자열-8` 공식보다
안정적임, 생성자 코드의 `&PTR_FUN_..._주소` 라벨에서 vtable 주소를 직접
읽는 게 더 빠름)까지는 확립했지만, **가상함수 호출은 자동 xref 추적이
안 돼서** "지우기 공식"까지는 확정 못 하고 사용자 지시로 중단함. 상세
기록·재현 방법·다음 단계는 `tools/toir/font_research/hangul_fix/SESSION_2026-08-19_baseline_float.md`
참고. **top 필드 직접 패치는 이번에도 (실수로 잠깐 배포된 것 외에는)
시도 안 함 — 여전히 금지 상태 유지.**

**같은 날 이어서(사용자가 "계속해"를 여러 번 반복 지시, 최종 정정):**
텍스처 가능성도 확인함 — `1_extracted/SystemTex`의 182개 파일 전부 육안
확인, 문제 라벨(카운트/코스트/발동전환 등) 전부 텍스처 아님을 확정(전부
동적 텍스트). 부수적으로 `0016.png`가 미번역 상태(이미 번역된 `0022`/`0149`의
중복 슬롯으로 추정, 실제 사용 여부 미확인)임을 발견.

이후 Ghidra 조사를 훨씬 깊이 이어가서(스크립트 70여 회), **"카운트"/
"코스트"/"발동 전환" 등 12개 라벨을 실제로 등록하는 함수를 문자열 주소
직접 검색으로 전부 특정**했고, 그 라벨 큐 삽입(`FUN_8112caa0` 계열) →
우선순위 정렬 연결리스트 삽입(`FUN_8101a972`) → 실제 GPU 그리기 호출
(`FUN_8101bd4c`, SceGxm류)까지 전체 렌더링 경로를 끝까지 추적함. **결론:
이 경로 어디에도 "이전 내용 지우기" 연산이 없음** — 순수하게 "그릴 것
추가"만 있고 지우는 코드 자체가 존재하지 않음. 이에 따라 **"잔상 버그가
top값 버그 때문"이라는 가설을 최종적으로 기각**하고, 대신 "라벨 큐의
항목 개수 카운터가 늘어날 때만 자동 갱신되고 줄어들 때는 명시적 리셋에
의존하는 구조라, 리셋이 누락되면 잔상이 남는다"는 새 가설로 정정함. **즉
"위로 뜨는" 버그와 "잔상" 버그는 서로 다른 원인의 별개 버그로 재분류함.**

**같은 날 더 이어서(사용자가 "GetName부터 다시 찾아봐" 지시, 최종
마무리)**: 캐릭터 이름("앙쥬"/"리카르도" 등) 자체를 그리는 함수를 찾으려
`CCharaNameMgr`(생성자 뒤 로더 함수가 `CharaNames.csv`의 실제 인코딩
공식(36바이트 간격)과 일치함까지 확인, 그러나 조회 함수는 못 찾음)
→ `CCharaResourceMgr`(이름과 무관, 실제로는 3D 모델/텍스처 로딩 전담
클래스였음) → `CCharaParamMgr`(전역 `DAT_811797f0`이 이 인스턴스임을
확정, 캐릭터별 구조체는 확인해보니 순수 수치 데이터뿐 이름 없음) 순으로
전부 훑었으나 **끝내 이름 문자열을 반환하는 전용 함수를 못 찾음.** 이
게임 코드가 접근자 함수 없이 호출부마다 직접 `포인터+고정오프셋`으로
필드를 읽는 스타일임을 반복 확인했고, **이 특정 질문은 정적 분석
(Ghidra)만으로는 한계가 있고 동적 분석(실행 중 디버깅)이 필요하다는
결론으로 마무리함**(사용자 지시로 종료). 잔상 버그의 핵심 결론(위 문단,
지우기 연산 자체가 없음)은 이와 무관하게 이미 확정된 상태.

상세 기록·재현 방법·스크립트 목록은 전부
`tools/toir/font_research/hangul_fix/SESSION_2026-08-19_baseline_float.md`에
갱신해뒀음. **다음에 이 잔상 버그를 더 파려면 정적 분석 재시도가 아니라
동적 분석(디버거) 쪽으로 접근할 것** — 같은 클래스들(CCharaNameMgr/
CCharaResourceMgr/CCharaParamMgr)을 다시 뒤지지 말 것, 이미 소진함.

---

## 추가 진행 (2026-08-20): 대사창 밖으로 넘치는 대사 줄바꿈 정리 + xdelta/l7c 재빌드 배포 완료

### 배경
사용자가 `Tales-of-Innocence-R/line/line_1.PNG` 스크린샷을 보여주며,
루카 아버지 대사가 대사창 밖(장식 테두리/아이콘 쪽)으로 삐져나오는 걸
발견. "세 줄 이상 줄바꿈된 대사들만" 정리해달라고 요청 → 이후 "119개
(Story.csv 전체 후보) 다 진행하고, Skit.csv도 이어서 확인해줘, 현재
번역된 내용은 건드리지 말고 줄바꿈 옵션만 바꿔서 자연스럽게" 로 범위
확정.

### 기준값 산출
`line_1.PNG`를 픽셀 단위로 크롭·색 임계값 분석: 23자 줄은 안전하게
안착(x=735), 27자 줄은 장식 테두리를 침범(x=856). **가시 문자(제어
태그 `{blue}`/`{variable}` 등 제외) 25자 이상이면 위험, 24자 이하를
목표로** 재배치하도록 임계값 확정.

### 알고리즘
`tools/toir/font_research/hangul_fix/rewrap/rewrap_lines.py` — 원래
줄 개수(N)를 그대로 유지한 채, 토큰(단어) 순서는 절대 안 바꾸고 "어디서
끊을지"만 이진 탐색 + DP로 재배치해서 최대 줄 폭을 최소화. 제어 태그는
폭 계산에서 제외하되 원래 붙어있던 단어에 붙여서 이동. `\u3000`
(전각공백, 장식용 들여쓰기/서명)이 들어간 항목은 자동 재배치 대상에서
제외(서식이 깨지므로).

### 적용 결과
- **Story.csv**: 119개 후보 중 **112개 적용**, 3개는 `\u3000` 서식이라
  제외, 4개는 최적 재배치로도 폭 초과(25~31자)라 제외
- **Skit.csv**: 103개 후보(Field가 `line`/`line_addition`인 행만 대상 —
  실제 대사창에 그려지는 텍스트) 중 **99개 적용**, 4개는 폭 초과(25~26자)
  로 제외(이번엔 `\u3000` 케이스 없음)
- 적용 스크립트: `apply_story_rewrap.py`, `apply_skit_rewrap.py` (둘 다
  `2_translated/script_wip/`, `2_translated/skit_wip/`를 원본 텍스트와
  정확히 일치하는 행에만 덮어쓰도록 안전장치를 둠 — unmatched 0건으로
  전량 안전하게 적용됨)
- **보류 11개 항목**(문구를 안 바꾸는 한 자동으로는 해결 불가) 목록·원문·
  출처는 `tools/toir/font_research/hangul_fix/rewrap/FLAGGED_11_items.md`
  참고. 3개는 전각공백 장식 서식, 8개는 폭 1~8자 초과. **다음 세션에서
  사용자에게 확인할 것: 대사창이 4줄까지 지원 가능한지, 아니면 이대로
  둘지.**

### 🔴 작업 중 발견·복구한 자체 버그 (참고용, 최종적으로 완전 복구됨)
첫 번째 `apply_story_rewrap.py` 실행이 이 프로젝트 CSV의 `\r\n` 개행
관례(`merge_script()`가 `newline=''`로 열어서 그대로 보존하는 방식)를
제대로 처리하지 못해서, 길이 측정 시 `\r`을 글자로 잘못 세는 바람에
후보 수가 119→157로 부풀려졌고, `script_wip/*.csv` 최대 30여 행에
`\r`/`\n`/`\r\n`이 섞인 손상된 개행이 실제로 기록됨. **Story.csv를
아직 한 번도 재생성(merge-script)하지 않은 세션 초반 스냅샷**을 "손대기
전 원본"으로 삼아 `restore_from_story.py`로 전량 복구, 이후 `rewrap_lines.py`/
`apply_story_rewrap.py`를 `\r\n` 보존하도록 재작성해서 재적용. 배포 전
`script_wip/`(530개)·`skit_wip/`(989개) 파일 전체를 필드 단위로 재검사
(개행 혼용 여부)해서 **손상 0건**까지 확인 후 진행함. 앞으로 이 종류의
CSV 재작성 스크립트를 새로 짤 때는 반드시 `newline=''`로 열고 `\r\n`을
문자열로 그대로 다루되, 길이/줄 수 계산 시에만 `\r\n`→`\n` 정규화한
사본을 별도로 써야 함(정규화된 값을 그대로 저장하면 이번과 같은 사고
재발).

### 배포 (실기 + xdelta 배포판 모두 완료)
1. `merge-script`/`merge-skit`으로 `2_translated/Story.csv`/`Skit.csv`
   재생성(각각 8162/8162, 19209/19236 번역 완료 — Skit 27개 미번역은
   이번 세션과 무관한 기존 상태), `check script`/`check skit` 각 1건은
   전부 이번 작업과 무관한 기존 이슈(용어집 불일치, 오타)로 확인.
2. `recompile_story`/`recompile_skits`를 **실기 설치**
   (`C:\Users\cywyb\AppData\Roaming\Vita3K\Vita3K\ux0\app\PCSG00009\`)를
   l7cdir 소스로 삼아 실행 → 스테이징 출력과 실기 설치를 diff해서 실제
   변경된 파일만 특정: **`_Data/Script/` 105개 + `_Data/Field/Skit/Data/`
   91개 = 196개**(`Twn/00/03/711.dat` — line_1.PNG 원본 파일 포함).
   기존 196개는 `deploy_backups/20260820_233445_rewrap/`에 백업 후
   실기 설치 + `3_patched/loose_files`에 배포, 재대조 불일치 0건.
3. **정식 xdelta 배포판도 재생성**: `l7ca_patch_multi.py`로 순정 원본
   `0_03/원본/toidata_release.l7c`(1,475,397,012바이트)에 `3_patched/
   loose_files`의 누적 전체 1,776개 파일 패치 → 새 `toidata_release.l7c`
   (1,783,701,241바이트) 생성 → 전체 재추출해서 `loose_files`와
   1,776/1,776 전수 일치 확인 → `xdelta3 -e -9 -S -s`로
   `toidata_release_ko.xdelta`(91,265,285바이트, ~87MB) 생성 → 원본+diff
   왕복 검증(바이트 단위 완전 일치) 통과. **`xdelta3` 바이너리는 이
   환경에 없어서 이번에도 GitHub(`jmacd/xdelta` v3.2.0 Windows 릴리스)
   에서 받아 씀(프로젝트에 커밋 안 함, 다음 세션에서 또 필요하면 재다운로드)**.
4. `3_patched/toidata_release_ko.xdelta`, `3_patched/xdelta_release/
   toidata_release_ko.xdelta`, `3_patched/ready_to_use/toidata_release.l7c`,
   `3_patched/loose_files/toidata_release.l7c` 전부 갱신(기존 파일은
   `deploy_backups/xdelta_pre_20260821_001242/`에 백업). `PATCH_적용법.md`
   (루트 + `3_patched` 양쪽) 새 해시로 갱신 완료.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-20 갱신) | 1,783,701,241 | `e6e5474323a4fd373a552c1c68b479e8e9d3a5ba1bb6aa954ad947387311a21b` |
| `toidata_release_ko.xdelta` | - | 91,265,285 | `122981d5455b7cba1f0b409642ac364414e6df0bdcba45666646951ad43e5de4` |

`eboot_ko.xdelta`는 이번 세션에서 `eboot.bin`을 안 건드렸으므로 그대로
유지.

### 다음 세션에서 이어서 할 일
- **보류 11개 항목 처리 방향 확인**(대사창 4줄 지원 가능 여부가 관건 —
  가능하면 4줄로 재배치, 안 되면 문구를 살짝 줄이는 것도 검토, 목록은
  위 `FLAGGED_11_items.md` 참고)
- Skit.csv에 남아있는 27개 미번역(빈 값) 행 — 이번 세션 범위 밖이라
  손대지 않았음, 필요하면 다음에 확인
- 이번에 만든 `tools/toir/font_research/hangul_fix/rewrap/` 안의
  스크립트들(`rewrap_lines.py`, `scan_story.py`/`scan_skit.py`,
  `apply_story_rewrap.py`/`apply_skit_rewrap.py`,
  `restore_from_story.py`)은 재사용 가능한 상태로 남겨둠 — MapData.csv
  에도 같은 종류의 넘침이 있는지는 아직 확인 안 함(사용자가 요청한
  범위는 Story/Skit까지였음)

---

## 추가 진행 (2026-08-22): 대사창 "누적 줄 수 초과" 발견 및 수정 + xdelta/l7c 재빌드 배포 완료

### 배경
사용자가 `line/line_2.PNG` 스크린샷을 보여줌 — 앙쥬가 "…나 진짜 어른이네"
라고 말하는 4번째 줄이 화면 하단 밖으로 잘려서(글자 윗부분만 조금 보임)
표시되는 사례. 2026-08-20에 이미 "대사창 밖 넘침"을 고쳤는데도 여전히
발생하는 걸 보고 원인을 다시 조사.

### 원인 (2026-08-20 수정과는 다른 종류의 버그)
소스를 추적한 결과 `Skit.csv` `4053.dat`, 앙쥬 대사 — `line`(idx41, 2줄:
"겉과 속이 없으니까 루카 군보다는" / "훨씬 상대하기 편할지도……") 뒤에
`line_addition`(idx44, 2줄: "하아……" / "이런 분석 같은 걸 하다니, 나
진짜 어른이네")이 같은 대사창에 이어붙어 총 4줄이 되고, 대사창은 3줄까지만
안전하게 표시된다는 것을 확인(`line_2.PNG` 픽셀 측정: 줄 간격 32px,
화면 높이 544px 기준 정확히 3줄까지만 화면 안에 들어옴). 일본어 원문도
동일한 2+2줄 구조라 번역이 길어져서 생긴 문제는 아님.

**2026-08-20 수정과의 차이**: 그 수정(`scan_skit.py`)은 "한 CSV 행(Field)
안에서 3줄 이상 줄바꿈된 경우"의 가로 폭 초과만 검사했음. 이번 건은
개별 행은 각각 3줄 미만이라도, **서로 다른 두 CSV 행(`line`+`line_addition`)이
같은 대사창에 누적되면서 생기는 세로(줄 수) 초과** — 애초에 검사 대상이
아니었던, 구조적으로 다른 버그.

같은 그룹 안에 여러 `line_addition`이 있을 때 전부 항상 같이 재생되는 건
아님(idx21은 line_2.PNG 스크린샷에 안 나타남 — 분기/조건부 트리거로 추정).
그래서 "line + 특정 addition 하나만 붙어도 3줄을 넘는가"를 개별 쌍 단위로
검사하는 게 가장 정확한 모델임을 4053.dat 실측 사례로 교차검증함(완전
보수적 가정 159건은 과다 플래그, 최단 addition만 보는 방식 82건은 오히려
실측 사례를 놓침 — 개별 쌍 검사 108개 그룹/167쌍이 정답).

### 수정 (1차: Story.csv용 24자를 그대로 재사용)
`tools/toir/font_research/hangul_fix/rewrap/`에 새 스크립트 추가:
- `scan_skit_group_overflow.py` / `_strict.py` / `_pair.py` — 세 가지 기준
  교차검증용(위 문단 참고, 최종적으로 `_pair.py` 기준 채택)
- `fix_skit_group_overflow.py` — 실제 수정 스크립트. **번역 문구는 절대
  안 줄임**(사용자 명시 지시) — 기존 줄바꿈(`\n`) 위치만 재배치. 1단계로
  `line_addition`만 줄여서 시도, 안 되면(주로 `line` 자체가 이미 3줄이라
  addition만 줄여선 불가한 경우가 다수) 2단계로 공유되는 `line` 필드도
  함께 줄여가며(1줄씩) 재시도.

1차 결과(167쌍 중 121쌍 자동 수정, 46쌍 보류)로 실기 배포까지 마쳤으나,
**사용자가 "24자는 line_1(Story) 기준이고, SKIT은 line_2 대사창 여백을
따로 봐서 새 기준을 만들어달라"고 명확히 정정**함 — 4줄 지원을 요청한
게 아니라 처음부터 "3줄 안에 최대한 담되, SKIT 고유의 여백을 반영해서
Story.csv의 24자보다 더 정확한 기준을 쓰라"는 요청이었음(Skit_wip을
`deploy_backups/skit_wip_pre_group_overflow_fix_20260822/`의 순정
백업에서 다시 복원하고 재작업).

### 수정 (2차, 최종): SKIT 전용 비대칭 안전폭(줄 위치별로 다름)
`line_2.PNG`를 다시 픽셀 분석한 결과, '다음' ▼ 아이콘(x=876~903)은
**대사창의 3번째(마지막) 줄 자리에만** 고정으로 걸쳐 있고, 1~2번째
줄은 그 아이콘과 안 겹쳐서 화면 오른쪽 끝(x=940 근방)까지 훨씬 더
넓게 쓸 수 있음을 확인 — Story.csv와 SKIT의 대사창 자체가 다르므로
당연히 안전폭도 달라야 했음. 좌측 시작 x=264, px/문자 비율 ≈24.4
기준으로 재계산:
  - **WIDE=27자**: 대사창 1~2번째 줄(아이콘과 안 겹침)
  - **NARROW=24자**: 대사창의 3번째(마지막) 줄(아이콘 시작 지점 이내로
    제한 — 기존 Story.csv 24자와 동일)
이번에 고치는 케이스는 항상 line+addition 합계가 3줄이 되도록 만드는
것이라, `line` 필드는 항상 1~2번째 줄에만 위치(WIDE 적용), `line_addition`
필드는 그 안의 마지막 줄만 NARROW·그 앞 줄(들)은 WIDE — 이 비대칭 재배치를
위해 `rewrap_lines.py`에 `best_wrap_with_caps()`(줄 위치별 서로 다른 폭
캡을 지원하는 DP)를 새로 추가하고, `fix_skit_group_overflow.py`를
`rewrap_line_field()`/`rewrap_addition_field()` 두 함수로 재작성.

**최종 결과**: 167쌍 중 **134쌍 자동 수정**(1차보다 13쌍 더 해결, 공유
`line` 필드 80개도 함께 재배치), **33쌍(23개 그룹)은 문구를 안 줄이는 한
해결 불가**로 보류 — 목록은
`tools/toir/font_research/hangul_fix/rewrap/FLAGGED_group_overflow.md`
참고(4053.dat 실제 사례는 WIDE=27을 적용해도 여전히 해결 안 됨 — line
2줄을 1줄로 합쳐도 33자, addition 2줄을 1줄로 합쳐도 30자라 문구를
줄이지 않는 한 근본적으로 불가능한 케이스로 확인됨. **더 이상 "4줄
지원되면 해결"이라는 프레이밍은 쓰지 말 것** — 사용자가 이 표현 자체를
정정했음, 3줄 안에서 여백만 다시 본 것임).

### 검증
- 수정 전 `2_translated/skit_wip/` 전체를
  `deploy_backups/skit_wip_pre_group_overflow_fix_20260822/`에 백업(1차·
  2차 모두 이 백업에서 시작)
- **재작업 시 주의(직접 겪은 실수)**: skit_wip을 백업에서 복원한 뒤
  `merge-skit`으로 `Skit.csv`를 반드시 다시 생성해야 함 — 이걸 빼먹고
  스캔/수정 스크립트를 돌리면 스크립트가 읽는 `Skit.csv`가 여전히 예전
  결과를 반영한 상태라 대상 쌍 개수(167이어야 할 게 155 등으로)가
  틀리게 나오고 unmatched 오류까지 발생함 — 복원 직후 항상
  `python tools/translate_helper.py merge-skit`부터 실행할 것.
- `merge-skit` 재생성(19209/19236 번역, 변동 없음), `check skit` 기존
  "리카르돈" 1건 외 새 이슈 없음
- 개행(`\r\n`) 혼용 손상 전수 검사 0건
- 재스캔(`scan_skit_group_overflow_pair.py`)으로 134건이 실제로 안전해지고
  정확히 33건(보류 목록과 일치)만 남았음을 교차 확인

### 배포 (실기 + xdelta 배포판 모두 완료, 1차 배포 후 2차로 갱신)
1. `recompile_skits`를 실기 설치(`...\Vita3K\ux0\app\PCSG00009\`)를 l7cdir
   소스로 스테이징 출력 → diff로 실제 변경 파일 특정. 1차: `_Data/Field/Skit/Data/`
   **93개**(백업 `deploy_backups/20260822_195857_group_overflow_fix/`). 2차
   (WIDE/NARROW 재작업 후, 1차 배포 상태 기준 diff): 추가로 바뀐 **11개**
   (백업 `deploy_backups/20260822_202716_group_overflow_fix_v2_widenarrow/`).
   두 배포 모두 재대조 불일치 0건.
2. `3_patched/loose_files`도 동일하게 동기화 후 `l7ca_patch_multi.py`로
   순정 원본(`0_03/원본/toidata_release.l7c`, 1,475,397,012바이트)에 누적
   전체 1,776개 패치 → 새 `.l7c`(최종 1,783,701,065바이트) 생성 → 전체
   재추출(14,076/14,076)해서 loose_files와 1,776/1,776 완전 일치 확인.
   **주의**: `loose_files` 루트에 `eboot.bin`/`toidata_release.l7c`가 같이
   있으면 `l7ca_patch_multi.py`가 그것들도 "패치 대상"으로 오인해서 실패함
   (`_Data`만 있어야 함) — 매번 임시로 밖으로 뺐다가 완료 후 복원해서
   해결. 다음에도 이 도구 쓸 때 주의할 것.
3. `xdelta3`는 이번에도 환경에 없어서 GitHub(`jmacd/xdelta` v3.2.0 Windows
   릴리스)에서 재다운로드(커밋 안 함, 스크래치 폴더에 zip으로 캐싱해서
   재사용). `xdelta3 -e -9 -S -s`로 `toidata_release_ko.xdelta`(최종
   91,265,291바이트) 생성 → 원본+diff 왕복 검증(바이트 단위 완전 일치) 통과.
4. `3_patched/toidata_release_ko.xdelta`, `3_patched/xdelta_release/
   toidata_release_ko.xdelta`, `3_patched/ready_to_use/toidata_release.l7c`,
   `3_patched/loose_files/toidata_release.l7c` 전부 최종본으로 갱신(기존
   파일들은 각 단계마다 `deploy_backups/xdelta_pre_*/`에 백업). `PATCH_적용법.md`
   (루트 + `3_patched` 양쪽) 최종 해시로 갱신 완료.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-22 2차 갱신) | 1,783,701,065 | `fd83cdd58490343041c3e6a2ee12b403752aceb534d2059cc1389dcaefcfea6f` |
| `toidata_release_ko.xdelta` | - | 91,265,291 | `058d48df184f4a2b3186f826af4241d92f0f9c5ffffe51ca0d86bea963b34fa4` |

`eboot_ko.xdelta`는 이번 세션에서 `eboot.bin`을 안 건드렸으므로 그대로 유지.

### 추가 수정 (3차, 최종): line/line_addition 경계를 넘긴 재배치 — 단, 안전한 경우만

**사용자가 2차 결과(134/33)에 대해 재차 지적**: "하아…… 를 윗줄로 올리고
이런 분석~ 을 3번째 줄로 하면 되지 않냐" — 즉 `line`의 마지막 단어와
`line_addition`의 첫 단어를 섞어서 재배치하면 되는 거 아니냐는 질문.
맞는 지적이었음 — 2차 알고리즘은 `line`과 `line_addition`을 서로 다른
단어 풀로 취급해서 절대 안 섞었는데, 실제로 둘은 순서대로 이어지는
하나의 발화라 단어를 섞어도 내용/순서는 전혀 안 바뀜.

**사용자 승인 하에 구현했다가, 직접 심각한 버그를 발견함**: 처음엔 이
경계-넘김을 **모든** 그룹에 적용했더니(167쌍 중 160쌍 해결, 7쌍만 보류
— 겉보기엔 훨씬 좋은 결과), 4053.dat에서 검증하다가 `line`이 여러
`line_addition`과 **공유**되는 경우 문제가 생기는 걸 발견함: `line`
idx41(2줄) 뒤에 `line_addition`이 idx21과 idx44 **두 개** 달려 있는데
(둘 중 매 재생마다 하나만 조건부로 나오는 것으로 추정, line_2.PNG
스크린샷에도 idx21은 안 보였음), idx44에 맞춰 `line`에서 "편할지도……"를
addition 쪽으로 옮겼더니 → **idx21이 재생되는 경우엔** "...훨씬
상대하기" + "뭐, 부정은 안 하지만……"으로 문장이 끊긴 채 나오게 됨(원래
"편할지도……"가 있어야 문법이 완성됨). `line`은 그 여러 addition
전부가 공유하는 공통 도입부라서, 특정 addition 하나를 위해 `line`의
단어를 빼거나(다른 addition과 짝지어질 때 문장이 끊김) 더하면(다른
addition이 나올 때도 그 단어가 항상 따라붙어버림) 실제로는 절대 같이
안 나타나야 할 조합에서 대사가 깨짐 — 이건 자동 재검증(줄 수 계산)으로는
안 잡히는, 사람이 직접 각 페어링을 읽어봐야 알 수 있는 버그였음(운 좋게
4053.dat을 직접 확인하다가 발견).

**최종 규칙 확정**: `tools/toir/font_research/hangul_fix/rewrap/fix_skit_group_overflow.py`를
다시 수정 —
- **`line_addition`이 1개뿐인 그룹**(167쌍 중 53쌍이 이 케이스): 경계를
  넘겨 자유롭게 재배치 가능(다른 addition과 공유되는 게 아니라서 안전).
- **`line_addition`이 2개 이상인 그룹**(나머지 114쌍): `line`의 단어는
  **절대** 밖으로 빼거나 다른 데서 들여오지 않음 — `line`은 자기
  단어끼리만, 각 `line_addition`도 자기 단어끼리만 재배치(2차와 동일한
  안전 방식, 그룹 전체가 공유하는 `line`은 한 번만 정해서 그 그룹의
  안 풀리는 addition들 전부에 동시 적용).

**최종 결과**: 167쌍 중 **139쌍 자동 수정**(경계-넘김 51 + 필드 내 재배치
88, 공유 `line` 필드 101개 재배치), **28쌍(18개 그룹)만 보류**. 4053.dat은
`line`이 idx21/idx44 두 addition과 공유돼서 경계-넘김이 안전하지 않은
케이스라 — 여전히 원문 그대로 남아있고 보류 목록에 있음(문구를 실제로
줄이지 않는 한 이 케이스 자체는 해결 불가, 사용자에게 이미 설명함).

재배포까지 완료: 실기 67개 파일 교체(백업
`deploy_backups/20260822_2057xx_group_overflow_fix_v3_safe_crossboundary/`),
`toidata_release.l7c` 재빌드(1,776/1,776 재추출 검증), `xdelta` 재생성
(원본+diff 왕복 검증 통과). `PATCH_적용법.md`(양쪽) 최종 해시로 갱신 완료.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-22 3차/최종 갱신) | 1,783,700,873 | `1f8027baed2adff95090a44c1f34b75a0347585d3b3cd6483792c3b095be86e7` |
| `toidata_release_ko.xdelta` | - | 91,265,246 | `369b6c8dab9843ccc130bce1c7493f24f0c9a730d35cdbe08e9b074193dbefc6` |

**교훈, 다음에 비슷한 작업할 때 적용할 것**: `line_addition`이 여러 개인
`line`을 만지게 될 때는, "이 `line`은 다른 어떤 addition들과도 공유되는
공통 부분"이라는 걸 항상 전제하고, 그 공유 텍스트의 단어를 특정
addition 하나만을 위해 빼거나 더하면 안 됨. 줄 수(overflow) 재검증만으론
이런 버그를 못 잡으므로(원인이 "줄이 넘친다"가 아니라 "문장이 끊긴다"),
자동화된 재스캔에만 의존하지 말고 실제 텍스트 내용을 몇 개 샘플로
직접 읽어서 검증할 것.

### 추가 수정 (4차, 최종): 4053.dat(원래 line_2.PNG 사례) 수동 정밀 조정

사용자가 "4053은 아예 조치 불가능한 거야?"라고 재차 질문 — 다시 계산해
보니 완전히 불가능한 건 아니고 **딱 1자 차이**였음. `line`이 idx21과
공유되는 케이스라 자동 로직은 경계를 안 넘겼지만, 수동으로 검토한 결과
**"하아……"(감탄사/한숨) 하나만 `line` 쪽으로 옮기면 안전함을 확인**:
idx21과 짝지어져도 "…훨씬 상대하기 편할지도…… 하아…… 뭐, 부정은 안
하지만……"으로 자연스럽게 읽힘("이런"까지 옮기면 idx21과 짝지어질 때
"이런"이 허공에 뜨는 어색한 문장이 되므로 "하아……"까지만). 결과:
- `line`(idx41): "겉과 속이 없으니까 루카 군보다는\n훨씬 상대하기 편할지도…… 하아……"
  (2줄, 각 18자/19자 — WIDE=27자 이내)
- `line_addition`(idx44): "이런 분석 같은 걸 하다니, 나 진짜 어른이네" (1줄,
  **25자** — NARROW=24자를 딱 1자 초과)

24자 기준 자체가 픽셀 측정 24.8자를 안전하게 내림한 값이라, 25자는
측정 오차 범위 안일 가능성이 높다고 판단 — **사용자 승인 하에 적용**함
(정확히 화면에서 잘리지 않는지는 실기 확인이 필요, 다음 세션에서 실제
플레이 시 이 씬을 확인해볼 것 — 4053.dat, 앙쥬가 "나 진짜 어른이네"라고
말하는 부분).

`2_translated/skit_wip/4053.dat.csv`에 정확히 이 두 값만 직접 수정
(csv 필드 exact-match 검증 후 반영), `merge-skit`/`check skit` 재검증
통과. 재배포까지 완료: `recompile_skits` diff 결과 정확히 `4053.dat`
1개만 변경 확인(백업 `deploy_backups/20260822_2*_4053_manual_fix/`),
`toidata_release.l7c` 재빌드(1,776/1,776 재추출 검증) → `xdelta` 재생성
(원본+diff 왕복 검증 통과) → `PATCH_적용법.md`(양쪽) 최종 해시 갱신.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-22 4차 갱신) | 1,783,700,873 | `5ba5a492608cea37f4d0b7a723dfcff7d2d6b0a23125ad3a75ef7591be6dfdf8` |
| `toidata_release_ko.xdelta` | - | 91,265,243 | `018a99e022b15a21e9257960d0e56fda9f1bcb25658b92dd27d6fa261bed2b2e` |

**이 세션 전체 결과(4차 시점): 167쌍 중 140쌍 해결(자동 139 + 수동 1), 27쌍
(17개 그룹) 보류.**

### 추가 수정 (5차, 최종): `line` 트레일링 개행 누락 버그 — line/line_addition이 줄바꿈 없이 붙어버림

사용자가 실기에서 `line_2_after.PNG` 스크린샷을 보여주며 "여전히 문제가
있다"고 지적 — 확인해보니 4053.dat이 `앙쥬 : 겉과 속이 없으니까 루카
군보다는 / 훨씬 상대하기 편할지도…… 하아……이런 분석 같은` 식으로 **`line`의
마지막 단어와 `line_addition`의 첫 단어 사이에 줄바꿈도 공백도 없이 그냥
붙어서** 표시되고 있었음(원래 의도는 "하아……"까지 2번째 줄, "이런
분석..."이 3번째 줄로 분리).

**원인**: 원본(순정) 데이터를 다시 보니, `line_addition`이 뒤따르는
`line` 필드는 예외 없이 **끝에 트레일링 개행(`\n`)**을 갖고 있었음
(예: 4053.dat 원본 `line`(idx41) = `"...편할지도……\n"` — 마지막에 `\n`
하나 더 있음). 이 개행이 단순히 "빈 줄 서식"이 아니라, **엔진이 "다음
내용을 새 줄에서 시작하라"고 해석하는 실질적 렌더링 지시**였음 — `line`과
`line_addition`은 별개 opcode로 저장되지만, 같은 텍스트 커서/스트림에
이어서 그려지는 걸로 보이고, opcode 전환 자체에는 암묵적인 줄바꿈이
전혀 없음(이전 세션 내내 "당연히 필드가 바뀌면 새 줄에서 시작하겠지"라고
잘못 가정했던 것). 그런데 `fix_skit_group_overflow.py`의 `rebuild_field()`가
재배치된 줄들을 `\n`으로 join만 하고 **끝에 트레일링 개행을 안 붙이고
있었음** — `line` 필드를 건드린 **101개 전부**(2026-08-22 세션에서
`line`을 수정한 모든 케이스, 자동 139건 중 상당수 + 수동 4053.dat 1건)에
이 버그가 있었던 것.

**수정**: `rebuild_field(row_korean, content_lines, field)`로 시그니처
변경 — `field == 'line'`이면 무조건 끝에 `\n`(또는 `\r\n`) 하나를 추가.
`line_addition`은 그 뒤에 아무것도 안 이어지므로 트레일링 개행 불필요.
4053.dat 수동 수정본도 동일하게 `line`에 트레일링 `\n` 추가해서 재적용.

**중요한 교훈**: 이 세션 초반에 "line_addition은 line이 끝난 뒤 당연히
새 줄에서 시작할 것"이라고 별다른 검증 없이 가정했던 게 근본 원인.
텍스트 CONTENT(단어/순서)만 맞다고 렌더링이 맞는 게 아님 — **자동
재검증(줄 수 계산, check skit)은 이런 종류의 "필드 경계에서 줄바꿈이
아예 사라지는" 버그를 전혀 못 잡음**(텍스트 내용 자체는 손상 없이 그대로
있으니까). 사용자가 실기 스크린샷으로 직접 확인해줘서 발견함 — **이런
류의 렌더링 관련 변경(특히 필드 경계를 다루는 코드)은 텍스트 diff
검증만으로 안심하지 말고, 최소 1~2개 샘플은 반드시 실기/에뮬레이터
스크린샷으로 직접 확인할 것.**

재배포 완료: `_Data/Field/Skit/Data/` **102개** 파일 재교체(백업
`deploy_backups/20260822_21*_group_overflow_fix_v5_trailing_newline_fix/`),
`toidata_release.l7c` 재빌드(1,776/1,776 재추출 검증) → `xdelta` 재생성
(원본+diff 왕복 검증 통과) → `PATCH_적용법.md`(양쪽) 최종 해시 갱신.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-22 5차/최종 갱신) | 1,783,701,065 | `440f57b5f0d3b6aeabfc252d2c0c47d8acf72eff77e479408f3d0e71f0e04a79` |
| `toidata_release_ko.xdelta` | - | 91,265,492 | `15060714c35ecc46007374d0372debe2b792acd138edc5cd1d4286ca1998ae7d` |

**실기 검증 완료(2026-08-22, 같은 세션)**: 사용자가 `line/line_2_good.PNG`
스크린샷으로 4053.dat을 직접 확인 — 정확히 의도한 대로 3줄 전부 안전하게
렌더링됨을 확인("앙쥬 : 겉과 속이 없으니까 루카 군보다는" / "훨씬
상대하기 편할지도…… 하아……" / "이런 분석 같은 걸 하다니, 나 진짜
어른이네", ▼ 아이콘과도 안 겹침). 트레일링 개행 수정과 SKIT 전용
WIDE=27/NARROW=24 비대칭 폭(25자도 실제로는 안전하게 렌더링됨 — 24자
기준의 보수적 여유가 실제로 있었다는 것도 함께 확인됨) 둘 다 검증
완료. **다음 세션에서 이 항목 재확인 불필요.**

## 추가 진행 (2026-08-23): Story.csv 대사창 가로 폭 초과 재조정(24자 → 26자)

사용자가 "Story.csv 쪽만 먼저 해결해보자"고 요청 — 지난 세션에 미해결로
남은 Story 7건(A1~A3, B1~B4)을 다시 봄.

### 재측정
`line_1.PNG`를 SKIT 때와 같은 방식으로 다시 픽셀 단위로 재측정. 텍스트
색(255,250,235, B채널>220)과 장식 테두리의 금색 하이라이트(B채널
179~210)가 미묘하게 달라서 이 둘을 구분하는 게 관건이었음. 결과:
- 3줄 모두 텍스트 좌측 시작 x=152, 장식 테두리 안쪽 시작 x≈845(모든
  줄 동일 — 테두리가 위/아래로 말리는 두루마리 모양이지만 실제 텍스트
  3줄 영역 안에서는 좌우 폭이 일정함, 처음엔 SKIT처럼 비대칭일 거라
  예상했으나 아니었음)
- '다음' 진주 아이콘은 3번째 줄에만 걸침(x=823~842), 그런데 이 폭이
  테두리(845)보다 살짝만 더 타이트해서 SKIT의 WIDE=27/NARROW=24처럼
  뚜렷한 차이가 아니라 26.5~27.4자 대 정도로 거의 비슷함(1자 미만 차이)
- 23자 줄 예시가 x=735에서 끝남(기존 2026-08-20 기록의 "23자 안전, x=735"와
  정확히 일치 — 재확인됨), 27자 줄은 x=855로 테두리를 침범(이것도 기존
  기록과 일치)
- 결론: Story도 SKIT처럼 뚜렷한 비대칭은 없고, 단일 기준 24자가
  실측보다 보수적이었던 것 — **26자로 상향**(사용자가 27자보다 보수적인
  26자 선택)

### 수정
`tools/toir/font_research/hangul_fix/rewrap/fix_story_26.py` 신규 작성.
문구는 그대로, 기존 줄바꿈 위치만 재배치(2026-08-20과 동일한 원리,
임계값만 26으로 변경). 전각공백 서명 서식이 있는 항목은 일반 rewrap을
그대로 쓰면 서명 줄이 앞 문장과 섞여버리는 문제가 있어(테스트 중 실제
확인함) 별도 처리:
- **B1~B4(전부 서식 없는 평문): 전부 26자 기준에 맞게 해결** —
  `Dn/08/01/701.dat` #189, `Fld/00/00/701.dat` #68, `Twn/16/00/700.dat`
  #185(이미 26자 기준에서 원문 그대로 최적이었음 — 파일 변경 없음),
  `Twn/26/00/760.dat` #273.
- **A2(`Twn/03/02/401.dat` #40, 서명 서식): 서명 줄("결심하는 모험가
  토니")은 완전히 그대로 두고 앞 문장 2줄만 재배치 → 26자 기준 충족,
  해결.**
- **A1(`Dn/08/01/701.dat` #90, 경전 인용체, 매 줄 앞에 전각공백 접두):
  각 줄이 자기 앞의 전각공백을 유지한 채 3줄에 재분배되도록
  비대칭 계산(`best_wrap_with_caps`)까지 시도했지만 최선이 마지막 줄
  기준 26자를 여전히 못 맞춤 — 보류 유지.**
- **`Twn/08/02/401.dat` #40(서명 서식, 원래 A3): 서명 줄 안 건드리고
  앞 문장만 재배치해도 최선이 27자 — 26자 기준으론 1자 부족.** 27자까지
  허용하면 해결되지만(실측 용량이 26.5~27.4자라 27자도 근거 있음),
  사용자가 A1과의 일관성을 위해 27자 옵션 대신 26자 유지를 선택함 —
  보류 유지.

**결과: Story.csv 7건 중 5건 해결, 2건(A1, `Twn/08/02/401.dat` #40) 보류.**
검증: `merge-script`(8162/8162, 변동 없음), `check script`(무관한 기존
이슈 1건만, 새 이슈 없음), 개행 혼용 손상 0건, 재스캔으로 새 기준 위반
0건(정확히 2건만 남음) 교차 확인.

### 부수 발견 → 같은 세션에서 바로 적용 완료
지난 세션에 만든 SKIT 전용 비대칭 폭([27,27,24])을 `FLAGGED_ALL_pending.md`의
B항목(SKIT 단일 `line`, addition 없음) 4건에 적용해보니:
- `0006.dat` #17, `0235.dat` #15: **이미 새 기준 통과, 손 안 대도 됨**
- `0334.dat` #324, `3037.dat` #98: **`best_wrap_with_caps([27,27,24])`로
  재배치하면 문구 그대로 해결됨**
처음엔 "Story.csv만 먼저"라는 요청이라 적용을 보류했었는데, 사용자가
바로 이어서 "지금 마저 진행해줘"라고 해서 같은 세션에서 적용까지 완료함
(`tools/toir/font_research/hangul_fix/rewrap/fix_skit_b_items.py` 신규
작성). `merge-skit`/`check skit`(기존 "리카르돈" 1건 외 새 이슈 없음),
개행 손상 0건 확인 후 배포.

### 배포 (Story 수정 1차 + SKIT B항목 2차, 둘 다 실기 + xdelta 배포판 완료)
1. 수정 전 `script_wip/` 전체 백업(`deploy_backups/script_wip_pre_story26_fix_20260823/`).
2. `recompile_story`를 실기 설치를 l7cdir 소스로 스테이징 → diff로 실제
   변경 파일 특정: `_Data/Script/` **4개**(`Twn/16/00/700.dat`은 원문이
   이미 최적이라 바이트 변경 없음 — 5건 해결 중 파일이 바뀐 건 4개뿐).
   백업 `deploy_backups/20260823_113458_story26_fix/`, 배포 후 재대조
   불일치 0건.
3. `3_patched/loose_files` 동기화 → `l7ca_patch_multi.py`로 순정 원본에
   누적 1,776개 패치 → 새 `.l7c` 생성 → 전체 재추출(14,076/14,076)해서
   1,776/1,776 완전 일치 확인.
4. `xdelta3 -e -9 -S -s`로 재생성 → 원본+diff 왕복 검증 통과.
5. `PATCH_적용법.md`(루트 + `3_patched` 양쪽) 새 해시로 갱신 완료.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 1차, Story) | 1,783,701,065 | `107e98452c8db56abe695ebfe9f0e079af3fb33581a66c431d5c831c78ab4ced` |
| `toidata_release_ko.xdelta` | - | 91,265,509 | `c0c7038ec5a962927bfc6f7fd4687f075e2ddffc47043f362e512bbe0f249407` |

**2차(같은 날, SKIT B항목)**: `recompile_skits`로 diff → `_Data/Field/Skit/Data/`
**2개**(`0334.dat`, `3037.dat`) 변경 확인(백업
`deploy_backups/20260823_*_skit_b_items_fix/`) → `loose_files` 동기화 →
`l7ca_patch_multi.py`로 1,776개 재패치 → 전체 재추출 1,776/1,776 일치 →
`xdelta` 재생성·왕복 검증 통과 → `PATCH_적용법.md`(양쪽) 갱신.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 2차/최종, Story+SKIT) | 1,783,701,065 | `a8ed4b61cfe0ed42df5aab705e2e2e849dcadbe31938d02c926eb9bf1e6870cc` |
| `toidata_release_ko.xdelta` | - | 91,265,510 | `41659ad794f6b814dadebde1d401dce87a3d54941bc9217568a334184153c078` |

**이 세션 최종 결과: Story 5/7 해결 + SKIT B항목 4/4 확인·해결(2건은
원래 안전, 2건은 재배치로 해결). 남은 보류는 Story 2건(A1, `Twn/08/02/401.dat`
#40) + SKIT C항목 27건 = 총 29건.**

## 추가 진행 (같은 날, 이어서): SKIT line/line_addition 그룹핑 버그 발견 — 대사창 누적 줄 수 초과 전면 재조사·사실상 전량 해결

### 발단
사용자가 "SKIT C항목들 중 line의 대사와 addition 대사가 동일한 화자인
애들만 추려봐"라고 요청 — 화자를 확인하려고 원본 `.dat`을 직접 파싱해서
`speakers` 필드를 보다가, **지금까지 써온 그룹핑 방식 자체가 틀렸다는
것**을 발견함.

### 근본 원인
이 프로젝트의 기존 추출 도구 `tools/toir/toir/formats/skits/extract.py`가
`Skit.csv`를 쓸 때 결정적인 버그가 있었음(`extract_skits()` 함수):

```python
elif isinstance(line, SkitLineAddition):
    rows.append([r + c - 9, path, 'line_addition', line.index, '', line.text])
    c += 1
for row in rows:
    writer.writerow(row)
```

`line_addition` 행들을 **즉시 쓰지 않고 별도 리스트(`rows`)에 모아뒀다가,
그 파일의 다른 모든 행(title/speaker/line/choice)을 전부 쓰고 난 뒤에야
맨 마지막에 일괄 기록**함. 그래서 Skit.csv/skit_wip만 보면 "그 파일의
**마지막** `line`에 그 파일의 **모든** `line_addition`이 달려있는 것처럼"
보이지만, 실제 게임 바이너리 순서(opcode 순회 순서)는 전혀 다름 — 대부분
서로 다른, 훨씬 앞쪽의 `line`들에 각각 1~2개씩 나뉘어 붙어 있고, 화자도
제각각인 경우가 많음.

`tools/toir/toir/formats/skits/skit.py`의 `SkitDeCompiler`(원본 `.dat`을
직접 파싱하는, 이 프로젝트에 이미 있던 도구)로 실측 확인:
- **4053.dat**: `line`(idx=18, 화자=앙쥬) → `addition`(idx=21)이 하나의
  완전히 별개인 대사창이고, `line`(idx=41, 화자=앙쥬) → `addition`(idx=44)이
  또 다른 별개의 대사창. 2026-08-22~23 내내 "line41이 addition21·44 둘
  다와 공유된다"고 믿고 경계-넘김을 막았던 건 완전히 허구였음 — 둘 다
  원래부터 진짜 단일-addition, 완전히 안전한 케이스였음.
- **0334.dat**: "line #420에 9개 addition이 달려있다"고 봤던 것도 허구.
  실제로는 서로 다른 8개의 `line`(idx 32,43,59,79,112,133,174,235)에
  각각 1~2개씩 붙어 있고(line32만 2개, 나머지는 전부 1개), 화자도
  계모/언니1/언니2/왕자/루카데렐라 등 대사마다 다름. `line #420`
  자체는 원래 **addition이 아예 없는 평범한 나레이션**이었음(그래서
  2026-08-20에 B7로 잡혔던 "가로 폭 초과"만 진짜 문제였고, C3로 잡았던
  "9개 addition과의 누적 초과"는 처음부터 존재하지 않는 문제였음).

### 영향 범위
`scan_true_groups.py`(원본 파싱 기반)로 전체 989개 SKIT 파일 재검사:
- 진짜 addition이 있는 그룹: **466개**(기존 CSV-순서 기반 그룹핑으로는
  281개로 잘못 집계됐었음)
- 그중 단일-addition 그룹 435개, 복수-addition 그룹 31개(기존엔 114개로
  잘못 집계 — 실제로는 대부분 단일-addition인데 파일당 하나로 뭉뚱그려져
  다중으로 오판됐던 것)
- **진짜로 L+A>3인 경우는 167쌍이 아니라 24쌍뿐**이었음(나머지 143쌍은
  전부 잘못된 그룹핑이 만들어낸 허상 — 특히 L이 이미 3인 "그 파일의
  마지막 line"에 무관한 addition들이 잔뜩 잘못 붙어서 자동으로
  "L+A>3"이 되어버린 경우가 다수).

**중요: 지금까지 배포됐던 수정이 잘못된 단어를 넣거나 내용을 훼손한 적은
없음**(문구 보존 원칙은 항상 지켰고, 원본-신규 텍스트 토큰 비교로 재검증
완료 — 순서 변경 없이 줄 경계만 이동했고, 실제로 경계를 넘겨 이동한
케이스도 앞뒤 합친 전체 단어열이 정확히 일치함을 확인함). 문제는
순전히 **그룹 판단 오류로 인한 과잉 보수적 처리/누락**이었음(진짜
단일-addition인데 다중으로 오판해서 경계-넘김을 안 썼거나, 애초에 스캔
대상에서 조용히 누락된 경우).

### 수정
- `tools/toir/font_research/hangul_fix/rewrap/true_groups.py` 신규 작성:
  `SkitDeCompiler`로 원본 `.dat`을 직접 파싱해서 **진짜** line→addition
  대응 관계를 구축(텍스트 내용 자체는 항상 최신 `Skit.csv`에서 가져오고,
  그룹 구조=어느 addition이 어느 line에 붙는지만 원본 바이너리 기준).
- `fix_skit_group_overflow_v2.py` 신규 작성: 위 진짜 그룹을 사용해서
  2026-08-22에 이미 검증된 것과 똑같은 규칙 적용(진짜 단일-addition
  그룹은 경계-넘김 자유 재배치, 진짜 복수-addition 그룹은 공유되는
  line 단어를 밖으로 안 빼고 각자 자기 단어끼리만 재배치). 재작업 전
  `skit_wip/`를 2026-08-22 세션 시작 시점의 순정 백업으로 완전히
  되돌린 뒤(그 이후의 모든 수정은 잘못된 그룹핑 기반이었으므로) 처음부터
  다시 적용.
- `scan_true_groups.py` 신규 작성: 독립 재검증용(진짜 그룹 기준으로
  잔여 L+A>3 쌍이 있는지 재스캔).

**최종 결과: 24쌍 전부 자동으로 해결(경계-넘김 21 + 필드 내 재배치 3),
보류 0건.** 4053.dat도 이번엔 완전히 자동으로 해결되어(line 41을 1줄로,
addition 44를 2줄로 재배치) 이전의 수동 25자 우회책이 필요 없어짐.

### 검증
- `merge-skit`(19209/19236, 변동 없음), `check skit`(기존 "리카르돈"
  1건 외 새 이슈 없음), 개행 손상 0건.
- **내용 보존 재검증**: 백업본과 새 결과물을 필드별로 비교해서 실제
  텍스트가 바뀐 모든 곳(23개 파일, 36개 필드)을 확인 — 단어 토큰 비교
  결과 전부 "같은 단어들을 줄바꿈 위치만 재배치"했거나(line/addition
  각자 내부), 경계를 넘겨 이동한 2쌍(0236.dat, 4053.dat)은 line+addition
  합친 전체 단어열이 이동 전후로 정확히 동일함을 확인 — 내용 손실·왜곡
  없음.
- `scan_true_groups.py`로 최종 재스캔: 잔여 L+A>3 쌍 0건.

### 배포 (실기 + xdelta 배포판 모두 완료)
1. `recompile_skits`로 diff → `_Data/Field/Skit/Data/` **108개** 파일
   변경(상당수는 잘못 적용됐던 기존 수정을 원상 복구, 23개 파일에
   실제 새 수정 적용) — 백업
   `deploy_backups/20260823_120*_group_overflow_TRUEGROUPING_fix/`,
   배포 후 재대조 불일치 0건.
2. `3_patched/loose_files` 동기화 → `l7ca_patch_multi.py`로 1,776개
   재패치 → 전체 재추출 1,776/1,776 일치 → `xdelta` 재생성·왕복 검증
   통과 → `PATCH_적용법.md`(양쪽) 갱신.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 3차/최종) | 1,783,701,257 | `f830c9d05df88cfedcbe15d65da843ec03c8f039df896a98c872570577b7a924` |
| `toidata_release_ko.xdelta` | - | 91,265,385 | `09a61324f99875403ad95936af45ab02cbbe5f22942fc6552632aa5aee76801c` |

### 교훈 (중요, 앞으로 SKIT 관련 작업 시 항상 적용할 것)
**`2_translated/Skit.csv`나 `skit_wip/*.csv`의 행 순서를 절대 신뢰하지
말 것 — `line_addition`은 실제 순서와 무관하게 파일 끝에 몰려서 기록됨
(기존 `extract.py`의 버그, 아직 안 고침). line↔line_addition의 진짜
대응 관계가 필요한 작업(그룹핑, 화자 확인, 인접성 판단 등)은 반드시
`tools/toir/font_research/hangul_fix/rewrap/true_groups.py`(또는
`SkitDeCompiler`로 원본 `.dat`을 직접 파싱)를 거칠 것.** 이번 버그는
정확히 "CSV에 있는 것을 그대로 믿었다"에서 비롯됐고, 이걸 몰랐던
2026-08-22 세션 내내 훨씬 복잡한 "공유 line 안전 규칙"을 만들어 대응했던
것 — 실제로는 애초에 그런 공유가 거의 없었음. `extract.py`의 이 버그
자체를 정식으로 고치는 것도 다음에 시간 되면 검토(지금은 우회만 함).

### 다음 세션에서 이어서 할 일
- **Story.csv 보류 2건만 남음** — A1(`Dn/08/01/701.dat` #90, 경전
  인용체), A2(`Twn/08/02/401.dat` #40, 서명체). 둘 다 26자 기준으로
  1~2자 부족, 문구를 안 줄이면 더 이상 방법 없음(4줄 지원도 고려
  대상 아님). 목록은
  `tools/toir/font_research/hangul_fix/rewrap/FLAGGED_ALL_pending.md` 참고.
- **`extract.py`의 `line_addition` 순서 버그를 정식으로 고칠지 검토**
  (지금은 `true_groups.py`로 우회만 함 — 근본 수정은 이 도구를 쓰는
  다른 모든 미래 작업에 도움이 될 것).

---

## 추가 진행 (같은 날, 이어서): 실기 테스트 중 발견 — MapData.csv(맵 화면 NPC 대사) 대사창 초과 43건 전량 해결 (2026-08-23, 4차)

사용자가 실제 PS Vita 하드웨어에서 패치를 테스트하다가 대사창 밖으로
글자가 넘치는 화면 3개를 캡처(`line/line_3.PNG`, `line_4.PNG`,
`line_5.PNG`) — "전부 Story.csv 대사창인 것 같다"고 판단했으나, 조사
결과 **UI는 Story.csv와 완전히 동일하지만 실제로는 `MapData.csv`
(`_Data/Field/MapData/`, 맵 화면에서 뜨는 NPC 잡담/여행기 대사) 항목**
이었음(전부 `Twn/03/02.dat` Section 2, #16/#34/#58). 이 파일은 이전
세션 어디에서도 대사창 폭 기준으로 점검한 적이 없었던, 완전히 새로운
카테고리(위 "다음 세션에서 이어서 할 일" 목록의 마지막 항목이었음).

Story.csv와 같은 안전폭(26자, 제어 태그 제외)을 그대로 적용해서
`2_translated/MapData.csv` 전체(3,153행)를 스캔한 결과 **43건 초과**
(캡처된 3건 포함). 문구는 한 글자도 안 바꾸고 기존 줄바꿈 위치만
재배치 — 전각공백(　) 서명 서식이 있는 3건(`Twn/03/02.dat` #58,
`Twn/09/00.dat` #54, `Twn/10/03.dat` #45, 전부 "OO 모험가/개척가 토니"
서명체)은 Story.csv 때와 같은 방식으로 서명 줄은 손대지 않고 앞 문장만
재배치. 원래 줄 수 그대로는 26자를 못 맞추는 6건(대부분 원래 1줄짜리)은
줄 수를 최대 3줄까지 늘려서 해결(대사창이 최대 3줄까지 지원하는 건
Story/SKIT 스크린샷으로 이미 확인된 사실이라 안전). **43건 전부
자동으로, 문구 변경 없이 해결됨** — Story.csv 때와 달리 이번엔 보류
항목이 0건.

작업 파일: `tools/toir/font_research/hangul_fix/rewrap/fix_mapdata_26.py`
(신규, `fix_story_26.py`와 동일한 구조). 원본-신규 토큰 비교로 43건
전부 내용 보존 확인 완료(불일치 0건). `map_wip/` 백업은
`deploy_backups/map_wip_pre_mapdata26_fix_20260823_214656/`.

실제로 바뀐 `.dat` 파일은 28개(예: `Dn/03/02.dat`, `Twn/03/02.dat`,
`Twn/09/00.dat` 등). 배포 절차는 기존과 동일: `l7ca_unpack.py`로
`_Data/Field/MapData/`만 원본에서 추출(전체 추출보다 훨씬 빠름, 세 번째
인자로 경로 지정 가능) → `toir.recompile_map_data()` 직접 호출로
재컴파일 → 라이브 설치·`3_patched/loose_files`와 대조해서 바뀐 28개
파일만 확인 → 양쪽에 배포 → `l7ca_patch_multi.py`로 1,776개 재패치 →
전체 재추출 1,776/1,776 일치 확인 → `xdelta` 재생성·왕복 검증(바이트
단위 완전 일치) 통과 → 라이브 설치의 루트 `toidata_release.l7c`까지
새 버전으로 교체 → `PATCH_적용법.md`(양쪽) + 이 파일 갱신.

**주의**: 이번에도 스크래치 디스크가 가득 차서(이전 세션들이 검증용으로
남겨둔 1.7GB짜리 `xdelta_verify_final*.l7c`/`loose_root_extras*` 여러
개가 쌓여있었음) 중간에 멈췄다가 오래된 검증 산출물을 지우고 재개함 —
디스크 여유가 부족하면 먼저 스크래치의 오래된 `.l7c` 복사본들부터
지울 것.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 4차) | 1,783,701,257 | `916638f1b392000a44e0a3b5a2c60e55e8cf07340b15ff8c84aff94e52a06fcd` |
| `toidata_release_ko.xdelta` | - | 91,265,444 | `e692688a3a0e14500cf613853f175452b336e580ec06420b8fcaeff62be4ecb4` |

### 다음 세션에서 이어서 할 일 (갱신)
- **Story.csv 보류 2건만 남음** — A1(`Dn/08/01/701.dat` #90), A2
  (`Twn/08/02/401.dat` #40). 위 내용과 동일, 아직 미해결.
- `extract.py`의 `line_addition` 순서 버그를 정식으로 고칠지 검토(여전히
  TODO, 우회만 된 상태).
- MapData.csv는 이번에 전량 점검·수정 완료(43/43 해결, 보류 0건) —
  더 이상 할 일 없음. 다만 MissionData.csv/ItemDataPack 계열 등 아직
  안 훑어본 다른 텍스트 카테고리가 더 있을 수 있음(확인 안 됨).
- 말투 정합성 검사(아래 참고)에서 나온 애매한 항목 3건(고신뢰 3건은
  이미 수정 완료)은 사용자 판단 보류 중 — 다시 논의하고 싶으면 아래
  섹션 참고.

---

## 추가 진행 (같은 날, 이어서): 말투(존댓말/반말) 정합성 검증 — 고신뢰 3건 수정 완료, 애매한 3건 보류 (2026-08-23, 5차)

사용자가 "말투 정합성"(원문이 존댓말이 아닌데 존댓말로 번역된 대사가
있는지) 재검증을 요청. 예시로 "에?!"를 "네?!"로 번역한 경우를 언급함.

**방법**: Story/Skit/MapData 전체(약 30,551행)를 스캔. (1) 일본어
원문에 です/ます 계열 표지가 없는 행 후보 추출(314건) → (2) 화자별
전체 대사에서 존댓말/반말 어미 비율로 프로필을 만들어(예: 루카
89% 반말, 이리아 97~99% 반말, 안쥬 32~57% 존댓말 등 캐릭터별로 뚜렷한
차이 확인됨) "평소 반말 캐릭터인데 이 대사만 존댓말"인 경우만 39건으로
압축 → (3) 39건을 하나하나 원문 뉘앙스로 재확인.

**결과**: 39건 중 대부분은 오탐(정규식이 「ました」「ございました」
같은 활용형이나 장음부호로 늘어진 「いたしまーす」 등을 존댓말 표지로
못 잡은 것, 「いただきます」류 고정 관용구, 농담·패러디로 일부러
존댓말로 전환한 연출 등 — 실제로는 번역이 맞았음). **진짜 어긋나는
3건만 발견해서 수정 완료**:
- `Skit/5036.dat` #97 루카: "もちろんだよ！"(반말) → "당연하죠!" →
  **"당연하지!"**
- `MapData/Twn_02_00.dat` §0 #30 아주머니: "受け取っておくれ"(반말) →
  "받아주세요" → **"받아주렴"**
- `MapData/Twn_02_00.dat` §0 #34 같은 아주머니: "ごめんよ"(반말) →
  "정말 미안해요" → **"정말 미안해"**

**애매해서 보류한 3건** (사용자가 원치 않으면 그대로 둬도 됨, 취향
판단 영역):
- `Story/Twn_00_03_730.dat` #119 루카: "ただいま"(중립 관용구) →
  "다녀왔습니다!" — 루카는 89% 반말 캐릭터라 "다녀왔어!"가 더 어울리나
  고정 인사말이라 애매
- `MapData/Twn_02_00.dat` §0 #26, #40 소녀: "遊んでね"(반말) →
  "놀아요" — "성녀님"께 드리는 대사라 한국어 정서상 아이가 존댓말 쓰는
  게 자연스러울 수 있어 의도적 로컬라이징일 가능성
- `Story/Twn_07_01_400.dat` #28 루카: "こんにちは"(중립) →
  "안녕하세요" — 직전 적병 대사도 똑같이 "안녕하세요"라서 패러디
  미러링(적이 비꼬듯 존댓말 쓴 걸 루카가 얼결에 따라한 것)일 가능성 높음

배포 완료(`5036.dat`, `Twn/02/00.dat` 2개 파일 변경) — 원본 추출→재컴파일
→라이브 설치·`3_patched/loose_files` 대조로 변경분 2개만 확인→양쪽
배포→`l7ca_patch_multi.py` 1,776개 재패치→전체 재추출 1,776/1,776
일치→xdelta 재생성·왕복 검증 통과→라이브 설치 루트 l7c까지 교체.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 5차) | 1,783,701,257 | `11066d75032d72b95c47e019ef985699cbda41113997e2379e0056c38d1ad3d1` |
| `toidata_release_ko.xdelta` | - | 91,265,440 | `d5410749b1836cf8138f172cbb7a502b8dbadd522f0dbe2717ad0ed062505910` |

### 추가 진행: 앙쥬(アンジュ) 말투 정합성 16건 수정 (2026-08-24, 9차)
사용자가 앙쥬 전용으로 전수 재점검 요청. 앙쥬 대사 1,756건(Story/Skit/
MapData 전체)을 원문 です/ます 유무 기준으로 다시 대조한 결과 143건
후보가 나왔으나, **앙쥬는 캐릭터 자체가 말투를 상황에 따라 바꾸는
유형**임을 확인함 — 일본어 여성어 어미(わ/の/ね/かしら/でしょ, 문법상
반말꼴이지만 존댓말 표지가 없을 뿐인 중립적 어미)를 파티원과 가벼운
잡담을 할 땐 반말로, 심각한 주제·낯선 사람 상대·격식 있는 장면에서는
존댓말로 옮기는 식으로 **일관되게** 번역돼 있었음(같은 わ/よ 어미가
"冗談よ"→"농담이야", "出血がひどいわ！"→"출혈이 심해요!"처럼 문맥에
따라 다르게 번역됨). 루카/아주머니 케이스(압도적 반말 캐릭터에 예외
1~2건)와는 성격이 달라서 정규식만으로 "정답"을 판단할 수 없었음.

사용자와 함께 판단 기준을 좁힘: **큐큐/콘웨이/리카르도를 제외한 주요
동료(스파다/이리아/루카/코다/에르마나)를 상대로 말하면서 원문이 반말
어미인 경우**만 반말로 통일(전투/종교/재판 같은 무거운 주제라도 이
5명이 대상이면 포함). 이 조건으로 16건 확정(대상이 하스타인 1건은
제외) — 어미/조사만 반말로 바꾸고 나머지 단어는 그대로 유지, 위드
넘침 재검사도 통과.

바뀐 파일: `_Data/Script/` 10개, `_Data/Field/Skit/Data/` 4개.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 9차) | 1,783,701,186 | `0cb32f4ab8ab92f4cd78d6f049f064aeae32855cae1562a7f283b6d4f5080c19` |
| `toidata_release_ko.xdelta` | - | 91,265,563 | `f528d9c7b61a52283a929e712760f488ff860e2279063a39e5e38ac199467c4d` |

### 교훈: 말투 정합성은 화자마다 "압도적 우세 어미가 있는 캐릭터"와
"상황따라 코드스위칭하는 캐릭터"를 구분할 것
루카/이리아/아주머니류(반말 비율 90%+)는 예외 슬립만 찾으면 되지만,
앙쥬처럼 폴라이트 비율이 20~57%로 뒤섞인 캐릭터는 애초에 "정답
말투"가 하나로 고정돼있지 않을 수 있음 — 무작정 반말/존댓말 어느
한쪽으로 통일하면 오히려 원래 있던 의도된 뉘앙스(친밀한 대상 vs
격식 차릴 대상)를 깨뜨릴 위험이 있음. 이런 캐릭터는 "누구를 상대로
말하는가" 축으로 사용자와 기준을 좁혀서 진행하는 게 안전함.

---

### 참고: 말투 정합성 재스캔이 다시 필요하면
위에서 쓴 스캔 방법(화자별 존댓말/반말 프로필 + 원문 です/ます 유무
대조)은 스크립트로 저장해두지 않았음(1회성 인라인 스캔) — 다시
필요하면 이 섹션의 방법론을 그대로 재현하면 됨. 정규식 오탐이 많으니
반드시 화자 프로필 대조 + 원문 뉘앙스 수동 확인 단계를 거칠 것(정규식
결과만으로 자동 수정하면 안 됨 — 「ました」류를 누락하는 등 정확도가
낮음).

---

## 추가 진행 (같은 날, 이어서): Story/Skit/MapData 외 다른 UI(스토리북·배틀북·칭호·몬스터도감·레코드·술기 화면) 텍스트 넘침 8건 수정 (2026-08-23, 6차)

"아직 안 살펴본 영역"(이전 섹션에서 예상했던 대로)에서 사용자가
`i_line_1~6.PNG`를 캡처해서 확인 요청. 결과:

**A. 순수 줄바꿈 넘침 3건 (문구 변경 없이 재배치)**
- `StoryBookDataPack.csv` StructId 0 text6/text7 (줄거리 "꿈에서 보는
  무인"): 안전폭 21자로 실측(21자 줄 2개가 딱 맞고 22자 줄이 테두리
  침범하는 걸 픽셀로 확인, 아주 타이트하게 잘 맞음)
- `BattleBookDataPack.csv` StructId 8 text6/text7 (배틀북 "도주 1"):
  StoryBook과 완전히 동일한 UI 템플릿(좌: 목록, 우: 설명 패널)이라
  같은 21자 기준 적용
- `ItemDataPack_CodeName.csv`/`ItemDataPack.csv` CodeName #103
  description (칭호 "모든 것을 꿰뚫어보는 여성"): **24자로 재배치했지만
  이 안전폭은 추정치임** — 확보한 스크린샷이 1장뿐이라 "19자는 여유
  있음(테두리까지 한참 남음) / 28자는 침범"이라는 넓은 범위만 확인했고,
  Story/StoryBook처럼 경계값(21↔22 같은) 정밀 실측은 못 함. 만약
  다음 실기 테스트에서 이 칭호(또는 같은 UI를 쓰는 다른 칭호)가 여전히
  넘치면 24자를 더 낮춰야 함.

**B. 고정폭 UI 라벨 3건 (줄바꿈 불가능 — 사용자 승인 하에 글자 수 축약,
"문구 안 줄이기" 원칙의 예외)**
- `eboot.csv` Pointer `81160184` "훔칠 수 있는 아이템"(몬스터 도감,
  값 컬럼과 겹쳐서 깨져 보임) → **"훔칠 아이템"**(바로 아래 "드롭
  아이템" 라벨과 길이를 맞춤, 실제로 그 라벨은 문제없이 표시되고
  있었어서 같은 길이면 안전하다고 판단)
- `eboot.csv` Pointer `811608C0`/`811608D8` "레이브 Lv.4 총/최대 시간"
  (레코드 화면, 숫자와 라벨이 붙어버림) → **"레이브 4 총/최대 시간"**
  (사용자가 "레이브 4 최대 시간처럼 의미 왜곡 없이"라고 지정한 형태,
  "Lv."만 제거)
- `eboot.csv` Pointer `8115ECF8`/`811613D0` "공중 가능"(술기 화면,
  옆 "카운트"/"소비TP" 칸을 침범 — 처음엔 폰트 글리프 깨짐인 줄
  알았으나 사용자가 확인해줘서 실제로는 옆 영역 침범이었음, 원문 CSV는
  "공중 가능"으로 정상이었음) → **"공중"**만 남기고 축약(사용자 지정)

**중요한 교훈**: `ItemDataPack_CodeName.csv`(및 같은 패턴의
`ItemDataPack_Weapon.csv` 등 카테고리별 분할 파일)는 **재컴파일에
쓰이는 실제 소스가 아님** — `toir/formats/dat/items.py`의
`recompile_items()`는 오직 `2_translated/ItemDataPack.csv`(전체
카테고리가 한 파일에 합쳐진 마스터 파일, 컬럼 `category,index,field,
japanese,translation`)만 읽음. 카테고리별 분할 파일은 참고/열람용
사본으로 보이며 수정해도 반영 안 됨 — **이 카테고리 계열(Item/Arts/
CharaAbility 등 헤더 없는 분할 CSV) 텍스트를 고칠 땐 항상
`ItemDataPack.csv`(마스터)를 같이/직접 고쳐야 함.** (이번엔 분할 파일을
먼저 고쳤다가 마스터에 반영이 안 된 걸 뒤늦게 알아채고 마스터에도
적용함 — 다음엔 처음부터 마스터부터 확인할 것.)

배포: `_Data/System/StoryBookDataPack.dat`, `BattleBookDataPack.dat`,
`ItemDataPack.dat`, `eboot.bin`(이번 세션 최초로 eboot.bin도 변경 —
`_Data/System/`만 원본에서 부분 추출해서 재컴파일, l7c 전체 재패치·
재추출 1,776/1,776 일치, `toidata_release_ko.xdelta` +
`eboot_ko.xdelta` 둘 다 재생성·왕복 검증 통과).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 6차) | 1,783,701,257 | `4a1be47176846181263572189ff2ec4c1f5fcf32dedd94021e1533abb332d449` |
| `toidata_release_ko.xdelta` | - | 91,265,452 | `101cee5e5d8ec5e044b5ee78caa664074eab9abd1c61809afc79d2e41ebf916b` |
| `eboot.bin` | 패치 후 | 1,543,133 | `64b8edad172a6aec0bf92677134d57e162dd5119e4206e527b56185f76eb44c2` |
| `eboot_ko.xdelta` | - | 10,626 | `105cd3de2405411c7667e8a07ecf746392b8e5f640c2215a49436bf7ef8536f1` |

### 다음 세션에서 이어서 할 일 (갱신)
- 아직 스크린샷으로 확인 못한 나머지 UI: `KizunaDataPack`(인연),
  `OperationDataPack`(조작법), `TutorialData`(튜토리얼),
  `MissionData`(미션), `ShopDataPack`(상점 이름). 실기에서 넘침
  발견되면 이번과 같은 방식(스크린샷 실측 → 안전폭 산출 → **그 UI를
  쓰는 파일 전체를 한 번에 스캔·수정**, 캡처된 예시 1건만 고치고
  끝내지 말 것 — 아래 교훈 참고)으로 처리.
- Story.csv 보류 2건(A1/A2), `ItemDataPack.csv`의 CodeName #58/#230/#235,
  Recipe #59(문구 안 줄이면 27자 못 맞춤) — 전부 보류 상태로 남아있음.
- **CodeName "이름(name)" 필드도 별도로 좁은 UI가 있음을 발견**(칭호
  목록 패널 + 장비창 칭호 슬롯, 둘 다 한 줄이라 줄바꿈 불가능, 안전폭
  약 12자로 실측). 288개 중 3개 초과: #16(13자), #103(14자, 사용자
  지시로 "모든 걸 꿰뚫어보는 여성"=13자로 수정했지만 #16과 같은 길이라
  여전히 살짝 잘릴 가능성 있어 재확인 필요), #194(13자, 미해결).
  다른 카테고리(Weapon/Armor 등)의 name 필드는 이 좁은 목록 UI에
  노출되는지 아직 확인 안 함 — 노출된다면 마찬가지로 12자 기준
  재점검 필요.

### 교훈 (중요): 새 UI 발견 시 캡처된 예시 1건만 고치지 말고 그 UI를 쓰는 파일 전체를 스캔할 것
6차에서 칭호(CodeName) 1건(#103)만 스크린샷 기준으로 고치고 끝냈는데,
사용자가 "캡처한 구간은 해결됐지만 같은 영역의 나머지는 안 됐다"고
재차 확인 요청(`i_line_7.PNG`) — 같은 UI를 쓰는 `ItemDataPack.csv`/
`ArtsDataPack.csv`의 다른 칭호/아이템/무기/특기 항목들이 여전히 넘쳐
있었음(76건). **MapData 때도 똑같은 패턴이었음**(캡처 3건 → 전체
스캔하니 43건). 새로운 UI 박스에서 넘침을 하나 발견하면, 그 예시만
고치지 말고 **곧바로 같은 파일(또는 같은 UI를 공유하는 파일 전체)을
안전폭 기준으로 스캔해서 한 번에 처리**할 것 — 사용자가 매번 새
스크린샷을 캡처해서 알려주는 비효율을 피할 수 있음.

또한 안전폭 추정 시, 스크린샷 1장으로 "이 길이는 안전, 이 길이는
침범"이라는 넓은 범위만 나왔다면 그 범위의 **중간~여유 있는 쪽**
(예: 19 안전/31 침범 → 24가 아니라 27)으로 잡는 게 나음 — 너무
타이트하게 잡으면(24) 안전하긴 해도 같은 파일의 다른 항목들이 실제
안전폭(27)보다 더 넘치는 걸 놓치진 않지만, 반대로 스캔 범위가 실제보다
넓어져서 안 고쳐도 될 항목까지 재배치하게 됨(문제는 없지만 비효율).

---

## 추가 진행: 앙쥬 말투 정합성 2차 전수 수정(80건 추가, 총 96건, 2026-08-24, 10차)

사용자가 `Dn/20/11/701.dat#41`("え？/どうして？"→"네?/어째서요?", 직전
대사 상대인 에르마나에게 반문하는 대사였음), `Dn/18/07/730.dat#125`
("みんな お願い"→"다들, 부탁이에요", 파티 전체에게 하는 애원이었음)
같은 사례를 지적하며 재점검 요청. 9차 때는 "해당 줄에 이름이 직접
언급된 경우"만 걸렀는데, **"みんな"로 파티 전체를 지칭하거나, 이름이
없어도 직전/직후 대사 상대가 타깃 5인(스파다/이리아/루카/코다/
에르마나)인 경우**를 놓쳤던 것.

원래 후보 143건(9차에서 다룬 16건 제외 127건)을 전부 **파일별 앞뒤
대사까지 같이 읽어서** 상대가 누구인지 하나하나 재판정(정규식만으로는
안 되고 대화 흐름을 직접 읽어야 함) → 80건 추가 확정(9차 16건+이번
80건=96건). 제외한 47건은: 리카르도/큐큐/콘웨이/하스타/시안/포우
할아범/알베르/마티우스/바르칸 사념체 등이 진짜 대상이거나, 대화
상대가 불명확한 독백·나레이션(전각공백 성전 인용구 등).

배포: `_Data/Script/` 33개, `_Data/Field/Skit/Data/` 18개,
`_Data/Field/MapData/` 2개(퍼즐 힌트 대사 — 같은 대사를 다른 파티원은
반말로 하는데 앙쥬만 존댓말이었던 케이스, `Dn/12/04.dat`,
`Dn/21/00.dat`).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 10차) | 1,783,700,899 | `29a9c39b593b89754e61f6f2d1eef70718c0bfe3c94de5e26cc49b1b729e0176` |
| `toidata_release_ko.xdelta` | - | 91,265,218 | `b2f3ea8b136db20648c49aadd5dd9aa25a3db2a8e8e64a0f1ec41f08c68778fd` |

### 교훈 추가: "누구에게 말하는가"는 그 줄만 봐서는 부족하다
9차 때 "해당 줄에 대상 이름이 나오는가"로만 걸렀다가, "みんな"로
지칭하거나 이름 없이 반문/맞장구치는 대사(직전 대사의 화자가 진짜
대상)를 다수 놓침. **말투 정합성 검사는 화자별 어미 패턴 대조뿐 아니라
각 후보 줄의 앞뒤 2~5줄을 같이 읽어서 "실제 대화 상대가 누구인지"를
확인하는 단계가 반드시 필요함** — 특히 앙쥬처럼 대상에 따라 말투를
바꾸는 캐릭터는 이 단계를 생략하면 정확도가 크게 떨어짐. 사용자에게
"전수 점검"을 요청받으면 정말로 후보 전체(이번엔 127건)를 문맥과 함께
다시 훑어야 함 — 이전 결과에서 눈에 띄는 것만 추리면 안 됨.

---

## 추가 진행: 앙쥬 말투 정책 변경 — "상대 무관, 원문 어미 그대로" 47건 추가 (2026-08-24, 11차)

사용자가 정책을 명확히 함: **앙쥬는 이제부터 상대가 누구든 상관없이
원문이 존댓말(です/ます 계열)이면 존댓말, 반말이면 반말**로 옮길 것
(9~10차의 "타깃 5인 기준" 예외 규칙 폐지). 9~10차에서 일부러 존댓말로
남겨뒀던 리카르도/큐큐/콘웨이/적/NPC 상대 대사 47건을 전부 반말로
수정.

**작업 중 발견한 중요한 함정**: 처음엔 "ましょう"(~합시다, 권유형)와
"いたし"(겸양 표현)도 존댓말 표지에 추가하려 했으나, 기존 번역
전체(이번 세션에서 손 안 댄 부분 포함)를 조사해보니 **"〜ましょう"
권유형은 원래부터 일관되게 반말 "~하자/~할까"로 번역돼 있었음**(예:
"一緒に行きましょう"→"같이 가자", "行きましょう"→"가자" 등 수백 건).
즉 ましょう는 문법상 ます 계열이지만 이 프로젝트의 기존 번역 관례상
"존댓말 표지"로 취급하면 안 됨 — 넣었다면 기존의 자연스러운 반말
권유문 수백 개와 충돌해서 전체 일관성이 깨졌을 것. **최종적으로 사용한
존댓말 판정 기준(9~11차 전부 동일)**: `です|ます|ました|ませ|でしょう
|でした|ございます|ございました|くださ|なさい|おられ|いらっしゃ|でして
|おりま` — ましょう/いたし는 제외.

또한 한 대사(여러 줄) 안에서도 줄마다 원문 어미가 다를 수 있음을
확인(예: 첫 줄은 진짜 です, 둘째 줄은 반말 어미) — 이런 경우 **줄
단위로** 존댓말/반말을 따로 판정해서 폴라이트한 줄은 그대로 두고
반말인 줄만 수정해야 함(전체 문장을 한 번에 반말/존댓말로 밀어붙이면
안 됨).

바뀐 파일: `_Data/Script/` 16개, `_Data/Field/Skit/Data/` 16개,
`_Data/Field/MapData/` 2개.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 11차) | 1,783,700,749 | `05eff8f5cefe272e80e7df71a677e5381e606c34e9e5d2d248b72bda97bd5cb9` |
| `toidata_release_ko.xdelta` | - | 91,265,086 | `307ec9159e2f58fabb05b3cb5f7144ca65744465bff208058241fef869227dd9` |

### 참고: 앙쥬 말투 정합성 확인은 이제 완료된 것으로 보임
9~11차를 거쳐 앙쥬의 "원문 반말+번역 존댓말" 불일치는 전수(143건)
해결됨. 반대 방향(원문 진짜 존댓말인데 번역이 반말인 경우)은 이번
세션에서 의도적으로 다루지 않음(スコープ 밖 — 사용자가 요청한 건
"존댓말로 잘못 번역된 반말 대사"뿐이었음). 만약 나중에 그 반대
방향도 점검해달라는 요청이 오면, 위에서 확정한 존댓말 판정 기준으로
`kr_polite_end`/`kr_casual_end` 정규식도 `습니다/합니다` 같은 받침
있는 활용형을 더 폭넓게 잡도록 보강해야 함(이번에 "감사합니다" 같은
표현을 못 잡는 걸 발견함 — `습니다` 리터럴만 찾고 `합니다`는 놓침).

---

## 추가 진행: 앙쥬 말투 일관성 보정 16건 (2026-08-24, 12차)

사용자가 11차 결과 예시("私もです/では行きましょう"→"저도요/그럼 가자")를
보고 규칙 2개를 명확히 함:
1. **한 대사(여러 줄) 안에서 어느 한 줄이라도 원문이 진짜 존댓말이면
   그 대사 전체를 존댓말로 통일**(반말·존댓말 혼용 금지) — 9~10차
   포함 전체를 다시 스캔해서 확인해야 함(11차에서만 생긴 문제가
   아니었음: 1320.dat#199/#273, 0060.dat#36도 여기 해당).
2. **권유형(ましょう) 대사는 대상이 평소 존댓말로 얘기하는 상대
   (리카르도/큐큐/콘웨이/알베르/낯선 사람 등, 반말 5인방 제외)면
   존댓말로 유지** — 11차에서 "ましょう=무조건 반말"로 일괄 처리했던
   걸 대상별로 재검토해야 함.

9~11차 전체(143건)를 원문 재대조해서 위 두 원칙과 어긋난 16건을
찾아 존댓말로 되돌리거나 통일(9~10차 3건: 1320#199, 1320#273,
0060#36 / 11차 13건). 바뀐 파일: `_Data/Script/` 5개,
`_Data/Field/Skit/Data/` 10개.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 12차) | 1,783,700,789 | `9e761ff616183e7ca9e63b3f362827fca6178a203b3c558bb3aa9de66f66bc8a` |
| `toidata_release_ko.xdelta` | - | 91,265,129 | `5c54e3004a150656d28b4311d37716bd24140ceb8583b96b6fc2e842f27d8f06` |

### 확정된 앙쥬 말투 규칙 (최종, 다음에 또 손댈 때 이 기준을 따를 것)
1. 원문 줄에 진짜 존댓말 표지(`です|ます|でしょう|ました|ませ|
   ございます|ございました|くださ|なさい|おられ|いらっしゃ|でして|
   おりま`, ましょう/いたし 제외)가 있으면 → 그 줄 존댓말. **단, 같은
   대사(다줄) 안에 하나라도 이 표지가 있으면 대사 전체를 존댓말로
   통일**(줄마다 따로 판정해서 섞지 말 것).
2. 표지가 전혀 없는 대사는: 권유형(ましょう)이면 → 대상이 스파다/
   이리아/루카/코다/에르마나(반말 5인방)면 반말, 그 외(리카르도/큐큐/
   콘웨이/알베르/적/NPC/낯선 사람 등)면 존댓말. 권유형이 아닌 순수
   여성어 어미(わ/の/ね/かしら/よ 등)면 → 대상 무관하게 반말(9~11차
   결론 그대로, 이건 안 바뀜).
3. `kr_polite_end` 정규식은 `습니다`뿐 아니라 `합니다`류(받침+니다)도
   못 잡을 수 있음 — 재검사할 땐 정규식 결과를 맹신하지 말고 직접
   읽을 것.

---

## 추가 진행: 앙쥬 → 큐큐도 반말 상대로 재분류(3건) (2026-08-24, 13차)

사용자가 큐큐(キュキュ)도 "존댓말 상대" 그룹(리카르도/콘웨이/알베르/
낯선 사람 등)에서 빼서 "반말 상대"(스파다/이리아/루카/코다/에르마나)로
재분류해달라고 요청. 근거: 원문을 보면 앙쥬가 큐큐를 부를 때 호칭
("キュキュさん")만 높이고 어미는 원래 반말인 경우가 많다는 관찰.

큐큐가 이름으로 직접 언급된 앙쥬 대사 30건을 전수 재확인:
- 27건은 이미 원래부터 정확히 처리돼 있었음(어미에 진짜 존댓말 표지
  `です/ます/でしょう/ました/ませ/ございます/くださ` 등이 있는 6건은
  존댓말 유지가 맞고, 나머지 21건은 이미 반말로 번역돼 있었음 — 9~12차
  작업과 무관하게 원래부터 맞았던 것들).
- 3건이 "원문엔 존댓말 표지가 전혀 없는데 번역만 존댓말"인 진짜
  불일치였음: `고마워요`→`고마워`, `왜요?`→`왜?`, `실례할게요`→`실례할게`.
  이 3건은 **기존 `kr_polite_end` 정규식이 못 잡는 패턴**이었음(받침
  있는 음절+워요/왜요/게요 — 예: "워"는 완성형 한 글자라 "어요"
  부분 문자열 매칭이 안 됨). 규칙적 정규식보다 "줄 끝이 요/니다/죠로
  끝나는가"를 직접 눈으로 다시 훑어서 찾음.

바뀐 파일: `_Data/Field/Skit/Data/` 3개(`5050.dat`#40, `5053.dat`#5,
`5054.dat`#104).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 13차) | 1,783,700,789 | `d5a9417a91ab5ef29770be675f3d0881e717491fe42764ca55b8be0a882e6413` |
| `toidata_release_ko.xdelta` | - | 91,265,114 | `48622c0b76e5aef9fc37691c3df9a016f47b01272861a48206ba03315a5e82e1` |

### 확정된 앙쥬 말투 규칙 (최종 갱신)
**"반말 상대" 그룹 = 스파다/이리아/루카/코다/에르마나/큐큐(6인).**
그 외(리카르도/콘웨이/알베르/적/NPC/낯선 사람 등)는 "존댓말 상대"로
유지. 나머지 규칙(줄 단위 진짜 존댓말 판정, 한 대사 내 일관성, 권유형
+상대별 처리)은 12차와 동일.

### 참고: 정규식 기반 존댓말 탐지의 한계 (재확인)
이번에도 정규식(`kr_polite_end`)이 실제 존댓말 어미를 놓치는 사례가
나옴(완성형 한글 음절이 자모 부분 문자열 매칭을 깨뜨리는 경우: 워요,
왜요, 게요 등). **말투 재검토를 요청받으면 정규식 스캔 결과만 믿지
말고, 최소한 "줄 끝이 요/니다/죠로 끝나는가"를 직접 읽어서 재확인할
것.** 특정 캐릭터(예: 큐큐) 관련 전수 재검토처럼 범위가 좁을 땐 아예
정규식 없이 전부 눈으로 읽는 게 더 안전함.

---

## 📌 2026-08-24 세션 총정리 (6~13차, 다음 세션은 여기부터 읽으면 됨)

이 세션 하루 동안 6차~13차까지 8라운드 진행함. 아래는 전체 요약과
**최종 확정 상태**. 각 라운드의 자세한 경위는 위쪽의 개별 섹션 참고.

### 무엇을 했나 (라운드별 한 줄 요약)
- **6차**: `i_line_1~6.PNG` 확인 → Story/Skit/MapData가 아닌 완전히
  새로운 카테고리(스토리북/배틀북/칭호/몬스터도감/레코드/술기 화면)
  에서 텍스트 넘침 8건 발견·수정. 이번 세션 최초로 `eboot.bin`도 변경.
- **7차**: 칭호(CodeName) 1건만 고치고 끝냈다가 사용자가 "같은 영역
  나머지도 확인해달라"고 재요청 → `ItemDataPack.csv`/`ArtsDataPack.csv`
  전체(1,330개 description 필드) 스캔해서 76건 추가 수정(안전폭
  24자→27자로 재조정, 스크린샷 2장으로 재실측).
- **8차**: 칭호 "이름(name)" 필드가 목록 패널·장비창에서 잘리는 문제
  발견(설명과는 별개 UI, 안전폭 약 12자). 사용자 지시로 CodeName #103
  이름만 축약.
- **9~11차**: 앙쥬(アンジュ) 말투 정합성 — 원문이 반말 어미인데 번역이
  존댓말인 경우를 전수 점검. 9차 16건(대상이 스파다/이리아/루카/코다/
  에르마나로 명시된 것만), 10차 80건(대상 이름이 안 나와도 "みんな"나
  앞뒤 문맥으로 대상이 확인되는 것까지), 11차 47건(사용자가 "상대가
  누구든 원문 어미 그대로"로 정책 변경 → 리카르도/큐큐/콘웨이/알베르/
  적 상대 대사까지 전부) — 총 143건.
- **12차**: 사용자가 "한 대사 안에서 어느 한 줄이라도 존댓말이면 전체
  통일" + "권유형(ましょう)은 상대가 평소 존댓말 상대면 존댓말 유지"
  2원칙 추가 → 9~11차 중 이 원칙에 어긋난 16건을 존댓말로 되돌림.
- **13차**: 사용자가 큐큐도 "반말 상대"로 재분류해달라고 요청 → 큐큐
  관련 대사 30건 전수 재확인, 진짜 불일치 3건 수정(정규식이 못 잡던
  "워요/왜요/게요" 패턴 — 정규식 대신 눈으로 직접 확인해서 찾음).

### 최종 확정 규칙 (다음에 이 프로젝트 만지면 이 기준을 그대로 따를 것)

**대사창 안전폭(문구 변경 없이 줄바꿈만 재배치)**
- Story.csv: 26자 균일(3줄 다 동일)
- SKIT 대사: WIDE(1~2번째 줄) 27자 / NARROW(3번째 줄, ▼아이콘과 겹침) 24자
- MapData.csv: Story.csv와 동일 UI → 26자
- 칭호/아이템/특기 설명(`ItemDataPack.csv`/`ArtsDataPack.csv`의
  description 필드, 이름+2줄 설명 UI): 27자, 최대 2줄까지 관측됨(3줄
  본 적 없음 — 늘려야 하면 먼저 스크린샷으로 확인할 것)
- 칭호 이름(name) 필드(목록 패널+장비창 슬롯, 설명과 별개 UI): 약 12자
  (한 줄이라 줄바꿈 불가 — 넘치면 글자 수를 줄여야 함, 이 프로젝트에서
  유일하게 "문구 안 줄이기" 원칙의 예외)
- 스토리북/배틀북(좌: 목록, 우: 설명 패널 UI 공유): 21자
- eboot.csv에 박힌 한 줄짜리 고정폭 UI 라벨(예: "레이브 Lv.4 최대
  시간", "훔칠 수 있는 아이템", "공중 가능")도 줄바꿈 불가 — 넘치면
  글자 수 축약 필요(이것도 "안 줄이기" 원칙의 예외, 케이스마다 개별
  판단)
- 아직 스크린샷으로 확인 못한 UI: `KizunaDataPack`(인연),
  `OperationDataPack`(조작법), `TutorialData`(튜토리얼),
  `MissionData`(미션), `ShopDataPack`(상점 이름) — 넘침 발견되면 같은
  방식으로 처리(스크린샷 실측 → 안전폭 산출 → **그 UI를 쓰는 파일
  전체를 한 번에 스캔·수정**, 캡처된 예시 1건만 고치고 끝내지 말 것)

**"카테고리별 분할 CSV vs 마스터 CSV" 함정 (중요)**
`ItemDataPack_CodeName.csv` 같은 `ItemDataPack_*.csv` 분할 파일들은
재컴파일에 실제로 안 쓰이는 열람용 사본임 — 진짜 소스는
`ItemDataPack.csv`(전체 카테고리 통합, 헤더 없음, 컬럼 순서
`category,index,field,japanese,translation`)뿐. **이 계열 텍스트를
고칠 땐 항상 마스터 파일(`ItemDataPack.csv`)을 직접 고칠 것** —
분할 파일만 고치면 반영 안 됨(이번에 그 실수를 했다가 뒤늦게 발견함).

**앙쥬(アンジュ) 말투 최종 규칙**
- "반말 상대" 그룹(6인): 스파다/이리아/루카/코다/에르마나/큐큐
- 그 외(리카르도/콘웨이/알베르/적/NPC/낯선 사람 등)는 "존댓말 상대"
- 판정 순서: (1) 원문 어느 줄이든 진짜 존댓말 표지(`です|ます|でしょう|
  ました|ませ|ございます|ございました|くださ|なさい|おられ|いらっしゃ|
  でして|おりま` — ましょう/いたし 제외)가 있으면 그 **대사 전체**를
  존댓말로 통일(줄마다 따로 판정해서 반말/존댓말 섞지 말 것). (2) 표지가
  전혀 없으면: 권유형(ましょう)은 대상이 반말 상대 6인이면 반말, 그
  외면 존댓말. 권유형이 아닌 순수 여성어(わ/の/ね/かしら/よ 등)는 대상
  무관하게 반말.
- **정규식 한계 재확인**: `습니다`는 잡아도 `합니다`류(받침+니다),
  `워요/왜요/게요`(완성형 음절이 자모 부분 문자열을 깨는 경우)는 못
  잡을 수 있음 — 재검토 요청받으면 정규식 결과만 믿지 말고 최소한
  "줄 끝이 요/니다/죠로 끝나는가"를 직접 읽어서 재확인할 것. 범위가
  좁으면(특정 캐릭터 한정 등) 아예 정규식 없이 전부 눈으로 읽는 게
  더 안전함.
- 다른 캐릭터(리카르도/이리아/코다 등)의 말투 정합성은 아직 이런
  방식으로 전수 점검한 적 없음 — 요청 오면 같은 방법론 적용.

### 14차(2026-08-24, line_6.PNG): 스킷 대사 줄바꿈 넘침 수정(콘웨이, 1건)
신규 실기 캡처 `line/line_6.PNG`에서 발견. `_Data/Field/Skit/Data/0070.dat`
(콘웨이·루카 대화, Skit.csv File=`0070.dat`/Field=`line`/Index=`16`,
Speakers=`1 [コンウェイ]`) 콘웨이 대사가 4줄로 줄바꿈되어 있었는데 스킷
대사창은 3줄까지만 표시 가능해서 4번째 줄이 화면 아래로 잘려나가던
문제. 원문은 3줄(`ボクは/キミたちの旅を見届けたい/最後の最後までね`).
문구는 전혀 줄이지 않고 줄바꿈 위치만 재조정(`나는\n너희들 여행을
끝까지\n지켜보고 싶어\n마지막의 마지막까지 말이야` (4줄) →
`나는 너희들 여행을\n끝까지 지켜보고 싶어\n마지막의 마지막까지 말이야`
(3줄, 10/11/14자 — 스킷 안전폭 [27,27,24] 이내)). 내용(토큰) 완전 보존
확인. `Story.csv`/`Skit.csv`(line 필드)/`MapData.csv` 전체를 3줄 초과
기준으로 재스캔해서 동일 유형 사례가 더 있는지 확인 — 이 건 외에는
0건(고립된 단일 사례, MapData/ItemDataPack처럼 카테고리 전체에 퍼진
문제는 아니었음). `2_translated/skit_wip/0070.dat.csv`를 소스로 수정,
`_read_skits`+`skit_replace_text`로 해당 파일만 개별 재컴파일해서 배포.

### 15차(2026-08-24, line_7.PNG): Story/MapData/Skit "마지막 줄" 안전폭 전수 재조정(93건)
`line_7.PNG`에서 6차에 이미 26자 기준으로 고쳤던 `Twn/26/00/760.dat
#273`(가드르 대사) 3번째 줄이 여전히 '다음' ▼ 아이콘 뒤로 잘려있는 걸
재확인. `line_5.PNG`(2번째 줄 27자, 안 잘림)와 비교해서 '다음' 아이콘은
줄 위치(1/2/3번째)가 아니라 **그 대사의 마지막 줄**(줄 개수 무관)에
항상 걸쳐있다는 걸 확인 — Skit.csv에서 이미 확립돼 있던 "WIDE=27(앞
줄들)/NARROW=24(마지막 줄)" 규칙(`fix_skit_group_overflow.py`의
`caps=[WIDE]*(n_lines-1)+[NARROW]`)과 정확히 같은 패턴인데, 이게
Skit.csv 전체에도, Story.csv/MapData.csv에도 한 번도 전수 적용된 적이
없었다는 걸 발견(기존엔 "3번째 줄"이라는 표현 때문에 3줄짜리에만,
그것도 "그룹 넘침"이라는 좁은 케이스에만 적용됐었음).

Story.csv/MapData.csv/Skit.csv(`line` 필드) 전체를 "마지막 줄 24자
초과" 기준으로 재스캔해서 문구 전혀 안 줄이고 줄바꿈 위치만 재배치:
- Story.csv: 28건 후보 → 27건 적용(`_Data/Script/` 26개 파일). 보류
  1건: `Dn/08/01/701.dat #90`(전각공백 인용구 서식, 기존부터 보류 중).
- MapData.csv: 30건 후보 → 28건 적용(`_Data/Field/MapData/` 20개
  파일). 보류 2건: `Twn/04/00.dat` Section4 #22(♪ 노래 가사, 전각공백
  서식), `Twn/03/04.dat` Section0 #6(3줄 상태로 최선 재배치해도 24자
  못 맞춤 — **신규 보류 항목**).
- Skit.csv(`line`): 65건 적용(`_Data/Field/Skit/Data/` 68개 파일).
  `line_addition`으로 이어지는 그룹 1건(`0235.dat #89`)은 진짜 마지막
  줄이 line_addition 쪽(9자)이라 원래도 안전 — 그대로 둠.

내용(토큰) 완전 보존 자동 검증 통과. 배포: 3개 파일에서 바뀐 `_Data`
파일 총 101개. l7c 재빌드(1,776/1,776 일치) → xdelta 재생성·왕복 검증
(SHA256 완전 일치) → 전 배포처 동기화 완료.

새로 쓴 스크립트(재사용 가능): `fix_story_lastline_24.py`,
`fix_mapdata_lastline_24.py`, `fix_skit_lastline_24_full.py`
(모두 `tools/toir/font_research/hangul_fix/rewrap/`) — 이후 같은 부류의
넘침이 또 보고되면 그대로 재실행 가능(멱등적: 이미 안전한 줄은 후보에서
자동 제외됨).

### 16차(2026-08-31, 동일 원문·다른 번역 통일): 25건
사용자가 "산할멈"(山姥가 `Skit.csv`에선 "산할멈", `Story.csv`
`Dn/17/04/710.dat`에선 "야마우바"로 갈라진 사례)을 예로 들며 같은
원문이 다르게 번역된 케이스를 찾아 통일해달라고 요청. `Story.csv`/
`Skit.csv`/`MapData.csv` 전체에서 "완전히 동일한 일본어 원문(길이 5자
이상, 순수 구두점/6자 이하 짧은 감탄사 제외)인데 한국어 번역이 다른"
케이스를 자동 스캔(스캔 스크립트: 세션 중 임시 작성, 저장 안 함 —
필요하면 같은 로직으로 재작성 가능: Story/Skit(`line`,`line_addition`,
`title`,`choice`)/MapData 세 파일을 (File/Path+Section+#) 키로 로드,
JP 텍스트가 완전히 동일한 그룹을 모아서 KR 값이 2가지 이상이면 후보)
→ **509건** 발견. 사용자가 "n≥5회 반복"만 먼저 처리하도록 범위를
지정해서 **25건**(반복 5회 이상) 통일 적용, 나머지 484건(대부분
우연히 1쌍만 겹친 것)은 `TERM_CONSISTENCY_CANDIDATES.md`(루트, 509건
전체 목록, 반복 횟수 내림차순)에 남겨둠.

적용한 25건 중 주요 패턴:
- **던전 이동 UI 문구가 `MapData.csv`(원본)와 `Story.csv`(같은 텍스트가
  컷신용으로 재사용된 사본)에서 각각 다른 시점에 번역되며 갈라짐** —
  `進む方向を選んでください`(57회), `真っ直ぐ進む`(37회) 등. 항상
  MapData 쪽 다수결 버전으로 통일, Story 쪽(소수)을 고침.
- **가람 각지에 복제된 원더셰프(장사꾼) 소개/판촉 대사**가 마을마다
  약간씩 다르게 번역됨(12회/11회, 3~4가지 버전) → 다수결로 통일.
- **던전 퍼즐방 하나(`Dn/06/00.dat`)가 같은 방을 복제한 다른 4개
  던전(`Dn/08/00`/`14/01`/`15/01`/`17/00`)과 번역이 갈라짐**(6건) —
  06_00을 나머지 4개에 맞춤. 단 그 4개끼리도 완전히 일치하진 않는
  항목(예: "지금 소서러 링의 힘이라면" vs "지금의 소서러 링 힘이라면")은
  가장 원문에 충실한 형태로 새로 합성해서 5곳 전부에 적용.
- **캐릭터 고유 말투(큐큐의 서툰 반말 "はい　わかた", 7회 4가지 버전)**
  → 어눌한 말투 캐릭터는 등장할 때마다 같은 방식으로 어눌해야 한다는
  원칙으로 "네, 알갯어" 한 가지로 통일.
- **1개는 의도적으로 통일하지 않고 남김**: `お気をつけて`(4회 "조심해"
  vs 1회 "조심히 다녀오십쇼") — 화자가 다른 캐릭터(치토세=파티원,
  용심봉=경호원 NPC)라 존댓말 차이가 의도된 것.
- 산할멈/야마우바 자체는 `2_translated/script_wip/Dn_17_04_710.dat.csv`
  3곳을 "산할멈"으로 수정(반말 언어유희 대사도 자연스럽게 유지됨).

배포: `_Data/Script/` 11개, `_Data/Field/MapData/` 20개,
`_Data/Field/Skit/Data/` 9개(총 40개 파일). l7c 재빌드(1,776/1,776
일치) → xdelta 재생성·왕복 검증(SHA256 일치) → 전 배포처 동기화 완료.

**다음 세션에서 이어갈 것**: `TERM_CONSISTENCY_CANDIDATES.md`의 나머지
484건(반복 5회 미만) 처리 여부를 사용자에게 물어볼 것 — 사용자가
"n≥5만 먼저"라고 범위를 명시적으로 좁혔으므로, 나머지는 요청이 있을
때만 진행. 재스캔 시 이번에 손댄 25건은 이미 해결됐으니 건너뛰어도 됨.

### 17차(2026-09-01, 새 세션): 아스라 → 브리트라 호칭 통일("유모"→"할멈")
사용자가 아스라(アスラ)가 브리트라(ヴリトラ)를 부르는 호칭도 안
통일돼 있다고 지적(16차의 산할멈 사례와 같은 종류의 재점검 요청).
원문 「ばあや」(어릴 때부터 자신을 키운 노년의 유모를 부르는 애칭)가
`Story.csv`(Twn/16/00/710, Twn/17/00/730, Twn/20/00/720 — 11회)에서는
"할멈", `Skit.csv`(3015.dat/3033.dat/3034.dat, 아스라 시대 회상
스킷 — 4회)에서는 "유모"로 갈라져 있었음.

찾는 방법: `speaker=='アスラ'`인 행 중 `ヴリトラ`를 언급하는 행은
Story/Skit 합쳐 딱 2건뿐이라 부족했음 — 대신 **호칭의 원문 자체
「ばあや」로 전체 재검색**해서 20건을 모아 비교(아스라가 브리트라를
부르는 모든 장면을 찾으려면 상대 이름이 아니라 호칭 단어로 검색해야
빠짐없이 잡힘, 앞으로 비슷한 "호칭 통일" 요청에 재사용 가능한
방법론). "할멈"이 다수(11회)이고, 특히 Twn/17/00/730의 핵심 장면
("알고 있다, 할멈……아니, 브리트라" — 아스라가 습관적으로 부르던
"할멈"을 스스로 "브리트라"로 고쳐 부르는, 호칭 전환 자체가 서사의
일부인 장면)에서 이미 "할멈"으로 확정돼 있었으므로 그쪽으로 통일.
`Skit.csv` 3개 파일(3015.dat #25, 3033.dat #5·#11, 3034.dat #2)의
"유모"를 "할멈"으로 수정.

배포: `_Data/Field/Skit/Data/` 3개(`3015.dat`, `3033.dat`, `3034.dat`).

### 최종 배포 상태 (17차 기준, 이게 지금 라이브에 실제로 깔려있는 버전)
| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-09-01 17차, 최종) | 1,783,701,026 | `104c1a555edd660f512969929be9dce7e325cdea80737a3879fd4d60eb3d1b31` |
| `toidata_release_ko.xdelta` | - | 91,265,401 | `7694cbc0988046c8fa99235387e9d6b4b9707f019c48d6f77d7d1306fe393c09` |
| `eboot.bin` | 6차에서 변경(레이브/훔칠/공중 라벨 축약) | 1,543,133 | `64b8edad172a6aec0bf92677134d57e162dd5119e4206e527b56185f76eb44c2` |
| `eboot_ko.xdelta` | - | 10,626 | `105cd3de2405411c7667e8a07ecf746392b8e5f640c2215a49436bf7ef8536f1` |

라이브 Vita3K 설치본(`...\Vita3K\ux0\app\PCSG00009\`)에도 전부 반영
완료. `3_patched/ready_to_use/`, `loose_files/`, `xdelta_release/` 전부
동기화 완료. 백업은 전부 `deploy_backups/`에 타임스탬프별로 보존
(이번 라운드: `deploy_backups/20260901_005608_asura_vritra_baaya_fix/`,
`deploy_backups/20260901_010500_asura_vritra_baaya_deploy/`,
`deploy_backups/<TS>_xdelta_pre_asura_vritra_baaya_fix/`).

### 다음 세션 TODO (이 세션이 남긴 것만, 최종본)
1. **다른 캐릭터·관계의 호칭도 재점검 요청이 올 수 있음** — 이번
   방법론(상대 이름이 아니라 "호칭 단어 자체"로 전체 재검색) 그대로
   재사용할 것. 아직 다른 관계는 점검 안 함(예: 이리아↔코다, 앙쥬가
   다른 파티원을 부르는 호칭 등 — 요청 없으면 먼저 손대지 말 것).
2. **`TERM_CONSISTENCY_CANDIDATES.md`의 나머지 484건**(반복 5회 미만,
   대부분 1쌍짜리 우연한 중복) — 사용자에게 추가 반영 범위를 물어볼 것.
3. **ItemDataPack.csv CodeName #58/#230/#235, Recipe #59** — 27자
   기준으로 이미 2줄인 상태에서 최선으로 재배치해도 초과. 문구를 안
   줄이면 방법 없음(보류).
4. **Story.csv `Dn/08/01/701.dat #90`, MapData.csv `Twn/03/04.dat`
   Section0 #6** — 문구를 안 줄이면 24자(마지막 줄) 못 맞춤(보류).
5. **칭호 이름 필드 #16("물건 잘 잃어버리는 소년", 13자)/#194("상냥한
   건지 엄격한 건지", 13자)** — 12자 안전폭 초과했지만 사용자가 #103만
   지정해서 이 둘은 아직 안 건드림.
6. **♪ 노래 가사(전각공백 서식) 2건 보류 중**: `Twn/04/00.dat` Section4
   #22(MapData), 다른 유사 서식 항목도 있을 수 있음 — 일반 rewrap 로직이
   전각공백 들여쓰기 서식을 깨뜨리므로 손대려면 전용 처리 필요.
7. `KizunaDataPack`/`OperationDataPack`/`TutorialData`/`MissionData`/
   `ShopDataPack` 대사창 넘침 여부 미확인(스크린샷 없음).
8. `extract.py`의 `line_addition` 순서 버그(2026-08-22 발견)를 정식
   으로 고칠지 검토 — 지금은 `true_groups.py`로 우회만 함.
9. 앙쥬 외 다른 캐릭터 말투 정합성은 아직 전수 점검 안 함.
10. 디스크 여유공간 37GB(93% 사용, 17차 세션 끝 기준) — 아직 여유
    있으나 라운드마다 계속 쌓이니 다음에 한 자리 GB로 떨어지면
    `deploy_backups/`의 오래된 `xdelta_pre_*` 백업부터 정리할 것.

### 18차(2026-09-01, 새 세션): 전투 보이스 자막(battle voice caption) 기능 — 신규 요청, 조사 착수만 완료

사용자가 디시인사이드의 「테일즈 오브 디 어비스」 팬 한글패치 전투음성 자막
게시물을 참고 예시로 들며, 이 프로젝트에도 같은 종류의 기능(원래 자막 없는
전투 보이스에 새로 자막을 붙여 화면 빈 공간에 표시)을 요청함. 지금까지 이
프로젝트에서 한 번도 다룬 적 없는 완전히 새로운 작업 갈래. 사용자가 "바이너리
후킹 조사 먼저"를 선택해서 이번 세션은 조사만 진행, 자막 문구 작성이나 실제
패치 적용은 아직 안 함.

**확인된 것** (자세한 내용·재현 방법은 `tools/toir/font_research/battle_voice_caption/README.md`):
- 대사창 등에 쓰이는 라벨 큐 텍스트 렌더링 시스템(`FUN_8112caa0`류, 이미
  `hangul_fix/SESSION_2026-08-19_baseline_float.md`에서 확인됨)이 전투 HUD
  라벨(카운트/소비TP/코스트 등)에도 이미 쓰이고 있음을 재확인 — 전투 화면에도
  같은 방식으로 자막을 등록할 수 있을 가능성이 높음.
- 전투 보이스 재생 경로 2종 확인: (A) 이름 있는 단발 클립(`Battle/Voice/
  Start/Player|Enemy/BST_*.at9`, `Battle/Voice/Result/RES_*.at9`,
  `Battle/Voice/GameOver/BGO_*.at9`) — 단일 디스패처 `FUN_81042e86`, 호출부
  3개 함수(`FUN_81034b4c`=결과/승리 추정, `FUN_810435c0`=전투시작,
  `FUN_81072d78`=게임오버) 전부 특정함. (B) 캐릭터별 다클립 팩
  (`Battle/Voice/NN.pck`, 공격/피격/오의 등으로 추정) — 프리로더
  (`FUN_810357d6`)만 찾았고, 실제 "이 상황에 이 인덱스 재생" 호출부는 아직
  못 찾음.
- 사용자가 "경로 B 계속 추적"을 선택해서 같은 세션에서 이어감 — **경로 B
  (전투 중 공격/피격/오의 등)의 후킹 지점도 완전히 특정 완료.** 단일 함수
  `FUN_8104c732(캐릭터, state, flag, extra)`가 state(0~0xf)를 카테고리(0~9)로
  매핑한 뒤 `FUN_8103d062`(실제 재생 슬롯 배정) → `FUN_81023cdc`(추정 오디오
  엔진 호출)로 이어지며, 이 함수 하나가 **17개 호출부 전부를 커버**함
  (`getReferencesTo`로 전수 확인). 즉 새 훅은 딱 두 지점만 필요: 전투 중
  대사는 `FUN_8104c732`, 승리 대사는 `FUN_81034b4c` (호출부마다 따로 훅 안
  해도 됨).
- **최종 결론: 승리 후 대사·전투 중 대사 둘 다 후킹 지점 확정 완료.** 남은
  건 (a) 각 state ID가 실제로 무슨 상황인지 실기/에뮬레이터로 검증(현재는
  코드 문맥 기반 추정치뿐), (b) armips 훅 코드 설계, (c) 자막 문구 확보.
- **막힌 지점(사용자 확인 필요, 변동 없음)**: 각 보이스 클립이 실제로 무슨
  대사인지 전혀 모름(파일명/인덱스뿐, 대본 없음) — Claude가 오디오를 직접
  들을 수 없어서, 자막 문구를 쓰려면 사용자가 직접 듣고 알려주시거나 기존
  대본 자료가 필요함.

- **중요 정정(같은 세션): 원래 계획했던 "명령어 4바이트를 B.W 디투어로
  바꾸는" 훅 방식은 시도하지 않고 보류함.** `number_bug/README.md`
  57~59번 항목에 이미 이 정확한 방식이 이 코드 세그먼트(`segment_0`)에서
  **세 번 부팅 크래시**를 냈다는 기록이 있었음(빈 공간 자체도 5곳뿐,
  3곳은 이미 다른 패치가 사용 중). 대신 사용자의 Vita3K 디버거(브레이크
  포인트+레지스터 확인 가능)로 **바이너리를 건드리지 않고** `0x8104C732`
  진입 시점 r1(state) 값을 직접 관찰하는 방식으로 전환함 — 아직 패치는
  전혀 작성/배포 안 됨.

- **같은 세션 추가: 사용자가 "마저 알아서 진행해줘"라고 해서, 오디오
  추출까지 진행함.** `Battle/Voice/{Result,Start,GameOver}`가 게임
  덤프에 이미 언패킹된 개별 `.at9`로 존재한다는 걸 확인하고,
  `vgmstream-cli.exe`로 254개를 wav 변환(`battle_voice_caption/audio_wav/`,
  변환 에러 0건). `RES_300~307`(8그룹)을 파티원 8명과 개수가 일치한다는
  이유로 "승리 대사"로 우선 추정해 변환했지만 **검증 안 된 추측**임 —
  실제로 들어봐야 확정. 캐릭터ID(300→?)와 `CharaNames.csv` 이름 매칭도
  안 함. Claude는 여전히 오디오를 못 들으므로(ASR 도구 확인 결과 없음),
  이 작업으로 "직접 추출하는 수고"는 덜었지만 "무슨 내용인지 듣고 받아
  적는" 단계는 그대로 사용자 몫으로 남아있음.

- **같은 세션 추가: 캐릭터 매핑 확정 + `p_memsz` 확장 무해 테스트 파일
  준비 완료.** 사용자가 `RES_300~307` = 루카/이리아/스파다/앙쥬/리카르도/
  에르마나/콘웨이/큐큐임을 확인해줘서 `audio_wav/Result_party/`를
  캐릭터별 폴더로 재정리함. 이어서 훅 주입 방법 재검토 중 Vita3K 소스가
  `p_memsz`로 세그먼트를 할당한다는 걸 재확인하고, `segment_0`의
  `p_memsz`를 늘리는(코드는 안 넣고 딱 8바이트 헤더 필드만 변경) 무해한
  부팅 테스트 파일을 만듦: `battle_voice_caption/_build_out/eboot_memsz_test.bin`.
  **아직 사용자가 실기/에뮬레이터로 부팅되는지 확인 안 함 — 다음 세션
  시작 전에 결과부터 물어볼 것.** 상세 절차는 README.md "✅ 무해한 부팅
  테스트 파일 준비 완료" 섹션. `vanilla_test/vita3k_profile`의 기존
  `eboot.bin`이 예상 해시와 달라서(다른 용도로 재사용된 듯) 그 프로필을
  그대로 쓰지 말고 새 격리 복사본을 만들라고 안내해둠.

- **같은 세션 추가: `eboot_memsz_test.bin` 부팅 성공 확인("잘되네") →
  실제 디투어+트램폴린 훅(무변경 pass-through 버전)까지 작성·적용
  완료.** `FUN_8104c732` 진입부를 트램폴린으로 우회시키되 원래 명령어를
  그대로 재실행하고 복귀만 하는, 기능적으로 원본과 100% 동일해야 하는
  버전. 원본 대비 정확히 16바이트만 다름(phdr 헤더 필드 + 후킹 4바이트
  + 트램폴린 8바이트), capstone로 역어셈블 재검증 완료. 결과 파일:
  `battle_voice_caption/_build_out/eboot_hook_passthrough_test.bin`.
  **아직 사용자가 실기 테스트 안 함 — 이번엔 부팅뿐 아니라 실제 전투에
  들어가서 공격/피격 몇 번 해봐야 함**(훅 걸린 함수가 전투 중 핫패스라
  부팅만으론 안 타짐). 다음 세션 시작 전에 결과부터 물어볼 것.

- **같은 세션 추가: pass-through 훅이 실전(전투 중 피격 여러 번)
  검증 통과("이상은 없었어") → 디투어+트램폴린 메커니즘 자체는 이제
  완전히 신뢰 가능.** 바로 이어서 트램폴린에 실제 동작 하나를 추가:
  state 값(r1)을 고정 스크래치 주소(`0x81175000`, segment_1의 진짜
  BSS 갭 안)에 매번 기록하도록 확장. 브레이크포인트 없이 메모리 뷰어로
  그 주소만 보면 state 실측이 가능해짐. 원본 대비 23바이트만 다름.
  결과 파일: `battle_voice_caption/_build_out/eboot_hook_stateprobe_test.bin`.
  **아직 사용자가 실기 테스트 안 함** — 다음 세션 전에 결과부터 확인할 것
  (부팅+전투 무이상 확인, 그리고 여러 상황에서 `0x81175000` 값과 실제
  상황을 대조한 표).

- **같은 세션 추가(핵심 돌파구): state 실시간 감지는 노이즈 때문에
  포기함**(전투 중 여러 캐릭터/적이 동시에 같은 훅을 트리거해서 값이
  섞임 — Monitor로 사용자와 함께 실시간 관찰했지만 행동-값 1:1 대응이
  안 됨). **대신 사용자 제안으로 atwiki 공략위키에 이미 정리된 전투 대사
  원문(일본어)을 찾음** — 8명 파티원 전원의 전투 중 대사·승리 대사
  원문 전체를 `battle_voice_caption/source_script_atwiki_raw.md`에 저장,
  `GLOSSARY_KO.md` 말투 규칙 적용해서 핵심 상황별 번역을
  `battle_voice_caption/battle_voice_translation.md`에 작성함(실제 원문
  기반, 창작 아님). **자막 "내용" 문제는 사실상 해결됨** — 남은 건 각
  번역문이 정확히 어느 `.wav` 파일에 대응하는지(파일↔대사 정밀 매칭)뿐.
- **중요, 다음 세션 시작 전 확인 필요: 실 설치본
  (`AppData\Roaming\Vita3K\...\PCSG00009\eboot.bin`)에 현재 state-probe
  테스트 빌드가 배포된 상태로 남아있음.** 정식 배포본(SHA256
  `64b8edad...`, `3_patched/loose_files/eboot.bin`에 원본 있음)으로
  되돌릴지, 계속 이 상태로 작업 이어갈지 사용자와 먼저 확인할 것.

- **같은 세션 추가: 실 설치본은 정식 빌드(SHA256 `64b8edad...`)로 이미
  되돌려놓음 — 확인 완료, 처리 끝.** 이어서 `battle_voice_translation.md`
  번역을 대폭 확장: 술기(스킬) 보이스 8명 전원(`ArtsDataPack.csv` 확정
  기술명 사용), 적 캐릭터(하스타/치토세/시안) 보이스, 2/3/4인 掛け合い
  (합동 승리 대사) 전부 번역 완료. **위키에서 가져올 수 있는 대사 번역은
  사실상 다 끝남** — 남은 건 (a) 조건별(특정 적 격파) 개인 승리 대사 중
  일부만 옮긴 것 마저 채우기, (b) 각 번역문↔`.wav` 파일 정밀 매칭,
  (c) 실제 자막을 화면에 그리는 훅 로직(라벨 큐 등록) 설계 — 이건
  아직 전혀 손 안 댐, 지금까지는 state 값을 스크래치 메모리에 쓰기만
  했을 뿐 화면 표시는 별도 작업.

- **같은 세션 추가: 화면 표시 훅(라벨 큐 소유자 찾기) 후보 4개를 정적
  분석으로 조사했으나 전부 막힘** — `FUN_8112ecfe`(카운트/소비TP 위젯,
  알고보니 기술메뉴 전용이라 상시 활성 아님), `CBattleStatusWindow`/
  `CBattleInfoWindow`(vtable에 생성자/소멸자뿐), `CBattleDamageMgr`(vtable
  동일, 생성자 호출부 0건), `CNumberFont`(생성자가 61곳에서 공유되는
  범용 기반 클래스 생성자라 고유 정보 없음). **결론: 정적 분석 수확
  체감, 다음은 동적 분석(실행 중 브레이크포인트로 `FUN_8101bd4c`/
  `FUN_81027326`가 매 프레임 어떤 객체에서 불리는지 직접 관찰) 또는
  자체 큐를 새로 만드는 우회안 중 하나로 접근 방향 전환 필요.** 상세
  내용·대안 설계는 README.md "🚧 화면 표시 훅" 섹션 참고.

**다음 세션 TODO**: 1) `battle_voice_translation.md` 번역과 `.wav` 파일
(`audio_wav/`) 대사를 대조해서 파일별 정확한 매칭 확정(사용자가 직접
들어보며 확인 권장). 2) 화면 표시 훅 — README.md의 "대안 설계 방향"
2개 중 하나로 접근(동적 분석 우선 권장). 3) 조건별 승리 대사 나머지
채우기(선택).

### 19차(2026-09-02, 다음날 새 세션): 화면 표시 훅 동적 분석 착수 → Vita3K 자체 부팅 행(hang) 문제로 막혀 조사 못 하고 종료

사용자가 "화면 표시 훅 동적 분석 방향으로 진행해줘"라고 요청. 전 세션이
남긴 `gdb_watch.py`(세션 종료 직전에 작성만 되고 한 번도 실행 안 된
상태였음 — README/이 파일 어디에도 기록 안 돼 있었음)를 발견함:
Vita3K 내장 gdbstub(포트 2159, `config.yml`의 `gdbstub: true`로 이미
켜져 있었음)에 소켓으로 직접 붙어서 `FUN_81027326`(라벨 큐 리셋)
호출 시 r0(this 포인터)를 자동 기록하는 스크립트 — 사용자가 수동으로
메모리 뷰어 볼 필요 없이 Claude가 직접 관찰 가능한 방식이라 이걸로
진행하려 함.

**그런데 실행 단계에서 막힘 — 코드/훅과 무관한 Vita3K 자체의 부팅
행(hang) 문제.** `VITA3K_NEW`로 실행할 때마다 부팅 도중 멈춤
(Windows가 `Responding: False`로 확인, 이벤트 로그에 "Vita3K.exe가
Windows와의 상호 작용을 중지" 기록, 중단 유형 "Unknown"). **이 문제는
이번 세션 개입 이전인 22:03/22:06에도 이미 있었음** — 이번 세션이
유발한 게 아니라 그날 밤부터 이미 있던 문제.

**시도했지만 전혀 효과 없었던 것들(다음에 다시 시도하지 말 것):**
- 강제 종료(`taskkill /F`) 후 재시작 — 첫 시도는 셰이더 컴파일
  `6/7`(꽤 늦은 단계, 정상은 6개 프로그램을 60ms 안에 다 끝냄인데
  40초+ 멈춤)까지 갔었으나, **강제 종료를 반복할수록 멈추는 지점이
  점점 더 앞당겨짐**(→ `libc.suprx` 모듈 로딩 직후로 고정, 3연속
  정확히 같은 로그 줄에서 멈춤). 이 패턴은 강제 종료로 정리 안 되는
  뭔가(의심: NVIDIA GPU 드라이버 쪽 프로세스별 상태)가 누적되고 있다는
  신호로 해석함.
- `cache/shaders`(132개 캐시 파일) 전체 삭제 후 재시도 — 효과 없음.
- `config.yml`의 `validation-layer: true → false` — 효과 없음(OpenGL
  디버그 검증 레이어라 오버헤드/드라이버 이슈 의심했었으나 아님).
- `config.yml`의 `discord-rich-presence: true → false` — 효과 없음
  (타이틀 로드 시점에 Discord IPC 블로킹을 의심했으나 아님). **주의:
  Discord는 사용자가 Claude Code(Orca 터미널)를 쓰는 데 쓰는 것과
  무관한 별개 앱, 끄면 안 되는 건 Orca 프로세스 쪽이었음(별개 사안,
  헷갈리지 말 것).**
- `eboot.bin`(SHA256 `64b8edad...`, 정상 원본과 일치 확인) /
  `libc.suprx`(8/11 수정 이력 없음, 스톡 파일) 둘 다 파일 무결성
  확인함 — 손상 아님.
- CPU 실사용률 10~20%대(여유 충분), 메모리 9.5GB 여유, 디스크 58GB
  여유 — 리소스 부족 아님. (주의: 처음에 `Orca` 프로세스의 **누적** CPU
  초를 현재 부하로 착각해서 잘못 의심했었음 — 순간 사용률로 다시
  확인해서 정정함. 다음에도 누적 CPU 초만 보고 "지금 바쁘다"고 단정하지
  말 것, `Get-Counter '\Processor(_Total)\% Processor Time'`처럼 순간
  값을 봐야 함.)
- `wait-for-debugger: false` 확인 — gdbstub이 디버거 연결을 기다리며
  부팅을 막고 있는 것도 아님.

**gdbstub으로 멈춰있는 상태를 직접 들여다보려던 시도는 실수로 막힘,
교훈만 남음:** 포트가 열려있는지 `Test-NetConnection`으로 먼저
찔러봤는데, **Vita3K의 gdbstub 구현이 연결을 한 번 받으면 이후
재리슨을 안 하는(1회용) 구조로 보임** — 그 핑 자체가 유일한 접속
기회를 소비해버려서, 바로 이어서 실제 RSP 클라이언트로 접속하려니
`ConnectionRefusedError`. **다음에 gdbstub에 붙을 때는 포트 확인용
사전 핑을 하지 말고 바로 실제 RSP 클라이언트로 접속할 것** — 이 프로젝트
전용 gdbstub 클라이언트는 `battle_voice_caption/gdb_watch.py`(브레이크
포인트+연속 관찰용)와 이번 세션에 급조한 `gdb_peek.py`(스크래치
디렉토리에 있었음, 브레이크포인트 없이 현재 레지스터/PC 1회 조회용,
재사용하려면 다시 작성 필요 — 스크래치 디렉토리라 세션 종료 시 사라짐)
둘 다 있음.

**결론 및 다음 세션 TODO:** 설정값을 더 바꿔가며 재시도하는 게
비효율적이라 판단, **사용자가 PC 재부팅하기로 함** — 강제 종료 반복 후
멈추는 지점이 계속 앞당겨지는 패턴이 GPU 드라이버 등 재부팅으로만
풀리는 상태 문제일 가능성이 높다고 보고 내린 결정. **재부팅 후
사용자가 알려주면 이어서:**
1. `VITA3K_NEW` 정상 부팅되는지부터 확인(재부팅으로 실제 해결됐는지).
   안 되면 이 목록의 "효과 없었던 것" 전부 다시 시도하지 말고, 다른
   원인(예: Vita3K 버전 자체 문제 — GitHub 이슈 확인, 또는 다른 렌더러
   백엔드(Vulkan)로 전환 시도 등)을 새로 검토할 것.
2. 정상 부팅되면 원래 목표로 복귀: 실제 전투 진입 후 `gdb_watch.py`로
   `FUN_81027326` 호출 시 r0(this 포인터) 관찰 → "항상 활성"인 라벨
   큐 소유 객체 찾기(README.md "🚧 화면 표시 훅" 섹션의 동적 분석
   방향). 이번 세션엔 이 실제 관찰을 한 번도 못 해봄 — 부팅 문제
   때문에 조사 자체가 시작도 못 함.
3. `config.yml`은 `validation-layer: false`, `discord-rich-presence:
   false`로 바뀐 채 남아있음 — 재부팅 후에도 이 값 그대로 써도 되고
   (문제 원인이 아니었으니 되돌릴 필요는 없지만, 원래 사용자 설정과
   다르다는 것만 참고).

### 20차(2026-09-03, 새 세션): 19차의 부팅 행(hang) **진짜 원인 확정** —
**`config.yml`의 `gdbstub: true`였음(GPU 드라이버/재부팅 문제 아니었음)**.
이어서 화면 표시 훅(라벨 큐 소유자 찾기) 동적 분석 재개, 진행 중.

**재부팅 후에도 사용자가 똑같은 증상("선택 창에서 크래시")을 보고해서
재조사.** 실제로는 크래시가 아니라 19차와 동일한 행(hang, "응답 없음")
이었고, 재부팅으로 전혀 해결 안 됐음이 확인됨 — **19차의 "GPU
드라이버 등 재부팅으로만 풀리는 상태 문제"라는 결론은 틀렸던 것으로
정정.**

**진단 과정(재현 가능한 방법):**
1. `Get-Process -Name Vita3K | Select Responding`로 행 상태 확인 →
   `vita3k.log`를 보니 매번 정확히 같은 지점(`libc.suprx` 모듈 세그먼트
   로딩 직후, 19차와 동일 지점)에서 로그가 끊김.
2. `backend-renderer`를 OpenGL→Vulkan으로 바꿔서 재시도 → **똑같이
   같은 지점에서 행** → 렌더러 백엔드 무관 확정(그래픽 파이프라인
   시작 전에 멈추는 거라 애초에 가능성 낮았음).
3. Windows 업데이트 이력(`Get-HotFix`), NVIDIA 드라이버 설치일
   (`Get-CimInstance Win32_PnPSignedDriver`), 디스크 여유공간
   (61GB), Windows Defender 상태 — 전부 최근 변경 없음/무관 확인.
4. **결정적 단서**: `$proc.Threads | Select Id,ThreadState,WaitReason`로
   보니 스레드 20개는 전부 `Wait` 상태인데 스레드 하나만 계속
   `Running` — `TotalProcessorTime`이 실측 경과시간과 정확히 1:1로
   계속 증가(22초→43초, 21.6초 동안 21.6초 증가)함을 재확인해서
   **데드락(뭔가를 기다리며 정지)이 아니라 스핀락/무한루프 형태의
   라이브락**임을 확정.
5. 이 프로젝트가 배틀 보이스 자막 조사(18~19차)를 위해 `gdbstub: true`
   /`wait-for-debugger: false`를 켜뒀던 걸 떠올리고 `gdbstub: false`로
   바꿔서 재시도 → **정상 부팅, 실제 배틀 맵/캐릭터 모델/BGM 파일
   로딩까지 정상 진행 확인.** `gdbstub: true`로 되돌려서 재현 테스트도
   해봐서(같은 지점에서 다시 행 재현) **`gdbstub: true` ↔ 부팅 행이
   1:1 인과관계임을 두 번의 온/오프 테스트로 확정.**
6. **사용자가 "혹시 배틀 보이스 작업 때 건드린 eboot 때문 아니냐"고
   의심해서 확인** — 라이브 설치본 `eboot.bin`의 SHA256을 직접
   계산해보니 `64b8edad172a6aec0bf92677134d57e162dd5119e4206e527b56185f76eb44c2`
   로, 18차 세션 끝에 이미 되돌려놓은 정식 빌드 해시와 정확히 일치
   (18차 노트가 맞았음, 재확인 성공). **eboot.bin은 원인이 아니었고,
   원인은 순전히 `config.yml`의 `gdbstub` 설정값이었음.**

**결론 — 다음에 부팅 행이 재발하면:**
- `config.yml`의 `gdbstub: false`인지부터 확인할 것(현재 `false`로
  남겨둠, 정상 작동 확인된 상태). **`gdbstub: true`는 이 환경에서
  100% 재현되는 부팅 라이브락을 유발하므로, 배틀 보이스 자막의 동적
  분석에 gdbstub이 필요해지는 시점마다 "켜는 즉시 부팅 불가"라는
  트레이드오프가 있다는 걸 항상 염두에 둘 것.** 왜 18차에서는
  gdbstub이 켜진 채로 실전 테스트가 됐었는지는 불명(그때는 문제
  없었음, 그 사이 뭐가 바뀌었는지 모름) — 재조사하지 말고 그냥
  "gdbstub 켜면 지금은 무조건 행"이라는 현재 사실만 신뢰할 것.
- 19차가 남긴 "효과 없었던 것" 목록(강제종료 반복/셰이더 캐시 삭제/
  validation-layer/discord-rich-presence 토글/디스크·CPU 확인)은
  여전히 전부 무관한 게 맞음 — 재시도하지 말 것. 다만 그 목록에
  gdbstub은 없었으니(19차는 gdbstub 자체를 의심하지 않았음), 다음에
  또 다른 원인 불명의 행이 생기면 **`gdbstub`부터 제일 먼저 의심할 것.**

**같은 세션 이어서, 화면 표시 훅(라벨 큐 소유자 찾기) 동적 분석
재개 — gdbstub이 막혀서 대체 기법으로 진행.** README.md의 "대안 설계
방향 1) 동적 분석"을 gdbstub 없이 수행하는 방법을 찾음:

- **`eboot.elf`(순수 ELF, SELF 래퍼 벗긴 것)를 처음부터 다시 만듦** —
  기존 파일이 사라져 있었음. `tools_sceutils/self2elf.py`
  (Python 2.7, `py -2`로 실행)로 재생성하려다 두 가지 버그를 만나서
  고침(둘 다 이 게임의 실제 SELF 헤더 레이아웃이 그 스크립트가
  가정한 것과 살짝 달라서 생긴 문제, 향후 세션도 다시 만들 때 이미
  고쳐진 상태로 바로 될 것):
  1. `scetypes.py`의 `SelfPlatform` enum에 `0x40`(VITA)만 있고 이
     게임의 실제 플랫폼 바이트값 `0xC0`이 없어서 예외 발생 →
     `VITA_C0 = 0xC0` 멤버 추가로 해결.
  2. `self2elf.py`가 컨트롤 인포 블록이 항상 2개(첫 번째는 범용/
     DIGEST, 두 번째가 실제 NPDRM) 있다고 가정하는데, 이 파일은
     블록이 1개뿐(첫 번째부터 바로 NPDRM_VITA 타입)이라 존재하지
     않는 두 번째 블록을 읽으려다 예외 발생 → 첫 블록이 이미
     NPDRM_VITA면 두 번째 읽기를 건너뛰도록 분기 추가로 해결.
  - 복호화된 결과: 이 게임의 모든 세그먼트가 `plaintext: YES`임을
    확인(즉 klicensee/RIF 없이도 완전히 복호화 가능 — Vita3K가 애초에
    키 없이 로드할 수 있었던 이유와 일치). 원본은
    `0_05/원본_eboot은 안건드리고/eboot.bin` 사용(패치 안 건드린
    최초 덤프본), 출력은 `tools/toir/font_research/eboot.elf`
    (`.gitignore` 대상일 수 있으니 다음 세션에 없으면 이 절차로
    재생성).
- **`disasm_range.py eboot.elf 0x81027326 ...`로 `FUN_81027326`(라벨
  큐 리셋)의 실제 진입 바이트 확인**: `push {r4,lr}` (2바이트) +
  `adds r4,r0,#0` (2바이트) = 정확히 4바이트, `b.w` 트램폴린 훅을
  걸기에 딱 맞음. `r0`이 곧 "this"(라벨 큐 객체 포인터)임을 재확인.
- **`hook_queue_owner.asm`(신규, `battle_voice_caption/asm_test/`)
  작성·배포 완료**: 18차의 검증된 "steal-bytes 트램폴린 + p_filesz/
  p_memsz 함께 확장" 기법을 그대로 재사용(스크래치 슬롯은
  18차에서 이미 안전하다고 검증된 `0x8116E3F0`, 세그먼트_0 자체
  코드 케이브 안 — `0x81175000`은 18차에서 실제 게임 힙으로 밝혀져
  위험하다고 이미 결론난 곳이니 다시 쓰지 말 것). `FUN_81027326`
  진입부를 훅해서 호출될 때마다 `r0`(this 포인터)을 `0x8116E3F0`에
  기록. `prepare_hook_space.py`(+0x100 확장) → `armips.exe
  hook_queue_owner.asm` → 라이브 설치본에 배포(백업:
  `deploy_backups/20260903_191458_queue_owner_hook_test/
  eboot.bin.canonical_backup`, 정식 해시 `64b8edad...`) → 부팅
  테스트 통과(세그먼트 크기 `0x16E4D8`로 정상 확장 로드 확인).
  **아직 실전(배틀 진입) 테스트 전 — 다음 확인할 것.**
- **gdbstub 없이 Claude가 직접 게스트 메모리를 읽는 기법 확립**:
  18차의 `watch_state.py`(ReadProcessMemory + 리터럴풀 바이트
  시그니처로 게스트 RAM 베이스 자동탐지, `host = 0x400000000 +
  guest_va`) 패턴을 그대로 재사용해 `watch_queue_owner.py`(신규,
  같은 폴더) 작성 — `0x8116E3F0`의 4바이트(포인터 전체)를 폴링.
  **메뉴 화면에서 이미 테스트해서 작동 확인**(base 자동탐지 성공,
  0x8baaf398라는 0이 아닌 값을 이미 읽음 — 즉 이 리셋 함수가 전투
  중만이 아니라 메뉴에서도 이미 불리고 있다는 뜻이므로, 전투 중
  관찰값과 메뉴 중 관찰값을 구분해서 봐야 함). **이 기법 자체가
  19차의 blocking 이슈(gdbstub 필요 → gdbstub이 부팅을 막음)를
  완전히 우회하는 대안이므로, 앞으로 이 프로젝트의 모든 동적 분석은
  gdbstub 대신 이 `ReadProcessMemory` 직접 폴링 방식을 기본으로
  쓸 것.**

**같은 세션, 바로 이어서 — 실전 테스트 완료, 라벨 큐 소유자 문제
해결됨.** 사용자가 실제 전투(~70초)에 들어가 있는 동안
`watch_queue_owner.py`를 Claude가 직접 실행해서 관찰: 전투 내내 서로
다른 this 값이 **딱 3개만** 나왔고(계속 새 값이 튀어나오지 않음), 그
중 하나(`0x8BAAF398`)는 전투 진입 **전** 메뉴 화면에서부터 이미
존재했다가 전투에도 그대로 이어짐 — 즉 3개 다 "상시 활성, 재사용되는
고정 인스턴스"임을 확인. 이어서 각 this 주소의 첫 4바이트(vtable
포인터)를 직접 읽어보니 **셋 다 동일한 vtable(`0x8116268C`)** →
같은 클래스의 인스턴스 3개. 그 vtable 안에서 RTTI 클래스 이름 문자열을
직접 읽어(Ghidra 없이 프로세스 메모리만으로) **클래스명
`CFontTextureBuffer`를 확정** — `FUN_81027326`(리셋)/`FUN_810272aa`
(항목 추가)가 바로 이 클래스 소속 함수였음. 이 과정에서 사라졌던
`eboot.elf`(순수 ELF, RE용)도 `tools_sceutils/self2elf.py`의 버그
2개를 고쳐서 재생성함(자세한 내용·재현 명령은 위 항목, 재현법은
`battle_voice_caption/README.md`에도 기록). **자세한 내용·다음 단계는
`battle_voice_caption/README.md`의 "✅ 화면 표시 훅... 20차에 해결됨"
섹션 참고 — 이 파일에는 요약만.**

**같은 세션, 3차 실측 추가**: `FUN_810272aa`(항목 추가)도 같이
후킹해서 재부팅 후 두 번째 전투(~90초)에서 관찰. 핵심 3개 인스턴스는
재부팅해도 정확히 같은 주소로 재현됨(ASLR 없음) 확인. 다만 `param1`
(r1)은 관찰된 모든 호출에서 예외 없이 0이라 인스턴스 용도 판별에
못 씀(다음에 다시 시도할 필요 없음) — 대신 이번엔 못 봤던 "일회성"
인스턴스 3개가 추가로 나타남(핵심 3개처럼 재사용 안 되고 1회만 관찰).
자세한 내용은 `battle_voice_caption/README.md`의 "20차 2회차 실측"
섹션 참고.

**같은 세션, 4차 실측 — 실제 함수 호출 주입 시도, 실패함(정식 빌드로
안전 복구 완료).** 정적 추적으로 `FUN_810272aa`(항목추가, 실제로는
텍스트가 아니라 "폭 예약"만 하는 함수였음) → `FUN_8100669a`(숫자→
문자열 포맷) → **`FUN_8100637e(sub_buffer, 문자열, x, y, extra)`**
까지 찾아서, 이게 진짜 문자열 등록 함수로 추정하고 실제로 호출하는
훅을 만들어 실기 테스트함(UTF-16LE "TEST" 문자열, x=100,y=100).
**결과: 부팅은 되지만 화면 진입 직후 접근 위반 예외 다발 + 화면
멈춤, 글자도 안 보임.** 원인 미확정(문자열 인코딩 추정이 제일 유력한
용의자 — 프로젝트의 기존 폰트 패치가 "인코딩은 유지, 글리프만 치환"
방식이었던 걸 고려하면 UTF-16이 아니라 Shift-JIS 계열일 가능성이
큼). **즉시 정식 빌드(SHA256 `64b8edad...`)로 복구 완료, 실기 확인
정상 작동, 세이브 등 실질 피해 없음.** 자세한 내용·다음에 확인할
것(재시도 전 반드시 인코딩부터 실측할 것)은
`battle_voice_caption/README.md`의 "다음 단계" 2번 항목 참고.

**같은 세션, 5차 실측 — `FUN_8100637e` 가설 폐기.** 크래시 없는 순수
관찰 훅으로 `FUN_8100637e`(실패한 주입 시도가 호출했던 함수)의 실제
호출을 다수 실측(메뉴+전투). 인코딩은 확정(1바이트, UTF-16 아님 —
유효한 성과) 됐지만, **`r1` 인자가 대부분(전투 중 폭발적으로 증가)
부동소수점 섞인 바이너리 구조체(좌표/변환행렬 추정)였고 진짜 문자열은
1건뿐** — 즉 이 함수는 텍스트 전용이 아니라 범용 "버퍼 갱신" 유틸로
결론. **`FUN_810272aa`→`FUN_8100669a`→`FUN_8100637e` 경로 전체가
막힌 길로 확정, 다음 세션에서 재추적할 필요 없음.** 라이브 설치본은
정식 빌드(SHA256 `64b8edad...`)로 이미 되돌려놓음.

**같은 세션, 6~7차 실측 — `FUN_8112ecfe` 재추적으로 진짜 텍스트
함수 확정, 실제 주입 2회 추가 시도, 둘 다 실패(둘 다 정식 빌드로
안전 복구 완료).** `FUN_8112ecfe`(검증된 실제 UI 텍스트 등록 함수)를
처음부터 재추적해서 **`FUN_810272aa(this, type, x, row=2,
content=UTF-8 문자열포인터, 0)`가 진짜 텍스트 등록 경로임을 확정**
(인코딩도 UTF-8로 재확인, 고정 주소의 실제 일본어 텍스트로 검증).
이 함수/인자/인코딩 지식은 신뢰도 높음, 재검증 불필요. 그런데 이
지식으로 실제 주입을 2번 시도했다가 둘 다 실패:
1. reset() 진입부에서 실행 전 주입 → 크래시는 없었지만 캡션도 안
   뜨고 **기존 기술 메뉴 텍스트가 사라짐**(타이밍 문제 — 주입한 게
   곧이어 실행되는 진짜 reset()에 지워짐).
2. reset()의 반환 주소를 lr 트릭으로 가로채서 완료 후 주입하도록
   재설계 → 부팅 후 필드 진입 시점(약 19초 후)에 **다른 종류의
   크래시**(`PC`가 코드 밖 주소, `Thumb: false`로 ARM 모드 전환됨 —
   명백한 제어흐름 파괴). 추정 원인: reset()이 항상 평범한 `bl`로만
   불린다고 가정했는데 실제로는 아닐 가능성(꼬리호출 등, 미검증).

**둘 다 정식 빌드(SHA256 `64b8edad...`)로 즉시 복구, 정상 부팅
확인.** 자세한 내용·다음에 시도할 대안(reset() 자체를 건드리지 않고
완전히 무관한 트리거로 갈아탈 것 권장)은
`battle_voice_caption/README.md`의 "2026-09-03, 같은 세션, 5차
실측 이후" 항목 참고.

**같은 세션, 8차 실측 — 화면에 실제 텍스트 표시 첫 성공, 그리고
마지막 남은 블로커 발견.** 조언대로 트리거를 `FUN_8104c732`(전투
보이스 디스패처, 훅 메커니즘 자체는 18차에 실전검증됨)로 바꿔서
재시도 → **크래시 없음, 기존 UI 전부 정상, 화면에 실제로 "TEST"
텍스트 표시 성공(사용자 실기 확인)** — 이 세션 최초의 실제 표시
성공. 그런데 대상 인스턴스를 고르는 방식(공유 미러에서 그때그때
읽기)이 **문제였음**: 같은 미러 값이 프레임마다 다른 인스턴스를
가리켜서, 어떤 프레임엔 안전한 인스턴스, 어떤 프레임엔 **스킬명↔
캐릭터 매핑 위젯 자체**를 가리켰고, 후자에 주입하면 크래시는 안 나도
스킬명 표시가 깨지고 깜빡거림(사용자 확인). 안전한 주소를 코드에
직접 박아 고정해봤으나, **재부팅 후 재테스트에서 그 주소가 이번
실행엔 아예 존재하지 않았음** — 인스턴스의 실제 메모리 주소가 실행
경로에 따라 달라진다는 뜻(하드코딩 불가로 확정). 최종적으로 정식
빌드(SHA256 `64b8edad...`)로 복구, 정상 확인 완료. 자세한 내용은
`battle_voice_caption/README.md`의 8차 실측 항목과 "⭐ 최우선" 표시된
다음 단계 참고.

**같은 세션, 9차 실측 — "가장 먼저 나타난 인스턴스 고정" 기법, 부작용은
없지만 화면에도 안 뜸.** 하드코딩 대신 리셋 훅에 "최초 1회만 기록"
래치를 추가해서 실행마다 자동으로 첫 인스턴스를 캡처하도록 재설계.
**결과: UI 부작용 전혀 없음(안전함 재확인)이지만 텍스트도 전혀 안
보임.** 즉 "가장 먼저 나타남" ≠ "화면에 실제로 그려짐" — 이 둘은
서로 다른 속성이고, 8차의 성공(`0x8BAAF398`)은 이 둘을 우연히 동시에
만족했던 운 좋은 케이스였던 것으로 재해석됨. 정식 빌드로 최종 복구,
정상 확인 완료. 자세한 내용은
`battle_voice_caption/README.md`의 9차 실측 항목 참고.

**같은 세션, 10차 실측(세션 마지막) — 타이밍 기반 휴리스미 완전히
소진됨, 세션 종료.** 래치를 리셋 훅에서 `FUN_8104c732`(전투 보이스
디스패처) 첫 호출 시점으로 옮겨서 재시도하는 과정에서 먼저 스크래치
주소 설계 버그(고른 주소가 하필 트램폴린 자신의 리터럴 풀과 겹쳐서
기능이 조용히 실패)를 발견·수정. 수정 후 재시도 → **텍스트는 다시
정상적으로 뜸(주입 메커니즘 자체는 재차 확인됨)이지만, 또 스킬명
UI가 깨짐** — "전투 보이스 첫 발동 시점"에도 하필 스킬 위젯이 활성
중이었던 경우. **결론: 이번 세션에 시도한 타이밍 기반 휴리스틱
3종(첫 리셋/전투 보이스 시점/하드코딩) 전부 신뢰 불가로 확정.**
사용자가 세션 종료 요청, 정식 빌드(SHA256 `64b8edad...`)로 최종
복구·확인 완료.

**다음 세션 TODO**: 1) **⭐ 최우선, 유일하게 남은 진짜 블로커**:
"안전하면서(다른 위젯 용도로 안 쓰임) 동시에 화면에 실제로 그려지는"
인스턴스를 **타이밍이 아니라 구조적으로**(또는 우리 소유의 새 인스턴스
생성으로) 식별하는 방법 찾기 — **타이밍 기반 방법은 이제 재시도하지
말 것, 이번 세션에 확정적으로 소진됨.** 두 가지 방향: (a) 인스턴스
구조체에서 "화면에 그려지는 중"임을 알려주는 필드 찾기, (b) 기존
인스턴스를 빌리는 대신 우리 소유의 새 `CFontTextureBuffer`를 직접
생성(생성자 주소 이미 확보: `FUN_8102720E`/`FUN_81027268`)하고
draw-list에 직접 연결하는 더 근본적인 경로(`FUN_8101a972`가 쓰는
"리스트 매니저" 객체 식별 필요, 전역 싱글톤 포인터
`0x81174294`까지는 이미 찾음). 함수 호출/인자/인코딩(UTF-8,
`FUN_810272aa` 시그니처 등)은 전부 이미 확정됐으니 재검증 없이 바로
이 문제부터 시작할 것. **새 스크래치 주소를 고를 때는 반드시 어셈블
후 그 주소의 정적 파일 바이트가 진짜 0인지 직접 확인할 것**(이번
세션에 실제로 겪은 실수). 2) 핵심 인스턴스 각각의 용도 특정은 여전히
미해결. 3) `.wav`↔번역문 매칭은 여전히 미착수(18차부터 남아있는
항목).

### 21차 (2026-09-05, 새 세션): 위 1번 항목 정적분석으로 완료됐으나
"기존 위젯 빌리기" 방향 전체가 막힌 길로 확정됨 — 진짜 블로커를
"그리기 호출 자체를 못 찾음"으로 재정의

**새 기법 확립(재사용 가치 있음)**: `hook_caller_id.asm`(reset() 호출
시 `this`뿐 아니라 `lr`도 128슬롯 링버퍼에 기록하는 순수 관찰 훅,
화면에 아무것도 안 그리고 UI도 안 건드림) + `watch_caller_id.py`로,
타이밍에 의존하지 않고 **정적분석만으로 각 인스턴스의 소유 함수를
전부 특정**함:
- `0x8BE30B70`(20차의 "나쁜" 스킬 위젯) → `FUN_8102ea4e`, 전역
  `DAT_811742dc`로 항상 직접 접근 가능함도 확인. 그런데 **사용자가
  이 위젯을 빌려 타협하는 방안을 명시적으로 거부함("타협하지마",
  "중지해") — 다시 제안하지 말 것.**
- `0x8BDCBA20`(용도 불명이었음) → `FUN_81073b06`, **캐릭터별(전역
  아님) 객체**라 접근자 호출을 거쳐야 함, 재사용 복잡.
- `0x8BAA6874`(일회성) → `FUN_81090b7a`, 배틀 씬 구성 시점에만 쓰이는
  1회성 상태 메시지 위젯으로 확인.
- `0x8BAAF398`(20차의 "안전+가시" 성공 사례) → `FUN_810c1a74`, 호출부
  단 1곳(`FUN_8107e17e`). **`FUN_8107e17e`는 배틀 씬이 아니라 필드
  씬의 생성자였음이 밝혀짐**(본문에 `"FieldBase"` 힙 이름 문자열
  확인 — 20차/21차 초반에 "배틀 씬"이라 부른 건 오판이었음, 정정).

**"현재 씬 객체" 전역 `DAT_811797d0` 발견 + 공식 폐기**: 씬 전환 처리부
(`FUN_810289f8`)가 이 전역이 가리키는 객체의 가상 소멸자를 호출하는
패턴에서 발견. `*(DAT_811797d0)+0xFB38`을 계산해 `0x8BAAF398`과 정확히
일치(vtable까지 일치)함을 라이브로 검증하고 이걸로 실제 주입 훅을
만들어 실전 테스트했으나, **전투 중에 재현 실패**(전투 시작 전후로
`DAT_811797d0` 값 자체가 안 바뀜 — 즉 배틀은 필드 씬을 교체하는 게
아니라 그 위에 오버레이로 얹히는 구조로 보임, 실제 배틀 로직
`FUN_8104c1e4`/`FUN_8104c732`도 `DAT_811797d0+0x108`/`+0x9c` 필드를
직접 참조함 — 그런데도 우리 위젯의 vtable은 전투 내내 계속 무효).
**이 공식은 재현성 없음으로 폐기, 다음에 재시도하지 말 것.**

**결정적 반증 — "기존 위젯 빌리기" 전체를 포기하고 우리 소유 인스턴스로
전환**: `hook_own_instance.asm`으로 스크래치 메모리(`0x8116EA20`)에
아무 것도 빌리지 않은 우리만의 `CFontTextureBuffer`를 직접
생성(`FUN_8102720e`+`FUN_81027268`)해서 테스트. **라이브 메모리 검증
결과 구성은 100% 완벽**(vtable=`0x8116268C`, 서브버퍼 2개 모두 유효한
힙 포인터, dirty flag=1 — add-item이 실제로 실행됨을 증명)했지만
**그래도 화면에 전혀 안 뜸.** 이건 "제대로 구성되고 add-item까지
정상 실행된 CFontTextureBuffer도 그 자체만으로는 절대 화면에 안 뜬다"는
결정적 증거 — 20차 8차 실측의 성공("TEST" 실제로 뜸)은 인스턴스 자체의
속성이 아니라 그 프레임에 소유 위젯의 자체 draw 호출이 우연히 실행되고
있었기 때문일 가능성이 높음.

**렌더 체인 전체를 처음부터 끝까지 추적함**: `add-item()`
(`FUN_810272aa`) → `FUN_8100669a`(UTF-8 디코드) → `FUN_8100637e`(진짜
줄바꿈/공백 처리 루프, `FUN_81005a4a`로 글리프 메트릭 조회 +
**`FUN_81144848`로 실제 글리프를 텍스처에 블릿**) — **텍스트 픽셀이
서브버퍼의 GPU 텍스처에 진짜로 그려지는 것까지는 확정 확인**. 서브버퍼
클래스 자체의 vtable(`0x81161ca4`)도 확인했으나 소멸자 2개뿐, Draw
가상함수 없음(`CFontTextureBuffer`와 동일 패턴 재확인). 서브버퍼
할당자 `FUN_8101d52e`는 그냥 범용 힙 할당(55곳에서 호출) — "고정
풀이라 자동으로 그려진다"는 가설은 폐기. 유력한 저수준 그리기
원시함수 `FUN_8102ae8c`(레이아웃 설정, 75곳 호출)+`FUN_8102afe2`(실제
그리기, 64곳 호출, `FUN_8101b5d2`라는 진짜 GPU 쿼드 그리기 호출로
연결됨)를 찾았으나, **우리 서브버퍼의 텍스처가 어떤 구체적인 함수를
통해 이 그리기 서술자에 담기는지는 아직 못 찾음.**

**다음 세션은 반드시 동적분석부터 시작할 것** — 정적분석은 여기서
수확 체감 구간에 도달함. 구체적 제안: `FUN_8101b5d2`(또는
`FUN_81144848`) 호출 시 텍스처 핸들 인자를 매번 기록하는 순수 관찰
훅을 걸고, 우리 소유 인스턴스(`hook_own_instance.asm`으로 이미 생성
가능)의 서브버퍼 텍스처 핸들과 대조해서 실제로 그려지는지 확인. 전체
경과와 재사용 가능한 도구(`hook_caller_id.asm`/`watch_caller_id.py`/
`hook_own_instance.asm`/`watch_inject_diag.py`, 전부 `asm_test/`에
있음)는 `battle_voice_caption/README.md`의 "21차" 섹션 전체 참고.
라이브 설치본은 세션 종료 시 정식 빌드(SHA256 `64b8edad...`)로 복구
완료.

### 22차 (2026-09-06, 새 세션): 21차가 간접 증거로만 냈던 결론("구성+
add-item만으로는 화면에 안 그려진다")을 완전히 독립적인 방법으로
직접 반증까지 확보 — "정방향 관찰"은 이걸로 소진, 다음엔 역방향 추적

`FUN_8101b5d2`(실제 그리기 호출) 진입부에 순수 읽기 전용 훅을 추가해서
매 호출의 텍스처 핸들 인자를 우리 소유 인스턴스(`hook_own_instance.asm`,
21차, 이미 안전성 검증됨)의 서브버퍼 핸들과 직접 대조하도록 만듦
(`asm_test/hook_diag_texture.asm`). 배포 전 Python `capstone`으로
역어셈블 검증(원본 대비 정확히 181바이트 diff, 전부 예상 범위 내)까지
거친 뒤 실전 배포. **사용자가 실제 전투에서 약 1분간 여러 번 공격 —
`FUN_8101b5d2`는 1,411회 호출되며 그때그때 다른 실제 텍스처 핸들을
계속 그렸고, 우리 인스턴스도 실제로 구성돼 유효한 핸들을 가졌지만
(add-item도 실행됨, 21차와 동일), 그 이후로도 계속된 호출 중
`match_count`는 단 한 번도 0에서 안 벗어남** — 즉 우리 텍스처가
`FUN_8101b5d2`에 인자로 전달된 적이 한 번도 없음을 **직접 관찰로
확정**함(21차는 "화면에 안 보였다"는 간접 증거뿐이었음).

**다음 세션 최우선**: 정방향(우리 인스턴스가 그리기 호출에 등장하는지
관찰)은 이제 완전히 소진됨. README 21차 섹션의 "다음 단계" 2번 — 이미
매 프레임 확실히 그려지는 기존 위젯(예: `FUN_8112ecfe`의 카운트/TP
라벨)의 소유 객체를 찾아서, "이 위젯의 draw는 무엇이 호출하는가"를
콜그래프로 역추적하는 방향 — 으로 반드시 전환해서 시작할 것. 자세한
내용(정확한 함수 시그니처, 훅 설계, 재사용 도구 목록)은
`battle_voice_caption/README.md`의 "22차" 섹션 전체 참고. 라이브
설치본은 세션 종료 시 정식 빌드(SHA256 `64b8edad...`)로 복구 완료.

### 22차 2라운드 (같은 세션, 이어서): 사용자 제안으로 "그리기 함수 직접
호출" 시도 → 실전 크래시 (호스트 레벨) → 원인 규명 → 다음 세션 필독

사용자 제안("다른 테일즈 게임처럼 새 자막 영역을 직접 만들 수 있다")에
따라, 위 22차 1라운드에서 찾은 진짜 범용 그리기 함수(`FUN_8101ada8`,
`FUN_8101b5d2`의 형제 함수, 86개 호출부)를 **우리가 직접 호출**해서
우리 텍스처를 매 프레임 제출하도록 시도. 정적분석으로 슬롯 매니저
프로토콜을 상당히 파악했고(카운터 자동증가 확인, 재진입 방지 설계까지
마침), capstone 역어셈블 검증도 통과해서 배포했으나, **실전 전투
진입 직후 Vita3K가 크래시**함(`ucrtbase.dll`의
`STATUS_STACK_BUFFER_OVERRUN` — 기존에 알려진 무관한 `0xC0000005`
버그와는 다른, 더 심각한 종류). **즉시 정식 빌드로 복구, 사용자
설치본 피해 없음.**

**사후분석으로 유력한 원인을 찾음**: 슬롯 배열의 정리 함수
(`FUN_8101a624`)를 분석한 결과, **슬롯 배열은 고정 크기이고 그 크기가
`*(int*)(배열베이스-4)`에 저장되는 표준 "array new[]" 패턴**임을
발견 — 우리 훅은 이 크기를 전혀 확인하지 않고 카운터를 그대로 믿고
인덱싱했음. `FUN_8101ada8`가 호출될 때마다(전투 중 프레임당 여러 번)
매번 추가 그리기를 얹었으므로, 배열 용량을 넘겨 인접 메모리를
손상시켰을 가능성이 매우 높음(100% 확정은 아니지만 가장 유력).

**⭐ 다음 세션 필독, 절대 반복하지 말 것**: `FUN_8101ada8`(또는 형제
함수)를 직접 호출하는 접근을 다시 시도하기 전에 **반드시 먼저**
(1) `*(int*)(*(int*)(*(int*)0x81174294+0x1c)-4)`로 배열의 실제 크기를
읽기 전용 훅으로 확인하고, (2) 슬롯 카운터(`매니저+0x48`)가 매 프레임
0으로 리셋되는 지점을 찾아 "프레임당 실제 여유 슬롯 수"를 계산할 것.
이 두 가지 확인 없이 다시 호출부터 거는 게 바로 이번 세션이 저지른
실수임. 자세한 내용, 재사용 가능한 스크립트 목록, 확정된 배열 관련
지식(다른 배열형 필드들의 원소 크기 등)은
`battle_voice_caption/README.md`의 "22차 2라운드" 섹션 전체 참고.

### 22차 3라운드 (같은 세션, 이어서): 읽기 전용 진단으로 실측 →
크래시 원인 가설을 "용량 초과"에서 "배열 재할당 전환 구간"으로 수정

읽기 전용 훅(`hook_slot_bounds_diag.asm`, 그리기 호출 없음, 완전
안전)으로 실전 전투에서 배열 크기+카운터를 실측함. **핵심 발견**: (1)
배열 크기가 부팅~전투 진입 초반에 128→512→1024→256으로 여러 번
재할당되다가 전투 안정 후 256으로 고정됨 — 크래시가 났던 "전투 진입
직후" 타이밍과 정확히 겹침. (2) 안정 상태의 실제 카운터 최댓값은
74/256(30% 미만) — 2라운드의 "용량 초과" 가설은 근거가 약해짐.
**수정된 결론: 크래시는 용량 초과가 아니라, 배열이 해제·재할당되는
짧은 전환 구간에 계속 그리기를 주입하다가 막 해제됐거나 아직 초기화 안
된 메모리를 건드렸을 가능성이 더 유력함.**

**⭐ 다음 시도 시 반드시 적용할 안전장치**: 배열 크기 또는 베이스
포인터가 직전 관측과 달라졌으면(=재할당 발생/진행 중) 그 프레임은 주입을
건너뛸 것. 그리고 여전히 "프레임당 최대 1회만 주입"은 미해결 과제임
(카운터가 예상보다 훨씬 자주 오르내려서 진짜 프레임 경계를 아직도 특정
못함). 자세한 내용은 `battle_voice_caption/README.md`의 "22차 3라운드"
섹션 참고. 이번 라운드는 순수 관찰이라 크래시 없음, 라이브 설치본은
정식 빌드로 정상 복구됨.

### 22차 4~5라운드 (같은 세션, 이어서): 안전장치를 추가해도 크래시가
그대로 재현됨 → 진짜 원인을 찾아냄 (⭐ 세션 최대 성과)

3라운드의 실측을 반영해 안전장치 4개(재할당 감지, 안정화 대기, 빈도
제한, 용량 체크)를 추가한 버전으로 재시도했으나 **똑같은 크래시가 똑같은
위치에서 재현됨** — "배열 재할당 타이밍" 가설이 틀렸다는 강한 신호.

**변수를 하나로 좁혀서 재검증**: 2~4라운드가 공통으로 썼던 "우리가 방금
패치한 함수(`FUN_8101ada8`)의 진입점을, 무한재귀를 피하려고 원본
프롤로그를 직접 복제해 수동으로 우회 점프하는" 기법 자체를 완전히
제거하고, 대신 **`ada8`는 전혀 후킹하지 않고 완전히 다른(패치 안 된)
위치에서 평범한 `bl 0x8101ada8`로 딱 한 번** 호출하도록 바꿔서 재시도.
**결과: 크래시 없음.** 슬롯 계산/인자 구성 등 나머지 로직은 전부
동일했는데 이 부분만 바꿨더니 안전해짐.

**⭐⭐ 확정된 근본 원인**: 크래시는 배열 타이밍도 용량도 아니라, **"방금
패치한 함수 자신의 진입점을 우리 코드가 다시 호출해야 하는 상황(재귀
방지용 수동 우회 점프)" 그 자체가 이 Vita3K 환경에서 안전하지 않았기
때문**이었음. ARM 게스트 코드 레벨에서는 완벽히 동일한 상태를 만드는
코드였지만 Vita3K의 CPU 에뮬레이션 내부(호출/복귀 추적 등)와 충돌했을
가능성이 높음.

**다음 세션 규칙**: 자막을 지속 표시하려면(매 프레임 그리기 필요)
`ada8` 자체를 후킹해서 피기백하지 말고, **`ada8`와 무관한, 진짜 매
프레임(또는 최소 매우 자주) 실행되는 다른 함수를 찾아 그 안에서 평범한
`bl`로 `ada8`을 호출**하는 방식으로 설계할 것 — 유력 후보:
`FUN_8101a972`(모든 그리기 계열 함수가 공통으로 거치는 최종 제출
함수, ada8 자신이 후킹 대상이 아니므로 안전) 후킹 후 거기서 `ada8`을
평범히 호출. 자세한 내용, 검증된 베이스 코드(`asm_test/hook_simple_flash.asm`)는
`battle_voice_caption/README.md`의 "22차 4~5라운드" 섹션 참고.

### 22차 6라운드 (같은 세션, 세션 마지막): 매 프레임 트리거는 성공했으나
새 부작용(기존 UI 깜빡임) 발견 — 빈도 문제 아님, 다음 세션 시작점

위 계획대로 `FUN_8101a972`를 후킹해서 매 프레임 `ada8`을 호출하도록
구현 — **크래시는 안 남**(5라운드의 "안전한 패턴" 결론 재확인). 하지만
**기존 전투 UI가 우리 자막과 함께 깜빡이는 부작용**이 실전에서
확인됨. 호출 빈도를 8분의 1 → 60분의 1로 7.5배 낮췄으나 **깜빡임이
그대로 재현됨 — 빈도(부하) 문제가 아님이 확인됨.**

**⭐ 다음 세션은 여기서 시작할 것**: 슬롯 구조체(0x60바이트)를 `ada8`이
초기화하는 필드 외에 **전부 필드별로 매핑**할 것 — 유력 가설은 같은
슬롯 메모리가 라운드로빈으로 재사용되면서, 직전에 다른 종류의 항목
(예: 9-슬라이스 박스, `FUN_8102ae8c`가 훨씬 많은 필드를 씀)이 남긴
쓰레기 값을 `ada8`이 안 건드리는 필드에 그대로 남겨서, 최종 렌더링
코드가 이를 잘못 해석해 인접 UI를 망가뜨렸을 가능성. **빈도를 더
낮추는 식의 재시도는 이미 효과 없음이 확인됐으니 반복하지 말 것.**
자세한 내용, 재사용 가능한 베이스 코드(`asm_test/hook_frame_persistent.asm`,
크래시 없음 확인됨)는 `battle_voice_caption/README.md`의 "22차
6라운드" 섹션 참고. 라이브 설치본은 정식 빌드로 최종 복구, 재확인
완료.

### 23차 (2026-09-09, 새 세션): 공유 슬롯 배열을 아예 안 쓰는 전용 객체
설계로 전환 — 18차부터 못 찾던 "진짜 그리기 함수"까지 이번에 확정함

사용자 지시로 "기존 요소에 영향 없는 새 영역"을 찾는 방향으로 전환.
조사 결과 큰 진전: `FUN_8101ada8`/`FUN_8101b5d2`는 그리지 않고 슬롯에
정점/UV만 채운 뒤 조건부로 `FUN_8101a972`(리스트 삽입)를 부르는
셋업 함수일 뿐이었고, **진짜 그리기는 `FUN_8101a8be`**(매니저의 우선순위
리스트를 순회하며 각 노드를 **가상함수**(vtable+0xc)로 호출)였음이 이번에
처음 확정됨. 슬롯은 vtable을 가진 폴리모픽 객체(vtable=`0x81162020`)였고,
그 reset()(`FUN_8101ac64`)이 정확히 22차가 찾던 "ada8이 안 건드리는
필드"(RGBA 색상, 블렌드 모드)에 안전한 기본값을 채우는 함수임을 확인 —
**22차 6라운드 깜빡임 버그의 진짜 원인**이 "공유 슬롯을 reset() 없이
재사용해서 이전 점유자의 색상/모드 쓰레기 값이 남는 것"이었음이 확정됨.
`FUN_8101a972`도 전체 디컴파일해서 노드가 공유 배열 안에 있어야 한다는
가정이 코드 어디에도 없음을 확인.

이를 근거로 **공유 배열을 한 바이트도 안 건드리는 전용 0x60바이트 객체**
(`SLOT_OBJECT @ 0x8116EA58`)를 스크래치 메모리에 만들어 vtable+reset값을
한 번만 채우고, 매 트리거마다 진짜 `FUN_8101ada8`을 이 객체 주소로
호출(내부에서 진짜 `FUN_8101a972`가 실제 리스트에 연결)하는 새 설계로
전환. 트리거는 22차 5~6라운드가 이미 검증한 안전 패턴(`FUN_8101a972`
진입부 훅+재진입가드+평범한 `bl`) 그대로 재사용. 새 훅
`asm_test/hook_isolated_caption.asm` 작성, capstone으로 전체 명령어와
리터럴 풀 값 재확인 완료, 원본 대비 242바이트 diff 전부 예상 범위 내.
**라이브 설치본에 배포 완료** (백업:
`deploy_backups/20260909_191940_isolated_caption_v1/`). **사용자의 실전
전투 테스트 결과 대기 중** — 다음 세션(또는 이 세션 후반)은 반드시
사용자 테스트 결과부터 확인할 것. 자세한 내용은
`battle_voice_caption/README.md`의 "23차" 섹션 전체 참고.

**23차 실전 테스트 결과 (같은 세션, 이어서): ⭐⭐ 배포한 설계가 부팅
자체를 막음 → 6단계 이분탐색 끝에 "어떤 함수를 후킹하든 상관없이
크래시"로 귀결됨.** `FUN_8101a972`/`FUN_8101ada8`/`FUN_8104c1e4`
셋 다 로직 전혀 없는 순수 통과형 훅(원본 명령어 재실행+복귀만 함)만
걸어도 실전에서 크래시 재현됨 — 즉 이번에 새로 쓴 로직의 버그가
아니라 **"이 함수들의 진입부를 건드리는 것 자체"**가 문제였음. 22차가
"안전하다"고 기록했던 `FUN_8101a972` 후킹도 이번 세션에 수정 없이
재배포해서 재확인한 결과 **똑같이 부팅을 막음** — 22차의 성공 판정이
콜드 부팅으로는 한 번도 검증되지 않았을 가능성이 높음. `VITA3K_NEW/vita3k.log`
(trace 레벨 로그, 매 실행마다 자동 기록됨 — 스크린샷 없이 직접
읽기/grep 가능, 재사용 가치 높음)를 분석한 결과, 크래시는 전투시작
보이스 재생 직후 `param_1=8`(명백히 잘못된 포인터)로 `FUN_8104c1e4`가
호출되면서 시작되고, 최종적으로 `segment_1`의 진짜 셰이더 바이너리
데이터(`GXP` 매직, 스크래치 공간이 아니었음 — 18~22차의 "62KB 여유
BSS" 가정은 앞부분에 한해 틀렸음, 다행히 실제로 쓰기를 한 적은 없어
손상은 없음)를 코드로 오인해 실행하다가 널로 분기해 죽는 것으로
확인됨. **잠정 결론(미확정): 원본 게임에 이미 있던 레이스 컨디션이,
우리 패치의 코드 레이아웃 변경만으로 Dynarmic JIT 타이밍이 달라지며
노출되는 것으로 추정** — 순정 빌드는 전투까지 정상 동작 재확인됨(사용자
직접 테스트, 매번 완전 종료 후 재부팅 확인함). gdbstub 재시도도 20차와
동일하게 그 자체가 부팅을 막아 사용 불가로 재확정. **다음 세션은 이
세 함수를 다시 후킹 대상으로 고르지 말고, 새 후보를 순수 통과형으로
먼저 저비용 검증할 것.** `FUN_8104c732`만 유일하게 안전한 것으로 남음.
자세한 내용, 재사용 가능한 이분탐색 스크립트/로그 분석 방법은
`battle_voice_caption/README.md`의 "23차 실전 테스트" 섹션 전체 참고.
라이브 설치본은 매 실패 직후 정식 빌드(SHA256 `64b8edad...`)로 복구,
세션 종료 시점 최종 확인 완료. `VITA3K_NEW/config.yml`의 `gdbstub`도
`false`로 원상복구 확인됨.

---

## 2026-09-15 (24차): SKIT `line`+`line_addition` 누적 줄 수 초과 17건 발견·수정 + MovieCaption 배포 — 번역-QA 스레드로 복귀

사용자가 `line/line_8.PNG`(4212.dat, 큐큐가 루카에 대해 말하는 스킷)에서
4번째 줄이 화면 밖으로 잘리는 걸 발견해 확인 요청. 18~23차의 배틀 보이스
자막 RE 스레드와는 별개로, 08-20~23차의 "대사창 넘침" 번역-QA 스레드로
복귀한 세션.

**근본 원인(신규 발견, 08-22/23차 수정의 사각지대)**: `scan_true_groups.py`가
복수-addition 그룹(하나의 `line`에 `line_addition`이 2개 이상 붙는
경우, 31건 중)에서 각 addition을 `line`과 **1:1 쌍으로만** 검사했음
(`L+A₁>3`, `L+A₂>3` 각각 확인). 하지만 실제 화면은 `line`+모든
addition이 **누적** 표시되므로 진짜 제약은 `L+A₁+A₂+...>3`이어야
했음 — 이 사각지대로 전체 31개 복수-addition 그룹을 재스캔하니 4212.dat
포함 **17건**이 걸림(`1003, 1359, 3000, 4050, 4052, 4075, 4095, 4102,
4105, 4106, 4212, 4234, 4250, 6022, 6042, 6045.dat`).

**해결 과정(여러 번 모델을 갈아엎음, 다음에 비슷한 걸 만나면 이 순서를
반복하지 말고 최종 결론부터 볼 것)**:
1. 1차 시도(v3, `fix_skit_group_overflow_v3_cumulative.py`): 각 필드가
   항상 자기만의 줄에서 시작/끝난다고 가정하고 필드별 독립 최소줄을
   합산 — 17건 중 11건은 풀렸지만 6건(4212 포함)은 "필드별 최소줄 합이
   이미 3 초과"로 자동 수정 불가 판정(그중 2건은 4-필드 그룹이라
   수학적으로도 불가능해 보였음).
2. 2차 시도(v4, `fix_skit_group_overflow_v4_flatgroup.py`): 필드
   경계에서도 화면 줄을 공유할 수 있다고 가정하고 그룹 전체를 하나의
   흐르는 텍스트로 재줄바꿈 — 17건 전부 수학적으로는 풀렸으나, **한
   번도 실기 검증된 적 없는 동작**이라 사용자가 제동을 걺.
3. **사용자가 결정적 단서 제공**: "예전에 앙쥬의 대사 오류에서 발견한
   해결방식이 있을 것, `line_2_after` 참조해서 이력 발굴해보라"는
   지시로 `line/line_2.PNG`(넘침)·`line_2_after.PNG`(수정본)를 재조사 →
   `4053.dat`(2026-08-22/23차에 이미 실기 검증·배포된 "경계-넘김" 사례)
   였음이 밝혀짐. 실제 데이터 확인 결과: `line`은 여전히 자기 줄에서
   끝나고(`line,41`="...훨씬 상대하기\n"), 넘친 단어("편할지도……")만
   `line_addition`(44)으로 **재배분**된 것 — 화면 줄을 공유시킨 게
   아니었음(v4의 가정이 틀렸었고, 최초의 v3식 "필드는 항상 자기 줄"
   가정이 옳았음, 다만 v3는 경계 이동을 아예 안 썼다는 게 부족했던 것).
4. 최종(v5, `fix_skit_group_overflow_v5_wordshift.py`): 2026-08-22/23차의
   `solve_pair_cross`(단일-addition 그룹용, 이미 실기 검증됨)와 동일한
   원리 — `line`↔addition, addition↔addition 경계에서 **단어만**
   재배분(화면 줄 공유 없음) — 를 다중-addition 그룹으로 일반화. 추가로
   두 가지 안전장치를 사용자 피드백으로 넣음: (a) `line` 필드가 완전히
   비워지지 않게 제약(비면 대화창이 열리는 순간 빈칸이 보일 위험 —
   4053.dat 선례는 `line`을 비운 적이 없었음), (b) 재배분 결과 인물
   이름(예: "루카")이 다른 필드로 넘어가 `check skit`의 용어집 검사
   (JP에 이름이 있으면 같은 행 KR에도 있어야 함)에 거짓 양성으로
   걸리는 걸 피하도록 2차 목표(이동 최소화보다 우선)로 추가.
   **17건 전부 자동 해결, 0건 보류.** 그룹 단위 토큰 비교로 문구 100%
   보존 재검증, `line` 필드 빈 문자열 0건, `check skit` 재실행 결과
   기존부터 있던 "리카르돈" 1건 외 새 이슈 0건, 전체 466개
   line/addition 그룹 재스캔 결과 초과 0건.

**교훈(중요)**: 이 프로젝트에서 "화면에 보이는 줄 수"를 다루는 버그를
새로 만날 때, 필드 경계가 항상 줄 경계인지 먼저 실사례로 확인할 것 —
`line` 필드는 (`PATCH_적용법.md`의 2026-08-22 업데이트(3차)에 이미
문서화되어 있듯) **항상 자기 뒤에 개행을 갖고 끝난다**는 게 이
게임/이 프로젝트 번역 파이프라인의 실제 규칙이고, "화면 줄 공유"
가설(v4)은 근거 없이 만든 것이었음 — 다음에 비슷한 유혹이 들면 먼저
기존 실기-검증 이력(`line_2_after.PNG`/`4053.dat` 같은)부터 뒤질 것.

**배포 완료(같은 세션, 사용자가 "moviecaption도 같이 컴파일해서
배포해줘"라고 요청)**: 위 17건(Skit 16개 파일) + 사용자가
`moviecaption_gui.py`로 직접 수정한 MovieCaption 자막(라이브 설치
대비 diff로 실제 변경분만 특정, 11개: `007, 008, 009, 010, 011, 012,
013, 014, 016, 017, 026.srt`)을 `toir.recompile_story`(check_integrity)/
`recompile_skits`/`recompile_map_data`/`insert_srt`로 한 번에
재컴파일(라이브 설치를 l7cdir 소스로 사용) → 라이브 설치 diff로 변경분
특정(Script/MapData 0건, Skit 16개, Movie 11개) → 배포 전 27개 파일
백업(`deploy_backups/20260915_204010_skit_group_overflow_v5_plus_moviecaption/`)
→ 라이브 설치 + `3_patched/loose_files` 양쪽 배포, 해시 3방향(스테이징/
라이브/patched) 전수 일치 확인 → Skit 16개 `SkitDeCompiler` 라운드트립
디코드 에러 0건. xdelta 배포판도 재생성: `xdelta3` 바이너리가 이번에도
환경에 없어서 GitHub `jmacd/xdelta` v3.2.0 릴리스에서 재다운로드(스크래치,
프로젝트에 커밋 안 함 — 다음 세션도 필요하면 다시 받을 것) →
`0_08/원본_eboot은 안건드리고/toidata_release.l7c`(순정, SHA256
`b93681dda8...` 재확인)에 `3_patched/loose_files`(1776개 파일, 루트의
`toidata_release.l7c`/`eboot.bin` 자기 사본 2개는 제외 — 기존에 알려진
필터링 필요 케이스) 패치 → 새 l7c(1,783,701,058바이트) → 4개 카테고리
1,699개 파일 재추출해서 `loose_files`와 전수 일치 확인 → `xdelta3 -e -9
-S -s`로 diff(91,265,462바이트) → 원본+diff 왕복 검증(새 l7c와 SHA256
완전 일치) 통과. `3_patched/toidata_release_ko.xdelta`,
`3_patched/xdelta_release/toidata_release_ko.xdelta`,
`3_patched/ready_to_use/toidata_release.l7c`,
`3_patched/loose_files/toidata_release.l7c` 전부 갱신, 기존 파일은
`deploy_backups/20260915_204010_.../xdelta_pre/`에 백업.
`PATCH_적용법.md`(루트 + `3_patched` 양쪽) 새 해시로 갱신 완료.
`eboot_ko.xdelta`/`eboot.bin`은 이번 세션에서 안 건드렸으므로 그대로 유지.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(순정 원본) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-09-15, 최종) | 1,783,701,058 | `20e7a63170abe24f4d3b04fcabf8d62784117a0ebe3d440e2d69fb67d6a01fdd` |
| `toidata_release_ko.xdelta` | - | 91,265,462 | `91e0b8d123bbdeaac4cadcfe10e36d743d8a763d6aed83c1924abaa086fc5775` |

**실기(Vita3K) 부팅 테스트는 이번엔 생략함**(텍스트 치환만 하는 기존
검증된 경로, 2026-08-11/17차와 동일한 판단) — 다음 세션에서 사용자가
실제 플레이 중 4212.dat(나와 큐큐 3, 큐큐 시점) 등이 3줄로 잘 나오는지
확인해주면 좋음. `git status` 기준 `2_translated/Field/Scene/*.tga`
6개와 `GLOSSARY_KO.md`/`tools/toir/toir/text.py` 등 문서/코드 파일도
커밋 안 된 변경이 있었으나 이번 세션 작업 범위 밖이라 손대지 않음 —
다음 세션에서 필요하면 확인할 것.

**재사용 가능한 신규 도구**: `tools/toir/font_research/hangul_fix/rewrap/fix_skit_group_overflow_v5_wordshift.py`
(다중-addition 그룹의 누적 줄 수 초과를 문구 변경 없이 해결하는 최종
버전 — `line` 비움 방지 + 용어집 이름 이동 회피 로직 포함, v3/v4는
과정 기록용으로 남겨둠, 재사용 시 v5만 쓸 것).

---

## 2026-09-20: 같은 대화, 날짜가 바뀐 뒤 이어서 — 배틀 보이스 자막
스레드(18~23차)로 복귀, 7연속 실기 크래시로 "안전한 크기"가 사실상
없다는 결론 — 자세한 내용은 `battle_voice_caption/README.md`의 "24차"
섹션 참고(그 파일 자체 번호로는 24차, 위 24차 번역-QA 작업과는 별개
스레드).

핵심만 요약: 23차가 제안했던 "1회 삽입만으로 매 프레임 계속 그려지는가"
테스트를 실기로 확인 → 크래시. 로직을 완전히 제거하고 크기만 같게
맞춘 버전으로 재확인해도 **바이트 단위로 완전히 동일한 크래시**가
재현되어, 문제가 우리 로직 내용과 무관하고 코드 캐비티 크기/레이아웃
자체에 있다는 게 명확해짐. 21~22차가 "안전하다"고 기록해둔 87바이트
기준선에 NOP 1개(2바이트)만 더해도 크래시 나는 것까지 이분탐색으로
확인(94/96/100/104/115/132/167~168바이트 전부 크래시) — **여유 공간이
사실상 0**. 다음 세션이 이 스레드를 재개하면 가장 먼저 87바이트
기준선 자체를 오늘 방식(완전 재부팅+실전투)으로 재검증할 것(이번
세션엔 재검증 안 하고 21~22차 기록만 믿었음 — 23차가 22차의 "안전
확인됨"이 콜드부팅 미검증이었다고 밝혀낸 것과 같은 함정일 수 있음).
안 걸리면 정말 하드 임계값이 87바이트 근방이라는 뜻이고, 그러면 이
기능은 `FUN_8104c1e4` 호출부의 `param_1`이 왜 가끔 무효해지는지 원인
자체를 찾는 훨씬 큰 작업 없이는 막혀 있음. 이번 세션의 이분탐색 빌드
8개는 `battle_voice_caption/asm_test/`에 전부 보존됨. 라이브 설치본은
매 크래시 직후 정식 빌드(SHA256 `64b8edad...`)로 복구, 세션 종료 시점
재확인 완료. Ghidra는 `patch/tools_re/ghidra_11.3.2_PUBLIC`에 있고
`JAVA_HOME`을 `patch/tools_re/jdk-21.0.12+8`로 잡아야 동작함(이 환경
PATH엔 java 없음) — 이전엔 어디에도 안 적혀 있던 정보라 기록해둠.

---

## 참고: 이 파일을 왜 만들었는지

세션이 끝나면 대화 맥락(이전 세션에서 있었던 논의, 합의된 경계 등)이
초기화되기 때문에, 매 세션 시작마다 프로젝트 상태와 이전에 합의한
작업 범위를 다시 알려줄 필요가 있습니다. 이 파일은 그 인수인계
역할을 합니다. 프로젝트 상태가 크게 바뀌면(예: Script/Skit 완료,
새로운 결정 등) 이 파일도 함께 갱신해주세요.
