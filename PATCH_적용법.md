# 한글화 패치 적용 방법 (xdelta 배포판)

**반드시 정식으로 구매/소유한 `테일즈 오브 이노센스 R`(PCSG00009) 게임 데이터가
있어야 적용할 수 있습니다.** 이 패치는 저작권 있는 게임 파일을 재배포하지
않는 `.xdelta` diff 2개 + 압축 해제 도구로 구성됩니다.

## 배포 파일

- `eboot_ko.xdelta` (9KB) — `eboot.bin`(실행 파일, 한국어 시스템 폰트를 쓰도록
  언어 코드 변경 + UI 문자열 약 600개) 패치
- `toidata_release_ko.xdelta` (61MB) — `toidata_release.l7c`(게임 데이터
  아카이브) 패치. 번역된 스토리·스킷·필드·아이템·시스템 텍스트, 텍스처,
  그리고 **게임 자체 내장 폰트 파일에 주입된 한글 글리프**가 여기 들어있습니다.
- `tools/toir/l7ca_unpack.py` — 패치된 `.l7c`를 실제 게임이 읽는 loose
  파일(`_Data/`)로 풀어주는 도구(Python, 이 저장소에 포함)

## 왜 이렇게 3단계가 필요한가 (배경)

1. **`eboot.bin`**: 원본 덤프의 `eboot.bin`은 아직 암호화된 SELF 상태입니다.
   xdelta는 원본을 먼저 Vita3K 등으로 정식 설치해서 **복호화가 끝난 후의**
   `eboot.bin`에 적용해야 합니다.
2. **`toidata_release.l7c`**: 순수 원본 그대로 두면 안 됩니다 — Vita3K가
   부팅 중 이 파일을 여는데, 우리 패치 도구를 한 번도 안 거친 순수 원본이면
   **에러 없이 조용히 멈춥니다**(실기 재현·확인됨). xdelta 패치를 적용하면
   이 문제가 해결됩니다.
3. **loose 파일 압축 해제**: 게임은 실행 중 개별 콘텐츠 파일(`_Data/Script/...`
   등)을 `.l7c` 안에서 직접 꺼내 읽지 못하고, **디스크에 loose 파일로 존재해야만**
   찾습니다. 그래서 패치된 `.l7c`를 실제로 압축 해제해서 `_Data/` 트리를
   만들어야 번역이 게임에 반영됩니다. (참고: 게임 자체 내장 폰트 파일도
   `_Data/Font/FontDataNormal.pgf` 형태로 이 안에 들어있어서, 이 단계를
   생략하면 한글이 전부 대시(`-`)로만 표시됩니다.)

## 적용 절차

### 1. 원본 게임을 정상적으로 설치

Vita3K(Windows/Android 등)에서 "Install Archive" 기능 등으로 원본 게임을
설치해서 정상 실행되는지 먼저 확인하세요. 설치 폴더 경로 예시:
`ux0:app/PCSG00009/` (Windows Vita3K: `%APPDATA%/Vita3K/Vita3K/ux0/app/PCSG00009/`)

### 2. xdelta 패치 적용

[xdelta3](https://github.com/jmacd/xdelta)가 필요합니다.

```
xdelta3 -d -s <설치폴더>/eboot.bin eboot_ko.xdelta eboot_ko.bin
xdelta3 -d -s <설치폴더>/toidata_release.l7c toidata_release_ko.xdelta toidata_release_ko.l7c
```

**xdelta가 체크섬/윈도우 불일치 에러로 실패하면**, 설치 폴더의 원본 파일이
이 패치 제작에 쓰인 원본과 바이트 단위로 다르다는 뜻입니다(덤프 버전 차이
등). 이 경우 xdelta 대신 정확한 원본 파일을 구하거나, 제작자에게 문의하세요.

### 3. 패치 파일로 교체

```
copy eboot_ko.bin        <설치폴더>/eboot.bin
copy toidata_release_ko.l7c  <설치폴더>/toidata_release.l7c
```

(원본은 미리 다른 곳에 백업해두는 걸 권장합니다.)

### 4. `.l7c`를 loose 파일로 압축 해제 (필수!)

Python 3가 필요합니다(추가 설치 라이브러리 없음, 표준 라이브러리만 사용).

```
python l7ca_unpack.py <설치폴더>/toidata_release.l7c <설치폴더>
```

`_Data/`로 시작하는 항목 전부(약 14,000개 파일, 수 분 소요)를 `<설치폴더>`
아래에 실제 파일로 풀어씁니다. 완료 메시지에 `written: N / N`이 N값 둘이
같게 나오면(실패 0건) 정상입니다.

### 5. 실행

원본 게임을 재설치하지 말고, 방금 파일을 교체한 그 설치를 그대로 실행하세요.
정상적으로 한글 텍스트가 나오면 성공입니다.

## ⚠️ 절대 하지 말 것: 패치본으로 "새로 설치"

패치된 `toidata_release.l7c`를 zip으로 묶어서 Vita3K의 "Install Archive"
(새로 설치) 기능으로 설치하려고 시도하지 마세요 — 반드시 실패합니다.
Vita3K의 설치 과정은 `sce_pfs/`(PS Vita 패키지 무결성·DRM 검증 시스템)로
각 파일 크기를 원본 패키지 매니페스트와 대조하는데, 패치본은 원본보다
커서(+약 300MB) 이 검증에 걸려 **압축 해제한 데이터를 전부 삭제**해버립니다.
이 검증은 **새로 설치할 때만** 동작하므로, 위 절차(원본을 먼저 정상
설치한 뒤 파일만 교체)는 이 문제와 전혀 무관합니다.

## 검증

이 패치는 다음을 실기(Windows Vita3K, Android Vita3K)로 직접 확인했습니다:
- xdelta 패치 적용 결과가 재컴파일 결과물과 SHA256 완전 일치
- 패치된 `.l7c`를 `l7ca_unpack.py`로 압축 해제한 결과가 원본 아카이브에
  저장된 CRC32와 전부 일치(14,076개 파일 전수 검사, 실패 0건)
- 실제 부팅 후 스토리·스킷·전투·메뉴·필드명 등 전반에서 한글 텍스트 정상 출력

## 알려진 이슈

- 스테이터스 화면 등 일부 UI에서 특정 숫자/글자가 겹쳐 보이거나 다른 글자로
  바뀌어 보이는 현상이 있을 수 있습니다(예: "훔"이 "었"으로 보임). 조사
  결과 번역 데이터 자체의 문제가 아니라 에뮬레이터의 폰트 렌더링 쪽 문제로
  추정되며, 실제 PS Vita 기기에서도 재현되는지는 아직 확인 전입니다.
