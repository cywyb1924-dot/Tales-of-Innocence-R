# 한글화 패치 적용 방법 (xdelta 배포판)

**반드시 정식으로 구매/소유한 `테일즈 오브 이노센스 R`(PCSG00009) 게임 데이터가
있어야 적용할 수 있습니다.** 이 패치는 저작권 있는 게임 파일을 재배포하지
않는 `.xdelta` diff 2개 + 압축 해제 도구로 구성됩니다.

## 배포 파일

- `eboot_ko.xdelta` (9KB) — `eboot.bin`(실행 파일, 한국어 시스템 폰트를 쓰도록
  언어 코드 변경 + UI 문자열 약 600개) 패치
- `toidata_release_ko.xdelta` (61MB) — `toidata_release.l7c`(게임 데이터
  아카이브) 패치. 번역된 스토리·스킷·필드·아이템·시스템 텍스트, 텍스처,
  그리고 **게임 자체 내장 폰트 파일에 주입된 한글 글리프**가 여기 들어있습니다.
- `tools/toir/l7ca_unpack.py` — 패치된 `.l7c`를 실제 게임이 읽는 loose
  파일(`_Data/`)로 풀어주는 도구(커맨드라인, Python 표준 라이브러리만 사용)
- `tools/toir/l7ca_unpack_gui.py` — 위 도구를 **커맨드라인 없이 마우스 클릭만으로**
  쓸 수 있게 만든 GUI 버전. 파일/폴더를 찾아보기 창으로 선택하고 버튼만
  누르면 됩니다. 일반 사용자에게는 이쪽을 권장합니다.

## 왜 이렇게 3단계가 필요한가 (배경)

1. **`eboot.bin`**: 원본 덤프의 `eboot.bin`은 아직 암호화된 SELF 상태입니다.
   xdelta는 원본을 먼저 Vita3K 등으로 정식 설치해서 **복호화가 끝난 후의**
   `eboot.bin`에 적용해야 합니다.
2. **`toidata_release.l7c`**: 순수 원본 그대로 두면 안 됩니다 — Vita3K가
   부팅 중 이 파일을 여는데, 우리 패치 도구를 한 번도 안 거친 순수 원본이면
   **에러 없이 조용히 멈춥니다**(실기 재현·확인됨). xdelta 패치를 적용하면
   이 문제가 해결됩니다.
3. **loose 파일 압축 해제**: 게임은 실행 중 개별 콘텐츠 파일(`_Data/Script/...`
   등)을 `.l7c` 안에서 직접 꺼내 읽지 못하고, **디스크에 loose 파일로 존재해야만**
   찾습니다. 그래서 패치된 `.l7c`를 실제로 압축 해제해서 `_Data/` 트리를
   만들어야 번역이 게임에 반영됩니다. (참고: 게임 자체 내장 폰트 파일도
   `_Data/Font/FontDataNormal.pgf` 형태로 이 안에 들어있어서, 이 단계를
   생략하면 한글이 전부 대시(`-`)로만 표시됩니다.)

## 적용 절차

### 1. 원본 게임을 정상적으로 설치

Vita3K(Windows/Android 등)에서 "Install Archive" 기능 등으로 원본 게임을
설치해서 정상 실행되는지 먼저 확인하세요. 설치 폴더 경로 예시:
`ux0:app/PCSG00009/` (Windows Vita3K: `%APPDATA%/Vita3K/Vita3K/ux0/app/PCSG00009/`)

### 2. xdelta 패치 적용

[xdelta3](https://github.com/jmacd/xdelta)가 필요합니다.

```
xdelta3 -d -s <설치폴더>/eboot.bin eboot_ko.xdelta eboot_ko.bin
xdelta3 -d -s <설치폴더>/toidata_release.l7c toidata_release_ko.xdelta toidata_release_ko.l7c
```

**xdelta가 체크섬/윈도우 불일치 에러로 실패하면**, 설치 폴더의 원본 파일이
이 패치 제작에 쓰인 원본과 바이트 단위로 다르다는 뜻입니다(덤프 버전 차이
등). 이 경우 xdelta 대신 정확한 원본 파일을 구하거나, 제작자에게 문의하세요.

### 3. 패치 파일로 교체

```
copy eboot_ko.bin        <설치폴더>/eboot.bin
copy toidata_release_ko.l7c  <설치폴더>/toidata_release.l7c
```

(원본은 미리 다른 곳에 백업해두는 걸 권장합니다.)

### 4. `.l7c`를 loose 파일로 압축 해제 (필수!)

Python 3가 필요합니다(추가 설치 라이브러리 없음, 표준 라이브러리만 사용).
**Python이 아직 없다면 아래 "Python 설치 방법" 절을 먼저 따라 하세요.**

**GUI 버전 (권장, 마우스로만 조작)**

`l7ca_unpack_gui.py` 파일을 더블클릭하거나(또는 `python l7ca_unpack_gui.py`로
실행), 창이 뜨면:
1. "① 패치된 toidata_release.l7c 파일" 옆 "찾아보기..."로 방금 3단계에서
   교체한 `.l7c` 파일 선택
2. "② 게임 설치 폴더" 옆 "찾아보기..."로 `eboot.bin`이 있는 그 설치 폴더 선택
3. "③ 압축 해제 시작" 버튼 클릭 → 진행바가 다 차고 "완료: 14076 / 14076개
   파일 작성됨"이 나오면 성공

**커맨드라인 버전**

```
python l7ca_unpack.py <설치폴더>/toidata_release.l7c <설치폴더>
```

`_Data/`로 시작하는 항목 전부(약 14,000개 파일, 수 분 소요)를 `<설치폴더>`
아래에 실제 파일로 풀어씁니다. 완료 메시지에 `written: N / N`이 N값 둘이
같게 나오면(실패 0건) 정상입니다.

### 5. 실행

원본 게임을 재설치하지 말고, 방금 파일을 교체한 그 설치를 그대로 실행하세요.
정상적으로 한글 텍스트가 나오면 성공입니다.

## SHA256 체크섬으로 검증하기

각 단계 전후로 파일이 올바른지 확인하고 싶으면 아래 값과 대조하세요.
PowerShell에서 `Get-FileHash <파일경로> -Algorithm SHA256`,
명령 프롬프트/기타 환경에서는 `sha256sum <파일경로>`로 계산할 수 있습니다.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `eboot.bin` | 패치 전(2단계 xdelta 적용 대상) | 1,543,133 | `72a23772e4b8cb91486e40a3ce07887549d1df587240947f62de3d4dc36546ad` |
| `eboot.bin` | 패치 후(3단계 결과물) | 1,543,133 | `275e61cfbc5ce7f55768714c47bc175d567cc0853dd65569385109ca06c79a8d` |
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(3단계 결과물) | 1,784,178,634 | `1365723876a01f75c2290eee59da857ce0298a14984b1856fef59260e15133f6` |

**"패치 전" 해시가 안 맞아도 당황하지 마세요** — 특히 `eboot.bin`은 Vita3K가
설치 과정에서 원본 SELF를 직접 복호화해서 만드는 파일이라, **Vita3K
버전/플랫폼(Windows·Android 등)에 따라 복호화 결과가 미세하게(실제로
1바이트) 다를 수 있음을 이번 패치 제작 중 직접 확인했습니다.** "패치 전"
해시는 참고용이고, 안 맞더라도 2단계 xdelta 적용 시 `xdelta3`가 자체적으로
`target window checksum mismatch` 에러로 알려주니 그때 대응하면 됩니다
(설치 폴더 원본이 다르다는 뜻 — 재설치 후 재시도, 또는 제작자에게 문의).
반면 **`toidata_release.l7c`의 "패치 전" 해시는 플랫폼에 관계없이 항상
동일해야 합니다**(암호화되지 않은 순수 덤프 데이터라서) — 이게 다르면
정말로 다른 버전/리전의 덤프를 갖고 계신 것입니다.

**"패치 후" 해시는 반드시 정확히 일치해야 합니다** — xdelta가 성공적으로
끝났다면 자동으로 이 값이 나옵니다(xdelta는 목적지 파일의 정확성까지
보장하는 알고리즘이라, 별도 검증 없이 이 표는 그냥 참고/확인용입니다).

## ⚠️ 절대 하지 말 것: 패치본으로 "새로 설치"

패치된 `toidata_release.l7c`를 zip으로 묶어서 Vita3K의 "Install Archive"
(새로 설치) 기능으로 설치하려고 시도하지 마세요 — 반드시 실패합니다.
Vita3K의 설치 과정은 `sce_pfs/`(PS Vita 패키지 무결성·DRM 검증 시스템)로
각 파일 크기를 원본 패키지 매니페스트와 대조하는데, 패치본은 원본보다
커서(+약 300MB) 이 검증에 걸려 **압축 해제한 데이터를 전부 삭제**해버립니다.
이 검증은 **새로 설치할 때만** 동작하므로, 위 절차(원본을 먼저 정상
설치한 뒤 파일만 교체)는 이 문제와 전혀 무관합니다.

## 검증

이 패치는 다음을 실기(Windows Vita3K, Android Vita3K)로 직접 확인했습니다:
- xdelta 패치 적용 결과가 재컴파일 결과물과 SHA256 완전 일치
- 패치된 `.l7c`를 `l7ca_unpack.py`로 압축 해제한 결과가 원본 아카이브에
  저장된 CRC32와 전부 일치(14,076개 파일 전수 검사, 실패 0건)
- 실제 부팅 후 스토리·스킷·전투·메뉴·필드명 등 전반에서 한글 텍스트 정상 출력

## Python 설치 방법 (Windows, 처음 하시는 분용)

4단계(`.l7c` 압축 해제)에는 Python이 필요합니다. 없으신 경우:

1. **Microsoft Store에서 설치 (가장 쉬움)**
   - 시작 메뉴에서 "Microsoft Store" 실행 → 검색창에 "Python 3.12" 입력
   - 파이썬 소프트웨어 재단(Python Software Foundation)이 배포한 "Python 3.12"
     선택 → "설치" 클릭 → 끝나면 별도 설정 없이 바로 사용 가능
2. **공식 사이트에서 설치**
   - https://www.python.org/downloads/windows/ 에서 최신 "Windows installer
     (64-bit)" 다운로드·실행
   - **설치 화면 맨 아래 "Add python.exe to PATH" 체크박스를 반드시
     체크**한 뒤 "Install Now" 클릭 (이 체크를 빠뜨리면 `python` 명령이
     인식되지 않습니다)

**설치 확인**: 명령 프롬프트(또는 PowerShell)를 새로 열어서 `python --version`
입력 → `Python 3.x.x` 같은 버전이 출력되면 정상 설치된 것입니다.

**"python을 찾을 수 없습니다" 에러가 계속 나올 때**: PATH 등록이 안 된
경우입니다. 아래처럼 python.exe의 전체 경로를 직접 써서 실행할 수도 있습니다
(설치 시 기본 경로 기준):
```
%LOCALAPPDATA%\Programs\Python\Python312\python.exe l7ca_unpack_gui.py
```

## 알려진 이슈

- ~~특정 한글 낱자가 다른 글자로 바뀌어 보이는 현상(예: "훔"이 "었"으로 보임)~~
  → **수정 완료.** 게임 자체 내장 폰트 파일(`FontDataNormal.pgf`)에 한글
  글리프를 주입하는 과정에서 생긴 버그(실사용 한글 1336자 중 230자가 charmap
  충돌로 엉뚱한 글자를 가리키고 있었음)를 찾아서 고쳤습니다. 자세한 내용은
  `tools/toir/font_research/hangul_fix/README.md` 참고.
- **스테이터스 화면의 물공/물방/술공 등 일부 스탯 숫자가 겹쳐 보이는 문제**는
  아직 원인 미해결입니다. 조사 결과 폰트 파일 자체는 원본과 완전히 동일함을
  확인했고(위 항목과는 무관한 별개 버그), HP/TP/NEXT 등 다른 숫자 표시는
  정상이며 4자리 숫자를 쓰는 특정 스탯 UI 위젯에서만 두 자리가 같은 위치에
  겹쳐 그려집니다. 게임 자체 코드 쪽 문제로 추정되나 정확한 원인은 추가 조사가
  필요합니다.
