# 한글화 패치 적용 방법

**반드시 정식으로 구매/소유한 `테일즈 오브 이노센스 R`(PCSG00009) 게임 데이터가
있어야 적용할 수 있습니다.**

## 방법 A. 파일 직접 교체 (검증 완료 — 권장 방법)

**게임을 원본 그대로 먼저 설치해서 정상 실행되는 걸 확인한 뒤**, 설치된
폴더 안의 파일 3개를 패치본으로 바꿔치기합니다. `3_patched/ready_to_use/`
(또는 `3_patched/loose_files/`)에 있는 파일들을 씁니다:

- `toidata_release.l7c` — 원본 자리에 (⚠️ **반드시 교체해야 합니다.** 아래
  "왜 3개 파일 다 필요한가" 참고)
- `eboot.bin` — 원본 자리에
- `_Data/` 폴더 전체 — 설치 폴더의 `_Data/` 위에 덮어쓰기(Vita3K 등 이미
  압축 해제된 loose 설치의 경우. 순수 `.l7c` 안에만 패키징되는 배포라면
  `.l7c` 교체만으로 충분합니다)

**절대 하지 말 것**: 이 파일들을 "설치용 zip"에 넣어서 새로 설치하려고
시도하지 마세요 — 아래 "알려진 제약" 참고, 실패합니다. 반드시 **원본을
정상적으로 설치·확인한 뒤, 그 설치 폴더 안의 파일만 바꿔치기**하세요.

### 왜 3개 파일(`_Data`/`eboot.bin`/`toidata_release.l7c`) 다 필요한가

- `_Data/`, `eboot.bin` 안에 실제 번역 텍스트가 들어있습니다.
- **`toidata_release.l7c`는 순수 원본 그대로 두면 안 됩니다.** 실제로
  재현·확인한 사실인데, Vita3K는 부팅 시 이 파일을 엽니다
  (`sceIoOpen: app0:/toidata_release.l7c`). 이때 **한 번도 우리 패치
  도구(`l7ca_patch_multi.py` 계열)를 거치지 않은 순수 원본 파일**이면 —
  크기가 원본과 정확히 같은 파일이어도 — **에러 없이 그 지점에서 조용히
  멈춥니다**(로그도 더 이상 진행 안 됨, CPU만 계속 씀). 반면 우리 패치
  도구를 한 번이라도 거친 `.l7c`는 내용이 얼마나 바뀌었든 상관없이 정상
  통과합니다. 이 증상은 에러창이 안 뜨고 화면이 그냥 멈추기 때문에
  "번역이 반영 안 됐다"(마지막 렌더링된 원문 화면이 남아있는 것뿐)로
  오인하기 쉬우니 주의하세요. `3_patched/ready_to_use/toidata_release.l7c`는
  이미 이 패치 도구를 거친 상태라 안전합니다.

## 방법 B. Vita3K 등 이미 압축 해제된(loose) 설치인 경우

방법 A와 동일하되, `.l7c`를 다시 패키징할 필요 없이 개별 파일을 설치
폴더 위에 그대로 덮어씁니다. `3_patched/loose_files/` 아래의 `_Data/`
폴더 전체 + `eboot.bin`을 설치 폴더의 같은 상대 경로에 덮어쓰고,
**`toidata_release.l7c`도 반드시 같이 교체**하세요(위 이유 참고). 이 방식은
실제 Vita3K 설치(PC·Android 둘 다)에서 **부팅·플레이까지 한글로 정상
동작함을 실기로 확인**했습니다.

## 방법 C. xdelta (원본이 패치 제작 원본과 정확히 같을 때만)

```
xdelta3 -d -s toidata_release.l7c toidata_release_ko.xdelta toidata_release_ko.l7c
xdelta3 -d -s eboot.bin eboot_ko.xdelta eboot_ko.bin
```

이 방법이 실패하면(체크섬/윈도우 불일치 에러 등) 원본 파일이 패치 제작에 쓰인
원본과 다르다는 뜻이니, 방법 A 또는 B로 진행하세요. **적용 후에도 방법 A와
동일하게 `toidata_release_ko.l7c`로 원본을 완전히 교체해야 합니다** — 이
xdelta 결과물 자체가 이미 패치 도구를 거친 것이므로 문제없습니다.

## ⚠️ 알려진 제약: 설치용 ZIP을 새로 만들어서 설치하는 방식은 안 됨

`toidata_release.l7c`를 패치본으로 교체한 뒤 그걸 zip으로 묶어서 Vita3K의
"Install Archive"(새로 설치) 기능으로 설치하려고 하면 **반드시 실패합니다.**
Vita3K의 설치 과정은 `sce_pfs/`(PS Vita 패키지 무결성·DRM 검증 시스템,
`files.db`+`unicv.db` 해시 트리)로 각 파일 크기를 원본 패키지 매니페스트와
대조하는데, 우리 패치본은 원본보다 커서(원본 대비 +300MB 안팎) 이 검증에
걸려 **"File toidata_release.l7c size incorrect" 에러와 함께 방금 압축
해제한 데이터를 전부 삭제**해버립니다(Windows/Android 둘 다 재현 확인함).

이 검증은 **설치 시점에만** 동작하고 이미 설치된 게임의 파일을 나중에
바꾸는 건 재검증하지 않으므로, **위의 방법 A/B(원본을 먼저 정상 설치한 뒤
파일만 바꿔치기)는 이 문제와 무관하게 정상 동작합니다.** 즉 정식 배포
방법은 항상 "원본 설치 → 파일 3개 교체"이며, "패치본을 zip으로 묶어
새로 설치"는 현재 불가능한 방법으로 취급하세요.

## 검증

방법 A/B 모두 SHA256 체크섬으로 검증할 수 있습니다. 개발 과정에서는
`toidata_release_ko.l7c`/`eboot_ko.bin`을 재컴파일 결과물과 SHA256 완전
일치까지 확인했고, xdelta 패치도 원본에 적용한 결과가 이 파일들과 바이트
단위로 동일함을 검증했습니다. 최종적으로 Windows Vita3K와 Android Vita3K
양쪽에서 실제 부팅 후 한글 텍스트가 정상 출력됨을 확인했습니다.

## 알려진 이슈

- 스테이터스 화면 등 일부 UI에서 특정 숫자/글자가 겹쳐 보이거나 다른 글자로
  바뀌어 보이는 현상이 있을 수 있습니다. 조사 결과 번역 데이터 자체의 문제가
  아니라 (에뮬레이터의) 한글 시스템 폰트 렌더링 쪽 문제로 추정되며, 실제
  PS Vita 기기에서도 재현되는지는 아직 확인 전입니다.
- 이 Windows용 Vita3K 빌드(`VITA3K_NEW`)는 암호화된 SELF/suprx를 직접
  복호화하는 기능이 없어서, 원본 zip을 그냥 풀어서 바로 부팅하려 하면
  "Decryption is not yet supported" 에러가 납니다. 원본 게임은 반드시
  Vita3K 자체의 "Install Archive" 설치 기능을 통해 설치하세요(이 과정에서
  내부적으로 복호화가 처리됩니다) — 그 뒤에 방법 A/B로 파일을 교체하면 됩니다.
- 다른 사용자에게 배포 가능한 정식 릴리즈는 별도 검증(전체 재추출 →
  변경/미변경 파일 전수 대조)을 거쳐 생성됩니다 — 자세한 내용은
  `tools/toir/PIPELINE_VERIFIED.md`, `NEXT_SESSION_PROMPT.md` 참고.
