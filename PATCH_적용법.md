# 한글화 패치 적용 방법 (xdelta 배포판)

**반드시 정식으로 구매/소유한 `테일즈 오브 이노센스 R`(PCSG00009) 게임 데이터가
있어야 적용할 수 있습니다.** 이 패치는 저작권 있는 게임 파일을 재배포하지
않는 `.xdelta` diff 2개 + 압축 해제 도구로 구성됩니다.

## 배포 파일

- `eboot_ko.xdelta` (9KB) — `eboot.bin`(실행 파일, 한국어 시스템 폰트를 쓰도록
  언어 코드 변경 + UI 문자열 약 600개) 패치
- `toidata_release_ko.xdelta` (87MB, 2026-09-15 갱신) — `toidata_release.l7c`(게임 데이터
  아카이브) 패치. 번역된 스토리·스킷·필드·아이템·시스템 텍스트, 텍스처,
  한글 폰트 글리프(`FontDataNormal.pgf`/`FontDataMin.pgf`, 실사용 한글
  1374자 + 특수문자 4자 커버, ≥와 홀로 쓰인 ㅇ 2자는 아직 미포함)가
  여기 들어있습니다 — 자세한 내용은 `NEXT_SESSION_PROMPT.md` 참고.
- `tools/toir/l7ca_unpack.py` — 패치된 `.l7c`를 실제 게임이 읽는 loose
  파일(`_Data/`)로 풀어주는 도구(커맨드라인, Python 표준 라이브러리만 사용)
- `tools/toir/l7ca_unpack_gui.py` — 위 도구를 **커맨드라인 없이 마우스 클릭만으로**
  쓸 수 있게 만든 GUI 버전. 파일/폴더를 찾아보기 창으로 선택하고 버튼만
  누르면 됩니다. 일반 사용자에게는 이쪽을 권장합니다.

## 왜 이렇게 3단계가 필요한가 (배경)

1. **`eboot.bin`**: 원본 덤프의 `eboot.bin`은 아직 암호화된 SELF 상태입니다.
   xdelta는 원본을 먼저 Vita3K 등으로 정식 설치해서 **복호화가 끝난 후의**
   `eboot.bin`에 적용해야 합니다.
2. **`toidata_release.l7c`**: 순수 원본 그대로 두면 안 됩니다 — Vita3K가
   부팅 중 이 파일을 여는데, 우리 패치 도구를 한 번도 안 거친 순수 원본이면
   **에러 없이 조용히 멈춥니다**(실기 재현·확인됨). xdelta 패치를 적용하면
   이 문제가 해결됩니다.
3. **loose 파일 압축 해제**: 게임은 실행 중 개별 콘텐츠 파일(`_Data/Script/...`
   등)을 `.l7c` 안에서 직접 꺼내 읽지 못하고, **디스크에 loose 파일로 존재해야만**
   찾습니다. 그래서 패치된 `.l7c`를 실제로 압축 해제해서 `_Data/` 트리를
   만들어야 번역이 게임에 반영됩니다. (참고: 게임 자체 내장 폰트 파일도
   `_Data/Font/FontDataNormal.pgf` 형태로 이 안에 들어있어서, 이 단계를
   생략하면 한글이 전부 대시(`-`)로만 표시됩니다.)

## 적용 절차

### 1. 원본 게임을 정상적으로 설치

Vita3K(Windows/Android 등)에서 "Install Archive" 기능 등으로 원본 게임을
설치해서 정상 실행되는지 먼저 확인하세요. 설치 폴더 경로 예시:
`ux0:app/PCSG00009/` (Windows Vita3K: `%APPDATA%/Vita3K/Vita3K/ux0/app/PCSG00009/`)

### 2. xdelta 패치 적용

[xdelta3](https://github.com/jmacd/xdelta)가 필요합니다.

```
xdelta3 -d -s <설치폴더>/eboot.bin eboot_ko.xdelta eboot_ko.bin
xdelta3 -d -s <설치폴더>/toidata_release.l7c toidata_release_ko.xdelta toidata_release_ko.l7c
```

**xdelta가 체크섬/윈도우 불일치 에러로 실패하면**, 설치 폴더의 원본 파일이
이 패치 제작에 쓰인 원본과 바이트 단위로 다르다는 뜻입니다(덤프 버전 차이
등). 이 경우 xdelta 대신 정확한 원본 파일을 구하거나, 제작자에게 문의하세요.

### 3. 패치 파일로 교체

```
copy eboot_ko.bin        <설치폴더>/eboot.bin
copy toidata_release_ko.l7c  <설치폴더>/toidata_release.l7c
```

(원본은 미리 다른 곳에 백업해두는 걸 권장합니다.)

### 4. `.l7c`를 loose 파일로 압축 해제 (필수!)

Python 3가 필요합니다(추가 설치 라이브러리 없음, 표준 라이브러리만 사용).
**Python이 아직 없다면 아래 "Python 설치 방법" 절을 먼저 따라 하세요.**

**GUI 버전 (권장, 마우스로만 조작)**

`l7ca_unpack_gui.py` 파일을 더블클릭하거나(또는 `python l7ca_unpack_gui.py`로
실행), 창이 뜨면:
1. "① 패치된 toidata_release.l7c 파일" 옆 "찾아보기..."로 방금 3단계에서
   교체한 `.l7c` 파일 선택
2. "② 게임 설치 폴더" 옆 "찾아보기..."로 `eboot.bin`이 있는 그 설치 폴더 선택
3. "③ 압축 해제 시작" 버튼 클릭 → 진행바가 다 차고 "완료: 14076 / 14076개
   파일 작성됨"이 나오면 성공

**커맨드라인 버전**

```
python l7ca_unpack.py <설치폴더>/toidata_release.l7c <설치폴더>
```

`_Data/`로 시작하는 항목 전부(약 14,000개 파일, 수 분 소요)를 `<설치폴더>`
아래에 실제 파일로 풀어씁니다. 완료 메시지에 `written: N / N`이 N값 둘이
같게 나오면(실패 0건) 정상입니다.

### 5. 실행

원본 게임을 재설치하지 말고, 방금 파일을 교체한 그 설치를 그대로 실행하세요.
정상적으로 한글 텍스트가 나오면 성공입니다.

## SHA256 체크섬으로 검증하기

각 단계 전후로 파일이 올바른지 확인하고 싶으면 아래 값과 대조하세요.
PowerShell에서 `Get-FileHash <파일경로> -Algorithm SHA256`,
명령 프롬프트/기타 환경에서는 `sha256sum <파일경로>`로 계산할 수 있습니다.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `eboot.bin` | 패치 전(2단계 xdelta 적용 대상) | 1,543,133 | `72a23772e4b8cb91486e40a3ce07887549d1df587240947f62de3d4dc36546ad` |
| `eboot.bin` | 패치 후(3단계 결과물, 2026-08-11 두번째 숫자표 수정판) | 1,543,133 | `56c244cdbc150cfde6f3a4f4825a7c4a5dff8d0d1ecf9af0fb327a14a168f318` |
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(3단계 결과물, 2026-09-15 SKIT line_addition 누적 줄 수 초과 + MovieCaption 수정 최종 갱신) | 1,783,701,058 | `20e7a63170abe24f4d3b04fcabf8d62784117a0ebe3d440e2d69fb67d6a01fdd` |

**"패치 전" 해시가 안 맞아도 당황하지 마세요** — 특히 `eboot.bin`은 Vita3K가
설치 과정에서 원본 SELF를 직접 복호화해서 만드는 파일이라, **Vita3K
버전/플랫폼(Windows·Android 등)에 따라 복호화 결과가 미세하게(실제로
1바이트) 다를 수 있음을 이번 패치 제작 중 직접 확인했습니다.** "패치 전"
해시는 참고용이고, 안 맞더라도 2단계 xdelta 적용 시 `xdelta3`가 자체적으로
`target window checksum mismatch` 에러로 알려주니 그때 대응하면 됩니다
(설치 폴더 원본이 다르다는 뜻 — 재설치 후 재시도, 또는 제작자에게 문의).
반면 **`toidata_release.l7c`의 "패치 전" 해시는 플랫폼에 관계없이 항상
동일해야 합니다**(암호화되지 않은 순수 덤프 데이터라서) — 이게 다르면
정말로 다른 버전/리전의 덤프를 갖고 계신 것입니다.

**"패치 후" 해시는 반드시 정확히 일치해야 합니다** — xdelta가 성공적으로
끝났다면 자동으로 이 값이 나옵니다(xdelta는 목적지 파일의 정확성까지
보장하는 알고리즘이라, 별도 검증 없이 이 표는 그냥 참고/확인용입니다).

## 2026-08-11 업데이트: 스킷 말투/호칭 일관성 수정 (toidata_release_ko.xdelta 재생성)

`_Data/Field/Skit/Data/` 아래 49개 스킷 파일이 이번 갱신으로 바뀌었습니다:

- **루카(주인공) 반말/존댓말 통일** (85줄): 앙쥬·리카르도·큐큐·콘웨이와의
  1:1 동료 이벤트 스킷에서 원문은 반말인데 번역만 존댓말로 되어있던
  대규모 불일치를 수정. 글로서리에 확립된 "수줍고 겸손한 반말" 캐릭터성에 맞춤.
- **호칭(呼称) 일관성 통일** (96줄): 캐릭터 간 상호 호칭(씨/군/오빠/언니 등)이
  같은 관계에서 들쭉날쭉하던 것을 다수결로 통일(예: 에르마나→루카/스파다는
  "오빠", 앙쥬→루카는 "군" 등).
- **오역 수정** (1줄): `0078.dat`에서 리카르도의 "………アンジュだな"(감탄 어미)가
  "………앙쥬군"으로 잘못 번역된 것을 "………앙쥬구나"로 수정(だな를 くん 존칭으로
  착각한 오역).

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-17 업데이트: 캐릭터별 말투 정합성 재검증 + 무비 자막 수정 (toidata_release_ko.xdelta 재생성)

`_Data/Script/` 31개, `_Data/Field/Skit/Data/` 42개, `_Data/Field/MapData/` 2개,
`_Data/Movie/` 10개, 총 85개 파일이 이번 갱신으로 바뀌었습니다:

- **캐릭터별 말투(반말/존댓말) 전면 재검증** (117줄): 앙쥬가 리카르도를
  제외한 다른 동료에게 존댓말을 쓰던 경우(42줄), 2026-08-11에 놓쳤던
  루카의 1:1 동료 이벤트 스킷 잔여 존댓말(72줄), 이리아 2줄, 콘웨이 1줄을
  원문 문법에 맞춰 반말로 수정. 스파다/콘웨이/큐큐/코다/에르마나/리카르도도
  같은 기준으로 전수 재검증(대부분 "잘 먹겠습니다" 관용구·사투리 흉내
  개그 등 의도된 것으로 확인, 콘웨이 1건만 추가 수정).
- **무비 자막(오프닝 등) 캐릭터 보이스 수정**: 사용자가 직접 검수해서
  `MovieCaption_000~006/019/024/027.srt` 10개 파일 수정 — 오프닝에서
  휴프노스가 갑자기 존댓말을 쓰던 대사를 반말로, 앙쥬(오리피엘)의 대사에
  경상도 사투리(에르마나 말투)가 잘못 붙어 있던 것을 존댓말로 바로잡음.

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-17 업데이트: 한글 글꼴 크래시 버그 수정 + 미표시 한글 1335자 추가

`_Data/Font/FontDataNormal.pgf` 1개 파일이 이번 갱신으로 바뀌었습니다:

- **한글 글자 추가 시 게임이 크래시하던 근본 버그 수정**: 글꼴 파일의 각
  글자(glyph) 레코드 맨 앞에 있는 "크기" 필드를 지금까지 0으로 잘못
  기록하고 있었는데, 실제 PS Vita 시스템 폰트 라이브러리가 이 값을
  글자 데이터를 복사할 메모리 크기로 직접 사용하는 것으로 확인됨(0으로
  들어가면 크기 계산이 음수로 넘쳐서 잘못된 메모리 영역을 읽어 크래시).
  이 필드를 정확한 값으로 계산하도록 수정.
- **여전히 밑줄(_)로 표시되던 한글 1335자를 새로 추가**: 위 버그 때문에
  추가하지 못하고 있던 나머지 한글 글자들을 전부 새로 그려서 폰트에
  포함시킴. 실기(Vita3K)에서 오프닝부터 필드·스킷 대화까지 15분 이상
  플레이하며 크래시 없음과 정상 렌더링을 확인.

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-18 업데이트: 한글 글자 잘림/렌더링 일관성/작은글꼴 미표시 + 캐릭터 이름 미번역 수정

`_Data/Font/FontDataNormal.pgf`, `_Data/Font/FontDataMin.pgf`,
`_Data/System/CharaParamDataPack.dat` 3개 파일이 이번 갱신으로 바뀌었습니다:

- **특정 한글 글자(같/겉/밑/받/떻 등)가 화면에서 오른쪽 받침이 잘려
  보이던 문제 수정**: 저장된 글자 데이터 자체는 완전하고 정확한데(직접
  디코딩해서 원본 래스터와 픽셀 단위로 대조 확인), 특정 자모 조합(받침
  ㅌ/ㄷ/ㅎ 등 폭이 넓은 획)을 그릴 때 PS Vita 실기의 글자 렌더러가
  잘못 처리하는 것으로 확인(인코딩 방식을 여러 가지로 바꿔봐도 증상
  동일). **폰트를 바꾸는 방식은 렌더링 일관성을 해치므로 사용하지
  않음** — 대신 이 글자들이 이미 이전 세션에 추가된 적 있는 한글
  글자(1107자 배치)에 포함되어 있고, 그 배치는 실기에서 잘림 문제가
  전혀 없었다는 것을 확인해서, **그 배치의 검증된 원본 글자 데이터를
  그대로 재사용**하는 방식으로 해결(추가하는 1335자 중 1106자가 이
  배치에 이미 있어서 전부 재사용, 나머지 229자만 새로 그림).
- **재사용한 글자와 새로 그린 글자의 렌더링이 서로 다르게 보이던
  문제(굵기 불일치) 수정**: 재사용한 1106자가 유독 굵어 보여서 확인한
  결과, 그 배치가 원래 "맑은 고딕 볼드"로 그려졌던 것으로 확인(같은
  크기로 볼드체 렌더링하면 저장된 크기·위치가 정확히 일치) — 나머지
  229자도 볼드체로 다시 그려서 전체 글자가 같은 굵기로 통일되도록 수정.
  메타데이터(글자 배치 보정값) 방식도 재사용 글자·새로 그린 글자
  모두 동일한 방식(원본 폰트에 이미 있던 고정 테이블 참조)으로 통일.
- **메뉴창 하단 키매핑 힌트("결정"/"돌아가기" 등)가 밑줄(_)로만
  표시되던 문제 수정**: 본문 대화창 폰트(`FontDataNormal.pgf`)와
  별도로 존재하는 작은 글꼴 파일 `FontDataMin.pgf`에 한글 glyph가
  전혀 없었던 것이 원인 — 동일한 수정을 이 파일에도 적용.
- **무기점/방어구점의 캐릭터 스탯 비교 패널에서 이름이 일본어
  가타카나(ルカ 등)로 남아있던 문제 수정**: 이 패널이 참조하는
  `_Data/System/CharaParamDataPack.dat` 파일이 지금까지 배포 파이프라인에
  단 한 번도 포함된 적이 없어(번역 작업 대상에서 누락) 순정 원본 그대로
  배포되고 있었음 — 파티 멤버 8명(루카/이리아/스파다/앙쥬/리카르도/
  에르마나/콘웨이/큐큐) 이름을 전부 한글로 교체해서 새로 포함.

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-18 업데이트: 앙쥬 말투(반말→존댓말 오역) + "え/에!" 감탄사 오역 수정

`_Data/Script/` 21개, `_Data/Field/Skit/Data/` 1개, 총 22개 파일이 이번
갱신으로 바뀌었습니다:

- **엘마나 합류 이후 앙쥬가 원문에서는 반말인데 존댓말로 번역되어 있던
  곳들 수정** (37줄): 원문이 ね/よ/の/わ 같은 반말 어미로 끝나는데
  한국어만 요/니다/세요 등 존댓말로 번역되어 있던 부분을 원문 기준
  반말로 수정. 리카르도에게 말하는 대사(정중한 원문)는 그대로 유지.
- **원문의 놀람 감탄사 "え/ええ"(에!)가 긍정 응답 "네!"로 오역된 곳들
  수정** (4줄): "え！？"는 "어(놀람)"이지 "네(긍정 대답)"가 아닌데
  일부 대사에서 "네!"로 잘못 번역되어 있던 것을 "뭐!?" 등 놀람
  표현으로 수정.

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-19 업데이트: 미표시(밑줄) 한글 39자 추가

`_Data/Font/FontDataNormal.pgf`, `_Data/Font/FontDataMin.pgf` 2개 파일이
이번 갱신으로 바뀌었습니다:

- **아이템/특기 이름 등에 쓰이는 한글 39자가 여전히 밑줄(_)로 표시되던
  문제 수정**: "아멧헬름"이 "아 _ 헬름"으로 보이는 등(스크린샷
  `font_error/after_font_6.PNG`). `2_translated/` 전체 CSV(1,705개
  파일, 72,751행)를 코퍼스 삼아 실제 쓰이는 한글 음절을 전수 추출해서
  두 폰트 파일의 glyph 테이블과 직접 대조한 결과, 이전 세션(08-17/08-18)의
  1335자 추가 배치에서 39자가 누락되어 있었음을 확인:
  `딕 샷 닉 밸 띤 텔 텝 톰 늄 멧 뮬 윙 짙 쫑 캔 펀 굿 뙤 랗 뱄 숄 슛 싣 썽
  쏙 엠 윗 잿 캣 퀵 헥 닛 롬 앎 왈 쿼 킥 펼 횡`. 이전 세션에서 실제 배포된
  glyph들의 원시 바이트를 직접 역산해서 인코딩 규칙(맑은 고딕 볼드 래스터,
  글자 레코드의 "크기" 필드 = 헤더+RLE 데이터의 정확한 바이트 길이 등,
  자세한 내용은 `tools/toir/font_research/hangul_fix/` 참고)을 재확인한
  뒤 동일한 방식으로 39자를 추가. 기존 1335자는 전부 픽셀 단위로 재검증해서
  손상 없음을 확인했고, 전체 코퍼스 기준 미커버 글자 0자를 확인. 과거
  이 종류의 작업에서 크래시가 난 전례가 있어(잘못된 "크기" 필드 → PS Vita
  펌웨어가 그 값을 memcpy 크기로 직접 사용 → 크래시), 이번에는 Windows
  Vita3K에 실제로 배포해서 오프닝 무비 재생 구간(과거 크래시 트리거 지점)을
  포함해 부팅 테스트까지 마친 뒤 배포함(크래시 없음, 사용자 실시간 확인).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-19 최종 갱신) | 1,783,700,274 | `44e52566eb53dc784899f2ab3d82b238cef10c8687bd75ef0820370e36c81e08` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-19 업데이트(2차): 미표시(밑줄) 특수문자 4자 추가(·ㆍ—−)

`_Data/Font/FontDataNormal.pgf`, `_Data/Font/FontDataMin.pgf` 2개 파일이
이번 갱신으로 또 바뀌었습니다:

- **한글 음절이 아닌 특수문자가 밑줄(_)로 표시되던 문제 수정**: "아—
  날씨 좋구먼…"이 "아 _ 날씨 좋구먼…"으로, "도구ㆍ식재료점"이 "도구 _
  식재료점"으로 보이는 등(스크린샷 `font_error/_issue_1~3.PNG`). 위
  39자 추가 작업이 한글 음절(가-힣)만 스캔해서 놓쳤던 항목 — 이번엔
  `2_translated/` 전체 코퍼스에서 ASCII를 제외한 모든 문자를 스캔해서
  가운뎃점(·), 한글 아래아(ㆍ), 줄표(—), 마이너스 기호(−), 이상 기호(≥),
  홀로 쓰인 자음 ㅇ까지 6자가 빠져있었음을 확인. 이 중 4자(·ㆍ—−)를
  이번 갱신에 포함(≥, 홀로 쓰인 ㅇ은 사용 빈도가 낮고 검증이 더 필요해
  다음으로 미룸).
- **1차 시도 실패 교훈**: 처음엔 한글 39자 때와 같은 방식(맑은 고딕
  볼드로 직접 래스터라이즈)으로 4자를 추가했다가, 실제로는 화면에
  공백으로 나오는 회귀가 발생했습니다. 원인은 한글 음절 전용으로
  역산했던 세로 위치("top") 계산 공식이 점·대시처럼 낮게 앉는 기호에는
  전혀 안 맞았던 것(원본 대비 20 이상 차이 나서 화면 밖으로 밀려남).
  또한 "−"(수학 마이너스, U+2212)는 맑은 고딕 볼드에 실제 글리프가
  없어 빈 상자만 래스터라이즈되고 있었음. **최종적으로는 새로 그리지
  않고, 이미 정상 작동하던 원본 글리프를 그대로 재사용**하는 방식으로
  해결: `·`/`ㆍ" ← 가타카나 가운뎃점(・, U+30FB) 레코드를 통째로 복사,
  `−` ← 하이픈(-, U+002D) 레코드를 통째로 복사, `—`(줄표)는 자체
  렌더링한 모양은 맞았으므로 top 값만 검증된 기준값(12)으로 교정.
  실제 Vita3K 부팅 테스트로 크래시 없음과 정상 렌더링을 사용자가 직접
  확인.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-19 2차 갱신) | 1,783,701,090 | `c9db2f91cf201b52d9e9737538444121e2f7bfe16524e18cd00b25b7664e13c7` |
| `toidata_release_ko.xdelta` | - | 91,264,951 | `4949feb51a6a950d67a2d1eef81eb53c501d61cf290a09c5d32076618bb91395` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-20 업데이트: 대사창 밖으로 넘치는 대사 줄바꿈 정리 (toidata_release_ko.xdelta 재생성)

`_Data/Script/` 105개, `_Data/Field/Skit/Data/` 91개, 총 196개 파일이 이번
갱신으로 바뀌었습니다:

- **대사창 밖으로 텍스트가 삐져나가던 문제 수정** (Story.csv 112줄 + Skit.csv
  99줄, 총 211줄): 3줄 이상으로 줄바꿈된 대사 중 한 줄이라도 대사창 안전
  폭(가시 문자 24자 상당)을 넘던 항목을 찾아서, **번역 문구는 한 글자도
  바꾸지 않고 줄바꿈 위치만 재배치**하는 방식으로 수정. 원래 줄 개수를
  유지한 채 단어 순서 그대로 다시 배분하는 DP 알고리즘으로 각 줄의 최대
  폭을 최소화(`tools/toir/font_research/hangul_fix/rewrap/` 참고). 재적용
  전 원본 텍스트와 정확히 일치하는 행에만 덮어쓰도록 안전장치를 둬서,
  Story 112줄·Skit 99줄 전부 매칭 실패 0건으로 안전하게 적용됨.
- **문구 변경 없이는 해결 안 되는 11개 항목은 보류**: 3개는 전각공백(`　`)을
  쓴 장식용 들여쓰기/서명 서식이라 재배치하면 서식이 깨지고, 8개는 최적
  재배치로도 안전 폭을 1~8자 초과해서 그대로 남겨둠 — 목록과 원문은
  `tools/toir/font_research/hangul_fix/rewrap/FLAGGED_11_items.md` 참고.
- **작업 중 발견·복구한 자체 버그**: 첫 작업 스크립트가 이 프로젝트 CSV의
  `\r\n` 개행 관례를 잘못 처리해서 `script_wip/` 일부 행이 개행 문자가
  섞여 손상되는 사고가 있었음(Story.csv를 아직 손대지 않은 세션 초반
  스냅샷으로 삼아 전량 복구). 배포 전 `script_wip/`(530개)·`skit_wip/`
  (989개) 전체를 재검사해서 손상 0건 확인 후 진행.

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-22 업데이트: 대사창 누적 줄 수 초과(line+line_addition) 수정 (toidata_release_ko.xdelta 재생성)

`_Data/Field/Skit/Data/` 93개 파일이 이번 갱신으로 바뀌었습니다:

- **`line`(대사) 뒤에 `line_addition`(추가 대사)이 이어붙을 때, 개별 항목은
  3줄 미만이라도 합쳐서 대사창의 안전 줄 수(3줄)를 넘던 문제 수정**
  (스크린샷 `line/line_2.PNG` — 앙쥬가 "…나 진짜 어른이네"라고 말하는 4번째
  줄이 화면 하단 밖으로 잘려나가던 사례). 2026-08-20 수정은 "한 CSV 행 안에서
  3줄 이상 줄바꿈된 가로 폭 초과"만 다뤘는데, 이번 건은 서로 다른 두 CSV
  행(`line`+`line_addition`)이 같은 대사창에 누적되어 만드는 **세로(줄 수)
  초과**라 별도로 스캔·수정이 필요했습니다.
- **SKIT 대사창 전용 안전폭을 `line_2.PNG` 픽셀 측정으로 새로 도출**:
  Story.csv용 24자를 그대로 재사용하지 않고, SKIT 대사창은 '다음' ▼
  아이콘(x=876~903)이 **3번째(마지막) 줄 자리에만** 고정으로 걸쳐 있고
  1~2번째 줄은 그 아이콘과 안 겹쳐서 화면 오른쪽 끝까지 더 넓게 쓸 수
  있다는 걸 확인 — 줄 위치별로 안전폭을 다르게 적용(1~2번째 줄 27자,
  마지막 줄 24자, 비대칭 재배치 알고리즘). 안전 줄 수(3줄)는 줄 간격
  32px·화면 하단 544px 기준으로 재검증.
- **`line`/`line_addition` 경계를 넘겨서 단어를 재배치(단, 안전한 경우만)**:
  `line` 뒤에 `line_addition`이 **딱 하나뿐인 그룹**은 `line`의 마지막
  단어와 `line_addition`의 첫 단어를 자유롭게 섞어서 3줄에 맞게 재배치.
  단, 같은 `line`에 `line_addition`이 **여러 개 달린 그룹**(전체의 대다수)은
  이 방식을 쓰지 않음 — `line`은 그 여러 line_addition이 공유하는 공통
  도입부라서(매 재생마다 그중 하나만 조건부로 재생되는 것으로 보임),
  특정 addition 하나에 맞춰 `line`의 단어를 빼거나 더하면 다른
  addition과 짝지어질 때 문장이 깨짐(실제로 처음에는 이 구분 없이
  전체 적용했다가 발견 — 자세한 경위는 `NEXT_SESSION_PROMPT.md` 참고).
- Skit.csv 전체에서 108개 그룹/167쌍을 찾아, **번역 문구는 한 글자도 안
  바꾸고 기존 줄바꿈 위치만 재배치**하는 방식으로 **139쌍을 자동
  수정**했고(공유되는 `line` 필드 101개도 함께 재배치해서 여유 확보),
  문구를 줄이지 않는 한(그리고 다른 addition과의 조합을 깨지 않는 한)
  안전 폭 안에 못 들어가는 28쌍(18개 그룹)은 보류했습니다.
- **`line/line_2.PNG`의 실제 사례(4053.dat)는 이후 수동으로 추가 수정**:
  이 `line`은 다른 `line_addition`(idx21)과도 공유돼서 자동 로직으로는
  경계를 못 넘겼지만, "하아……"(감탄사, 어느 쪽 addition과 짝지어도
  자연스러움)만 `line` 쪽으로 옮기면 마지막 줄이 25자로 안전 기준(24자)을
  딱 1자 초과하는 수준까지 좁혀짐을 확인 — 이 정도는 픽셀 측정의 여유
  범위 안일 가능성이 높아 사용자 승인 하에 적용. **최종 27쌍(17개
  그룹)만 보류** — 목록은
  `tools/toir/font_research/hangul_fix/rewrap/FLAGGED_group_overflow.md`
  (2026-08-20의 보류 11건과 합친 통합 목록은
  `tools/toir/font_research/hangul_fix/rewrap/FLAGGED_ALL_pending.md`) 참고.
- 수정 전 `skit_wip/` 전체를 백업했고, `merge-skit`/`check skit` 재검증(기존
  "리카르돈" 1건 외 새 이슈 없음), 개행(`\r\n`) 혼용 손상 0건, 재스캔으로
  자동 139건 + 수동 1건이 실제로 안전해지고 정확히 27건만 남았음을 교차
  확인 후 배포.
- **배포 후 사용자가 실기에서 직접 렌더링을 확인하다가 추가 버그 발견 및 수정**:
  `line` 필드를 수정한 케이스들에서, `line_addition`의 텍스트가 새 줄로
  안 넘어가고 `line`의 마지막 단어 바로 뒤에 그대로 이어붙어 표시되는
  문제가 있었음(`line/line_2_after.PNG`로 확인). 원인은 원본 데이터의
  `line` 필드가 뒤에 `line_addition`이 있을 때 항상 **끝에 트레일링
  개행(`\n`)**을 갖고 있었다는 것 — 이 개행이 실제로 "다음 내용을 새
  줄에서 시작하라"는 렌더링 지시였는데, 재배치 스크립트가 이걸 그냥
  버리고 있었음. `line`을 수정한 101개 필드 전부(2026-08-22 세션에서
  손댄 모든 케이스)에 영향이 있었던 구조적 버그라 전량 재수정·재배포함.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-22 최종 갱신, 트레일링 개행 버그 수정 반영) | 1,783,701,065 | `440f57b5f0d3b6aeabfc252d2c0c47d8acf72eff77e479408f3d0e71f0e04a79` |
| `toidata_release_ko.xdelta` | - | 91,265,492 | `15060714c35ecc46007374d0372debe2b792acd138edc5cd1d4286ca1998ae7d` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-23 업데이트: Story.csv 대사창 가로 폭 초과 재조정(24자 → 26자 실측 재검증)

`_Data/Script/` 4개 파일이 이번 갱신으로 바뀌었습니다:

- **Story.csv 안전폭을 `line_1.PNG` 재측정으로 24자 → 26자로 상향
  조정**: 2026-08-20에 쓰던 24자 기준이 실측보다 보수적이었음을 확인 —
  1~2번째 줄(장식 테두리만 영향)은 실측 약 27자, 3번째 줄('다음' 진주
  아이콘도 영향)은 약 26자까지 안전. 사용자 판단으로 26자를 통일 기준으로
  채택(더 넉넉한 27자는 보수적으로 보류).
- **`FLAGGED_11_items.md`의 미해결 7건 중 5건 해결**: `Dn/08/01/701.dat`
  #189, `Fld/00/00/701.dat` #68, `Twn/16/00/700.dat` #185(이미 26자
  기준에서는 최적 상태였음, 파일 변경 없음), `Twn/26/00/760.dat` #273 —
  번역 문구는 한 글자도 안 바꾸고 줄바꿈 위치만 재배치. `Twn/03/02/401.dat`
  #52(전각공백 서명 서식)는 서명 줄("긍정적인 모험가 토니")은 완전히
  그대로 두고 앞 문장 2줄만 재배치해서 해결.
- **2건은 여전히 보류**: `Dn/08/01/701.dat` #90(전각공백 경전 인용체,
  최선의 재배치로도 마지막 줄 부족), `Twn/08/02/401.dat` #40(전각공백
  서명체, 26자 기준으론 1자 부족 — 27자까지 허용하면 해결되지만 사용자가
  보수적인 26자 유지를 선택함). 통합 목록은
  `tools/toir/font_research/hangul_fix/rewrap/FLAGGED_ALL_pending.md` 참고.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 갱신) | 1,783,701,065 | `107e98452c8db56abe695ebfe9f0e079af3fb33581a66c431d5c831c78ab4ced` |
| `toidata_release_ko.xdelta` | - | 91,265,509 | `c0c7038ec5a962927bfc6f7fd4687f075e2ddffc47043f362e512bbe0f249407` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-23 업데이트(2차): SKIT 가로 폭 초과 4건 추가 해결(비대칭 폭 [27,27,24] 적용)

`_Data/Field/Skit/Data/` 2개 파일이 이번 갱신으로 바뀌었습니다:

- 2026-08-22 세션에서 만든 SKIT 전용 비대칭 안전폭(1~2번째 줄 27자/
  마지막 줄 24자)을 `0006.dat` #17, `0235.dat` #15, `0334.dat` #324,
  `3037.dat` #98 4건(전부 단일 `line` 필드, `line_addition` 없음)에
  재검증 — **`0006.dat`/`0235.dat`는 이미 새 기준 통과(파일 변경 없음)**,
  **`0334.dat`/`3037.dat`는 문구 그대로 줄바꿈만 재배치해서 해결**.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 2차 갱신) | 1,783,701,065 | `a8ed4b61cfe0ed42df5aab705e2e2e849dcadbe31938d02c926eb9bf1e6870cc` |
| `toidata_release_ko.xdelta` | - | 91,265,510 | `41659ad794f6b814dadebde1d401dce87a3d54941bc9217568a334184153c078` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-23 업데이트(3차, 최종): SKIT line/line_addition 그룹핑 버그 발견·수정 — 대사창 누적 줄 수 초과 사실상 전량 해결

`_Data/Field/Skit/Data/` 108개 파일이 이번 갱신으로 바뀌었습니다(그중
상당수는 잘못 적용됐던 이전 수정을 원래대로 되돌린 것, 23개 파일에
실제 새 수정 적용).

**근본 원인 발견**: 이 프로젝트의 기존 추출 도구(`extract.py`)가
`Skit.csv`를 쓸 때 `line_addition` 행을 실제 순서가 아니라 **각 파일의
맨 끝에 전부 몰아서** 기록하는 버그가 있었음 — 그래서 지금까지(2026-08-22
세션 포함) CSV/skit_wip만 보고 그룹을 판단하면 "그 파일의 마지막
`line`에 모든 `line_addition`이 달려있는 것처럼" 보였지만, 실제 게임
바이너리 순서는 전혀 다름(대부분 서로 다른 `line`에 각각 1개씩 붙어
있음, 화자도 다른 경우가 많음). 원본 `.dat`을 직접 파싱(`SkitDeCompiler`)
해서 **진짜 구조**로 다시 그룹핑한 결과:
- 이전에 "167쌍, 그중 27쌍 보류"라고 봤던 것이 사실은 **진짜 겹치는
  경우는 24쌍뿐**이었고(나머지는 잘못된 그룹핑이 만든 허상), **24쌍
  전부 자동으로 해결됨**(경계-넘김 21 + 필드 내 재배치 3). 문구는 한
  글자도 안 바꿈.
- 이전에 수동으로 임시 조정했던 `4053.dat`(line_2.PNG 사례)도 이번엔
  올바르게 "line 41 + addition 44"만의 진짜 단일-addition 쌍으로
  인식되어 **경계-넘김 재배치로 완전히, 자동으로 해결**됨(수동 조정
  불필요해짐, 25자짜리 우회책도 더 이상 필요 없음).
- **보류 항목이 0건이 됨** — SKIT의 "누적 줄 수 초과" 문제가 사실상
  전부 해결됨.
- 원본-신규 텍스트 토큰 비교로 내용 보존도 재검증(순서 변경 없이 줄
  경계만 이동, 실제 이동한 2개 쌍도 앞뒤 합친 전체 단어열이 정확히
  일치함을 확인).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 3차/최종 갱신) | 1,783,701,257 | `f830c9d05df88cfedcbe15d65da843ec03c8f039df896a98c872570577b7a924` |
| `toidata_release_ko.xdelta` | - | 91,265,385 | `09a61324f99875403ad95936af45ab02cbbe5f22942fc6552632aa5aee76801c` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-23 업데이트(4차): 실기 테스트 중 발견된 MapData.csv(맵 화면 NPC 대사) 대사창 초과 43건 수정

사용자가 실제 PS Vita 하드웨어에서 패치를 테스트하던 중 대사창 밖으로
글자가 넘치는 화면을 3건 캡처(`line_3~5.PNG`) — Story.csv 대사창과 UI가
동일해서 Story.csv 문제인 줄 알았으나, 조사 결과 **전부 `MapData.csv`
(맵 화면 NPC 대사, `_Data/Field/MapData/`) 항목**이었음. 이 파일은 지금
껏 어느 세션에서도 대사창 폭 기준으로 점검한 적이 없었음(스토리는
2026-08-20/23에, SKIT은 2026-08-22에 각각 점검·수정됐지만 MapData는 처음).

Story.csv와 완전히 동일한 UI/안전폭(26자, 제어 태그 제외)을 적용해서
`2_translated/MapData.csv` 전체(3,153행)를 스캔한 결과 **43건이 초과
상태**였음(캡처된 3건 포함). 문구는 한 글자도 안 바꾸고 기존 줄바꿈
위치만 재배치(전각공백 서명 서식 3건은 서명 줄 그대로 두고 앞 문장만
재배치)해서 **43건 전부 해결**(원래 줄 수로 안 됐던 6건은 최대 3줄까지
늘려서 해결, 서명 3건 포함). 원본-신규 토큰 비교로 내용 보존도 검증
완료(43건 전부 일치).

실제로 바뀐 `.dat` 파일은 28개(`_Data/Field/MapData/Dn/03/02.dat` 등).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 4차 갱신) | 1,783,701,257 | `916638f1b392000a44e0a3b5a2c60e55e8cf07340b15ff8c84aff94e52a06fcd` |
| `toidata_release_ko.xdelta` | - | 91,265,444 | `e692688a3a0e14500cf613853f175452b336e580ec06420b8fcaeff62be4ecb4` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-23 업데이트(5차): 말투(존댓말/반말) 정합성 재검증 — 3건 수정

화자별 전체 말투 프로필(존댓말/반말 비율)과 원문의 です/ます 존재 여부를
대조해서, "평소 반말 캐릭터인데 이 대사만 존댓말로 번역된" 경우를
전수 스캔(Story/Skit/MapData 전체 약 30,551행). 후보 상당수는 정규식이
「ました」 등 활용형을 놓친 오탐이었고, 실제로 원문과 어긋나는 3건만
수정:

- `Skit/5036.dat` #97 루카: 원문 "もちろん**だよ**！"(반말) → "당연하죠!" →
  **"당연하지!"**로 수정
- `MapData/Twn_02_00.dat` §0 #30 아주머니: 원문 "受け取って**おくれ**"(반말체
  부탁) → "받아주세요" → **"받아주렴"**으로 수정
- `MapData/Twn_02_00.dat` §0 #34 같은 아주머니: 원문 "ごめん**よ**"(반말) →
  "정말 미안해요" → **"정말 미안해"**로 수정

나머지 애매한 항목(고정 인사말/관용구, 존댓말 캐릭터가 확실한 경우,
농담·패러디로 의도적인 존댓말 전환 등)은 오류가 아닌 것으로 판단해서
그대로 둠.

실제로 바뀐 파일은 2개(`_Data/Field/Skit/Data/5036.dat`,
`_Data/Field/MapData/Twn/02/00.dat`).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 5차 갱신) | 1,783,701,257 | `11066d75032d72b95c47e019ef985699cbda41113997e2379e0056c38d1ad3d1` |
| `toidata_release_ko.xdelta` | - | 91,265,440 | `d5410749b1836cf8138f172cbb7a502b8dbadd522f0dbe2717ad0ed062505910` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta를
교체해서 다시 적용하면 됩니다(재설치 불필요, 3단계만 다시 하면 됨).

## 2026-08-23 업데이트(6차): Story/Skit/MapData 외 다른 UI(스토리북·배틀북·칭호·몬스터도감·레코드·술기 화면) 텍스트 넘침 8건 수정

사용자가 실기에서 추가로 캡처한 `i_line_1~6.PNG` 확인 결과, Story/Skit/
MapData가 아닌 완전히 다른 텍스트 파일 3개에서 대사창 넘침 3건과,
`eboot.csv`에 박힌 고정폭 UI 라벨 3건이 넘치는 것을 발견. 전자는
기존과 동일하게 줄바꿈만 재배치(문구 변경 없음), 후자는 애초에 줄바꿈이
불가능한 한 줄짜리 라벨이라 글자 수를 줄여서 해결(사용자 승인).

**A. 줄바꿈 재배치로 해결(문구 변경 없음, 3건)**
- `StoryBookDataPack.csv` StructId 0 (줄거리 "꿈에서 보는 무인"): 마지막
  줄이 박스 테두리를 침범 → text6/text7 경계에서 재배치(스크린샷 실측
  안전폭 21자, 22자부터 침범 확인)
- `BattleBookDataPack.csv` StructId 8 (배틀북 "도주 1"): 같은 UI
  템플릿이라 동일하게 21자 기준 재배치
- `ItemDataPack.csv`/`ItemDataPack_CodeName.csv` CodeName #103 description
  (칭호 "모든 것을 꿰뚫어보는 여성"): 24자 기준으로 재배치(이 박스는
  참고할 스크린샷이 하나뿐이라 안전폭이 Story/StoryBook만큼 정밀하게
  실측되지 않음 — 실기에서 계속 넘치면 알려주세요)

**B. 고정폭 라벨이라 글자 수 축약(사용자 승인, 3건)**
- `eboot.csv` "훔칠 수 있는 아이템"(몬스터 도감) → **"훔칠 아이템"**
  (옆 "드롭 아이템" 라벨과 같은 길이로 맞춤)
- `eboot.csv` "레이브 Lv.4 총 시간"/"레이브 Lv.4 최대 시간"(레코드
  화면) → **"레이브 4 총 시간"/"레이브 4 최대 시간"**
- `eboot.csv` "공중 가능"(술기 화면, 옆 칸 침범) → **"공중"**

바뀐 파일: `_Data/System/StoryBookDataPack.dat`, `BattleBookDataPack.dat`,
`ItemDataPack.dat`, `eboot.bin`(이번 세션 최초로 eboot.bin 변경 —
`eboot_ko.xdelta`도 함께 갱신).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 6차 갱신) | 1,783,701,257 | `4a1be47176846181263572189ff2ec4c1f5fcf32dedd94021e1533abb332d449` |
| `toidata_release_ko.xdelta` | - | 91,265,452 | `101cee5e5d8ec5e044b5ee78caa664074eab9abd1c61809afc79d2e41ebf916b` |
| `eboot.bin` | 패치 후 | 1,543,133 | `64b8edad172a6aec0bf92677134d57e162dd5119e4206e527b56185f76eb44c2` |
| `eboot_ko.xdelta` | - | 10,626 | `105cd3de2405411c7667e8a07ecf746392b8e5f640c2215a49436bf7ef8536f1` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로 xdelta
2개(`toidata_release_ko.xdelta`, `eboot_ko.xdelta`)를 모두 교체해서
다시 적용해야 합니다(이번엔 eboot도 바뀌었습니다).

## 2026-08-23 업데이트(7차): 칭호/아이템/술기 설명(description) 텍스트 넘침 76건 전수 수정

6차에서 고친 칭호(CodeName #103) 1건은 실기에서 잘 해결됐지만, 같은
UI(이름+2줄 설명, `i_line_5`/`i_line_7.PNG`)를 쓰는 **다른 칭호/
아이템/무기/방어구/특기 항목들은 그대로 넘쳐있었음**(`i_line_7.PNG`로
재확인). 이 UI의 안전폭을 두 스크린샷으로 재실측한 결과 20자는 여유
있고 31자는 마지막 2글자가 화면 밖으로 통째로 잘림(29번째 글자까지는
온전히 보임) — 안전폭은 24~29자 사이로, 여유를 두고 **27자**로 통일
적용.

`ItemDataPack.csv`(전체 아이템/무기/방어구/장신구/재료/칭호/특기
어빌리티 등 12개 카테고리 통합, `2_translated/ItemDataPack_*.csv`는
이 마스터의 열람용 사본이라 실제로는 안 쓰임 — 반드시 마스터를
고쳐야 함)와 `ArtsDataPack.csv`(천술/오의 등 술기 설명)의
`description` 필드 전체(1,330건)를 27자 기준으로 스캔·재배치(문구
변경 없음, 필요시 1줄→2줄로 확장 — 이 UI가 최대 2줄까지는 실사용
확인됨). `CharaAbility.csv`(어빌리티 설명, 192건)도 같이 점검했으나
전부 이미 안전폭 이내였음.

**결과: 76건 자동 해결(내용 보존 토큰 비교 완료, 불일치 0건), 4건은
문구를 줄이지 않고는 27자를 못 맞춰서 보류**(전부 이미 2줄인 상태에서
최선으로 재배치해도 초과 — Story.csv A1/A2와 같은 성격의 예외):
- `ItemDataPack.csv` CodeName #58, #230, #235
- `ItemDataPack.csv` Recipe #59

바뀐 파일: `_Data/System/ItemDataPack.dat`, `ArtsDataPack.dat`.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 7차 갱신) | 1,783,701,257 | `60093cf4957c4d0ce1b5d9781ed6f9464821bf55e2d81140a1305e2f5bd71707` |
| `toidata_release_ko.xdelta` | - | 91,265,585 | `7f5ce96940f89e90729fecd3a0a752d7f124d5749cb520c8f4d5f5cc5d015bd8` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다(이번엔
eboot은 안 바뀌어서 `eboot_ko.xdelta`는 그대로 둬도 됩니다).

## 2026-08-23 업데이트(8차): 칭호 이름(name) 필드가 목록/장비창에서 잘리는 문제 1건 수정

7차까지는 칭호의 "설명(description)" 필드만 점검했는데, `i_line_5_after.PNG`
(패치 적용 후 실기 재캡처)로 확인해보니 **칭호 목록 패널**과 **장비창
칭호 슬롯**에 표시되는 "이름(name)" 필드도 폭이 좁아서 넘치는 게
있었음(설명과는 별개의 UI, 둘 다 줄바꿈이 불가능한 한 줄짜리 항목).

`ItemDataPack.csv`의 CodeName `name` 필드 288개를 안전폭 12자 기준으로
스캔한 결과 3건(#16 "물건 잘 잃어버리는 소년" 13자, #103 "모든 것을
꿰뚫어보는 여성" 14자, #194 "상냥한 건지 엄격한 건지" 13자)이 초과.
사용자 지시로 **#103만** "모든 걸 꿰뚫어보는 여성"(13자, "것을"→"걸")로
수정 — 단, 13자는 #16(현재도 13자에서 살짝 잘리는 걸로 확인된 상태)과
같은 길이라 여전히 아주 살짝 잘릴 가능성 있음, 실기 재확인 권장.
#16/#194는 이번에 손대지 않음(사용자가 103번만 지정).

바뀐 파일: `_Data/System/ItemDataPack.dat`.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-23 8차 갱신) | 1,783,701,257 | `09f99b7c1f3db704a3c642e1a57dfa378fd87326d7b6674778f25108f637720f` |
| `toidata_release_ko.xdelta` | - | 91,265,594 | `ca4f0b543e4a02566479a131491fe2e0c0cd1495e284c0c0e465a2d3e01175d5` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## 2026-08-24 업데이트(9차): 앙쥬(アンジュ) 말투 정합성 16건 수정

사용자 요청으로 앙쥬 전체 대사(1,756건, Story/Skit/MapData)를 원문
어미(です/ます 계열 유무) 기준으로 전수 대조. 앙쥬는 일본어 여성어
어미(わ/の/ね/かしら/でしょ)를 상황에 따라 반말/존댓말 어느 쪽으로도
번역해온 캐릭터라(친밀한 잡담엔 반말, 격식 있는 장면엔 존댓말) 기계적
오탐이 많았음 — 큐큐/콘웨이/리카르도를 제외한 주요 동료(스파다/이리아/
루카/코다/에르마나)를 상대로 말하면서 원문이 반말 어미인데 번역이
존댓말인 경우만 사용자와 함께 추려서 16건 확정(전투/종교/재판 등
무거운 주제라도 이 5명이 대상이면 포함, 대상이 다른 인물인 1건은 제외)
— 어미/조사만 반말로 바꾸고 나머지 단어는 그대로 유지.

바뀐 파일: `_Data/Script/` 10개(`Dn/08/01/701.dat` 등), `_Data/Field/Skit/Data/`
4개(`0078.dat`, `0252.dat`, `0256.dat`, `1320.dat`).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 9차 갱신) | 1,783,701,186 | `0cb32f4ab8ab92f4cd78d6f049f064aeae32855cae1562a7f283b6d4f5080c19` |
| `toidata_release_ko.xdelta` | - | 91,265,563 | `f528d9c7b61a52283a929e712760f488ff860e2279063a39e5e38ac199467c4d` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## 2026-08-24 업데이트(10차): 앙쥬(アンジュ) 말투 정합성 2차 전수 수정(80건)

사용자가 Dn_20_11_701#41, Dn_18_07_730#125 등 여전히 남아있는 사례를
지적, 9차 때 놓쳤던 부분(같은 문장에서 이름이 명시적으로 안 나오고
"みんな"로 지칭하거나, 직전/직후 대사 상대가 스파다/이리아/루카/코다/
에르마나인 경우)까지 포함해서 앙쥬의 원래 143건 후보 전체를 문맥과
함께(파일별 앞뒤 대사 확인) 다시 정밀 재검토. 이번엔 "이 대사가 누구를
향한 것인가"를 대사 하나하나 앞뒤 맥락으로 확인해서 80건을 추가로
반말로 수정(9차 16건 + 이번 80건 = 총 96건). 큐큐/콘웨이/리카르도・
적・NPC・낯선 사람에게 하는 말이거나 특정 대상 없는 독백은 그대로
존댓말 유지.

바뀐 파일: `_Data/Script/` 33개, `_Data/Field/Skit/Data/` 18개,
`_Data/Field/MapData/` 2개(`Dn/12/04.dat`, `Dn/21/00.dat` — 같은 퍼즐
힌트를 다른 파티원들도 각자 반말로 말하는데 앙쥬만 존댓말이었던 항목).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 10차 갱신) | 1,783,700,899 | `29a9c39b593b89754e61f6f2d1eef70718c0bfe3c94de5e26cc49b1b729e0176` |
| `toidata_release_ko.xdelta` | - | 91,265,218 | `b2f3ea8b136db20648c49aadd5dd9aa25a3db2a8e8e64a0f1ec41f08c68778fd` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## 2026-08-24 업데이트(11차): 앙쥬 말투 정책 변경 — "상대가 누구든 원문 어미 그대로" 47건 추가 수정

사용자 지시로 정책 변경: 이제부터 앙쥬는 **상대가 누구든 상관없이
원문이 존댓말(です/ます 계열)이면 존댓말, 반말이면 반말**로 통일.
9~10차에서 "대상이 스파다/이리아/루카/코다/에르마나일 때만" 반말로
바꾸고 리카르도/큐큐/콘웨이/적/NPC 상대 대사는 존댓말로 남겨뒀던
나머지 47건을 전부 반말로 수정(대상 구분 폐지).

한 대사 안에서도 줄마다 원문 어미가 다른 경우(예: 앞줄은 반말, 뒷줄은
진짜 です/ます 존댓말)는 존댓말인 줄만 그대로 두고 반말인 줄만 수정 —
예: "私もです/では行きましょう"(私もです=진짜 존댓말, 行きましょう=
반말 취급) → "저도요/그럼 가자"(첫 줄 유지, 둘째 줄만 수정).

바뀐 파일: `_Data/Script/` 16개, `_Data/Field/Skit/Data/` 16개,
`_Data/Field/MapData/` 2개.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 11차 갱신) | 1,783,700,749 | `05eff8f5cefe272e80e7df71a677e5381e606c34e9e5d2d248b72bda97bd5cb9` |
| `toidata_release_ko.xdelta` | - | 91,265,086 | `307ec9159e2f58fabb05b3cb5f7144ca65744465bff208058241fef869227dd9` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## 2026-08-24 업데이트(12차): 앙쥬 말투 일관성 보정 16건

사용자 지시 2건 반영: (1) 한 대사 안에서 어느 한 줄이라도 원문이 진짜
존댓말(です/ます/でしょう 등)이면 그 대사 전체를 존댓말로 통일(반말·
존댓말 혼용 금지), (2) 권유형(ましょう) 대사는 평소 존댓말로 얘기하는
상대(리카르도/큐큐/콘웨이/알베르/낯선 사람 등)를 향한 말이면 존댓말로
유지. 9~11차에서 이 두 원칙과 어긋났던 16건(9~10차 3건, 11차 13건)을
존댓말로 되돌리거나 통일.

바뀐 파일: `_Data/Script/` 5개, `_Data/Field/Skit/Data/` 10개.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 12차 갱신) | 1,783,700,789 | `9e761ff616183e7ca9e63b3f362827fca6178a203b3c558bb3aa9de66f66bc8a` |
| `toidata_release_ko.xdelta` | - | 91,265,129 | `5c54e3004a150656d28b4311d37716bd24140ceb8583b96b6fc2e842f27d8f06` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## 2026-08-24 업데이트(13차): 앙쥬 → 큐큐 상대 대사도 반말 예외에 추가(3건)

큐큐(キュキュ)를 리카르도/콘웨이/알베르 등 "존댓말 상대" 그룹에서
제외하고 스파다/이리아/루카/코다/에르마나와 같은 "반말 상대"로 재분류.
사용자 관찰대로 앙쥬가 큐큐에게 말할 때는 호칭("큐큐 씨")만 높이고
어미는 원래 반말인 경우가 대부분이었음 — 큐큐 관련 대사 30건을
전수 재확인한 결과 27건은 이미 정확히 반말로 번역돼 있었고(9~12차와
무관하게 원래부터), 어미에 진짜 존댓말 표지(です/ます 등)가 있는
6건은 그대로 존댓말 유지가 맞음. 원문에 표지가 없는데 번역만 존댓말인
3건을 찾아 반말로 수정(정규식이 "고마워요/왜요?/실례할게요" 같은
받침+워요/왜요/게요 패턴을 못 잡던 걸 발견해서 직접 재확인함).

바뀐 파일: `_Data/Field/Skit/Data/` 3개(`5050.dat`, `5053.dat`,
`5054.dat`).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 13차 갱신) | 1,783,700,789 | `d5a9417a91ab5ef29770be675f3d0881e717491fe42764ca55b8be0a882e6413` |
| `toidata_release_ko.xdelta` | - | 91,265,114 | `48622c0b76e5aef9fc37691c3df9a016f47b01272861a48206ba03315a5e82e1` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## 2026-08-24 업데이트(14차): 스킷 대사 줄바꿈 넘침 수정(콘웨이, 1건)

신규 실기 캡처(`line_6.PNG`)에서 발견된 문제. `_Data/Field/Skit/Data/0070.dat`
(콘웨이·루카 대화, Index 16, 콘웨이 대사)에서 한글 번역이 4줄로 줄바꿈되어
있었는데, 스킷 대사창은 3줄까지만 표시 가능해서 4번째 줄이 화면 아래로
잘려나가고 있었음. 원문은 3줄(`ボクは/キミたちの旅を見届けたい/最後の最後までね`).

기존: `나는\n너희들 여행을 끝까지\n지켜보고 싶어\n마지막의 마지막까지 말이야` (4줄)
수정: `나는 너희들 여행을\n끝까지 지켜보고 싶어\n마지막의 마지막까지 말이야` (3줄,
각 줄 10/11/14자로 스킷 안전폭 27/27/24자 이내)

문구는 전혀 줄이지 않고 줄바꿈 위치만 재조정. 내용(토큰) 완전 보존 확인 완료.
Story.csv/Skit.csv(line 필드)/MapData.csv 전체를 재스캔해서 3줄 초과 사례가
더 있는지 확인했으며, 이 건 외에는 없음(고립된 단일 사례).

바뀐 파일: `_Data/Field/Skit/Data/0070.dat` 1개.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 14차 갱신) | 1,783,700,789 | `0c302b2dde23db65f18925c08eea8e46064c969613cc3881af486cdad5b64c42` |
| `toidata_release_ko.xdelta` | - | 91,265,105 | `cf61748611f39bc77df50983aef17249f7899f274bf37531f8bfa75682801e1a` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## 2026-08-24 업데이트(15차): Story/MapData/Skit 대사창 "마지막 줄" 안전폭 전수 재조정(93건)

`line_7.PNG` 실기 캡처에서 6차에 이미 26자 기준으로 재배치했던
`Twn/26/00/760.dat #273`(가드르 대사)의 3번째 줄이 여전히 '다음' ▼
아이콘 뒤로 잘려있는 걸 재확인. `line_5.PNG`(2번째 줄 27자, 안 잘림)와
비교 분석한 결과, '다음' 아이콘은 항상 대사창의 **마지막 줄**(줄 개수와
무관하게)과 같은 높이에 걸쳐있어서, 그 줄만 더 좁은 폭(24자)이 필요함을
확인 — Skit.csv에서 이미 확립되어 있던 "WIDE=27(앞줄들)/NARROW=24(마지막
줄)" 규칙과 정확히 같은 패턴. 다만 이 규칙이 Skit.csv에서도 특정
"그룹 넘침" 케이스에만 적용되고 전체 `line` 항목에 전수 적용된 적은
없었고, Story.csv/MapData.csv는 지금까지 "26자 균일" 기준만 썼음.

Story.csv/MapData.csv/Skit.csv(`line` 필드) 전체를 마지막 줄 24자 초과
기준으로 재스캔해서 문구를 전혀 줄이지 않고 줄바꿈 위치만 재배치:
- Story.csv: 28건 후보 중 27건 적용(`_Data/Script/` 26개 파일 변경).
  나머지 1건(`Dn/08/01/701.dat #90`)은 전각공백 들여쓰기 인용구 서식이라
  기존에도 보류 중이던 항목, 이번에도 보류.
- MapData.csv: 30건 후보 중 28건 적용(`_Data/Field/MapData/` 20개 파일
  변경). 2건 보류: `Twn/04/00.dat` Section4 #22(♪ 노래 가사, 전각공백
  서식이라 일반 재배치 시 서식이 깨짐), `Twn/03/04.dat` Section0
  #6(이미 3줄 상태에서 최선으로 재배치해도 24자 못 맞춤 — 문구를 안
  줄이면 방법 없음, 신규 보류 항목).
- Skit.csv(`line` 필드): 65건 적용(`_Data/Field/Skit/Data/` 68개 파일
  변경). `line_addition`으로 이어지는 그룹 1건(`0235.dat #89`)은 진짜
  마지막 줄이 line_addition 쪽(9자)이라 원래도 안전해서 그대로 둠.

내용(토큰) 완전 보존을 모든 적용 건에 대해 자동 검증(순서/단어 불변,
줄바꿈 위치만 이동). 배포: 위 3개 파일에서 변경된 `_Data` 파일 총
101개(Script 26 + MapData 20 + Skit 68 — 일부 파일은 리컴파일러 특성상
겹쳐서 계산됨, 실제 배포본은 정확히 101개 확인됨).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-24 15차, 최종) | 1,783,700,789 | `378d052b8db2df69390ecf4d8139d7a8ee2fa9206ed78feb93fa66fef7b25810` |
| `toidata_release_ko.xdelta` | - | 91,265,300 | `4d5b24ada5839004222ad928aef180b1d7b0a98710261d213e714e39deda07a3` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## 2026-08-31 업데이트(16차): 동일 원문·다른 번역 용어 통일(25건)

사용자가 "산할멈"(山姥)처럼 같은 원문이 서로 다르게 번역된 사례를
찾아서 통일해달라고 요청. `Story.csv`/`Skit.csv`/`MapData.csv` 전체를
"완전히 동일한 일본어 원문인데 한국어 번역이 다른 경우" 기준으로
전수 스캔(구두점/짧은 감탄사 제외)해서 509건을 찾았고, 그중 반복
5회 이상(가장 시급한 것부터) 25건을 이번에 통일. 대부분 던전 이동
UI 문구나 상인 대사처럼 여러 파일에 복제된 텍스트가 각각 다른
시점에 번역되며 갈라진 경우였음.

주요 사례:
- 山姥(야마우바/산할멈): `Skit.csv`는 "산할멈", `Story.csv`(`Dn/17/04/710.dat`)는
  "야마우바"로 갈라져 있던 것을 "산할멈"으로 통일 (마지막 말장난
  대사 "산할멈이면 할망구잖아!"는 오히려 더 자연스러워짐)
- `進む方向を選んでください`(방향 선택 UI, 57회) → "나아갈 방향을
  선택해 주세요"로 통일(다수결)
- `真っ直ぐ進む`(37회) → "똑바로 나아간다"로 통일
- 원더셰프 소개/판촉 대사(가람 각지에 복제, 12회/11회, 각 3~4가지
  버전) → 다수결 버전으로 통일
- 큐큐(キュキュ)의 어눌한 말투 "はい　わかた"(7회, 4가지 버전) →
  "네, 알갯어"로 통일 (큐큐는 비원어민 특유의 서툰 말투 캐릭터라
  등장할 때마다 같은 방식으로 어눌해야 함)
- 던전 퍼즐방 하나(`Dn/06/00.dat`)가 같은 방을 복제한 다른 4개
  던전(`Dn/08/00`/`14/01`/`15/01`/`17/00`)과 대사가 갈라져 있던
  것도 함께 통일(6건)
- 예외로 남긴 것: `お気をつけて`(4회 "조심해" vs 1회 "조심히
  다녀오십쇼")는 화자가 서로 다른 캐릭터(치토세 vs 용심봉/경호원
  NPC)라서 의도된 존댓말 차이 — 통일하지 않음

나머지 484건(반복 5회 미만, 대부분 우연히 겹친 1쌍짜리)은
`TERM_CONSISTENCY_CANDIDATES.md`에 정리해둠 — 다음에 추가로 반영
요청 시 참고.

배포: `_Data/Script/` 11개, `_Data/Field/MapData/` 20개,
`_Data/Field/Skit/Data/` 9개 파일 변경(총 40개).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-08-31 16차, 최종) | 1,783,701,010 | `268f7702ae28e730e9bad08839bbc9c28c9eef9cd3e7c54ccbbefb8c1ed4cdf4` |
| `toidata_release_ko.xdelta` | - | 91,265,381 | `687708b86bb8ef805b379ff79e551daebe71e8e30467529fd789a1d9b1287f0e` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## 2026-09-01 업데이트(17차): 아스라 → 브리트라 호칭 통일("할멈"/"유모" → "할멈")

사용자가 아스라(アスラ)가 브리트라(ヴリトラ)를 부르는 호칭도 통일이
안 되어있다고 지적. 원문 「ばあや」(어릴 적부터 자신을 돌봐준 노년의
유모를 부르는 애칭)가 `Story.csv`(Twn/16/00/710, Twn/17/00/730,
Twn/20/00/720 — 11회)에서는 "할멈"으로, `Skit.csv`(3015/3033/3034 —
4회, 아스라 시대 회상)에서는 "유모"로 갈라져 있었음. 스토리 본편에서
이미 "할멈"으로 굳어져 있었고, 특히 Twn/17/00/730의 핵심 장면
("알고 있다, 할멈……아니, 브리트라" — 아스라가 습관적으로 "할멈"이라
부르다 스스로 "브리트라"로 고쳐 부르는, 호칭 전환 자체가 서사의
일부인 장면)에서도 "할멈"이 쓰였으므로, 다수결이자 서사상 더 이른
확정 표현인 "할멈"으로 통일. `Skit.csv` 3곳(3015.dat #25, 3033.dat
#5·#11, 3034.dat #2)의 "유모"를 "할멈"으로 수정.

배포: `_Data/Field/Skit/Data/` 3개(`3015.dat`, `3033.dat`, `3034.dat`).

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-09-01 17차, 최종) | 1,783,701,026 | `104c1a555edd660f512969929be9dce7e325cdea80737a3879fd4d60eb3d1b31` |
| `toidata_release_ko.xdelta` | - | 91,265,401 | `7694cbc0988046c8fa99235387e9d6b4b9707f019c48d6f77d7d1306fe393c09` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## 2026-09-15 업데이트: SKIT 대사창 누적 줄 수 초과 17건 + MovieCaption 자막 11건 수정 (toidata_release_ko.xdelta 재생성)

`_Data/Field/Skit/Data/` 16개 + `_Data/Movie/` 11개, 총 27개 파일이 이번
갱신으로 바뀌었습니다:

- **SKIT `line`+`line_addition` 그룹의 "누적" 줄 수 초과 17건 수정**
  (`1003, 1359, 3000, 4050, 4052, 4075, 4095, 4102, 4105, 4106, 4212,
  4234, 4250, 6022, 6042, 6045.dat`): 2026-08-22/23차 수정은 그룹 안에
  addition이 2개 이상일 때 각 addition을 `line`과 1:1 쌍으로만
  검사했는데(L+A₁>3, L+A₂>3 각각), 실제 화면은 `line`+모든 addition이
  누적 표시되므로 진짜 제약은 "L+A₁+A₂+...>3"이어야 했음 — 이 사각지대를
  `line/line_8.PNG` 스크린샷(4212.dat, 4줄째가 화면 밖으로 잘림)으로
  실제 발견. `line`↔addition, addition↔addition 경계에서 단어를
  재배분하는 기존 검증된 방식(2026-08-22/23차 `solve_pair_cross`와 동일
  원리, `4053.dat`/`line_2_after.PNG`가 그 실사례)을 다중-addition
  그룹까지 일반화해서 해결. **문구는 한 글자도 안 바꿈**(그룹 단위 토큰
  비교로 전량 재검증), `line` 필드는 항상 비지 않게 제약(끝의 필드가
  화면이 열리는 순간 잠깐 빈칸으로 보일 위험을 배제), 인접 addition으로
  넘어간 단어가 우연히 등장인물 이름과 겹쳐 `check` 도구의 용어집
  검사에 걸리는 경우는 피하도록 보강. `tools/toir/font_research/hangul_fix/rewrap/fix_skit_group_overflow_v5_wordshift.py`
  참고.
- **MovieCaption 자막 11개 파일 수정 반영** (`MovieCaption_007, 008, 009,
  010, 011, 012, 013, 014, 016, 017, 026.srt`): 사용자가 `tools/moviecaption_gui.py`로
  직접 검수·수정한 내용을 반영.

배포 전 `line`/`line_addition` 필드가 관련된 전체 466개 그룹을 재스캔해서
잔여 초과 0건, `check skit`으로 기존부터 알려진 "리카르돈" 1건 외 새
이슈 0건, Skit 16개 전부 `SkitDeCompiler`로 라운드트립 디코드 검증(에러
0건) 확인. `toidata_release.l7c` 재생성 후 `_Data/Script`·
`_Data/Field/Skit/Data`·`_Data/Field/MapData`·`_Data/Movie` 4개 카테고리
1,699개 파일을 재추출해서 `3_patched/loose_files`와 전수 일치 확인.

| 파일 | 상태 | 크기(byte) | SHA256 |
|---|---|---|---|
| `toidata_release.l7c` | 패치 전(원본 덤프 그대로) | 1,475,397,012 | `b93681dda81184e80c7a8128318d74a895243d4238167c7235d50ace0b45a51d` |
| `toidata_release.l7c` | 패치 후(2026-09-15, 최종) | 1,783,701,058 | `20e7a63170abe24f4d3b04fcabf8d62784117a0ebe3d440e2d69fb67d6a01fdd` |
| `toidata_release_ko.xdelta` | - | 91,265,462 | `91e0b8d123bbdeaac4cadcfe10e36d743d8a763d6aed83c1924abaa086fc5775` |

이 패치를 이미 적용해서 플레이 중이었다면, 위 표의 새 해시로
`toidata_release_ko.xdelta`를 교체해서 다시 적용하면 됩니다.

## ⚠️ 절대 하지 말 것: 패치본으로 "새로 설치"

패치된 `toidata_release.l7c`를 zip으로 묶어서 Vita3K의 "Install Archive"
(새로 설치) 기능으로 설치하려고 시도하지 마세요 — 반드시 실패합니다.
Vita3K의 설치 과정은 `sce_pfs/`(PS Vita 패키지 무결성·DRM 검증 시스템)로
각 파일 크기를 원본 패키지 매니페스트와 대조하는데, 패치본은 원본보다
커서(+약 300MB) 이 검증에 걸려 **압축 해제한 데이터를 전부 삭제**해버립니다.
이 검증은 **새로 설치할 때만** 동작하므로, 위 절차(원본을 먼저 정상
설치한 뒤 파일만 교체)는 이 문제와 전혀 무관합니다.

## 검증

이 패치는 다음을 실기(Windows Vita3K, Android Vita3K)로 직접 확인했습니다:
- xdelta 패치 적용 결과가 재컴파일 결과물과 SHA256 완전 일치
- 패치된 `.l7c`를 `l7ca_unpack.py`로 압축 해제한 결과가 원본 아카이브에
  저장된 CRC32와 전부 일치(14,076개 파일 전수 검사, 실패 0건)
- 실제 부팅 후 스토리·스킷·전투·메뉴·필드명 등 전반에서 한글 텍스트 정상 출력

## Python 설치 방법 (Windows, 처음 하시는 분용)

4단계(`.l7c` 압축 해제)에는 Python이 필요합니다. 없으신 경우:

1. **Microsoft Store에서 설치 (가장 쉬움)**
   - 시작 메뉴에서 "Microsoft Store" 실행 → 검색창에 "Python 3.12" 입력
   - 파이썬 소프트웨어 재단(Python Software Foundation)이 배포한 "Python 3.12"
     선택 → "설치" 클릭 → 끝나면 별도 설정 없이 바로 사용 가능
2. **공식 사이트에서 설치**
   - https://www.python.org/downloads/windows/ 에서 최신 "Windows installer
     (64-bit)" 다운로드·실행
   - **설치 화면 맨 아래 "Add python.exe to PATH" 체크박스를 반드시
     체크**한 뒤 "Install Now" 클릭 (이 체크를 빠뜨리면 `python` 명령이
     인식되지 않습니다)

**설치 확인**: 명령 프롬프트(또는 PowerShell)를 새로 열어서 `python --version`
입력 → `Python 3.x.x` 같은 버전이 출력되면 정상 설치된 것입니다.

**"python을 찾을 수 없습니다" 에러가 계속 나올 때**: PATH 등록이 안 된
경우입니다. 아래처럼 python.exe의 전체 경로를 직접 써서 실행할 수도 있습니다
(설치 시 기본 경로 기준):
```
%LOCALAPPDATA%\Programs\Python\Python312\python.exe l7ca_unpack_gui.py
```

## 알려진 이슈

- ~~특정 한글 낱자가 다른 글자로 바뀌어 보이던 문제(예: "스킵"이 "스었"으로
  보임), 일부 글자가 밑줄(_)로만 표시되던 문제~~ — **2026-08-19 기준
  완전히 해결됨.** 초기에는 글리프를 여러 개 동시에 추가하면 Vita3K가
  크래시하는 문제 때문에 "틀린 글자 대신 빈칸(대시)으로 표시"까지만
  적용했었으나, 이후 크래시의 진짜 원인(글자 레코드의 "크기" 필드를 0으로
  잘못 기록해서 PS Vita 폰트 라이브러리가 그 값을 memcpy 크기로 잘못
  사용 → 크래시)을 찾아 근본 수정했고, 실사용 한글 전체 1374자를 전수
  대조해서 빠짐없이 추가 완료(`FontDataNormal.pgf`/`FontDataMin.pgf` 둘
  다). 이어서 한글 음절이 아닌 특수문자(가운뎃점 `·`/`ㆍ`, 줄표 `—`,
  마이너스 `−`)도 같은 방식으로 누락이 있었음을 발견해 추가함 — 단,
  한글용으로 역산한 세로 위치 계산 공식이 이런 낮게 앉는 기호에는 안
  맞아서 처음엔 화면에 공백으로 나오는 회귀가 있었고, 이미 정상 작동
  하던 원본 글리프를 그대로 재사용하는 방식으로 최종 해결함(자세한
  내용은 `NEXT_SESSION_PROMPT.md` 2026-08-19 항목 참고). **이상 기호
  `≥`와 홀로 쓰인 자음 `ㅇ` 2자는 사용 빈도가 낮아(각 2회) 아직
  미포함 — 다음에 필요하면 추가할 것.** 자세한 조사 과정은
  `tools/toir/font_research/hangul_fix/README.md` 참고(단, 크래시 관련
  초기 결론은 이후 갱신되어 최신 상태는 `NEXT_SESSION_PROMPT.md`의
  2026-08-17~19 항목 참고).
- **스테이터스 화면의 물공/물방/술공 등 일부 스탯 숫자가 겹쳐 보이는 문제**는
  아직 원인 미해결입니다. 폰트 파일은 원본과 완전히 동일함을 확인했고(위
  항목과 무관한 별개 버그), HP/TP/NEXT 등 다른 숫자는 정상입니다. Ghidra로
  게임 바이너리를 직접 디컴파일해서 숫자를 그리는 `CNumberFont`/
  `CNumberSprite` 클래스와 자리별 X좌표 계산 공식까지는 찾았지만, 정확히
  어느 값이 잘못 설정되는지는 특정하지 못했습니다. 자세한 조사 기록은
  `tools/toir/font_research/number_bug/README.md` 참고.
- **일부 연출 장면(예: `font_error/no_trans.PNG`의 아스라 독백 장면)은
  대사가 이미지 스프라이트(`_Data/Field/Effect/Sprite/*.pck`)에 미리
  구워진(baked) 형태라 번역이 적용되지 않습니다.** 이 장면은 일반
  `Story.csv`/스크립트 대사 시스템을 아예 타지 않는다는 것과, 실제 텍스트가
  960x544(Vita 화면 해상도) 픽셀 데이터로 `.pck` 파일 안에 직접 들어있다는
  것까지는 확인했지만, 정확한 픽셀 인코딩 방식(단순 raw grayscale로 읽으면
  글자 위치는 맞지만 속이 빈 윤곽선만 나옴 — 실제 화면은 두꺼운 채움 글씨라
  인코딩 해석이 아직 부정확함)은 밝혀내지 못해 이번 버전에서는 보류했습니다.
  자세한 조사 기록은 `tools/toir/font_research/sprite_text_pck/README.md`
  참고.
